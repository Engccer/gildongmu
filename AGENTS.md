> 🤖 **이 파일은 자동 생성됩니다. 직접 수정하지 마세요.**
> 정본은 `CLAUDE.md` 입니다. 내용을 바꾸려면 `CLAUDE.md` 를 수정한 뒤
> 프로젝트 루트에서 `python sync_agent_docs.py` 를 실행하세요.
> 이 파일을 직접 고치면 다음 동기화 때 경고와 함께 덮어쓰기 대상이 됩니다.

<!-- SYNC-BODY-START: 이 줄 아래 본문은 CLAUDE.md 와 100% 동일하게 자동 생성됨 -->
# CLAUDE.md — 길동무 (gildongmu)

> Next.js 16 주의: 이 버전은 학습 데이터와 컨벤션이 다를 수 있다. 코드 작성 전 `node_modules/next/dist/docs/`의 관련 가이드를 먼저 읽을 것 (요청 API 전부 비동기: `await params`, `await cookies()`; `middleware.ts` 대신 `proxy.ts`).

## 프로젝트 정체성

**국내 서비스 연동 실험실.** 네이버·카카오를 시작으로 대한민국 로컬 서비스 API(지도, 내비게이션, 장소, 예약, 관광 등)를 계속 발굴·추가하며, 접근성 우선 미니멀 UI로 실험한다. 두 사용자 집단이 1급 시민:

1. **시각장애인** — 스크린 리더만으로 전체 흐름(검색 → 장소 정보 → 길찾기)이 완결되어야 한다.
2. **한국 방문 외국인** — 한국어를 몰라도 쓸 수 있는 미니멀한 영어 UI.

**궁극 목표**: 여기서 검증된 기능을 `~/Mac-Projects/dodo-planet/`(가족 여행 가이드 PWA)에 통합한다. 이 저장소는 인큐베이터 — 따라서 **스택·컨벤션을 dodo-planet과 일치**시키고(next-intl 4, zod 4, Vitest 4, App Router), `src/lib/`는 React/Next 비의존으로 유지해 이식성을 보장한다.

## 절대 원칙: 접근성

- **정보의 정본은 리스트/텍스트 UI다. 지도는 시각 보조 레이어다.** 네이버·카카오 지도 SDK는 캔버스 렌더링이라 스크린 리더 접근 불가 — 지도에만 존재하는 정보가 있으면 그것은 버그다.
- 상태 변화(검색 결과 수, 오류, 경로 안내)는 `aria-live` 영역으로 통지한다.
- 모든 인터랙티브 요소는 키보드 도달 가능 + `:focus-visible` 스타일 필수.
- 터치 타깃 최소 44×44px (`min-h-11` 이상).
- UI 메뉴/버튼 라벨에 이모지 금지 (워크스페이스 공통 원칙).
- 과잉 ARIA 정리 (2026-06-15): 글로벌 "First Rule of ARIA" 기준에 따라 노이즈 요소 제거 — skip link 삭제(단일 화면이라 WCAG 2.4.1 비해당, heading 내비가 진입점), offline 페이지 `<section aria-label>` 중복 레이블 제거, `PlaceCard` 버튼의 시각 텍스트를 덮던 `aria-label` 제거(분류·주소가 다시 낭독됨), `ResultList` 무명 `<section>`→`<div>`. 관련 i18n 키(`nav.skipToContent`·`place.openDetail`) 제거. 기준 정본은 글로벌 `~/.claude/CLAUDE.md`.
- **자동 등장 보조 섹션은 region 랜드마크(`<section aria-labelledby>`)를 유지한다** (2026-06-20): 미니멀 ARIA 정리(a2bf5d1→일괄제거 11d17f2)가 14개 컴포넌트의 `<section aria-labelledby>`를 일괄 `<div>`로 바꾸고 미사용처럼 보인 `useId`/`headingId`까지 제거했는데, 이때 **`AirQuality`·`StationMeta`처럼 버튼 없이 장소 상세에 자동 fetch되어 조용히 등장하는 보조 섹션**까지 div가 되면서 위원장이 "대기 질 표시가 사라졌다"고 신고했다(데이터·렌더는 정상, **사라진 것은 스크린 리더 회전자의 region 랜드마크**). 자동 등장 섹션은 버튼이라는 발견 경로가 없어 **region이 유일한 발견 수단**이므로 First Rule of ARIA의 "불필요한 region 금지"에 해당하지 않는다(불필요하지 않음). 따라서 이 둘은 region 복원(`useId`+`<section aria-labelledby>`+`<h3 id>` 재도입), **버튼으로 펼치는 패널(내 주변 6종·역 시설·경로 브리핑)은 버튼 자체가 발견 경로라 `<div>` 유지**(정책 부합). 판단 규칙: "사용자가 이 섹션을 직접 펼쳤는가(버튼) vs 조용히 나타났는가(자동)" — 후자면 region 유지. h3 heading만으로는 부족(자동 섹션은 heading 탐색 전에 존재 자체가 안 알려짐). ⚠ 죽은 코드 청소(미사용 `useId` 제거)를 할 때 이 두 자동 섹션의 `aria-labelledby`는 "잇다 만 흔적"이 아니라 의도된 region이므로 제거 금지.
- **definition list(`<dl>/<dt>/<dd>`)는 단순 라벨-값에 쓰지 않는다** (2026-06-19): `PlaceDetail`·`StationMeta`·`AirQuality`가 분류·주소·전화·노선·오염물질 등급을 `<dl>`로 표시했는데, 스크린 리더가 항목마다 "용어(term)/정의(definition)" 역할과 `<dt>` 안의 콜론(`:`)을 별도 개체로 낭독해 "표처럼" 읽혔다(위원장 피드백). **평문 `<p><span class="font-medium">라벨</span> 값</p>`으로 전환**(콜론 제거, 라벨은 볼드 시각 구분만) → "분류 음식점"처럼 한 호흡에 읽힌다. 진짜 표 데이터가 아닌 단순 메타 쌍에 definition list 시맨틱은 노이즈(First Rule of ARIA). `lang="ko"`(영문 UI의 한글 고유명 음성 엔진 정합)는 `<dd>`→`<span>`으로 그대로 이전.

## 아키텍처

```
클라이언트 컴포넌트 ──fetch──▶ Route Handler (src/app/api/*) ──▶ 외부 API
                                 (Secret은 서버 전용 env에만 존재)
```

- **Provider 추상화** (`src/lib/providers/`): 도메인별 단일 진입점(예: `searchPlaces()`)이 키 유무로 provider를 자동 선택. **새 국내 서비스 추가 시 이 패턴을 따른다** — provider 파일 추가 → 진입점에 선택 로직 → mock 폴백 유지.
  - 장소 검색 우선순위: **kakao-local(15건) > naver-local(5건) > mock**. `PLACES_PROVIDER` env로 강제 지정(A/B 실험).
  - **en 로케일은 카카오 + TourAPI 병합**(`searchPlacesMergedEn`): TourAPI는 관광 콘텐츠만 커버해 일상 장소(학교·카페 등)를 못 찾으므로, 카카오를 기본으로 두고 TourAPI 영문 관광 정보를 보강한다. 두 소스를 병렬 호출해 한쪽 실패해도 다른 쪽 실데이터는 보존하고, 둘 다 실패하면 에러를 던진다. 중복은 좌표 4자리(약 11m)로만 판정해 카카오를 우선 남긴다(이름은 한/영으로 갈려 비교 불가). en+TourAPI만 있으면 TourAPI 단독, 카카오만 있으면 카카오 단독.
  - **en 병합 결과의 카카오 카드는 영문 주소 보강**(C2-a, `enrichEnglishAddresses` → 폴백 체인): 카카오는 한글 주소만 주므로 공식 영문 도로명주소를 채운다. **폴백 우선순위 juso(행안부 공식) → NCP**(`juso-address.ts`의 `geocodeEnglishAddressJuso` ?? `ncp-geocode.ts`의 `geocodeEnglishAddress`) — 둘 다 best-effort(HTTP·네트워크·에러코드 모두 null 흡수, throw 안 함)라 `??`로 합성하고, **각 provider는 자기 키가 있을 때만 호출**(빈 키 fetch 낭비 차단). 호출 게이트는 `hasJusoKey() || hasNcpMapsKeys()`(둘 다 없으면 enrich 자체 skip). juso 키 없으면 NCP 현행 경로 그대로(회귀 0). 둘 다 실패면 한글 주소로 graceful degrade. TourAPI 카드(kakao- 접두 아님)는 이미 영문 주소라 건드리지 않는다. UI는 영문을 메인·한글을 보조(`lang="ko"`)로 표시. juso `engAddr`은 NCP `englishAddress`와 달리 **국가명("Republic of Korea") 미포함**. 2026-06-19 실호출 검증(`/api/places?lang=en` 서울시청→juso 형식 영문주소).
  - **주소·우편번호 검색** (C2-b, `juso-address.ts`의 `searchJusoAddresses` + `/api/address/search` + `AddressResultList`): 카카오 POI 검색과 **보완**(POI=상호, juso=도로명/지번/우편번호 주소). **단일 검색창에서 장소·주소를 병렬 검색**(2026-06-19 토글 제거 — 검색 종류 토글이 미니멀 단일 검색창 레이아웃을 무너뜨려 UX 저하, 위원장 피드백). **세 진입점(타이핑 `runSearch`·음성 `handleTranscribed`·`?q=` 첫 마운트 자동검색)이 단일 `runQuerySearch`를 공유**해 `performSearch`(장소)와 `performAddressSearch`(juso)를 함께 발사하고, juso는 무료·무제한이라 매 검색 병렬 호출의 비용 부담이 없다. ⚠ **회귀 교훈(2026-06-19 수정)**: 통합 직후엔 이 "동시 발사"가 세 곳에 복붙돼 있었고 `?q=` 자동검색 경로만 `performSearch`만 부르고 주소 호출이 빠져, **URL `?q=` 진입·새로고침·언어전환 시 주소 섹션이 통째로 미노출**됐다(타이핑·음성은 정상이라 발견이 늦음). 단일 함수로 묶어 재발을 구조적으로 차단. node-env Vitest엔 컴포넌트 와이어링 테스트 레인이 없어 실호출(`?q=주소` 진입 시 주소 섹션 렌더)이 머지 게이트. 결과 화면은 두 섹션(장소·주소)을 **결과 있는 섹션을 위로**(`src/lib/search-sections.ts`의 `orderResultSections` — 빈 섹션 숨김·건수 내림차순·동률 시 장소 우선) 적응형 배치하고, **단일 섹션일 땐 구분 헤딩 없이**(흔한 장소-only 경험 보존) 둘 다일 때만 `h3`("장소"/"주소") 구분(이때 `ResultList`는 `headingLevel=4`로 카테고리 헤딩을 낮춰 outline 정합). 통지는 **단일 polite 채널 합산**(`combinedLiveMessage` — 둘 다면 "장소 N건, 주소 M건", 한쪽만이면 해당 통지, **장소 검색 에러는 무음 회귀 방지로 `placeErrored`→`search.error` 통지**), 포커스는 장소·주소가 **모두 settled된 뒤 1회만** 결과 헤딩으로(`focusedForSearchRef`). 주소 검색 에러는 장소 결과를 죽이지 않음(주소 0 취급·섹션 미렌더), `coordError`는 검색 완료와 시점이 달라 경합 없이 우선 노출. `canSearchAddress`(`hasJusoKey()`) 게이트로 juso 키 없으면 주소 호출·섹션·import 없이 순수 장소 검색(死기능·회귀 0). **좌표는 juso가 아니라 기존 `/api/geocode`(카카오) 재사용** — juso=공식 주소/영문/우편번호 정본, 카카오=좌표 정본 역할 분리. 항목 선택 → 카카오 지오코딩(limit=1) → `jusoAddressToPlace`로 `Place` 합성(`englishAddress`는 **en 로케일에서만** — ko UI 영문 누수 방지) → `openDetail`로 기존 상세 흐름(길찾기·교통) 진입. 좌표 변환 실패는 `coordError`로 통지하고 상세 미진입(graceful), in-flight ref로 중복 진입 차단. `searchJusoAddresses`는 검색 본류라 에러 throw(라우트 502), 무결과는 errorCode "0"+빈 배열. 캐시 `revalidate:3600`(준정적·무료/무제한이나 관행). 순수 로직(`orderResultSections`·`combinedLiveMessage`·`normalizeJusoResults`·`extractEnglishAddressJuso`·`jusoAddressToPlace`) fixture 테스트. 설계 `docs/superpowers/specs/2026-06-19-unified-place-address-search-design.md`(통합)·`2026-06-19-juso-official-address-design.md`(원 juso), 계획 `docs/superpowers/plans/2026-06-19-unified-place-address-search.md`. 2026-06-19 실호출 검증(세종대로 110→실주소 2건 영문·우편번호, 무결과 graceful, 빈query 400; 통합 후 dev 스모크 `/api/places` 경복궁 15건·`/api/address/search` 세종대로 110 2건·`/ko` 렌더 토글 0건). **프로덕션 배포 완료(2026-06-19)** — `JUSO_CONFM_KEY` Production 등록 + push 자동배포, `gildongmu.vercel.app/api/address/search`·`/api/places?lang=en` 실호출 검증.
  - 실데이터 호출 실패 시 mock으로 조용히 폴백하지 않는다(가짜 실데이터 금지).
- **좌표는 WGS84 십진 도로 통일**. 네이버 지역 검색의 `mapx/mapy`(×10⁷ 정수)는 provider 안에서만 존재. 카카오는 WGS84 그대로.
- **현재 위치는 공유 스토어 1곳에서만 획득** (`src/lib/geolocation.ts` 모듈 싱글턴 + `src/hooks/useGeolocation.ts`, 2026-06-19): 과거엔 "내 주변" 컴포넌트 5종이 각자 `navigator.geolocation.getCurrentPosition`을 버튼마다 호출해 **권한 팝업이 컴포넌트마다·누를 때마다 반복**됐다. 이제 `awaitGeolocation()`(멱등·inflight 가드·세션 캐시 `maximumAge:300s`)가 좌표를 1회만 획득해 모든 소비자가 공유한다 → 첫 1회 권한 획득 뒤로는 getCurrentPosition 재호출 없음(팝업 0). `PlaceSearch`가 **홈 마운트 시 `requestLocation()`을 1회** 호출(앱 시작 권한 요청)하고, 좌표는 `useGeolocation()`으로 파생해 거리 정렬에 쓴다. **신규 "내 주변" 기능은 getCurrentPosition을 직접 부르지 말고 `awaitGeolocation()`을 쓴다.** GeoState는 idle/locating/ready/denied/unsupported(거부·위치불가·타임아웃은 denied로 합침). useSyncExternalStore 패턴(서버 스냅샷 stable idle, hydration 안전), 순수 로직이라 navigator mock으로 결정적 테스트.
- **"내 주변" 패널은 닫기 버튼·Esc·아코디언으로 접는다** (2026-06-19 닫기버튼, 2026-06-20 Esc·아코디언): 6종 nearby 컴포넌트(지하철·버스·따릉이·소아진료·아이놀곳·둘러보기)의 done 섹션은 `actions.close` 버튼으로 idle 복귀해 펼친 내용을 다시 감춘다. 닫기 시 포커스를 트리거 버튼(`triggerRef`)으로 되돌려 스크린 리더 사용자가 맥락을 잃지 않게 한다(닫기 버튼이 언마운트되며 포커스가 body로 유실되는 것 방지). **dismiss 3경로**(2026-06-20): (1) 닫기 버튼 (2) PC Esc 키 (3) 다른 둘러보기 버튼을 누르면 펼쳐둔 패널이 자동으로 닫힘(아코디언, 한 번에 하나만). 구현은 **`src/lib/nearby-panel-store.ts`**(모듈 싱글턴 단일 출처 — "지금 어느 패널이 화면을 점유 중인가", geolocation.ts 동형 + useSyncExternalStore)와 **`src/hooks/useNearbyPanel.ts`**(active 구독·Esc keydown·자동닫힘·언마운트 cleanup). 각 컴포넌트는 `load()` 시작 시 `claim()`으로 점유를 가져가고(다른 펼친 패널이 active 변화를 구독해 스스로 닫힘), `close(restoreFocus)`로 통일. **포커스 비대칭이 핵심**: 직접 닫기·Esc는 `restoreFocus=true`로 trigger 복귀(사용자가 이 패널에 있었음), 다른 패널이 점유를 가져가 자동으로 닫힐 때는 `restoreFocus=false`로 포커스를 옮기지 않는다(방금 누른 다른 버튼에서 포커스를 빼앗지 않음). **`close`는 일부러 `release`를 부르지 않는다**(의도적 비대칭) — stale activeId는 닫힌 패널이 `engaged=false`라 무해하고, `close→release→claim` 순환 의존을 피한다. **BusArrivals/BikeStations는 `mode==="current"`(홈)일 때만** 아코디언 활성, `mode==="place"`(상세)는 단일 패널이라 `engaged=false`로 비활성. store 순수 로직 8개 단위테스트, 컴포넌트 와이어링은 node-env 테스트 레인이 없어 lint+build가 게이트.
- **내비게이션은 딥링크로 네이티브 앱 위임**: `src/lib/deeplink.ts`(nmap://), `src/lib/deeplink-kakao.ts`(kakaomap://). NCP/카카오내비 Directions는 자동차 전용이라 도보·대중교통 자체 구현 대상이 아니다.
- **자동차 경로 텍스트 브리핑** (`/api/route/car` + `CarRouteBriefing` 컴포넌트): 카카오모빌리티 directions의 `guides[].guidance`(완성된 한국어 안내문)를 낭독 정본으로 사용. 실주행 내비가 아니라 "출발 전 경로 미리 듣기" — 실주행은 딥링크 위임 원칙 유지.
- **영문 자동차 경로** (C1, `ncp-directions.ts` provider): 카카오모빌리티는 **ko 안내문 전용**이라 외국인(en)에게는 한국어가 낭독된다. `/api/route/car`가 **`lang`+키 유무로 provider 디스패치** — `lang=en`+NCP 키면 **NCP Directions(5)** `lang=en` 영문 턴바이턴(`instructions`: "Turn right toward 'Cheongpa-ro'"), 그 외(ko, 또는 en이지만 NCP 키 없음)는 카카오 한국어로 graceful 폴백. 두 provider가 **동일 `CarRouteBriefing` shape** 반환 → `CarRouteBriefing` 컴포넌트는 불변(`useLocale()`로 `lang` 쿼리만 추가). **⚠ 단위 함정**: NCP `duration`은 summary·guide 모두 **밀리초**인데 카카오·`durationSeconds`는 **초** → `normalizeNcpRoute`가 `/1000` 변환(미변환 시 28분→468시간 오발화). NCP guide엔 카카오의 `name`(랜드마크)+`guidance` 2필드가 없고 `instructions` 단일 문장만 있어 `guidance←instructions`·`name←""`. 헤더는 ncp-geocode 동형(`x-ncp-apigw-api-key-id`/`-key`), option=trafast, no-store. `code!==0`/경로없음→throw("경로 탐색 실패" 토큰으로 라우트가 입력성 오류 분류). 2026-06-17 실호출 검증(en 영문 14단계·ko 한국어 8단계, durSec 1559 단위 정합). 에러 메시지는 라우트가 ko/en 모두 한국어 문자열 반환(기존 동작, en 회귀 아님 — 후속 개선 여지).
- **버튼 비활성화는 `disabled` 대신 `aria-disabled` + 핸들러 가드** — `disabled`는 포커스를 제거해 스크린 리더 사용자가 맥락을 잃는다 (a11y 감사 반영, 2026-06-13). 단 `aria-disabled`만으로는 빠른 더블클릭/Enter 반복 시 같은 렌더의 클로저 가드가 중복 호출을 못 막으므로, 비동기 트리거에는 **in-flight ref 가드**(`useRef(false)` + `finally` 해제)를 병행한다(codex 리뷰 반영, 2026-06-14).
- **검색 → 결과 → 장소 상세 흐름 (v2, 2026-06-14)**: 입력은 검색창 하나. 결과는 카테고리 버킷 칩으로 필터(`ChipFilter`), 장소를 고르면 **같은 페이지 내 뷰 전환 + History API**로 상세(`PlaceDetail`)를 연다 — 카카오 로컬은 ID 단건 조회가 없어 메모리의 `Place`로 상세를 그린다. `openDetail`이 `pushState`로 백버튼 포착용 trap 엔트리를 쌓고, `popstate`가 단일 수렴점으로 목록 복귀 + 결과 헤딩 포커스 이동을 담당(딥링크 상세 복원은 비목표). 검색은 `?q=` URL 동기화 + request-id ref로 stale 응답을 버린다. 상세에 길찾기 딥링크·자동차 브리핑·(역이면)역 편의시설을 집약.
- **역 교통약자 편의시설** (`korail-facilities` provider + `/api/station/facilities` + `StationFacilities`): 철도공사 API(15125774)가 역명 필터를 무시해 406역 전체를 받아 `normalizeStationName`으로 클라이언트 매칭(일 1회 revalidate). 교통약자(`/weekPersonFacilities`)와 엘리베이터(`/stationFacilities`)를 **`stn_cd` 조인**(역명 조인은 동명이역 혼입 위험). **정본 정확성**: "0대"와 "정보 없음"을 뭉개지 않음(`num→number|undefined`), 주 데이터 upstream 장애는 throw→502(미커버 `null`과 구분). 도시철도(지하철)는 미포함이라 매칭 실패=graceful degrade.
- **서울 지하철역 교통약자 시설** (`seoul-metro-facilities.ts` provider + `/api/station/metro-facilities` + `SeoulMetroFacilities`): 코레일 편의시설의 **도시철도(서울 1~8호선) 공백 보완**. data.go.kr 서울교통공사_교통약자이용정보(15143843, `apis.data.go.kr/B553766/wksn`, `DATA_GO_KR_API_KEY` 공유), **9 오퍼레이션**(엘리베이터·에스컬레이터·휠체어리프트·무빙워크·휠체어급속충전기·안전발판·수어영상전화기·도우미·장애인화장실). 코레일과 동형(`parseStationItems` 재사용, 주 fetch 장애 throw→502, 키없음/전종류빈결과 null). **시설마다 필드가 달라** 공통 코어(name)+시설별 정규화(위치 `dtlPstn`/`bgngFlrDtlPstn`·층 `bgngFlr~endFlr`·가동현황 `oprtngSitu`·화장실 `rstrmInfo` 등)로 투영 — 카운트가 아니라 **위치·층·가동현황 인스턴스 목록**(시각장애인 정합). **`stnNm`은 "포함" 필터**라(강동→강동구청 혼입) `normalizeStationName` 정확매칭으로 제외. **`oprtngSitu` M=정상·그 외(S)=점검·중지** 보수 매핑(과소경고 회피). 9 오퍼레이션 `Promise.all` 병렬, **`totalCount>numOfRows(300)`면 throw**(포함필터 환승역 silent truncation 방지). 2026-06-17 실호출 검증(강동 4종·신도림 18시설·서울역 23시설·없는역 null). 도시철도 외 역=graceful degrade.
- **전국 도시철도역 메타** (A3, `subway-stations.ts` + `/api/station/meta` + `StationMeta` 컴포넌트): 1,098개 도시철도역의 **한/영/한자 역명 + WGS84 좌표 + 노선·환승·운영기관·도로명주소**. 출처는 전국도시철도역사정보 표준데이터(공공데이터포털 15013205)인데 **OpenAPI가 아니라 국가철도공단 레일포털(`data.kric.go.kr`)이 연 1회 갱신하는 XLSX 파일데이터**(차기 2026-12). 활용신청·키 게이트 없음 → **정적 seed 번들**이 정답. `scripts/build-subway-stations.py`(XLSX→JSON 변환기, 헤더 정합 가드·좌표 필수)로 `src/lib/data/subway-stations.json`(307KB) 생성, 갱신 시 최신 XLSX로 재실행. `subway-stations.ts`는 **순수 로직(데이터 주입형)**(`matchStationsByName` 정규화 정확매칭·`nearestStations` Haversine 근접·`summarizeStation` 노선 집계)과 **seed 바인딩 공개 API**(`findStationsByName`·`findStationsNear`·`findStationMeta`)를 분리 — 순수 로직은 fixture로 결정적 테스트(연간 seed 갱신과 독립). seed는 **서버 전용 import**(클라 번들 제외)라 `/api/station/meta` 경유. 장소 상세가 역이면(`isStation`) `StationMeta`가 진입 시 자동 fetch해 **영문역명·노선·환승을 표시**(en 로케일은 영문역명 메인·한글 보조, A1/A2 받침대 + 외국인 정합 실현). 매칭 실패=조용히 숨김(보조 정보, live region 불필요). 환승역은 같은 역명 다중 노선 행을 `lines`로 집계. 2026-06-17 실호출 검증(강남 2호선+신분당선·서울역 1·4·공항철도·부산역·대전 전국 커버·고속터미널 3·7·9 환승·없는역 null). **A2(서울 지하철 실시간)는 운영 중**(2026-06-17, 아래 `seoul-subway-arrival.ts`) — 역 식별은 이 seed가 받침대.
- **TAGO 시내버스** (`tago-bus.ts` provider + `/api/bus/*` + `BusArrivals`/`BusRouteStops`): 국토부 TAGO 3종(근접정류소 15098534 → 정류소별 도착예정 15098530 → 노선 경유정류소 15098529, `DATA_GO_KR_API_KEY` 공유). 좌표 근접 정류소를 받아 Haversine로 거리 정렬(A-2가 거리순 미보장 — 산술은 코드 책임), 도착예정의 `vehicletp`로 **저상버스 정본**(`includes("저상")`) 판정. data.go.kr 표준 envelope·빈결과 `items:""`는 코레일 편의시설과 동일 가정(2026-06-14 실호출로 구조·필드명 일치 검증). **⚠ 서울 시내버스 미수록**(서울은 TOPIS 별도 운영) — 경기·지방·부산만 커버, 서울에선 정류소 0건이라 안내 메시지(`bus.empty`)로 graceful degrade. 서울 버스는 다음 마일스톤(키 전파 대기 — 아래 표 `SEOUL_OPEN_DATA_KEY` 참조).
- **따릉이 서울 공공자전거** (`seoul-bike.ts` provider + `/api/bike/nearby` + `BikeStations`): 서울 열린데이터광장 `bikeList`(OA-15493, `SEOUL_OPEN_DATA_KEY`). `openapi.seoul.go.kr:8088/{KEY}/json/bikeList/{start}/{end}/`. **bikeList엔 좌표 필터가 없어 전체(~2,720) 페이지 루프로 받아 서버 Haversine 정렬→1km cap→상위 5**. ⚠ 실측: `list_total_count`는 "전체 수"가 아니라 "그 페이지 row 수"라 **종료 조건은 "받은 row 수 < 1000"**(필드 신뢰 금지 — 버스 `totalCount` 교훈과 동형). envelope `rentBikeStatus.RESULT.CODE`(`INFO-000`만 정상, 빈결과도 `INFO-000`+`row:[]`라 그 외·RESULT부재는 throw→502)·`row[]`(`stationName` 번호접두 포함·`parkingBikeTotCnt`=대여가능·`rackTotCnt`=거치대). **캐시 60초 revalidate**(버스 no-store와 달리 전체목록 호출이 비싸고 분 단위 변동이라 허용). `canShowBike`(`hasSeoulOpenDataKey()`) 게이트로 장소상세·홈 두 곳 노출(BusArrivals와 동형 a11y). 2026-06-16 실호출 검증(길동 마루빌딩 236m 등 5건, 대여가능 실시간 반영).
- **내 주변 소아 야간·휴일 진료** (B1, `night-clinic.ts` provider + `/api/clinic/nearby` + `NightClinicsNearby`): NMC 15000736 **`getBabyListInfoInqire`(달빛어린이병원·소아전문센터 목록)** — `apis.data.go.kr/B552657/HsptlAsembySearchService`, `_type=json`, `DATA_GO_KR_API_KEY` 공유. **가족→dodo 다리 1순위**(여행 중 아이 아플 때 안전망). **소스 결정의 데이터 현실**: 연구 문서가 지목한 15001674(HIRA 소아야간)는 **좌표가 없어** 좌표 근접 불가 → 좌표+요일별 진료시간+전화를 다 주는 NMC 달빛 목록이 정본(2026-06-17 실호출로 규명). **전국 ~152개로 적어 한 번에(numOfRows=200) 받아 서버 Haversine 정렬→20km cap→상위 5**(따릉이 패턴, 좌표/반경 파라미터 없음). 목록은 분단위 변동 없어 하루 캐시(`revalidate:86400`). **진료 상태 3-state(open/closed/`unknown`)**: 라우트가 **서버 KST(+9) 요일·시각**으로 `clinicOpenStatus` 계산해 부여 — 해당 요일 `dutyTime{1..8}`(1=월…7=일·8=공휴일)이 비거나 `0000~0000`이면 **"마감"이 아니라 "정보 없음"**(시각장애인 오판 방지, arrivalStatus·metro "0대≠정보없음" 교훈). 종료 `2400`=자정. **공휴일 자동판정은 V1 비포함**(한국 공휴일=음력 포함 비결정적 → 가짜 판정 금지, 공휴일 진료시간은 표시만). 좌표 누락 행 skip·`totalCount>numOfRows` throw(silent truncation 방지). **mock 폴백 없음**(의료 정보 — 키 없음→빈배열·섹션 미노출, upstream 장애→502). `canShowClinic`(`hasDataGoKrKey()`) 게이트로 홈 "내 주변" 4번째 섹션(지하철→버스→따릉이→소아진료). 전화는 `tel:` 링크 1탭 통화. 순수 로직(parseClinics·rankClinicsByDistance·clinicOpenStatus·dayToHoursIndex)은 fixture 결정적 테스트 22개. 설계 `docs/superpowers/specs/2026-06-17-nearby-night-clinic-design.md`. 2026-06-17 실호출 검증(강동 길동→새솔어린이병원 등 5곳 거리순·진료중 상태·부산 빈배열·범위밖 400).
- **이 지역 공기질** (B2, `air-quality.ts` provider + `/api/air-quality/nearby` + `AirQuality` 컴포넌트): 에어코리아 15073877·15073861(`apis.data.go.kr/B552584`, `DATA_GO_KR_API_KEY` 공유). 장소 좌표 → 가장 가까운 측정소의 통합대기환경지수(KHAI)·미세먼지(PM10)·초미세먼지(PM2.5)를 **장소 상세 + 홈 idle 양쪽에 노출**(영유아 외출·시각장애인 이동 판단). **홈(idle) 현재 위치 자동 카드**(2026-06-20): `PlaceSearch`가 보유한 `userCoords`(useGeolocation ready)를 `AirQuality`에 넘겨 "내 주변 공기질"을 버튼 없이 자동 표시 — `AirQuality`는 `{lat,lng}` props 컴포넌트라 장소 상세와 **동일 컴포넌트 재사용**(인터페이스 변경 0), region 랜드마크도 그대로(자동 등장 섹션 정책 부합). 좌표 미준비(권한 전/거부)면 미노출(graceful), 홈 마운트 시 `requestLocation()`이 좌표를 확보하므로 곧 등장. 내 주변 버튼 6종(지하철~둘러보기)과 달리 **버튼 없이 자동 등장하는 카드**이며, 위치는 **nearby 그룹 맨 아래(둘러보기 버튼 아래)**(위원장 선호, 2026-06-20 — 렌더 순서만 조정, 컴포넌트·region 구조 불변). **⚠ 좌표계 함정**: 측정소 API만 **TM중부원점 좌표**를 요구(앱 전역 WGS84와 다름) → `proj4` **EPSG:2097**(Bessel+towgs84) 변환. 카카오/네이버가 쓰는 EPSG:5181(GRS80)이 아님 — false E/N이 같아 혼동하기 쉬우나 결과 Δ300m+(역삼동 정본 `getTMStdrCrdnt`값 대조로 확정). **2-call 체인**: `getNearbyMsrstnList(tmX,tmY)`(API가 거리순 정렬→첫 항목, 자체 Haversine 불필요) → `getMsrstnAcctoRltmMesureDnsty(stationName,dataTerm=DAILY)`(측정소명 단건이 시도별보다 깔끔). **⚠ envelope 차이**: 에어코리아(B552584)는 다른 data.go.kr 서비스(`items.item`)와 달리 **`body.items`가 직접 배열**(실응답 검증, extractItems가 두 형태 모두 처리). **3-state(좋음/보통/나쁨/매우나쁨/unknown) — Flag→unknown**: `pm10Flag`/`pm25Flag`(측정 장애)가 non-null이면 value:null·grade:"unknown" 강제, 컴포넌트는 grade unknown 시 "정보 없음"+수치 숨김(해석 불가한 숫자 노출 금지 — arrivalStatus·B1 unknown 교훈). 등급 단어가 낭독 정본, 수치는 보강. 등급 1좋음·2보통·3나쁨·4매우나쁨. 거리는 API `tm`(km) 정본. **mock 폴백 없음**: 키 없음→`{air:null}`·섹션 미노출, 근접측정소/측정데이터 없음→null(graceful), upstream 장애(resultCode≠00·HTTP실패)→throw→502("조회 실패"≠"정보 없음"). 캐시: 측정소 목록 `revalidate:86400`(거의 불변)·실시간 측정 `revalidate:600`(시간 단위 갱신). `canShowAir`(`hasDataGoKrKey()`) 게이트, `StationMeta` 동형 자동 fetch(보조 정보라 실패/미커버는 조용히 null, live region 불필요). 순수 로직(wgs84ToTm·parseNearestStation·parsePollutant·parseAirMeasure·gradeFromCode)은 fixture 결정적 테스트 19개. 설계 `docs/superpowers/specs/2026-06-17-air-quality-design.md`. 2026-06-17 실호출 검증(강동 길동→천호대로 0.5km KHAI 229 나쁨·부산→전포동 0.1km·범위밖 400).
- **서울 지하철 실시간 도착** (A2, `seoul-subway-arrival.ts` provider + `/api/station/subway-arrival` + `SeoulSubwayArrival`): 서울 열린데이터광장 OA-12764(`http://swopenapi.seoul.go.kr/api/subway/{KEY}/json/realtimeStationArrival/0/20/{역명}`). **TAGO 미커버 도시철도(서울 1~9호선+연계)의 실시간 정본**, A3 seed의 역 식별을 받침대로 "다음 열차/현재 위치"를 텍스트 낭독. **키는 `SEOUL_SUBWAY_REALTIME_KEY` — 일반키(`SEOUL_OPEN_DATA_KEY`)와 별도 "실시간 데이터 인증키" 계열**(일반키는 `ERROR-338`). **응답 envelope가 정상/에러에서 다름**: 정상 `{errorMessage:{code:"INFO-000"},realtimeArrivalList:[...]}`(중첩 code) / 데이터없음 `{status:500,code:"INFO-200"}`(최상위 code, 래퍼 없음) → `resultCode()`가 `errorMessage?.code || code`로 두 위치를 모두 읽음. **INFO-000 파싱·`INFO-200`→null(미커버 graceful)·그외 코드/HTTP실패→throw→502**(seoul-metro-facilities 정책 동형 — 시각장애인에게 "정보 없음"≠"조회 실패"). `arvlMsg2`(완성 한국어 도착 문장 "강남 진입"·"전역 출발"·"[3]번째 전역" — **낭독 정본**)·`barvlDt`(초)·`bstatnNm`(종착, `trainLineNm`에 이미 포함)·`subwayId`→노선명 매핑표(1002=2호선·1077=신분당선·1063=경의중앙·1065=공항철도, 미매핑은 graceful undefined)·`updnLine`(상/하/내/외선)·`btrainSttus`(급행). **실시간이라 캐시 안 함**(fetch `no-store` + route `force-dynamic`), 컴포넌트는 BusArrivals 동형(자동폴링 없이 수동 새로고침 + 조회시각 `asOf`). 도착 항목은 **평면 리스트**로 각 li 자체완결(환승역은 노선이 섞여 방향 그룹핑이 외려 혼란 — 미니멀 접근성). `canShowSubway`(`hasSeoulSubwayRealtimeKey()`) 게이트로 키 없으면 버튼 미노출. en 영문 역명은 swopenapi 미인식→graceful 숨김(한계). 2026-06-17 실호출 검증(강남 2호선+신분당선·서울역 1·4·경의중앙·공항철도 20건·부산/없는역 null).
- **내 주변 지하철 실시간 도착** (A2 홈 진입점, `subway-nearby.ts` + `/api/station/subway-arrival/nearby` + `SubwayArrivalsNearby`): 홈(idle)에 **지하철→버스→따릉이** 순 "내 주변" 3종 중 첫 항목. 버스/따릉이 nearby는 좌표를 그대로 API에 넘기지만, **서울 지하철 실시간은 역명 기반**이라 좌표를 직접 못 쓴다 → A3 정적 seed `findStationsNear`(Haversine, 반경 1km·dedupeByName·상위 3역)로 근접역을 식별한 뒤 역별 `fetchSubwayArrivals`를 **`Promise.allSettled` 병렬** 호출해 합성한다. **부분 실패 불변식**(`BusStop.arrivalStatus` 동형): 근접 3역 중 일부 실시간 조회가 throw해도 나머지 실데이터는 보존하고 그 역만 `arrivalStatus:"unavailable"`("조회 실패"와 "도착 열차 없음"을 뭉개지 않음 — 시각장애인 정본). **fulfilled & null(비서울 INFO-200)은 제외**(부산 등은 seed 근접역이 잡혀도 합성 단계에서 숨김→빈 섹션 graceful), **시도가 전부 실패해야 throw→502**(전부 null이면 정상 빈 배열). 순수 로직 `buildNearbyArrivals`(settled→역별 투영, 전부실패 throw)와 seed+fetch 합성 `fetchNearbySubwayArrivals`를 분리해 부분/전체 실패를 seed·네트워크 없이 결정적 테스트. 도착 렌더는 상세(`SeoulSubwayArrival`)와 공유하는 `SubwayArrivalList`로 DRY(드리프트 차단). 키는 A2와 동일(`SEOUL_SUBWAY_REALTIME_KEY`), 쿼터는 사용자 액션당 근접역 수(≤3)만큼 호출. 2026-06-17 실호출 검증(강남 좌표→강남 31m·신논현 726m·역삼 834m 각 실시간 도착, 부산 좌표→빈 배열, 좌표 범위 밖→400).
- **근처 아이 놀 곳** (B3, `kids-places.ts` provider + `/api/places/kids` + `KidsPlacesNearby`): 기존 **카카오 로컬 키워드 검색**에 좌표(`x`/`y`/`radius`/`sort=distance`)를 더한 좌표 근접 — **신규 API·게이트 없음**(기존 `KAKAO_REST_API_KEY` 재사용, `canShowKids`=`hasKakaoKey()`). 홈(idle) "내 주변" 5번째 섹션(지하철→버스→따릉이→소아진료→**아이 놀 곳**). 키워드 3종(키즈카페·놀이터·어린이공원) `Promise.allSettled` 병렬→dedupe(카카오 id)·거리 재정렬·상위 8. **⚠ 핵심 데이터 현실 — 키워드 매칭 ≠ 키즈 장소**(실호출로 규명, 정적 리뷰 불가): "놀이터" 키워드 검색에 스킨스쿠버·노인복지시설·동우회·방탈출카페·당구장·도서관·미술학원이 섞여 나옴. **시각장애인은 화면으로 노이즈를 못 거르므로** 키워드 신뢰가 아니라 **`category_name` 계층 화이트리스트**(`classifyKidsPlace`)가 정본: `유아 > 놀이시설`(키즈카페/놀이터)·`유아교육 > 놀이교육`(플레이타임, **substring 아닌 계층 앵커링** — `학원>놀이교육학원` 오통과 방지)·`공원`+이름신호(`어린이`/`놀이`/`유아`, 일반 근린공원 차단). **실내/실외 3-state**: 키즈카페·놀이센터=실내·공원=실외·**놀이터는 모호→이름 신호(`실내`/`공원·자연·산·광장`) 없으면 unknown**(잘못된 단정 금지 — B1·B2 unknown 교훈), V1은 라벨만(우천 자동필터 비포함, 사용자가 듣고 판단). **부분 실패 불변식**(subway-nearby 동형): 일부 키워드 실패해도 보존, 전부 실패만 throw→502("조회 실패"≠"근처 없음"). 거리는 카카오 `distance`(m) 정본, 좌표 WGS84 그대로. 자기완결 정보 리스트(상세 연동 비포함)+전화 `tel:` 링크(키즈카페 예약, clinic 동형)+카카오맵 링크. 33개 단위테스트(거짓양성 reject·3-state·dedupe/정렬/cap·부분실패). code-reviewer 5건 검토(놀이교육 앵커링·전화링크 2건 수정, 캐시주석·park 테스트 2건 보강, `role=status`+`aria-live` 중복은 형제 4컴포넌트 동형이라 일괄정리 보류). 설계 `docs/superpowers/specs/2026-06-18-kids-places-design.md`. 2026-06-18 실호출 검증(강동 길동 8건 노이즈 0·부산 서면 playcenter/kidscafe/playground·범위밖 400).
- **내 주변 둘러보기** (기능 A, `surroundings.ts` provider + `/api/places/around` + `SurroundingsNearby`): BlindSquare·Soundscape의 *"내 주변에 뭐가 있는지" 상시 인지(ambient awareness)* 차용. 카카오 로컬 **카테고리 검색**(`category_group_code` 10종: 편의점 CS2·지하철역 SW8·음식점 FD6·카페 CE7·은행 BK9·약국 PM9·병원 HP8·마트 MT1·공공기관 PO3·관광명소 AT4) 좌표 근접(`x`/`y`/`radius=500`/`sort=distance`) `Promise.allSettled` 병렬 → dedupe(카카오 id)·거리 재정렬·상위 12. **신규 핵심 = 방향**: 두 좌표 간 **북 기준 8방위**(`src/lib/geo/bearing.ts` 순수: `bearingDegrees`·`bearingToCompass8`·`haversineMeters`)를 거리와 함께 낭독("편의점 · 남동쪽 · 약 40m"). ⚠ **heading 미보유라 "2시 방향"식 정면-상대 표기 금지** — 사용자가 바라보는 방향을 모르므로 방위(북/북동/…)로 정직 표기(spec §5-4). 거리는 카카오 `distance`(m) 정본, 누락 시 haversine 폴백. **신규 API·게이트 없음**(기존 `KAKAO_REST_API_KEY`, `canShowSurroundings`=`hasKakaoKey()`). 홈(idle) "내 주변" 6번째 섹션(지하철→버스→따릉이→소아진료→아이놀곳→**둘러보기**). 부분 실패 불변식(kids-places 동형, allSettled·전부 실패만 throw→502). 캐시 300초. kids-places가 키워드+화이트리스트로 노이즈를 걸렀다면 이쪽은 `category_group_code` 검색이라 카테고리 정제됨(미매핑 코드 거부). 카카오 장소명은 `lang="ko"`. bearing 17 + provider 9 단위테스트. 설계 `docs/superpowers/specs/2026-06-20-surroundings-awareness-design.md`, 계획 `docs/superpowers/plans/2026-06-20-surroundings-awareness.md`. **기능 B(OSM 횡단보도·점자블록 + data.go.kr 음향신호기)는 후속 마일스톤** — Overpass 실호출 검증(2026-06-20)으로 길동 OSM 보행 태깅 희박(음향신호기 0·보도 0) 확인, OSM은 카카오가 비운 칸만 채우는 보완재. ⚠ Overpass는 User-Agent 누락 시 406.
- **대중교통 길찾기** (`odsay.ts` provider + `/api/route/transit` + `TransitRouteBriefing`): ODsay 대중교통 길찾기(`searchPubTransPathT`) — 출발→도착 버스+지하철 환승 경로를 텍스트 브리핑("출발 전 미리 듣기", 자동차 브리핑 동형, **딥링크 위임 원칙을 자체 텍스트로 확장**, 실주행은 여전히 딥링크). 입력은 **현재위치 기본 + "출발지 바꾸기" 인라인 검색**(기존 `/api/places` 재사용), 결과는 **추천경로 1개 + 대안 펼치기**. provider가 ODsay `path/subPath`를 자체 `TransitRoute` shape로 정규화해 종속을 격리(컴포넌트·route는 ODsay 필드 모름, 이식성). **2026-06-18 실호출로 실계약 확정**: `subPath.trafficType` 1=지하철/2=버스/3=도보, 지하철 `lane.name`("수도권 5호선")·버스 `lane.busNo`("30-3"), 단위 `totalTime`/`sectionTime`=분·`payment`=원·`totalWalk`=미터. **⚠ 환승 도보는 `{distance:0, sectionTime>0}`(역내 통로)** → leg 리스트에서 제외(노선 전환·`transfers`로 표현)하되 `walkMinutes`(총 도보)는 환승 통로 시간까지 전체 합으로 정직 산출. **3-state**(경로없음≠조회실패): 경로 없음/error `-98`("출,도착지 700m 이내")→graceful null(라우트 200 `{result:null}`), 그 외 error/HTTP 실패→throw→502, **분기는 순수 `normalizeOdsayRoute`에 단일화**(getTransitRoute는 I/O만, 테스트 가능). en은 t.rich `<line>/<from>/<name>` 빈태그 핸들러로 **고유명만 `lang="ko"`**(구조는 영문, "Board 5호선 at 길동, N stops"). `canShowTransit`(`hasOdsayKey()`) 게이트, **mock 없음**(실데이터 원칙). 캐시 `revalidate:3600`(준정적·1,000회/일 쿼터 보호). **⚠ ODsay Server 방식은 공인 IP 화이트리스트라 Vercel 서버리스(가변 IP) 프로덕션은 미해결**(개발 머신 IP만 등록 — 별도 마일스톤). 순수 로직 8 단위테스트(추천/대안 분리·환승도보 skip·walkMinutes 전체합·버스/지하철 투영·transfers·error -98 null/그외 throw). 설계 `docs/superpowers/specs/2026-06-18-odsay-transit-routing-design.md`. 2026-06-18 실호출 검증(강동 길동→강남 33분·환승 2·도보 8분·대안 2(버스 30-3 등)·가까움 graceful null·범위밖 400).
- i18n: next-intl, **5개 언어**(`/ko` `/en` `/es` `/fr` `/it`, dodo-planet과 동일) 경로 프리픽스, 메시지는 `messages/*.json`. **로케일별 단일 언어**(혼용 제거) + 언어 선택 버튼(`LanguageSwitcher`, 2026-06-19 재작성)이 경로·`?q=`(`replaceState` 후 커스텀 이벤트로 동기화)·`NEXT_LOCALE` 쿠키를 보존. SSG hydration 안전(`useSyncExternalStore`, 서버 스냅샷 `""`).
  - **언어 선택 UI는 disclosure 메뉴**(버튼 `aria-expanded` + 링크 목록) — 5개를 가로 나열하면 미니멀 레이아웃이 무너져 버튼을 눌러 펼친다. `role="menu"`의 화살표 키 모델 대신 네이티브 `<Link>` 목록(Tab 순회)을 쓰고(미니멀 접근성·dodo는 Popover지만 gildongmu는 의존성 0), Esc 닫기+버튼 포커스 복귀, 포인터/포커스가 컨테이너(`<nav>`) 밖으로 나가면 자동 닫힘. 국기 이모지 금지(UI 라벨 이모지 규칙)라 각 언어를 자국어 텍스트로 표기(`lang` 속성으로 SR 음성 엔진 정합), 현재 언어는 `aria-current="page"`+Check 아이콘(`aria-hidden`).
  - **UI 언어 ≠ 데이터 언어 분리** (`src/lib/data-locale.ts`의 `dataLocale`/`prefersEnglish`): 외부 API(카카오·TourAPI·NCP·juso·카카오모빌리티)는 ko/en만 제공하므로 **비한국어 로케일(en/es/fr/it)은 영문 데이터를 공유**한다 — `dataLocale(locale)=locale==="ko"?"ko":"en"`이 (1) `/api/places` zod enum(ko\|en) 400 (2) 동일 영문 데이터의 로케일별 캐시 분절을 함께 막는다. UI 텍스트는 5개 언어 모두 번역되지만 장소명·주소·역명의 비한국어 표기는 영문(데이터 현실). **외부 데이터 fetch·영문표기 분기에는 절대 `useLocale()` 원시값을 직접 쓰지 말고 `dataLocale`/`prefersEnglish`를 거친다**(places·car·transit fetch, jusoAddressToPlace, AddressResultList·StationMeta 영문 분기 — 신규 추가 시 동일 적용). **예외: STT(Deepgram nova-3)는 es/fr/it를 직접 인식**하므로 매핑 없이 실제 로케일 전달(`stt-validate`의 `SUPPORTED_LOCALES` 5개·`SttLocale`). 메시지 키 일관성은 `i18n-messages.test.ts`(ko 기준 키 집합·ICU 플레이스홀더·t.rich 태그 동일)가 머지 게이트.
- **음성 받아쓰기 (v2.1, dodo-planet 수입)**: 검색창 마이크 버튼(`VoiceRecordButton`, 탭-토글)이 `useVoiceRecorder`(MediaRecorder)로 녹음 → `/api/speech-to-text`(Deepgram **nova-3**, **로케일 명시** `language=ko/en`) → 전사 텍스트로 자동 검색. 파서·파라미터 빌더는 `src/lib/deepgram.ts`(순수, `buildDeepgramParams`). **⚠ STT 결함 교훈(2026-06-19, 실호출 확정)**: dodo-planet에서 수입한 `detect_language=true`(다국어 자동감지)가 **한국어 발화를 베트남어(vi)로 오인식해 빈 전사("")를 반환** → 받아쓰기 전면 실패("음성 인식에 실패")했다. gildongmu는 next-intl 로케일을 알고 라우트에 `locale`을 넘기므로 자동감지(latent·오류 가능)를 버리고 `language`를 명시(deterministic)해 오인식 경로 자체를 제거했다. 모델도 `nova-2-conversationalai`(deprecated→nova-3로 silent 라우팅)에서 현행 `nova-3`로 명시(ko 정확도도 우위: nova-2 "길동 맛집 검색" vs nova-3 "강동구 길동 맛집 검색"). 회귀 방지 단위테스트(`buildDeepgramParams`가 detect_language 미사용·language 명시 검증) + dev 서버 실호출 머지 게이트(`강동구 길동 맛집 검색` 정상 전사) 통과. 마이크 권한은 별도 사전요청 없이 `startRecording`의 `getUserMedia` **단일 경로**로 처리 — 네이티브 프롬프트가 뜨고, 거부/실패를 `NotAllowedError` 기준으로 `mic_denied`/`mic_failed`로 분류한다(getUserMedia 중복 호출·오분류 제거, codex 잔여리뷰 반영 2026-06-15). 자원 안전: `getUserMedia` 직후 언마운트 가드(마이크 스트림 누수 차단)·recorder 셋업 실패 시 트랙 정리·in-flight ref·`AbortController`·`mountedRef`. **접근성**: 전사·검색 통지는 polite 단일 채널로 순차화(전사 텍스트는 부모 PlaceSearch가 통지), 훅 오류는 **코드→로케일 번역**(외국어 사용자도 자국어 오류, 한국어 하드코딩 금지), 미지원/거부 graceful. **시작/정지/취소는 스크린 리더 음성 안내 대신 효과음**(`useRecordingSound`+`recording-tones.ts`, 2026-06-19 — 매 토글마다 장황한 음성이 거슬린다는 위원장 피드백): Web Audio 합성(상승음=시작·하강음=정지·단음=취소, mp3 의존 0, dodo의 파일 기반 `useSound`를 합성으로 적응)으로 비언어 신호를 주고, 버튼의 `aria-label`/`aria-busy` 상태 변화가 SR용 보강 신호다(효과음 미지원/음소거 대비). **단 Esc 취소만 효과음+`voice.cancelled` polite 통지 병행** — Esc는 전역 keydown이라 포커스가 버튼 밖일 수 있어 음소거 시 인지 사각이 생기므로(시작/정지는 사용자가 버튼을 직접 눌러 포커스가 버튼에 있음). 효과음 합성은 `AudioContext` lazy 생성(제스처 후)·언마운트 close·`closed` 상태 가드+`try/catch`(빠른 unmount/remount 시 `InvalidStateError` graceful). 오류 통지용 assertive announcer만 유지. dodo 의존(sonner·cn·모달)은 제거하고 aria-live·토큰으로 적응.
- **PWA (v2.1)**: `app/manifest.ts`(standalone·theme_color accent·아이콘) + **수제 서비스워커**(`public/sw.js` — Serwist `@serwist/next`가 Next 16 Turbopack 미지원이라 폴백). document는 network-first(stale 페이지 금지), **API/검색은 비캐시**(`/api/` early-return — 가짜 캐시 금지), 로케일별 오프라인 폴백(`/ko/offline`·`/en/offline` 둘 다 precache), 캐시 버전드+`skipWaiting`/`clients.claim`. `SWRegister`는 프로덕션 한정 등록.
- **채팅 인터페이스** (`src/lib/chat/*` + `src/lib/gemini/*` + `/api/chat` + `src/components/chat/*` + `ModeToggle`, 2026-06-20): Gemini function-calling 채팅 모드 — 13개 외부 서비스 도구를 자연어로 호출. dodo-planet 아키텍처 이식(`executeFunction` 라우터 + `declarations` + `ExecutionContext`)이라 `src/lib/chat`·`src/lib/gemini`는 **React 비의존**(dodo 이식성). 검색⇄채팅 **모드 토글**(헤더 버튼 1개 + `Ctrl+Shift+C`/`Ctrl+Shift+S` + `Shift+Esc` 편집창, `keyboard-shortcuts.ts`·`mode-state.ts`), `canShowChat`(`hasGeminiKey()`) 게이트로 키 없으면 토글·단축키·ChatInterface 미등록(순수 검색 회귀 0, mode는 SSR 안전 useEffect 초기화).
  - 엔진: `@google/genai`, `GEMINI_MODEL=gemini-3.5-flash`(dodo 메인, FC eval 96%). `/api/chat`은 **generateContent 2-pass**(`availableDeclarations()` 주입 → `parts`에서 `functionCall` 필터 → `executeFunction` → model content history push(Gemini3 규약) → `functionResponse`엔 **summary만**(render는 클라 응답으로만 — PII 누출 차단) → 산문). 키 없으면 502 `chat_unavailable`, Gemini 에러 502 `chat_failed`.
  - ⚠ **옵션 C — self-fetch 컴포넌트 파라미터 마운트**(위원장 결정 2026-06-20): nearby/리치 컴포넌트(`SubwayArrivalsNearby`·`BusArrivals`·`AirQuality`·`StationMeta` 등) 대부분이 좌표/역명을 props로 받아 **스스로 fetch**라, 채팅 router는 데이터를 가져오지 않고 좌표/파라미터만 추출(지명→`searchPlaces` geocode 첫 결과 또는 `ctx.userLocation`)해 **마운트 지시 `RenderPayload`** 반환 → `MessageBubble`이 컴포넌트를 파라미터로 마운트하면 컴포넌트가 self-fetch. **`search_places`·`search_address`만 props-driven**(`ResultList`·`AddressResultList`) 데이터 재사용. `ToolResult{summary, render?}`(dodo는 string만 반환 — 이 확장은 dodo 역수입 가능). 설계 정본 `docs/superpowers/specs/2026-06-20-chat-tools-option-c-design.md`. ⚠ MessageBubble의 places/addresses `onOpen`/`onSelect`는 V1 no-op(채팅→상세 진입 비목표, 후속 백로그).
  - **13개 도구**: `search_places`·`search_address`·내주변4(`get_subway_arrivals`·`get_night_clinics`·`get_kids_places`·`get_surroundings`, 현재위치)·좌표3(`get_bus_arrivals`·`get_bike_stations`·`get_air_quality`)·역명2(`get_station_meta`·`get_station_facilities`)·길찾기2(`get_car_route`·`get_transit_route`). 각 declaration **게이트**(`hasKakaoKey`·`hasDataGoKrKey`·`hasSeoulSubwayRealtimeKey`·`hasSeoulOpenDataKey`·`hasJusoKey`·`hasOdsayKey`, station-meta는 정적 seed라 게이트 없음), `availableDeclarations()`가 게이트 통과분만 Gemini 노출(키 없는 도구는 모델이 호출 불가). `get_bus_route`는 V1 제외(routeId 자연어 진입 모호). 좌표 도구는 args의 지명 없으면 현재위치(`ctx.userLocation`)·둘 다 없으면 render 생략 graceful.
  - 접근성: **단일 polite live region**(새 assistant 산문만 통지, 카드는 자체 시맨틱), 단축키는 `appendShortcutHint`로 **aria-label에 합침**(`aria-keyshortcuts` 금지 — VoiceOver 접두사 함정), 모드 전환 시 새 입력창 포커스 이동(마운트 시 `isMountRef` 가드로 `?q=` 자동검색 포커스와 경합 차단), `aria-disabled`(disabled 회피). UI 텍스트 5개 언어(`chat.*`). 설계 `docs/superpowers/specs/2026-06-20-chat-interface-design.md`, 계획 `docs/superpowers/plans/2026-06-20-chat-interface.md`. **2026-06-20 로컬+프로덕션 실호출 검증**(`gildongmu.vercel.app/api/chat` 길동 카페 10건·강남 station-meta·세종대로 110 주소 2건·도구불필요 메시지 text만).

## API 키 현황 (2026-06-13)

| 키 | 상태 | 비고 |
|----|------|------|
| `KAKAO_REST_API_KEY` | **동작 확인 (2026-06-12)** | dodo-planet 카카오 앱(ID 1383407) 키 재사용(.env.local). 카카오맵 제품 활성화 완료. 이 키 하나로 **로컬 검색 + 주소 지오코딩 + 카카오모빌리티 자동차 경로**까지 모두 동작 (모빌리티는 별도 활성화 불필요, 2026-06-13 검증) |
| `TOUR_API_KEY` | **동작 확인 (2026-06-13)** | data.go.kr 국문·영문 GW 활용신청 승인(만료 2028-06-13), 실응답 검증 완료(빈 결과 `items:""`, contenttypeid 라벨 매핑 확정). 신형 GW는 **hex 64자 단일 키**(Encoding/Decoding 구분 없음, 승인 후 전파 ~10분간 401). **개발계정은 기능당 일 1,000건**(상향은 운영계정 신청). en 로케일 장소 검색은 카카오와 병합(`searchPlacesMergedEn`)되어 일상 장소+관광 영문 정보를 함께 노출 — 로컬 실호출 검증됨(2026-06-13) |
| `DATA_GO_KR_API_KEY` | **동작 확인 (2026-06-14)** | **`TOUR_API_KEY`와 동일 값** — data.go.kr은 계정당 단일 인증키라 모든 승인 API가 공유. "TOUR" 이름에 묶이지 않는 정식 별칭으로 추가(향후 TAGO 버스·열차·무장애여행 등 data.go.kr 서비스 공용). 현재 **한국철도공사 편의시설(15125774)** 승인·실호출 검증 완료: `apis.data.go.kr/B551457/convenience`의 `/stationFacilities`·`/weekPersonFacilities`(교통약자: 장애인화장실유무 `pwdbs_tolt_estnc`·휠체어리프트수 `whlch_liftt_cnt`)·`/parkingLots`·`/codes`, 각 일 10,000건, 전국 406역. **TAGO 시내버스 3종(15098534 정류소근접·15098530 도착예정·15098529 노선)도 같은 키로 활용신청·자동승인·실호출 검증 완료(2026-06-14)** — envelope(`response.body.items.item`, 빈결과 `items:""`, `resultCode "00"`)·필드명(`citycode/gpslati/gpslong/nodeid/nodenm`, `arrtime/arrprevstationcnt/routeno/routetp/vehicletp`)·저상버스 판정(`vehicletp` 실값 `"저상버스"`/`"일반차량"`) 모두 실응답 일치. **⚠ 서울 시내버스 미수록(서울시청·강동구청 totalCount 0)** — 서울은 TOPIS로 별도 운영하기 때문. **경기·지방·부산은 정상 커버**(성남 13건·하남 25건·부산역 20건 실응답). 서울 버스는 다음 마일스톤(서울 TOPIS API). **프로덕션 실호출 검증 완료(2026-06-14): `gildongmu.vercel.app/api/bus/nearby` 성남 정상(341번·9800 도착, 저상 판정)·서울 빈결과(`{"stops":[]}`), `/api/bus/route` 노선 경유정류소 정상.** **서울교통공사_교통약자이용정보(15143843, `B553766/wksn`, 9 오퍼레이션)도 같은 키로 활용신청·자동승인·실호출 검증 완료(2026-06-17)** — 서울 1~8호선 지하철역 교통약자 시설(엘리베이터·에스컬레이터·화장실 등), 코레일 도시철도 공백 보완. envelope·`oprtngSitu`(M/S)·`stnNm` 포함필터 모두 실응답 일치. **B1·B2 4종도 같은 키로 활용신청·실호출 검증 완료(2026-06-17, 전파 ~5분)**: 소아야간 `B551182/spclMdlrtHospInfoService1/getChildNightMdlrtList1`(15001674, XML, `yadmNm·addr·시도/시군구코드·telno` — **⚠ 좌표 없음**), NMC 병의원 `B552657/HsptlAsembySearchService/getHsptlMdcncListInfoInqire`(15000736, XML, Q0/Q1 지역필터, `dutyName·dutyAddr`), 에어코리아 측정소 `B552584/MsrstnInfoInqireSvc/getNearbyMsrstnList`(15073877, JSON, **TM좌표**→`stationName·tm(거리)·addr`)·대기오염(15073861, JSON, `pm10/pm25Value·khaiValue·grades·dataTime`). **B1(소아진료) 구현 완료(2026-06-17)** — 좌표 코어는 NMC `getBabyListInfoInqire`(15001674는 좌표 미보유). **B2(이 지역 공기질) 구현 완료(2026-06-17)** — 단 실시간은 시도별(`getCtprvnRltmMesureDnsty`)이 아니라 **측정소명 단건 `getMsrstnAcctoRltmMesureDnsty`**가 정본(단일 측정소·전 오염물질·통합지수 한 번에), 좌표는 **WGS84→TM EPSG:2097(proj4)** 변환 필요, ⚠ 에어코리아 envelope는 `body.items`가 **직접 배열**(다른 data.go.kr 서비스의 `items.item`과 다름, 실호출로 규명). 상세 `docs/superpowers/specs/2026-06-16-implementation-roadmap-design.md` §B1·B2 게이트 확정. 신규 API 추가는 같은 키로 data.go.kr 활용신청만 하면 즉시 자동승인(전파 ~5~30분) |
| `NAVER_LOCAL_CLIENT_ID/SECRET` | 미발급 | developers.naver.com 수동 등록 필요 (Claude in Chrome이 해당 도메인 차단) — 결제수단 불필요, 일 25,000회 |
| `NCP_MAPS_CLIENT_ID/SECRET` | **동작 확인 (2026-06-13)** | 결제수단 등록 후 Maps 구독 + Application `gildongmu` 등록(API 6종 전체 체크, Web URL: vercel.app·localhost:3000·3001). Geocoding(`englishAddress` 포함)·Directions 5 실호출 검증. 호스트 `maps.apigw.ntruss.com`, 헤더 `x-ncp-apigw-api-key-id`/`x-ncp-apigw-api-key`. **en 검색 카카오 카드의 영문 주소 보강**(`ncp-geocode.ts`, 2026-06-13)에 더해 **en 자동차 경로(C1, `ncp-directions.ts`, 2026-06-17 연결)에도 사용 중** — 카카오모빌리티는 ko 안내문 전용이라 영문 턴바이턴(`lang=en`)은 NCP가 정본. Directions는 카카오와 중복이 아니라 **언어 보완**(ko=카카오, en=NCP) |
| `DEEPGRAM_API_KEY` | **동작 확인 (2026-06-14, 2026-06-19 설정 수정)** | dodo-planet `.env.local`에서 수입(공유 키). 음성 받아쓰기 STT — `/api/speech-to-text`가 Deepgram **nova-3**(smart_format·punctuate, **`language=ko/en` 로케일 명시**)로 전사. **⚠ `detect_language=true`는 한국어를 vi로 오인식해 빈 전사를 반환하므로 절대 쓰지 않는다**(2026-06-19 실호출 확정, 위 음성 받아쓰기 아키텍처 항목 참조). 서버 전용(클라 노출 금지). **프로덕션 키 401 사고+복구(2026-06-19)**: 프로덕션 env의 DEEPGRAM_API_KEY가 무효(401 Unauthorized)라 라우트가 502→hook이 `stt_failed`로 통지("음성 인식에 실패했습니다")했다 — 사용자가 본 프로덕션 받아쓰기 실패의 1차 원인은 코드가 아니라 무효 키였다(detect_language 버그는 키를 고쳐야 드러나는 2차 잠재 결함). 정상 로컬 `.env.local` 키로 프로덕션 env 재동기화 + 재배포해 복구, 실호출 200 확인. **교훈: 프로덕션 STT가 502면 코드보다 키 유효성을 먼저 의심**(런타임 로그 `[stt] Deepgram 오류: <status>`로 401/400/429 구분). 검색창 마이크 버튼이 이 라우트로 녹음 오디오를 전송 |
| `SEOUL_OPEN_DATA_KEY` | **동작 확인 (2026-06-16)** | 서울 열린데이터광장(data.seoul.go.kr) **일반 인증키**(30자 hex). data.go.kr과 별개 포털 — 서울 네이티브 서버(`openapi.seoul.go.kr:8088` 등) 즉시 동작. **따릉이(`bikeList` OA-15493) 실호출 검증 완료**(`INFO-000`, 길동 근처 5건). 서버 전용. **⚠ 서울 시내버스(ws.bus.go.kr/TOPIS)는 이 키로도 미동작** — data.go.kr 서울버스 3종(15000303·15000314·15000193) 활용신청 [승인]·기간 정상이나 **ws.bus.go.kr 키 동기화 배치(익일 추정) 전까지 headerCd 7**(포털 자체 미리보기도 동일 실패 → 코드 아닌 전파 문제 확정, 2026-06-16). 서울 버스는 전파 후 재개(정본 plan `docs/superpowers/plans/2026-06-15-seoul-bus-api.md`). 지하철 실시간(swopenapi)은 또 별도 인증키(`SEOUL_SUBWAY_REALTIME_KEY`, 아래 행 — 발급 완료). 프로덕션 env 미등록(따릉이 배포 시 등록+재배포 필요) |
| `SEOUL_SUBWAY_REALTIME_KEY` | **동작 확인 (2026-06-17)** | 서울 열린데이터광장 **"실시간 데이터 인증키"**(일반키 `SEOUL_OPEN_DATA_KEY`와 **별도 계열**, 30자 hex). 발급 경로: 데이터셋 OA-12764(서울시 지하철 실시간 도착정보) → "인증키 신청 (지하철)" 버튼 → 약관·개인정보 동의 후 신청 → **즉시 발급**(인증키 관리에서 조회). 일반키로 `swopenapi`를 호출하면 `ERROR-338`. **일 1,000회/키**(활용사례 갤러리에 등록·승인 시 무제한). `swopenapi.seoul.go.kr` 전용. 실호출 검증(2026-06-17: 강남 2호선+신분당선·서울역 1·4·경의중앙·공항철도 정상, 부산/없는역 null·INFO-200). 서버 전용. **프로덕션 env 등록 완료(2026-06-17)** — Vercel Production에 `SEOUL_SUBWAY_REALTIME_KEY` Encrypted 등록(`canShowSubway` 게이트 통과). 역 상세 실시간 도착 + 홈 "내 주변 지하철 도착 정보"(A2 nearby) 둘 다 프로덕션 노출 |
| `JUSO_CONFM_KEY` | **동작 확인 (2026-06-19)** | 행안부 도로명주소 검색 API 승인키(business.juso.go.kr 주소기반산업지원서비스). 발급: 본인인증 로그인 → 주소정보 API 연계 → 도로명주소 **검색 API**(JSON) 신청(민간기관/인터넷망/**운영**=영구) → **즉시 승인**. base64형(끝에 `=`, raw 사용 OK). 엔드포인트 `https://business.juso.go.kr/addrlink/addrLinkApi.do`(`confmKey`·`currentPage`·`countPerPage`·`keyword`·`resultType=json`). envelope `results.common`(`errorCode "0"`=정상·`errorMessage`·`totalCount`)+`results.juso[]`. 필드: `roadAddr`(전체 도로명)·`roadAddrPart1`·`jibunAddr`·**`engAddr`**(공식 영문주소 "110 Sejong-daero, Jung-gu, Seoul" — ⚠ NCP `englishAddress`와 달리 국가명 미포함)·`zipNo`(우편번호). **검색 API 하나로 C2-a(영문주소 `engAddr`)·C2-b(주소검색) 모두 커버** → 영문/좌표 API 별도 신청 불요. 무료·트래픽 제한 없음. 서버 전용. 실호출 검증(2026-06-19: 세종대로 110→영문·우편번호 정상, 무결과 `totalCount 0`). **C2-a(영문주소 폴백 체인)·C2-b(주소 검색 진입점) 구현 완료(2026-06-19)** — 위 아키텍처 §C2-a/C2-b 참조, 실호출 머지 게이트 통과(`/api/places?lang=en` juso 영문주소·`/api/address/search` 실주소). 설계 `docs/superpowers/specs/2026-06-19-juso-official-address-design.md`. **프로덕션 env 등록+배포 완료(2026-06-19)** — Production에 `JUSO_CONFM_KEY` Encrypted 등록, push 자동배포, prod 실호출 검증(`canSearchAddress` 게이트 통과) |
| `ODSAY_API_KEY` | **개발 동작 확인 (2026-06-18)** | ODsay LAB(lab.odsay.com) **대중교통 길찾기** apiKey. 발급: 회원가입 → 애플리케이션 등록(서비스유형 **Basic 무료 6개월** ~2026-12-18, 1,000회/일, 개인/학생/5인이하) → 발급. 영숫자라 **URL 인코딩 불필요(raw 사용)**. **⚠ Server 방식은 공인 IP 화이트리스트 필수** — 등록 시 호출 서버 공인 IP 입력(youjung-macmini `119.71.23.38` 등록). **Vercel 서버리스는 가변 IP라 프로덕션 미동작** → 고정 IP 애드온/프록시/유료 또는 다른 발급 방식 검토(별도 마일스톤). 서버 전용(클라 노출 금지). 실호출 검증(2026-06-18: 강동 길동→강남 33분 등, IP 화이트리스트 통과). **프로덕션 env 미등록**(IP 문제로 보류) |
| `GEMINI_API_KEY` | **동작 확인 (2026-06-20)** | Google Gemini **function-calling 채팅 엔진**(서버 전용, 클라 노출 금지). dodo-planet `.env.local`의 `GOOGLE_GENERATIVE_AI_API_KEY`와 **동일 값 공유**(gildongmu는 `GEMINI_API_KEY` 이름으로 수입). `GEMINI_MODEL=gemini-3.5-flash`. `canShowChat`(`hasGeminiKey()`) 게이트로 채팅 모드 노출 제어(없으면 순수 검색, 회귀 0). 유료 API(사용량 기반). **프로덕션 등록+배포 완료(2026-06-20)** — ⚠ **vercel CLI 54.12.2 `env add` 비대화형 빈값 버그**로 첫 등록이 빈 값→prod `/api/chat` 502(`chat_failed`, Gemini ApiError, 로컬은 정상). `env pull`로 빈값 확인 → **`npm i -g vercel@latest`(54.14.2) 업글 후 재등록+재배포로 복구**, 실호출 200 검증. 교훈: prod 채팅 502면 코드보다 키 등록 유효성 먼저 의심(메모리 `vercel-env-add-noninteractive-bug`, Deepgram 401 사고와 동형) |

상세 조사: `docs/RESEARCH-2026-06-naver-api-ecosystem.md`, `docs/RESEARCH-2026-06-kakao-api-ecosystem.md`, **`docs/RESEARCH-2026-06-domestic-api-expansion.md`**(우편번호·버스·지하철·맛집·예약·접근성 6개 도메인 + KRIC 교통약자 §I). 설계 결정: `docs/SPEC.md`.

## 배포

- **Vercel 프로덕션**: https://gildongmu.vercel.app (2026-06-13 최초 배포, 팀 `hunyong-kims-projects`)
- 프로덕션 환경변수 현황(2026-06-14): `KAKAO_REST_API_KEY`(Production), `TOUR_API_KEY`(Production/Preview/Development), `NCP_MAPS_CLIENT_ID`·`NCP_MAPS_CLIENT_SECRET`(Production — en 영문 주소 보강용), `DATA_GO_KR_API_KEY`(Production — 역 교통약자 편의시설), `DEEPGRAM_API_KEY`(Production — 음성 받아쓰기 STT, dodo-planet과 공유 키, 2026-06-14 추가), `SEOUL_OPEN_DATA_KEY`·`SEOUL_SUBWAY_REALTIME_KEY`(Production), `JUSO_CONFM_KEY`(Production — 행안부 도로명주소 C2, 2026-06-19 등록+배포). `vercel env ls production`으로 확인.
- **환경변수는 배포 시점에 함수로 주입된다** — 키를 추가/변경한 뒤에는 반드시 재배포(`vercel deploy --prod --yes` 또는 push)해야 이미 떠 있는 배포에 반영된다. 키만 추가하고 재배포 안 하면 기존 함수는 옛 env를 본다(2026-06-13 NCP 키 등록 시 실측).
- 비대화형 등록: `printf '%s' "$VALUE" | vercel env add <KEY> production`. 주의: CLI `vercel env add <key> preview`는 비대화형에서 `git_branch_required`로 멈추는 결함(54.12.2에서도 재현) — Preview 등록은 REST API(`POST /v10/projects/{id}/env`) 또는 대시보드 사용
- GitHub 저장소(`Engccer/gildongmu`)가 Vercel에 연결됨 — **push하면 자동 배포**된다. push는 사용자 요청 시에만 하는 워크스페이스 규칙이 곧 배포 게이트.
- 수동 배포: `vercel deploy --prod --yes`

## 명령어

```bash
npm run dev        # 개발 서버 (localhost:3000)
npm run build      # 프로덕션 빌드
npm run lint       # ESLint
npm run test:run   # Vitest (게이트 테스트 — 매 커밋 통과 필수)
```

## 개발 규칙

- 기능·버그픽스는 같은 커밋에 테스트 동반 (워크스페이스 공통).
- 커밋 이메일 `engccer@gmail.com` (dodo-planet과 동일).
- 코드 주석·커밋 메시지·문서: 한국어. 변수/함수명: 영어.
- a11y 변경 후에는 `a11y-auditor` 서브에이전트로 점검.
- 새 서비스 실험을 추가할 때는 `docs/SPEC.md`의 "실험 백로그" 표를 갱신할 것.
