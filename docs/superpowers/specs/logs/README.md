# 실기기 계측 로그 보관소

실보행·실주행에서 회수한 `guide-diag.log` 원본(gzip)의 **목록과 판독 요지**. 각 로그는 그것을 판정 근거로 쓴 spec·BACKLOG 항목이 소비자다. 판독은 `gunzip -c <파일> | less`.

⚠ **로그 파일 자체는 이 저장소에 없다.** 초 단위 위경도는 개발자의 실제 이동 경로라 공개 저장소에 싣지 않는다(`.gitignore`가 `docs/superpowers/specs/logs/*.log*`를 막는다). 원본은 개발자가 저장소 밖(`~/gildongmu-private/field-logs/`)에 보관하며, 아래 표는 그 파일들의 색인이다. 게이트 테스트가 쓰는 부분만 익명화 fixture로 떼어 두었다: `src/lib/__tests__/fixtures/guide-diag-2026-08-09-walk.json`(경도 평행이동, 거리·방위 불변)·`guide-diag-2026-08-13-final-approach.json`(t·event만)·`guide-diag-2026-09-01-brief-window.json`(09-01 등굣길 tooClose 인계 이후 brief 행의 t·acc·usable·dist·nearby + 경도 -0.05° 평행이동 좌표 — A31 간략 창 리플레이 게이트). 재현 연구에 원본이 필요하면 저자에게 요청한다.

⚠ **로그가 답하지 못하는 질문은 앱 URLCache가 답한다**(2026-08-16 A18 판정에서 확립). `GuideDiag`는 스텝 **인덱스**만 남기므로 "그 스텝이 무슨 문장이었나"·"경로를 어느 좌표에서 조회했나"는 로그만으로 못 푼다. 앱이 주고받은 요청 URL과 응답 본문은 `Library/Caches/<bundle id>/`의 `Cache.db`(+`-wal`·`-shm`)와 `fsCachedData/`에 남아 있다 — 넷을 함께 회수해 `cfurl_cache_response`(요청 URL·타임스탬프)와 `cfurl_cache_receiver_data`(본문, `isDataOnFS=1`이면 `fsCachedData/<파일명>`)를 조인한다. **정식판(`space.dodoplanet.gildongmu`)에도 있다** — 계측이 없는 정식판 세션에서 유일하게 남는 증거다.

⚠ **대중교통은 계측 파일이 따로다**(`Documents/transit-guide-diag.log`, `TransitGuideDiag.swift`). 도보·자동차 `guide-diag.log`에는 대중교통 세션이 한 줄도 남지 않으므로 회수할 때 두 파일을 모두 가져온다. 기기 파일 목록은 `xcrun devicectl device info files --domain-type appDataContainer --domain-identifier space.dodoplanet.gildongmu.dev --subdirectory Documents`로 먼저 확인한다.

| 파일 | 내용 | 소비자 |
|---|---|---|
| `guide-diag-2026-08-09.log.gz` | 도보 281 fix(길동, 0.9m/s) + 자동차 2,131 fix(17.7km). **A6 방위 축 판정 근거** — 보행 courseAcc 중위 83°로 기기 course 전제 무효화. 같은 로그로 위치 이력 유도 방위(1안)의 성립을 실증(2026-08-10, spec §3.0) | BACKLOG A6, spec `2026-08-09-off-route-course-axis-design.md` §3.0 |
| `guide-diag-2026-08-10.log.gz` | 회전 로그 원본이라 08-09 저녁 세션과 겹친다. 오늘분 둘: ①오전 실보행(KST 09:16~09:28, 임박 큐 10m→25m 상향 판정 근거) ②저녁 왕복 실보행(KST 20:00~20:25, 자택↔주택 A, 両다리 arrived=true. **유도 방위 축 첫 실보행 — verdict=on 240행**(08-09는 전량 unknown), imminent 14·announceSteps 14·offRoute 2·uncertain 1쌍) | BACKLOG "도보 임박 큐·도착 종 실보행 판정" ①~⑥, A6 spec §7 3단계 검증 보행 |
| `guide-diag-2026-08-11.log.gz` | 회전 로그 원본이라 08-09·08-10 세션을 포함한다. 오늘분 둘(집↔학교 왕복 실보행): ①등교(KST 08:10~08:22, 719행. offRoute 3회 모두 재조회로 이어짐, imminent 6·announceSteps 6, 최종 접근 진입 offset 58.8m·41틱 58.9→40.6m·**arrived=true 0건**) ②하교(KST 17:12~17:26, 842행. **finalApproachEnter가 `projectionJumped` 가드에 버려졌는데 phase는 이미 커밋돼 리듀서가 영구 no-op** — 마지막 30초 d=501.2 고착·도착 판정 없음. verdict=off 1회) | BACKLOG "도보 임박 큐·도착 종 실보행 판정", 최종 접근 진입 가드 결함 |
| `guide-diag-2026-08-13.log.gz` | 회전 로그 원본이라 08-09~08-12 세션을 포함한다. 오늘분: ①등교 실보행(KST 08:20~08:34, 841 fix. imminent 9·offRoute 3·**verdict=off 21행**, 최종 접근 26틱 43.6m로 끝나고 arrived=false) ②귀가 세션(KST 17:03~) — **잊힌 안내 세션 실사고**의 그 세션이라 최종 접근 진입 뒤 로그가 이어진다(도착 추정 자동 종료 설계의 근거) | spec `2026-08-13-presumed-arrival-auto-end-design.md`, BACKLOG E13 |
| `guide-diag-2026-08-15.log.gz` | 회전 로그 원본이라 08-11~08-13 세션을 포함한다(회전 경계를 걸쳐 `.old`+현행을 시간순으로 이어 붙였다). 새 구간은 08-13 17:08 이후 5,521행: ①08-14 등교 실보행(KST 08:47~09:03, 578 fix, verdict=on 323. 최종 접근 121틱 48.6→36.8m, arrived 없이 **`presumedArrival reason=stationary dist=36.8` 첫 실발동**) ②08-14 차량(KST 10:45~10:58, 3.0km) ③08-14 저녁 실보행 둘(16:45~17:01 **arrived=true dist=12.3**, offRoute 12·verdict=off 11 / 17:14~17:24 finalEnter offset=12.5인데 acc 50.8m로 열화돼 60.6m에서 종료, arrived=false) ④08-15 차량(KST 09:23~10:07, 22.7km. uncertain 6쌍·reacquiring 1) | BACKLOG E13(도착 추정 상수 4종 실보행 게이트), A12 실보행 판정, A6·최종 접근 상수 |
| `guide-diag-2026-08-16.log.gz` | 회전 로그 원본이라 08-15 세션을 포함한다(2,146행). 새 구간은 **08-16 KST 21:44~21:52 귀가 도보 390행** — 마을버스 하차 직후 자동 이어진 세션이고 **A18의 판정 근거**다. 첫 fix부터 `d=92.4`(=3×acc 30.8, 창 끝 적중)로 시작해 2초 뒤 105.7(전역 최근접)로 착지 — 경로가 실제 위치와 115m 어긋났다는 지문. `announceSteps`는 [2]·[3]뿐이고 [4]·[5]는 4분짜리 offRoute(21:47:35~21:51:41, uncertain 2쌍) 뒤 `backOnRoute`의 `restateAt`이 유닛 끝으로 래치를 올려 전문 없이 `imminent([5])`·`imminent([6])`만 났다. `arrived=true dist=11.4`로 종료. ⚠ **경로 원본은 로그가 아니라 URLCache에 있다**(위 주석) — 스텝 문장·origin 좌표는 거기서만 나온다 | BACKLOG A18 |
| `guide-diag-2026-08-23.log.gz` | 회전 로그 원본(`.old`+현행 이어 붙임, 2026-08-22 16:58 KST 회전). 새 구간: **2026-08-22 KST 16:52~ 자동차 실주행(자택 → 송추가마골, 실험판, fix 2,424건, 최고 26.9m/s)** + 같은 날 KST 04:56 짧은 도보 33 fix. ⚠ 수단 표식 없음(속도로 가른다). **판독 완료(2026-08-23, K2 spec §0)**: 속도 p50 8.6·p90 20.8m/s, fix 간격 p50 1.0초, uncertain 3구간(315·752·313초, 터널 acc 300m+) 동안 발화 0, 복귀 뒤 창이 150m씩 기어가며 지난 교차로 3개 전문이 1초 간격으로 발화 — 임박 시간 축(5+1초)·표본 부재 60m·`silentCatchUp`의 근거. 2026-08-23 이후 로그는 세션 첫 줄 `session kind=walk|car listener=…`로 수단을 가른다 | BACKLOG K2·B1, spec `2026-08-23-car-guidance-completion-design.md` §0 |
| `guide-diag-2026-08-29.log.gz` | 회전 로그 원본(08-22 주행 포함, 5,960행). 새 구간: **2026-08-29 KST 18:46~19:28 자동차 실주행(서울역 부근 → 자택, 실험판, `session kind=car listener=passenger`, fix 2,548건, 22.6km)**. fix 간격 >20초 **0건**(1초 주기 유지), 도플러 정지 연속 65·65·73초(신호 대기 3회). **마지막 줄이 19:28:50 `finalApproachEnter`**이고 세션은 새벽까지 살았는데 로그 0줄 — 자동차 라우트에 `finalApproach` 기하가 없어 간략 인계로 빠졌고 그 경로가 무계측이었다(K2-a). 08-22 구간의 터널 fix 공백 46~183초와 함께 car 도착 추정 두절 120초·안전망 두절 600초의 근거(spec `2026-08-31-car-session-end-design.md` §0·§3.2·§4) | BACKLOG K2-a·B1, spec `2026-08-31-car-session-end-design.md` |
| `guide-diag-2026-09-01.log.gz` | 회전 로그 원본(08-29 자동차 주행 포함). 새 구간: **2026-09-01 등굣길·퇴근 도보 두 세션**. ①KST 08:34~09:05(961행) — 목적지 12.3m에서 `briefHandoff reason=tooClose`로 간략 인계된 뒤 `dist=24.3` 고정·`nearby=true`로 20분을 살다 `sessionIdleEnd reason=stationary`로 끝났다. **A31 축 ①의 판정 근거**(간략 인계 세션에 도착 종료가 없다). ②KST 17:08~17:21(733행) — 같은 코스 역방향이 `finalEnter offset=12.5` → `arrived=true`로 정상 종료해 대조군이 된다. 갈림은 코스가 아니라 최종 접근 진입 시점의 종점 오프셋이다 | BACKLOG A31 |
| `transit-guide-diag-2026-08-16.log` | 대중교통 계측 전량(회전 없음, 28행). 세션 셋. ①**2026-08-14 KST 09:57~10:34 지하철 9호선 급행** — 대기 폴 3회는 전부 건강(`ok(3) candidates=1 vehIdless=0 terminates=0 directionUncertain=false reason=-`)이라 #4 원인 6후보 중 **대기 후보 필터 계열은 무고**. 승차 35분 내내 `signal=notYetVisible`(추적 신호 미부착 → 사다리 통지 0), `legIndex=0`에서 끝나 `legAdvanced`·`arrived` 0건. `boardingReset` 2회는 자동 전이가 아니라 사용자의 "탑승 변경"이고 각각 8~9초 뒤 재탑승했다 ②08-16 KST 01:35 대기 폴 2회 `status=empty`(세션 미성립) ③**2026-08-16 KST 21:34~21:44 마을버스 강동01 — 서울버스(TOPIS) 추적 첫 완주**. 대기 폴 12회가 20초 주기를 정확히 지켰고(전경 유지), 탑승 8초 뒤 `trackingStarted`(잔여 5), 사다리 3·1, `arrived(certain: true)`, `legAdvanced(final: true)`까지 전 국면 통과. **잔여 2 결번**과 "1분32초 후" 통지 16초 뒤 도착은 riding 폴 계측이 없어 원인 미판정(A16 미확정 ①). 전 폴 `directionUncertain=true`는 버스라서 구조적(A17) | BACKLOG F-a 라운드1 재판정 ③(대기 고정 #4 원인 판별)·⑨, B2 실승차 ①, A16 미확정 ①②, A17 |
| `transit-guide-diag-2026-08-29.log` | 대중교통 계측 전량(92행, 08-16·08-21 세션 포함). 새 구간: **2026-08-29 KST 16:07~17:03 2호선 시청→충정로**(경로 도보 5분 → 시청 → 충정로). 시작 직후 `waitPoll` 대기 국면(후보 2, 전부 건강) — **승차 전 도보 구간이 국면으로 존재하지 않는다**는 A25의 판정 근거. 16:22 선택 → "전역 도착" 추적 → 16:24 `boarded(declared)` → 17:03 `arrived(certain: false)` → `signalLost`. `controlFocus changeBoarding·advance landed=false`(A19 계열 착지 실패 2건). 같은 날 URLCache 사본 `urlcache-2026-08-29/`(경로 원본 entry 775) | BACKLOG A25, A19 |
| `audio-signal-diag-2026-08-19.log.gz` | 음향신호기 BLE 진단(E20) 1차 실측 전량(`.old`+현행 이어 붙임, 26,578행). 스캔 17회·발견 표본 26,540줄, **`AHG001` 0건·UART 서비스 UUID 광고 0건**. 2024 대한신호 교체 기기 6m 지점 2분 스캔 포함. ⚠ 좌표 열은 세션 시작 fix 고정(경로 아님), 무명 기기 식별자 없음 — 판독과 계측기 한계는 research `RESEARCH-2026-08-16-audio-signal-ble-control.md` §11 | BACKLOG E20, research §11 |
| `a6-derived-bearing-analysis.py` | 유도 방위 오차 전파 실측(상대 잡음·자동차 교차 검증·도보 지터·커버리지·표결 시뮬). spec §3.0.1~3.0.3 수치의 산출 스크립트 | spec §3.0 재현 |
| `closer-interval-replay.py` | 상세 모드 closer 톤 간격을 데드밴드별로 재는 리플레이(`<로그> [데드밴드…]`). 도보 세션만 골라 간격 중위·분포를 낸다. ⚠ 로그의 `d`는 **경로상 진행 거리**라 추세 축(잔여)은 `-d`다 | BACKLOG F-a "2026-08-11 추가분", `docs/INTEGRATIONS.md` §톤 계층 |
| `a6-chain-u-replay.py` | 사슬 자기일관성 U 검증 + 합성 이탈(궤적 회전) 검출 지연. spec §3.0.4~3.0.5 수치의 산출 스크립트 | spec §3.0 재현 |
