# iOS 백로그 병렬 작업 계획

기준 SHA: `3fb486ea2dac8bc6fe52dbdd342c90f063254245`. 코디네이터가 범위·제품 판정·리뷰 수용·로컬 통합을 맡는다. 구현은 독립 Codex CLI 세션과 worktree에서 한다. 모델은 머신 기본값(`gpt-6-astra`, high), CLI 0.154.0이다.

## 범위와 소유권

| 세션 | 작업 | 소유 파일 |
|---|---|---|
| ios-briefing-empty-names | E45 인접 결함: 빈 역명으로 승차·하차 문장을 만들지 않는다. 언어 선택·역 진입점·하차 줄을 같은 부재 기준으로 검사한다 | `ios/Gildongmu/RouteBriefing.swift`, 관련 Kit 이름·문구 함수 및 테스트, 해당 소스 가드. 새 Kit 파일을 만들면 Android 등록부의 pending 항목만 추가 |
| ios-briefing-corpus | E47 ① 중 iOS E45 검증: 저장 응답으로 표본 구성과 Kit 조인을 재검증하는 `--from-corpus` 모드 | `scripts/verify-briefing-station-join.mjs`, 전용 helper·테스트. 필요 시 `BriefingStationJoinGateTests.swift`만 |
| ios-endpoint-state | E28 최근 라틴 표기, E43 ④ 주소 요청 정합성 | 끝점·최근 저장·프리필·주소 상태 코어와 전용 테스트. 아래 추가 범위의 파일 목록 |
| ios-location-ui | E43 ①②③⑤ 상태·동작 가용성 | 위치 표시·주변 오버레이·지도 액션, iOS 카탈로그와 전용 테스트. 아래 추가 범위의 파일 목록 |
| coordinator | 판정, 독립 리뷰, 통합, 문서 | 이 계획, BACKLOG, CHANGELOG, PROGRESS, PATTERNS/CLAUDE 및 생성 문서 |

네 구현의 소유 파일과 선행 관계가 겹치지 않아 병렬로 진행한다. 공통 문서는 코디네이터, iOS 카탈로그 생성물은 위치 UI 세션만 처리한다. 코디네이터는 구현 파일을 직접 수정하지 않는다.

## 판정

- E45 빈 이름은 정보 부재다. 빈 문자열·공백만 있는 값이 이름처럼 읽히지 않게 하되, 없는 역 이름을 추정하지 않는다. 정상 값의 출력과 한 줄 한 객체 계약을 보존한다.
- E47은 iOS 검증 지원 도구에 한정한다. 서버·웹 런타임, 캐시 수명, 정적 seed 정책, 다른 ODsay 게이트 확장은 제외한다. 검증용 실호출도 하지 않는다.
- 저장 코퍼스는 당시 응답의 증거다. 재실행 시 수집 시각·언어를 보존하고 신규 실호출로 표시하지 않는다. 잘못된 파일·부분 표본·언어 불일치가 성공으로 위장하지 않아야 한다. 오프라인 모드는 env·provider 번들·네트워크가 없어도 동작해야 한다.
- B11 방향 추가는 사용자 선택 대기다. 응답 전에는 현행 유지하며 작업 두 건을 계속 진행한다.
- A42/A44/A45와 E35/E42는 서버 변경 또는 동결 해제가 필요하다. E14 ③은 별도 설계, G3와 나머지 실기기 항목은 실사용 판정이 선행이다. 봉인을 해제하지 않는다.
- 브리핑 두 작업의 설계 단계 별도 적대적 리뷰는 생략한다. 기존 문장 부재 계약의 국소 보정과 검증 도구의 무호출 실행으로, 새 안내 상태 머신·외부 계약·저장 형식을 도입하지 않는다. 구현 완료 후 독립 컨텍스트 리뷰로 양쪽 변경과 회귀를 검사한다. 끝점 상태는 아래처럼 별도 설계 리뷰를 받는다.

## 실행과 검증

worktree는 `/Users/hunyongkim/gildongmu-wt/<세션>`이다. 착수 프롬프트·보고·실행 기록은 같은 부모의 `ios-sweep-20260919/`에 둔다. 작업 세션의 공유 TTS 파일 쓰기는 금지하고 `TTS_SUMMARY=off`를 적용한다.

의존성 설치는 순차로 진행한다. 작업별 집중 테스트는 세션이 수행하고, 전체 Vitest 및 Swift 테스트·앱 빌드는 코디네이터가 순차 배정한다. 보고에는 기준 SHA, 변경 파일, 정확한 검증 명령·결과, 남은 판정, 공유 자산 반영 요청을 포함한다.

통합 순서는 빈 이름 처리, 코퍼스 재검증, 끝점 상태, 위치 UI, 공통 문서다. 독립 리뷰 통과 후 소유 세션이 로컬 커밋하고, 코디네이터가 `test/ios-backlog-20260919` 브랜치의 별도 worktree에서 합쳐 검증한다. 로컬 main 반영 여부는 사용자 선택을 받은 뒤 결정한다. origin push·Vercel 배포·env 변경·실기기 설치·App Store 제출은 이번 실행에 포함하지 않는다. 동결 가드는 유지한다.

## 종료 판정

작업 프로세스나 터미널 idle을 성공으로 보지 않는다. 보고 전문, 실제 diff, 집중 테스트 및 통합 게이트를 확인한다. 자동 테스트와 실제 VoiceOver 판정을 구분해 잔여를 BACKLOG에 남긴다.

## 추가 범위: 최근 위치와 주소 응답 정합성

E28의 명시된 코드 잔여와 E43 ④의 재현 가능한 응답 경합을 `ios-endpoint-state` 세션으로 묶는다. 둘 다 `DirectionsTabView.swift`를 수정하므로 한 소유자에게 배정한다.

- E28: `RecentEndpoint.labelRoman: String?`를 기존 저장 키 아래 additive로 저장한다. 과거 레코드는 `decodeIfPresent`로 nil, 새 레코드는 지정 시점에 이미 가진 표기만 쓴다. 기록·최근 경로·고정 토글·최근 항목 선택·프리필 소비의 왕복 전체에서 보존한다. id와 중복 판정은 기존 좌표 축을 유지한다. nil인 기존 항목은 정직한 원명 폴백이며 추가 조회·클라이언트 로마자 생성·저장 데이터 초기화는 하지 않는다.
- E43 ④: `syncCurrentAddress`에서 늦은 이전 역지오코딩 결과가 최신 주소를 덮지 않도록 기존 latest-wins 패턴을 적용한다. 취소된 화면/조회와 언어가 달라진 응답도 폐기해야 한다. 주소 부재와 오류는 최신 요청에 대해서만 기존 정책을 적용한다. GPS 정확도·수동 위치의 제품 정책은 바꾸지 않는다.
- 소유: `RecentSearchStore.swift`와 관련 테스트, `DirectionsTabView.swift`, `DirectionsEndpointSearchView.swift`, 프리필 기록을 담당하는 `GildongmuApp.swift`, 필요한 전용 소스 가드. 생성 카탈로그와 다른 세션 파일은 제외한다.
- 저장 형식의 additive 확장이 있으므로 착수 전에 별도 컨텍스트 설계 리뷰를 받는다. 구현 후 리뷰·검증은 앞 두 세션과 같은 방식이다. 통합은 앞 두 세션 뒤에 한다.
- 설계 리뷰: BLOCKER/MAJOR 없음. 주소 함수 내부의 세대만으로는 부족하다. 측위 await 이전에 소유 세대를 발급하거나 부모 Task를 추적해, cancel/clearResults 이후 늦은 측위가 새 주소 조회를 만드는 경로까지 무효화한다.

## 추가 범위: iOS 상태·동작 표시

`ios-location-ui` 세션은 E43 ①②③⑤를 맡는다. Android 기능을 그대로 복사하는 것이 아니라, 현재 iOS 코드에서 확인한 기존 계약 위반을 고친다.

| 항목 | 확정 범위 |
|---|---|
| ① | NearbyOverlay 기본 위치 취득 실패와 서버 조회 실패의 문구를 분리한다. WalkInfra의 명시 override도 같은 기준으로 검사한다 |
| ② | 버스 경유 정류소 목록의 통지를 주변 정류소 통지와 분리한다. 수량 문구는 기존 ICU 계약을 따른다 |
| ③ | 딥링크 빌더가 nil인 장소에서 지도 앱 버튼과 검색 로터 액션을 노출하지 않는다. 가능한 URL은 기존대로 연다 |
| ⑤ | 위치 표시줄이 권한 거부, 대략적 위치만 허용, 위치 취득 실패를 기존 카피로 구분한다. 정확도 허가의 표시용 observable 값을 필요 시 추가하되 안내·측위 판정은 변경하지 않는다 |

소유: `Nearby/NearbyOverlay.swift`, `Nearby/WalkInfraNearbyView.swift`, `Nearby/NearbyLoadState.swift`, `Nearby/BusRouteStopsView.swift`, `PlaceDetailView.swift`, `SearchView.swift`, `LocationBarView.swift`, `LocationService.swift`, 해당 테스트, `ios/i18n/ios-extra/*.json`, `ios/i18n/arg-order.json`, iOS xcstrings 생성물. 소스 문자열과 생성물은 이 세션만 담당한다. 웹 messages와 서버는 변경하지 않는다.

설계 전 별도 리뷰 생략: 기존 상태의 표시·동작 가용성만 고치며 위치 취득·안내 상태 머신과 저장 계약은 바꾸지 않는다. 완료 시 접근성 관점의 별도 컨텍스트 리뷰를 받는다. 실기기 VoiceOver 확인은 별도 잔여다. E43 ⑥ 수동 지정 시 권한 요청은 사용자 선택 대기이며 응답 전에는 고치지 않는다.

## 실행 결과와 판정

### 작업 인계

실제 Codex CLI 4개를 Terminal.app의 개별 창으로 열고 서로 다른 worktree에서 구현했다. 짧은 설계·코드·접근성 리뷰는 별도 컨텍스트 서브에이전트가 수행하고, 코디네이터가 아래 지적을 수용했다. 구현자는 자기 소유 파일만 커밋했으며 코디네이터는 구현 코드를 직접 수정하지 않았다.

| 작업 | 작업자 커밋 | 코디네이터 판정 |
|---|---|---|
| E45 빈 이름 | `3c6ed8af` | 개행 공백이 문장에는 없는데 로터 조인에는 남는 리뷰 지적을 수정. 세 진입점 회귀 테스트와 재리뷰 통과 |
| E47 저장 코퍼스 | `bab54dd4` | 독립 리뷰 지적 0. 네트워크·env 접근 없는 실행과 실제 Kit 실패 전파 수용 |
| E28·E43 ④ 끝점 상태 | `5db3219a` | 설계·구현 리뷰의 blocker 0. 측위 await 이전부터 요청 소유, 취소 후 재진입, 라틴 표기 왕복 수용 |
| E43 ①②③⑤ 위치 UI | `64253b8f` | 구현 리뷰 지적 0. 전체 검사에서 검출한 네 언어 문구 불일치는 기존 Android 원문 재사용으로 수정, 제한 재리뷰 지적 0 |
| 위치 UI 테스트 타입 | `c1bd8431` | 생성기의 반환 구조를 전용 테스트에 명시. 앱·리소스·기대값 변경 없이 신규 TS7053 제거, 코디네이터 diff 수용 |

통합 검증용 worktree는 `~/gildongmu-wt/ios-backlog-integration`, 브랜치는 `test/ios-backlog-20260919`다. 원래 main은 사용자 선택 전까지 기준 SHA에 유지한다. 실행 프롬프트·작업 보고·상세 로그는 `~/gildongmu-wt/ios-sweep-20260919/`에 모았다. 원자료나 로그를 공개 저장소에 추가하지 않았다.

### 작업별 검증

| 작업 | 코디네이터 전체 검사 | 추가 증거 |
|---|---|---|
| 빈 이름 최종 수정본 | Vitest 4,352 통과·3 skip, Swift 797 통과 | 실제 앱 문장 함수 추출 스모크 ko 33·en 33 통과. 구버전 앱 함수는 ko 15·en 15 실패 |
| 저장 코퍼스 | Vitest 4,385 통과·3 skip | CLI 집중 37 통과, 합성 ko/en 표본 각각 구성 6/6·Kit 조인 20/20, 조인·디코딩 변이 exit 1 |
| 끝점 상태 | Vitest 4,357 통과·3 skip, Swift 795 통과 | Release 시뮬레이터 앱 빌드 통과 |
| 위치 UI 수정본 | Vitest 4,360 통과·3 skip | 문구 드리프트 수정 전 1 실패, 수정 후 집중 91 통과 |

저장 코퍼스의 실제 과거 dump는 확인한 비공개 경로에서 찾지 못했다. 합성 표본은 실행 계약 검증이며 신규 실응답의 증거가 아니다. 외부 네트워크 차단 아래 실제 Kit 실행과 원본 SHA-256 불변을 확인했고 신규 API 호출은 0회다.

### 검사 중 발생한 문제

- 빈 이름의 `normalizeStopName`은 개행을 제거하지 않았다. 표시용 부재 술어를 조인 입구에도 적용하고 LF·CRLF·유니코드 공백 조합에 대한 `.walk`·`.transit`·`.alight` 회귀 검사를 추가했다.
- 위치 UI의 첫 전체 검사는 `android-strings-drift.test.ts` 1건이 실패했다. 새 경유 정류소 키의 en/es/fr/it 원문·ICU 배치가 기존 Android와 달랐으며 iOS 원문·생성 카탈로그·전용 기대값만 맞췄다. 드리프트 예외나 Android 구현은 바꾸지 않았다.
- 코퍼스·위치 UI 브랜치의 전체 검사에서 미변경 `TransitGuidePanel.test.tsx:1500`이 간헐 실패했다. 독립 진단에서 원래 main `3fb486ea`의 해당 테스트를 5회 실행해 4 통과·1 실패로 동일 증상을 재현했다. 버튼은 자식 커밋에 나타나지만 통지는 effect를 거쳐 부모의 추가 렌더에서 갱신되는데, 테스트는 버튼만 기다린 뒤 통지를 동기 단언한다. iOS 회귀가 아닌 기존 웹 테스트 경합이며 이번에는 코드를 바꾸지 않았다. 수정 후보는 통지 단언 자체의 `waitFor`이고 웹 런타임 수정 근거는 없다.
- `npx tsc --noEmit`은 미변경 `android-strings-drift.test.ts`의 타입 추론 오류 8건으로 exit 2다. 원래 main에서 같은 명령을 실행했고 오류 출력이 바이트 동일했다. 이번 iOS 범위와 분리하며 전체 타입 검사 통과로 표시하지 않는다. ESLint는 오류 0·기존 경고 7로 exit 0이다.
- 문서 동기화 스크립트가 repo 루트에 없어 설치 스킬의 기존 문서 생성 함수를 사용했다. 생성본 본문이 기준 커밋의 정본과 같은지 먼저 검증하고 루트 한 쌍만 갱신했다. 시스템 Python 3.9의 `Path.write_text(newline=...)` 미지원은 설치된 Python 3.12로 재실행해 해결했다. 스킬 복사·홈 설정 변경은 하지 않았다.

### 유지하는 잔여

통합 코드 HEAD는 `ffd96a03`이고, 순서대로 `83f3b42c`·`28354a4f`·`0ee2d676`·`b8c846be`·`ffd96a03`에 작업자 커밋을 재현했다. `git show --format= --binary` 출력으로 원본 커밋과 각 통합 커밋 5쌍의 파일 패치가 바이트 동일함을 확인했다. 공통 문서는 별도 커밋으로 관리한다. 웹·서버·CLI/MCP·Android 런타임 변경은 없다. Android 변경은 신규 Kit 상태 코어의 pending 등록 한 건뿐이다.

통합 게이트: 최종 타입 보강 뒤 `npm run test:run`을 다시 실행해 366 파일·4,411 테스트 통과(1 파일·3 테스트 skip)를 확인했다. `swift test --package-path ios/GildongmuKit`은 66 suite·807 테스트 통과다. `check-xcstrings-keys.mjs`는 앱 1,084 참조·1,314키와 Kit 61 참조·120키를 통과했다. 신규 전용 테스트의 생성기 반환형 추론 오류는 수정했고, `npx tsc --noEmit --incremental false`에는 기존 Android 오류 8건만 남는다(기준선과 진단 출력 바이트 동일). ESLint는 오류 0·경고 7, 타입 후속 파일의 전용 ESLint는 오류·경고 0이다.

앱은 XcodeBuildMCP CLI로 iOS 18.6 iPhone 16 Pro 시뮬레이터 대상 `Release`·`Experimental` 빌드를 순차 실행해 모두 성공했다(각 33.8초·32.0초). 실행·설치하지 않았다. 두 구성 모두 기존 `@Entry` 클로저 관찰과 headermap 경고는 남는다. 정확한 빌드 로그는 실행 기록 폴더의 `integration-release-build.log`·`integration-experimental-build.log`다.

같은 통합 코드에서 합성 ko/en 코퍼스를 외부 네트워크·env 읽기 차단 아래 각각 실행했고, 표본 구성·동치 6/6 및 실제 Kit 테스트가 모두 통과했다. 저장 코퍼스 검증이 앱의 빈 이름 처리 변경과 함께 동작하는 범위까지 확인했다.

- B11 한국어 도착 줄 방향 추가, E43 ⑥ 수동 지정 시 GPS 권한 팝업 생략은 사용자 선택 전까지 현행 유지다.
- 실제 VoiceOver 낭독·행 정체성·설정 복귀·로터 가용성은 BACKLOG §2에서 별도 판정한다. 자동 테스트와 시뮬레이터 빌드는 이를 대신하지 않는다.
- 서버 도보 leg의 한글·영문 행선지 불일치 가능성, 다른 ODsay 게이트, 캐시·seed 정책, 실시간 안내 봉인 해제는 범위 밖이다.
- push·Vercel 배포/env·실기기 설치·App Store 제출을 하지 않고 pre-push 동결 가드를 유지한다.
