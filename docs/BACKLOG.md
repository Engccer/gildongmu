# BACKLOG — 길동무 (gildongmu)

> **열려 있는 항목의 정본.** 완료 이력·실호출 검증 로그는 `PROGRESS.md`, 설계는 `docs/superpowers/specs`, 항구 규칙은 `CLAUDE.md`가 정본이다. 이 파일엔 **아직 하지 않은 것**만 둔다 — 완결 항목은 남기지 않고 PROGRESS로 넘긴다.
>
> **기준선(2026-08-02 재실측)**: 게이트 전량 green(웹 1,620 · Kit 241 · CLI 172 · MCP 5 · lint 0 errors · `tsc --noEmit` 웹·CLI), prod 스모크 6경로 green(A2 마커 포함), iOS **1.0·1.1·1.2 전부 READY_FOR_SALE**·빌드 8 VALID(`node ios/scripts/asc-submit.mjs --check`), CLI/MCP npm **v0.8.0 발행 완료**, dodo 전량 이식 완결.
>
> **B 축이 비었다**(2026-08-02): 마지막 항목 B1이 위원장 실보행 통과로 확정됐다. 남은 열린 항목은 **전부 위원장 몫이거나 편승 대기**이고, 코드만으로 단독 착수할 마일스톤은 없다. 다만 이 결론이 나오면 반드시 **축 1(도달)부터 다시 본다**(2026-08-02에 같은 결론이 축 1의 부재 때문에 틀렸다).

## 판정 기준

항목을 남기는 축 다섯 가지. 어디에도 안 걸리면 폐기 목록으로 내린다. **섹션은 위에서부터 우선순위**이고, 알파벳은 순위가 아니라 **재사용하지 않는 식별자**다(D의 번호 규칙과 같은 정신이다. 다른 문서가 "백로그 B1"으로 참조하므로 재배치해도 글자는 그대로 둔다).

1. **도달**: 구현이 끝난 것이 사용자에게 닿았는가. 웹은 push=배포라 이 축이 없지만 **iOS·CLI는 릴리스가 별도 행위**라, 끝난 일이 아무에게도 가지 않은 채 쌓인다. 2026-08-02에 이 축이 없어 기준선이 "코드 마일스톤 0"이라고 틀리게 결론냈다.
2. **정합**: 이미 있는 기능이 틀린 답을 내는가
3. **시한**: 안 하면 언제 고장 나는가
4. **플랫폼 갭**: 같은 앱인데 웹과 iOS가 다른가
5. **데이터 실재**: 그 기능을 뒷받침할 데이터가 실제로 존재하는가. ⚠ **문서가 아니라 실호출로 판정한다.** 실시간 혼잡도는 이 축의 오판으로 보류돼 있다가, 실호출 한 번에 보류 사유가 사라지고 그날 출시됐다(2026-08-01).

G·A·B·C·E는 이 다섯 축으로 판정한다. **D·F는 축이 아니라 처리 방식이 다른 보관함**이다. D는 단독으로 열지 않고 다음 관련 작업에 편승시키는 것, F는 **코드로는 닫을 수 없어 위원장이 직접 해야 하는 것**(실기기·실보행 검증과 사람이 해야 하는 액션).

**종결된 식별자**(다른 문서가 참조할 수 있어 행선지만 남긴다. 내용은 `PROGRESS.md`):

| 식별자 | 무엇이었나 | 종결 |
|---|---|---|
| B1 | 목적지 거리 비컨 | 2026-08-02 (실보행 통과로 확정) |
| A2 | TAGO 미커버 지역 버스 안내 | 2026-08-02 |
| G2 | CLI/MCP v0.8.0 npm 발행 | 2026-08-02 |
| B2 | 길찾기 탭 자동 초점 이동 점검(M2) | 2026-08-02 |
| D2 · D3 | 내부 품질 정리 묶음 | 2026-08-01 |

⚠ **B2는 한 번 재사용된 식별자다.** 2026-08-01 이전에는 "대중교통 브리핑 출발지 변경"(B3과 함께 코드 대조로 전제 소멸 → 폐기)을 가리켰고, 그 뒤 "길찾기 탭 자동 초점 이동 점검"에 같은 글자가 다시 붙었다. `PROGRESS.md`에 두 뜻이 모두 남아 있으므로 **"백로그 B2" 참조를 만나면 날짜로 판별**한다. 위 재사용 금지 규칙이 지켜지지 않은 유일한 사례이고, 이 줄이 그 비용이다.

**항목은 코드 대조·실호출로 만료 점검한다.** 네 가지 방식으로 낡는다. ①다른 마일스톤이 지나가며 절반을 해결해 놓고 백로그만 낡는다(CLI 계단 회피 플래그: 카탈로그 배선은 이미 끝나 있었다) ②보류 사유 자체가 사실이 아닌 채로 남는다(실시간 혼잡도) ③**항목의 상태 서술이 틀려 있다**(B1을 "웹조차 부재"로 적어 뒀으나 웹 구현이 prod에 나가 있었다, 2026-08-01 정정) ④**끝난 일이 백로그에 도착하지 못한다**: PROGRESS의 마일스톤 줄에 "후속 관찰"·"확인 대기"로 적힌 미결이 백로그로 옮겨지지 않아, 백로그만 보면 열린 항목이 없어 보인다(2026-08-02 전수 대조로 5건 회수: F-a 4건·C4 쿼터 1건).

③④가 앞의 둘보다 위험하다. 잘못되거나 누락된 상태 서술은 우선순위 판단과 "다음 마일스톤" 결론까지 통째로 오염시킨다. **마일스톤을 닫을 때 그 줄에 남긴 관찰·대기 항목을 이 파일로 옮기는 것이 그 마일스톤의 마지막 단계**다.

---

## G. 릴리스 대기 (구현 완료 · 사용자 미도달)

**축 1(도달).** 여기 있는 것은 만들 게 남은 게 아니라 **보낼 게 남은 것**이다. 미도달분의 실기기 검증(F-a 2026-08-02 추가분)은 개발 빌드로만 가능하므로, 릴리스 지연이 검증 부채의 상환 비용까지 올린다.

### G1. iOS 1.3: 심사 제출본(빌드 8) 이후 쌓인 것

1.2는 2026-08-02 승인·출시됐고(READY_FOR_SALE), 그 뒤로 쌓인 것이 **1.3 후보**다. 내용은 접근성 수정이 압도적이다.

⚠ **커밋 수를 문서에 박지 않는다.** 필요하면 `git rev-list --count 81a312b..HEAD -- ios/`로 그 자리에서 센다(결과 중 1건은 앱 바이너리 밖 ASC 스크립트다). 종전엔 이 제목과 PROGRESS에 숫자를 적어 뒀는데 **사흘에 세 번 낡았고**(20 → 22 → 23 → 24), 그중 한 번은 백로그 기준선의 결론까지 오염시켰다. 커밋이 쌓일 때마다 갱신해야 하는 값은 문서가 아니라 명령이 들고 있어야 한다.

- **포커스 착지 6건**: 길찾기 시트 확정 후 착지, 시트 진입 검색 필드, 첫 후보 착지(Bool 바인딩 오용 + 오프스크린 AX 컬링), 검색 탭 동일 패턴, "내 주변"/"이 장소 주변" 4종 로드 완료 착지, 조회 후 포커스 유지 전환
- **통지 문장 정리 35건**: 자명한 꼬리 문장 제거 + 타 앱 유도 문구 제거(6로케일)
- **지하철 4-state**: 심야 "주변에 지하철역이 없습니다" 오낭독 제거
- **지역별 미제공 표기 + 최근접 역 안내**, 장소 상세 "이 장소 주변"에 지하철 행 추가, **TAGO 미커버 지역 버스 안내(A2)**
- **목적지 거리 추적**(비컨). **2026-08-02 실보행 통과로 확정**됐다(종전엔 이것이 G1의 선행 게이트였다). 같은 날 피드백 2건 반영: 추적 화면 **모달 분리**, 거리 표기 `1km 200m`.
- **거리 표기 전면 변경**: `formatDistance` 3벌 동조 + 지역 사본 4곳 제거. 앱 전체의 거리 낭독이 바뀌므로 실기기 확인 범위가 비콘에 한정되지 않는다(검색·내 주변·경로 요약 전부).

**선행 조건 2**(종전 3에서 하나 줄었다): ①`./ios/deploy-device.sh` 실기기 배포(2026-08-02 재확인 시에도 `devicectl list devices`가 `unavailable`, 기기 미연결) ②F-a의 2026-08-02 부채 실기기 VO 확인. **D4(ASC 쓰기 경로 미검증)를 여기에 편승**시킨다.

**G1은 축 1에 남은 유일 항목이다.** CLI/MCP는 v0.8.0 발행으로 빠졌고 웹은 애초에 이 축이 없다.

---

## A. 정합 결함 (있는 기능이 틀린 답을 냄)

### A1. 막차 후 중간 정류장 오강등 — **동결**

첫차·막차가 차고지 출발 기준이라 막차 직후에도 승차 가능한 중간 정류장 구간이 `outside`로 나온다. 강등일 뿐 제외가 아니고, 정확히 하려면 노선별 정류장 소요시간이 필요하다. 데이터 확보 전까지 수용(2026-08-01 판정). 기록만 유지.

**이 축은 A1(동결) 한 건이다.** 새 정합 결함이 발견되면 다른 무엇보다 먼저 올라온다.

---

## B. 플랫폼 갭 (웹에 있고 iOS에 없음)

전수 갭 조사(2026-07-29) 잔여분. 주 사용 창구가 iOS라 같은 장소에서 정보량이 다르면 결함으로 본다.

**이 축은 2026-08-02 비었다.** 마지막 항목 B1(목적지 거리 비컨)이 위원장 실보행 통과로 확정됐다. 남아 있던 것은 구현이 아니라 판정이었고, 그 판정은 코드로 대신할 수 없었다(자율성 헌장 §리뷰 계층의 "실사용이 정본"). 새 갭이 발견되면 여기 올라온다.

**신규 기능의 갭 예방 규칙**: 아래 E의 신규 도메인은 착수 시점에 웹·iOS를 같은 마일스톤에 넣는다. B 축이 존재했던 이유가 "웹 먼저 내고 iOS는 나중에"의 누적이었고, 공유 계층(`useNearbyFetch`/`NearbyPanelShell` · `NearbyLoadCore`/`RevealWindow`)은 정확히 그 동시 착수를 싸게 만들려고 뽑아 둔 것이다.

---

## C. 시한 있는 유지보수 (지금 착수 X, 시한 3개월 전 착수)

| 시한 | 남은 기간 | 항목 | 미조치 시 결과 |
|---|---|---|---|
| 2027-01-04 | 5개월 | ODsay URI 앱 키 만료 | 대중교통 길찾기 전면 중단 |
| 2027-06-30 | 11개월 | 네이버 지역검색 → NAVER API Hub(NCP) 인증 이관 | ko 장소 병합 사망. NCP 계정 기보유·비용 영향 없음 |
| 2027-07 | 11개월 | iOS 프로비저닝 프로파일 만료 | 실기기 배포 중단 |

착수 트리거: **2026-10** (ODsay 3개월 전).

### C4. data.go.kr 공유 쿼터: 날짜가 아니라 **사용량 트리거**

지하철 4-state(2026-08-02)가 `/api/station/subway-arrival/nearby`에서 처음으로 `DATA_GO_KR_API_KEY` 일 1,000회 공유 쿼터를 쓰기 시작했다(실시간이 빈 역에만 시간표를 조인). 원 관찰은 "배포 후 Vercel 로그로 호출 빈도 확인"이었는데, **코드 대조로 상한이 확인돼 로그 확인은 불필요해졌다**: TAGO 호출은 전부 `fetchTago`(`revalidate: 86_400`)를, 공휴일 판정은 `fetchIsHoliday`(동일 86,400)를 지나므로 **하루 비용이 요청 수가 아니라 "그날 실제로 조회된 고유 (역·노선·방향) 조합 수"로 묶인다**. 개인 사용 규모에서 안전하다.

**다만 상한이 쿼터보다 크다**(역당 최대 ~17콜 × 심야에 훑는 역 수). 남기는 것은 **관측 트리거 하나**다: 코레일·NMC 소아진료·에어코리아·기상청·무장애 중 **어느 하나라도 쿼터 초과가 관측되면 이 라우트를 가장 먼저 의심**한다(이 라우트만 IP 레이트리밋이 없다, 선재 조건). 잦은 재배포는 fetch 캐시를 비워 상한을 다시 태우므로 배포 직후 심야 사용이 겹치는 날이 최악이다.

---

## D. 내부 품질 (사용자 비가시: 기본은 편승, 편승할 작업이 없으면 별도 정리)

⚠ **편승은 얹힐 작업이 실제로 올 때만 유효한 정책이다.** 남은 D1·D4·D5는 다음 도보 작업·다음 릴리스·다음 커버리지 수정이 반드시 그 코드를 지나가므로 성립한다. 그렇지 않았던 D2·D3은 얹힐 작업(따릉이+문화행사 동시 수정, 라우트 14곳 동시 통과)이 예정에 없어 **편승 정책이 무기한 보류로 작동**했고, 결국 2026-08-01 단독 정리로 종결했다. 번호는 이력 추적용이라 재사용하지 않는다.

**그래서 D에 새 항목을 넣을 때는 "무엇에 얹을 것인가"를 함께 적는다.** 그 답이 없으면 편승이 아니라 **크기가 작을 뿐인 독립 항목**으로 취급한다.

- **D1. 카카오 도보 잔여**: 2026-08-01 spec §테스트 계획 대 실제 테스트 대조로 두 건으로 좁혔다.
  - **미커버 1건**: ACCESSIBLE 요청 → 무계단 경로 부재 → **기본 모드 재호출이 throw**하는 경로. `walk-route.ts`의 마지막 `fetchPrimaryOrFallback`이 던지면 502로 전파되는데 테스트가 없다(재호출의 *null* 케이스만 `walk-route.test.ts:210`에 있다).
  - **문구 부정확 1건**: ACCESSIBLE 응답이 왔는데 계단 문구가 남아 `no_stepfree_route`로 강등될 때(fail-closed), 안내 문장은 "계단 없는 경로를 찾지 못해 **일반 경로를 안내합니다**"라고 말하지만 실제로 반환하는 것은 ACCESSIBLE 경로다. 안전 방향의 오차라 급하지 않으나 사실과 다르다.
  - 다음에 도보를 손댈 때 편승.
- **D4. ASC 제출 스크립트의 쓰기 경로가 미검증**: `ios/scripts/asc-submit.mjs`는 읽기 경로(인증·앱 조회·버전/빌드 조회·로케일 매칭·드라이런)만 실호출로 확인됐다(2026-08-01). **PATCH·POST(버전 생성·What's New 입력·빌드 연결·심사 제출)는 한 번도 실행된 적이 없다**: 심사 중인 버전에 시험 삼아 쓸 수 없어 구조적으로 확인이 미뤄졌다. **편승 대상이 G1(1.3)으로 확정됐다**(1.2가 승인돼 심사 중인 버전이 없으므로 쓰기 차단도 풀렸다). `--apply`(제출 없이) → ASC 초안 육안 확인 → `--submit` 순으로 닫는다. ⚠ 미검증 상태에서 `--apply --submit`을 한 번에 부르지 말 것: 쓰기 실패가 절반만 적용된 초안을 남기면 브라우저로 되돌아가 수습해야 하고, 그게 이 스크립트를 만든 이유(세션 만료 시 사람 로그인 없이 멈추는 문제)를 되살린다.
- **D5. TAGO 커버리지 seed의 무해한 오염 항목**(2026-08-02 리뷰 검출): `src/lib/tago-coverage.ts`의 `COVERED` 집합이 `Math.floor(code / 1000)`로 도 접두를 만드는데, 2자리 코드(세종 12·광역시 21~26·제주 39)에서는 이 값이 0이 되어 `"0:세종특별시"` 같은 항목이 섞인다. **지금은 무해하다**: 그 지역들은 `WHOLE_PROVINCE_COVERED`·제주 분기가 먼저 잡아 이 매칭에 도달하지 않는다. 다만 무해한 이유가 "다른 분기가 먼저 잡아서"라는 우연에 가까워, 분기 순서를 바꾸는 수정이 조용히 이 오염을 깨울 수 있다. seed 적재 단계에서 2자리 코드를 제외하는 한 줄이면 끝난다. 다음에 이 계층을 손댈 때 편승.

---

## E. 신규 기능 후보

**실호출로 확인한 것과 문서로만 아는 것을 섞지 않는다.** 아래 순위는 2026-08-01 실호출 재조사 결과다.

> 종전 1순위였던 **실시간 인구 혼잡도는 2026-08-01 출시**했다(상세 `PROGRESS.md`). 보류 사유였던 "영역 경계 미공개"는 전제가 틀렸음이 실호출로 드러났다: 전체 `citydata`가 구성 정류장 좌표를 준다. **문서가 아니라 실호출로 판정한다는 '데이터 실재' 축이 항목 하나를 통째로 되살린 사례다.**

### 후보

| # | 항목 | 선행 조건 | 판정 |
|---|---|---|---|
| E1 | 지하철 혼잡도 통계 (서울교통공사 OA-12928) | 기보유 키 | 요일·30분 단위 분기 갱신 통계라 실시간이 아니고, 1~8호선 한정(9호선·코레일 공백). ⚠ API 서비스명 미확인(후보 3종 전부 `ERROR-500`) — 착수 전 실호출 확인 필요 |
| E2 | ITS 돌발상황 브리핑 (사고·공사·통제) | **위원장 액션**: its.go.kr 자체 가입·apiKey 발급(LINK형이라 data.go.kr 키 불가) | 커버리지가 고속도로·국도 중심이라 도심 이면도로 공백. 자동차 브리핑 한정 가치 |
| E3 | 장거리 교통 (고속·시외버스·열차) | 기보유 키 | 앱 스코프(도시 내 이동)와 어긋남 — 낮음 |

---

## F. 위원장 몫 (실사용 검증 + 직접 액션)

여러 마일스톤에 흩어져 있던 "실기기 VoiceOver 확인 잔여"를 한 곳에 모은다. 실사용에서 자연 관찰하거나, 별도 QA 세션으로 한 번에 소화한다(선택은 위원장).

### F-a. 실기기·실사용 검증 부채

**1.2가 출시돼(2026-08-02) 아래 1.2 포함분은 이제 스토어 배포본으로도 확인된다.** 1.3 후보분(G1)만 개발 빌드가 필요하다.

- 내 주변 → 장소 상세 iOS 이식 (2026-07-26)
- 소아 진료 커버리지 확장 (2026-07-26)
- 웹→iOS 갭 이식 1차: 계단 회피 토글·보행 인프라 화면·추천 질문 (2026-07-29)
- 웹 nearby 공유 계층 4시나리오: 패널 교대·로딩 중 Esc·done 재조회·트리거 복원 (2026-07-30)
- 단축어 진입 마이크 행 포커스 착지 (2026-07-26)
- **iOS 채팅 멀티 턴 먹통 재발 관찰** (2026-07-20 원인 확정·수정 후 관찰 중)
- 장소 상세 "이 장소 주변" 3행: 섹션 heading 발견 경로, push 후 뒤로가기 맥락 회수 (2026-08-01)
- **1.2 신기능 3종**: 혼잡도 등급 행(`ConditionsView` 안, 날씨·공기질과 한 그릇), 문화행사 "내 주변" 목록, 대중교통 경로의 운행 밖 표기. 셋 다 시뮬 실측·실호출은 끝났으나 실기기 VO 확인 기록이 없다. **스토어 배포본으로 확인 가능**(1.2 출시 완료).

**2026-08-02 추가분(전부 G1 = 1.3 후보, 개발 빌드 필요).** PROGRESS의 마일스톤 줄에만 적혀 있던 것을 이 파일로 회수했다:

- **"이 장소 주변"·"내 주변" 4종 로드 완료 포커스 착지**: 판정이 **실기기 VO뿐**이다. 시뮬 AX 트리는 "어느 행이 포커스를 주장하는지"를 보여주지 않아 착지 성패를 원리적으로 검출하지 못한다. 지하철·버스는 역/정류소 heading, 따릉이는 첫 행, 날씨·공기질은 첫 섹션 헤더에 닿아야 한다. 새로고침 때는 **이동하지 않는 것이 정상**(첫 로드에만 착지).
- **장소 상세 "이 장소 주변" 지하철 행**: 시뮬 UI 자동화가 그 세션에서 입력을 전혀 받지 못해 **화면 실측 자체를 못 했다**(빌드·리뷰까지가 게이트). 3행 순서와 낭독 확인 필요. ⚠ 역 장소에서는 첫 항목이 그 역 자신이라 인라인 "역 도착 정보"와 데이터가 겹치는데, **숨기지 않기로 판정**한 결과가 실제로 거슬리는지가 확인 포인트다.
- **채팅 심야 지하철 상태 구분**(실사용 관찰): `get_subway_arrivals`가 4-state 필드를 패스스루만 하고 declaration에 상태 설명을 넣지 않았다(프롬프트 덧대기는 실측으로 필요가 입증될 때만). 심야에 채팅으로 물어 **"운행 종료"와 "정보 없음"을 혼동해 답하지 않는지** 확인. 혼동하면 그때 declaration을 보강한다.
- **채팅 지역별 미제공 요약**(실사용 관찰): 서울 밖에서 따릉이·문화행사를 물었을 때 채팅이 `unavailableHere`를 **"근처에 없음"으로 요약하지 않는지**. 도구 `data`에 notice 문장을 함께 넘겨 뒀다. "이 지역엔 서비스가 없다"와 "근처에 없다"는 사용자 행동이 갈리는 다른 사실이다.
- **버스 미제공 안내 낭독**(A2 iOS 몫): 강릉 등 TAGO 미커버 지역에서 "내 주변"·"이 장소 주변" 버스를 열면 `APIError.unavailableHere(.noBusData)` 경로로 "이 지역은 정류소 정보가 제공되지 않습니다"가 나와야 한다. 로컬·prod 실호출과 시뮬 빌드까지는 확인했으나 **실기기 VO 낭독 기록이 없다**. 확인 포인트는 이 문장이 0건("주변에 정류소가 없습니다")과 **다르게 들리는가**다. 두 상태를 구분하려고 만든 기능이라 낭독이 같으면 만든 값이 없다.
- **채팅이 타 앱을 다시 권하지 않는지**(실사용 관찰): systemInstruction의 "구체적 대안 한 가지"가 "네이버 지도·카카오맵을 이용하세요"를 낳아 **긍정 트리거 최소판**으로 교체했다(2026-08-02). 프롬프트 행동은 latent라 실호출 3건으로는 표본이 얇다. 다양한 실패 상황(권한 거부·조회 실패·미제공 지역)에서 타 앱 유도가 재발하면 그때 금지문을 덧댄다. **실측으로 필요가 입증되기 전에는 덧대지 않는다**([[prompt-edits-minimalism-first]]).
- **거리 추적 모달 포커스 2종**: 열릴 때 **중지 버튼**에, 닫힐 때 **시작 버튼**에 착지해야 한다. 시뮬 AX 트리는 어느 요소가 포커스를 주장하는지 보여주지 않아 **판정이 실기기 VO뿐**이다. 중지 버튼·스와이프·VO escape 세 경로가 모두 추적을 멈추고 같은 자리로 돌아오는지 함께 본다. ⚠ 도착지를 "현재 위치"로 바꾸는 경로는 추적 중 물리적으로 닿을 수 없다고 코드로 확인했으나 실기기로는 미확인이다(리뷰 Minor 2). 시도해 볼 여지가 있으면 빈 시트가 스치는지 본다.
- **거리 표기 전면 변경 낭독**(범위가 비콘 밖이다): `1km 200m` 표기가 검색 결과·"내 주변" 각 도메인·경로 요약·현재 위치 정위까지 **앱 전역**에 적용됐다. 특히 **경로 요약 2종**(자동차 `총 …`, 도보 `총 …, 약 N분`)은 문구에서 단위를 빼고 `{distance}`가 통째로 넣도록 바꿨으므로 단위 누락·이중 표기가 없는지 확인. 6로케일 전부 같은 치환이라 en에서도 본다.

### F-b. 코드로 못 닫는 직접 액션

- **거리 추적 간섭 시나리오**(실보행 자체는 2026-08-02 통과, 아래는 주변 경로): ①추적 중 다른 탭 전환 후 복귀 ②백그라운드 왕복 후 절대거리 재발화 없는지. ⚠ **종전 항목이던 "추적 중 목적지 재검색 + 받아쓰기"는 모달 전환으로 도달 경로가 사라졌다**(추적 중엔 시트가 검색 버튼을 덮는다). `outputSuppressed` 가드는 이중 방어로 남겨 뒀으므로 확인 대상에서만 뺀다. (길찾기·검색 포커스 이동은 2026-08-02 실기기 확인 완료.)
- **Windows 다운로드 폴더의 ASC 개인키 정리**: `hunyong-samsung` 다운로드 폴더에 `AuthKey_SZXQP7DLC7.p8` 원본과 2026-07-14 자 키 2개(`8A857RVBN2`·`R8Q8M92P9Y`)가 상주 중이다(Tailscale `scp`로 가져오기만 하고 지우지 않았다). App Manager 권한 자격 증명이라 정리를 권한다. **파괴적 삭제라 자율 진행 대상이 아니다**(현행 키는 `~/.appstoreconnect/private_keys/`에 권한 600으로 사본이 있고 .p8는 재다운로드 불가이므로, 지우기 전에 사본 존재를 먼저 확인할 것).

---

## 폐기 (재부활 금지 — 근거 포함)

같은 조사를 반복하지 않기 위해 왜 뺐는지를 남긴다.

| 항목 | 폐기 근거 |
|---|---|
| **웹 거리 추적도 모달로** | 2026-08-02 판정. 모달의 동기는 "걸으면서 목록 속 중지 버튼을 찾는 비용"인데 **웹앱 주 사용처가 컴퓨터라 실보행 자체가 성립하지 않는다**(B1 게이트 재정의와 같은 근거). 없는 문제를 위해 구조를 바꾸지 않는다. 웹은 disclosure 패널 유지, 거리 표기만 정본 공유로 함께 바뀐다. iOS·웹 UI 구조가 갈리는 것은 의도이지 갭이 아니다 |
| **딥링크 수단 선택** | 2026-08-01 코드 대조로 전제 소멸. 웹 `RouteLinks.tsx`도 수단별 링크를 내지 않는다(프로바이더당 대표 1개, "모드 선택은 길찾기 뷰 책임" 2026-07-30 스펙 §4). 남는 차이는 여는 화면(웹=장소 핀, iOS=도보 경로)이고 선택 UI 부재와 별개라 기능 갭이 아니다 |
| **대중교통 브리핑 출발지 변경** | 2026-08-01 코드 대조로 이미 구현 확인. iOS `DirectionsTabView`에 출발지 필드(`searchTarget = .from`)와 스왑이 있다 |
| **en 도보 경로** | 데이터 부재 3중 확정: Google Routes 한국 WALK 200+빈 배열, Tmap 보행자 en 미지원(3방식 실측), 카카오 도보 `lang`·`Accept-Language` 무시. 해결 수단 없음 |
| **Google Maps provider 교체** | 2026-08-01 4구간 동좌표 실호출 평가 탈락. 도보·자동차 기능 부재, ko는 지번 주소 반환으로 카카오 열위, 무료 한도도 국내보다 낮음 |
| **idle 홈 heading 레벨 점프** | 2026-07-30 옴니박스 IA 재편으로 전제 소멸. 현행 계층은 h1(Header) → h2(NearbyHub·검색 결과) → h3(패널) → h4(항목)로 점프 없음(실측 확인) |
| **둘러보기 기능 B (OSM 횡단보도·점자블록)** | 2026-07-22 보행 인프라로 출시 완료. 중복 항목 |
| **자전거 라우팅** | 국내 공개 REST 라우팅 API 부재, OSM 태깅 편차로 부적합 |
| **점자블록 턴바이턴** | 통합 공공데이터 부재. 국내 벤치마크조차 소수 역세권 자체 구축 |
| **실시간 칸별 혼잡도 / 장애인콜택시 연계** | 전자는 유료(Tmap Puzzle), 후자는 일별 통계 API만 존재 |
| **상업 예약(캐치테이블·야놀자) / 카카오 T 택시** | 공개 API 전무 재검증 완료(2026-06-14). 딥링크 링크아웃이 최대치 |
| **지도 렌더링** | 의도적 미도입. "지도 없이 완결되는 흐름"이 구조적 보장 장치(SPEC §3) |
| **KRIC 교통약자 이동경로 2종** | 2026-06-14 가입 후 무승인. 그 사이 코레일 15125774·서울교통공사 wksn·엘리베이터 OA-21212·음성유도기 OA-22526 4겹으로 역 접근성이 충족됨 → **동결**. 승인 통보가 오면 그때 재평가 |
| **NCP Directions ko 자동차 A/B** | 2026-07-30 ko 기본 Tmap 확정(완성 문장 우위). NCP는 en 전용으로 역할 확정 |
| **검색 자연어 라우터** | 2026-06-27 폐기 확정. deterministic 위에 얹은 LLM 안티패턴 |
| **명소 별도 섹션 / 버킷 섹션 그룹핑** | 2026-07-20·21 폐지 확정. 정확도순 전환으로 랜드마크가 자연 부상, 고정 서열이 1위를 매몰시킨 실측 6사례 |
| **택시 요금·통행료 표기 / 정거장별 하차 안내 / 저상버스 판정 / 배차간격** | 전부 기구현(2026-07-21 코드 대조 및 구현 완료) |

---

## 다음 마일스톤

직전 마일스톤(거리 추적 실보행 통과 + 피드백 반영)은 2026-08-02 완결. 상세는 `PROGRESS.md`.

**다음은 신규 기능이 아니라 릴리스다.** 축 1(도달) 신설로 드러난 결론이고, 순서는 하나뿐이다. 위 섹션을 재서술하지 않는다. 종전에 이 자리에 두었던 후보 표가 §B의 게이트 재정의를 따라가지 못해 혼자 낡은 전례가 있다.

1. **QA 세션 1회**(위원장): F-a의 2026-08-02 부채를 소화한다. **기기 연결이 그 세션의 시작**이고, 연결되는 즉시 `./ios/deploy-device.sh`가 첫 단계다. 이번에 늘어난 확인 범위는 거리 표기가 **앱 전역**이라는 점이다(비콘만이 아니다).
2. **G1 iOS 1.3 제출**: D4(ASC 쓰기 경로 검증)를 편승시킨다. 거리 추적이 확정돼 **릴리스 내용이 고정**됐다(종전의 "F-b 판정이 폐기면 빼고 제출" 분기는 소멸).

나머지는 전부 조건 대기다: **C**는 2026-10 트리거(ODsay 3개월 전)와 쿼터 관측, **E1**은 착수 전 서비스명 실호출, **E2**는 위원장의 its.go.kr 가입, **E3**은 앱 스코프와 어긋나 사실상 보류, **D1·D5**는 각각 다음 도보·커버리지 작업 편승.

⚠ **"위원장 몫밖에 없다"는 결론이 나오면 축 1(도달)부터 다시 본다.** 2026-08-02에 그 결론이 틀렸던 이유가 정확히 이 축의 부재였다.
