# BACKLOG — 길동무 (gildongmu)

> **열려 있는 항목의 정본.** 날짜별 완료 이력은 `CHANGELOG.md`, 현재 동작 상태는 `PROGRESS.md`, 설계·마일스톤 검증 상세는 `docs/superpowers/specs`·`plans`, 항구 규칙은 `CLAUDE.md`가 정본이다. 이 파일엔 **아직 하지 않은 것**만 둔다 — 완결 항목은 남기지 않고 `CHANGELOG.md`로 넘긴다(2026-08-08 문서 체계 재편으로 행선지가 PROGRESS에서 CHANGELOG로 바뀌었다).
>
> **기준선(2026-08-08 재실측)**: 테스트 전량 green(웹 2,019/185파일 · Kit 378/27스위트 · CLI 182 · MCP 5), prod 스모크 4경로 green(도보 계단회피+기하 · 대중교통 · 지하철 도착 4-state · 해외 좌표 커버리지 마커), iOS **1.0~1.3 전부 READY_FOR_SALE**·빌드 9 VALID(`node ios/scripts/asc-submit.mjs --check`), CLI/MCP npm **v0.8.0**, 워킹 트리 clean·origin/main 동기.
>
> ⚠ **정적 게이트 기준선(2026-08-08 갱신)**: `npm run lint`는 **error 0**(warning 4)로 회복했다 — E5가 같은 파일을 만지며 편승 정리했다. `tsc --noEmit`은 **선재 5 error**가 그대로다(D10 잔여). 0을 기준으로 쓸 수 없는 게이트는 새 결함을 자기 노이즈에 숨긴다.
>
> **결함 축(A)은 비었고 갭 축(B)에 1건이 올라왔다**(2026-08-08): A는 A1(동결) 하나뿐이고, B는 2026-08-02부터 0건이었다가 a11y 감사가 B4(웹 세션 시작 포커스 착지 부재, 낮음)를 올렸다. 지금 열려 있는 것은 **도달(G)·판정(F)·편승(D)·신규(E)**뿐이다. ⚠ B의 "비었다"에는 예외 각주가 하나 붙는다(봉인된 실시간 안내 계열 — 정의상 갭이지만 G3에 계상). ⚠ 이 결론이 나올 때마다 **축 1(도달)부터 다시 본다** — 2026-08-02에 같은 결론이 축 1의 부재 때문에 틀렸다.

## 판정 기준

항목을 남기는 축 다섯 가지. 어디에도 안 걸리면 폐기 목록으로 내린다. **섹션은 위에서부터 우선순위**이고, 알파벳은 순위가 아니라 **재사용하지 않는 식별자**다(D의 번호 규칙과 같은 정신이다. 다른 문서가 "백로그 B1"으로 참조하므로 재배치해도 글자는 그대로 둔다).

1. **도달**: 구현이 끝난 것이 사용자에게 닿았는가. 웹은 push=배포라 이 축이 없지만 **iOS·CLI는 릴리스가 별도 행위**라, 끝난 일이 아무에게도 가지 않은 채 쌓인다. 2026-08-02에 이 축이 없어 기준선이 "코드 마일스톤 0"이라고 틀리게 결론냈다.
2. **정합**: 이미 있는 기능이 틀린 답을 내는가
3. **시한**: 안 하면 언제 고장 나는가
4. **플랫폼 갭**: 같은 앱인데 웹과 iOS가 다른가
5. **데이터 실재**: 그 기능을 뒷받침할 데이터가 실제로 존재하는가. ⚠ **문서가 아니라 실호출로 판정한다.** 실시간 혼잡도는 이 축의 오판으로 보류돼 있다가, 실호출 한 번에 보류 사유가 사라지고 그날 출시됐다(2026-08-01).

G·A·B·C·E는 이 다섯 축으로 판정한다. **D·F는 축이 아니라 처리 방식이 다른 보관함**이다. D는 단독으로 열지 않고 다음 관련 작업에 편승시키는 것, F는 **코드로는 닫을 수 없어 위원장이 직접 해야 하는 것**(실기기·실보행 검증과 사람이 해야 하는 액션).

**종결된 식별자**(다른 문서가 참조할 수 있어 행선지만 남긴다. 내용은 `PROGRESS.md`):

| 식별자 | 무엇이었나 | 종결 |
|---|---|---|
| B1 | 목적지 거리 비컨 | 2026-08-02 (실보행 통과로 확정) |
| A2 | TAGO 미커버 지역 버스 안내 | 2026-08-02 |
| G2 | CLI/MCP v0.8.0 npm 발행 | 2026-08-02 |
| B2 | 길찾기 탭 자동 초점 이동 점검(M2) | 2026-08-02 |
| D2 · D3 | 내부 품질 정리 묶음 | 2026-08-01 |
| D4 | ASC 제출 스크립트 쓰기 경로 검증 | 2026-08-04 (1.3 제출로 전 구간 완주) |
| G1 | iOS 1.3 릴리스 | 2026-08-06 (`READY_FOR_SALE`) |
| A4 | 계단 회피 상태의 실시간 도보 안내 전달 | 2026-08-08 |
| D1 | 카카오 도보 잔여 2건(throw 경로 무테스트 · 문구 부정확) | 2026-08-08 (A4에 편승) |
| E5 | 지하철 빠른하차 출입문 병치 | 2026-08-08 (실호출 게이트 통과, ⏳ 실승차 판정은 F-a) |
| D10 lint 절반 | `TransitGuidePanel` 렌더 중 ref 대입 | 2026-08-08 (E5 세션 화면 작업에 편승, `npm run lint` error 0) |
| D11 | `CLAUDE.md` 재팽창 | 2026-08-08 (86,021 → 64,718자. 헌장 중복 제거 + 통합 상세 8절을 `docs/INTEGRATIONS.md`로 분리) |

⚠ **B2는 한 번 재사용된 식별자다.** 2026-08-01 이전에는 "대중교통 브리핑 출발지 변경"(B3과 함께 코드 대조로 전제 소멸 → 폐기)을 가리켰고, 그 뒤 "길찾기 탭 자동 초점 이동 점검"에 같은 글자가 다시 붙었다. `PROGRESS.md`에 두 뜻이 모두 남아 있으므로 **"백로그 B2" 참조를 만나면 날짜로 판별**한다. 위 재사용 금지 규칙이 지켜지지 않은 유일한 사례이고, 이 줄이 그 비용이다.

**항목은 코드 대조·실호출로 만료 점검한다.** 네 가지 방식으로 낡는다. ①다른 마일스톤이 지나가며 절반을 해결해 놓고 백로그만 낡는다(CLI 계단 회피 플래그: 카탈로그 배선은 이미 끝나 있었다) ②보류 사유 자체가 사실이 아닌 채로 남는다(실시간 혼잡도) ③**항목의 상태 서술이 틀려 있다**(B1을 "웹조차 부재"로 적어 뒀으나 웹 구현이 prod에 나가 있었다, 2026-08-01 정정) ④**끝난 일이 백로그에 도착하지 못한다**: PROGRESS의 마일스톤 줄에 "후속 관찰"·"확인 대기"로 적힌 미결이 백로그로 옮겨지지 않아, 백로그만 보면 열린 항목이 없어 보인다(2026-08-02 전수 대조로 5건 회수: F-a 4건·C4 쿼터 1건).

③④가 앞의 둘보다 위험하다. 잘못되거나 누락된 상태 서술은 우선순위 판단과 "다음 마일스톤" 결론까지 통째로 오염시킨다. **마일스톤을 닫을 때 그 줄에 남긴 관찰·대기 항목을 이 파일로 옮기는 것이 그 마일스톤의 마지막 단계**다.

---

## G. 릴리스 대기 (구현 완료 · 사용자 미도달)

**축 1(도달).** 여기 있는 것은 만들 게 남은 게 아니라 **보낼 게 남은 것**이다.

### G3. 실시간 안내 계열: 봉인 해제 대기 (판정이 선행)

**G1(iOS 1.3)은 2026-08-06 `READY_FOR_SALE`로 닫혔다**(2026-08-08 `--check` 재확인). 그 릴리스가 담은 것 — 포커스 착지 6건, 통지 문장 35건, 지하철 4-state, 지역별 미제공 표기, A2, 거리 표기 전면 변경 — 은 전부 스토어 배포본으로 도달했다. 내역은 `PROGRESS.md`.

**남은 미도달은 봉인된 실시간 안내 계열 하나다.** 수단별 경로 안내·거리 추적·백그라운드 사운드·계단 회피 통지가 여기 속한다. 봉인은 손이 아니라 빌드 구성이 한다(`AppConfig.realtimeGuidanceEnabled`의 `#if EXPERIMENTAL`, 2026-08-08 코드 대조로 유효 확인).

- **선행 조건은 코드가 아니라 판정이다**: F-a의 실시간 안내 계열 판정(E4 잔여 · B1 실주행 · B2 실승차 · 백그라운드 톤 10종 · 계단 회피 3종). 통과해야 `#if`를 지우고(플래그 졸업) 다음 버전에 실을 수 있다.
- ⚠ **이건 B 축의 정의에도 걸린다**(웹에 있고 iOS에 없음 — 같은 기능이 웹 prod에는 나가 있다). **의도된 봉인이므로 여기 G3로 세고 B에는 중복 계상하지 않는다.** 다만 B가 비어 있는 이유가 "갭이 없어서"가 아니라 "이 갭을 결함으로 세지 않기로 해서"라는 것은 기록해 둔다 — 봉인이 길어질수록 이 구분이 흐려진다.
- ⚠ **커밋 수를 문서에 박지 않는다.** 필요하면 `git rev-list --count <직전 릴리스>..HEAD -- ios/`로 그 자리에서 센다. 종전엔 숫자를 적어 뒀는데 **사흘에 세 번 낡았고**(20 → 22 → 23 → 24) 그중 한 번은 기준선의 결론까지 오염시켰다. 갱신이 필요한 값은 문서가 아니라 명령이 들고 있어야 한다.

⚠ **릴리스 지연이 더는 검증을 막지 않는다**(2026-08-04 이후): 실험판은 번들 ID가 달라 공식판과 공존하므로, 판정에 필요한 개발 빌드를 넣어도 스토어판이 사라지지 않는다. 종전에 이 축이 무거웠던 이유가 그 결합이었다.

---

## A. 정합 결함 (있는 기능이 틀린 답을 냄)

### A1. 막차 후 중간 정류장 오강등 — **동결**

첫차·막차가 차고지 출발 기준이라 막차 직후에도 승차 가능한 중간 정류장 구간이 `outside`로 나온다. 강등일 뿐 제외가 아니고, 정확히 하려면 노선별 정류장 소요시간이 필요하다. 데이터 확보 전까지 수용(2026-08-01 판정). 기록만 유지.

### A3. iOS "현재 위치" 단발 취득 정확도: **2026-08-02 종결 (⑤까지)**

①~④ 닫힘(커밋 `209ff5b`) + ⑤(임시 정밀 허가)도 같은 날 저녁 위원장 피드백으로 착수·완결됐다(실기기 동작 확인). 설계 정본 `docs/superpowers/specs/2026-08-02-location-accuracy-design.md`(§4가 ⑤ 구현 계약, §4.1이 "항상 허용" 불가 확정, 재조사 금지).

### A5. 상세 안내 주기 통지가 방금 읽은 문장을 통째로 반복

위원장 실보행 피드백(2026-08-08): "같은 메시지가 연속으로 두 번씩 낭독된다. 특정 간격으로가 아니라 한 번 읽을 때 두 번 읽어주는 느낌이어서 노이즈로 들린다."

**게시가 두 번 되는 것이 아니다.** `announce()`는 단일 경로이고 `guideStep`은 fix당 이벤트를 하나만 낸다. 결함은 **이벤트의 내용이 직전 발화와 같은지를 아무도 검사하지 않는 것**이다. 실호출 경로(길동역→강동구청, 2,761m/17스텝)에 보행 1.2m/s를 태운 시뮬레이션 로그:

```
 160s (announceSteps) 훈훈한내과 앞에서 왼쪽으로 돌아 T world까지 양재대로를 따라 417m 이동
 175s (periodic)  22m 앞 훈훈한내과 앞에서 왼쪽으로 돌아 T world까지 양재대로를 따라 417m 이동
1448s (announceSteps) 다음 안내 2개. 농협입구교차로에서 11m 이동, 횡단보도 이용, 음향신호기 있음. 퍼리모먼츠…
1485s (bundleReread)  (같은 문장) → 1500s (또 같은 문장)
```

| # | 지점 | 결함 |
|---|---|---|
| A5-1 | `route-guide.ts:590` / `RouteGuide.swift:508` | `bundleReread(indices)`가 `announceSteps(indices)`와 **동일 indices** → `GuideText.unit`이 글자까지 같은 문자열을 만든다 |
| A5-2 | `route-guide.ts:577` / `RouteGuide.swift:496` | `periodic`이 `steps[stepIndex+1].description` **전문**을 매번 다시 싣는다. 첫 구간에서 같은 문장이 9회 낭독됐다 |
| A5-3 | `lastAnnouncedAt` 갱신 시점 | 발화 **시작** 시각이다. 40~50자 문장의 VoiceOver 낭독은 10초 이상 걸리는데 주기 하한이 15초라, 낭독이 끝나고 몇 초 만에 같은 문장이 시작된다. 이것이 "한 번 읽을 때 두 번"의 기제다 |

**설계 의도는 옳았고 구현이 그것을 초과했다.** 스펙 `2026-08-03-realtime-route-guidance-design.md` §110이 "`bundle`에서는 주기 통지가 묶음 재통독이다(리뷰 #5 — 첫 통독을 소음으로 놓쳐도 자동 채널이 침묵하지 않는다)"라고 적었는데, "놓쳤을 때를 위한 1회 보험"이 "무한 반복"이 됐다.

**처방 방향**(착수 시 spec으로): 이미 말한 문장은 다시 말하지 않고 바뀐 값(거리)만 말한다. `GuideState`에 "주기 통지가 마지막으로 전문을 낭독한 대상 스텝"을 들고, 같은 대상의 2회차부터는 전문을 빼고 거리만 낸다. `announceSteps`·`farNotice`도 그 대상의 전문을 낭독하므로 **같은 기준을 함께 갱신**해야 한다(그러지 않으면 임박 낭독 15초 뒤 첫 주기가 전문으로 나가 A5-2가 그대로 남는다). `bundleReread`는 `periodic`으로 흡수하면 이벤트를 둘로 둘 이유가 없어진다. A5-3은 주기 하한에 낭독 예상 시간(문자 수 근사, 상한 필요)을 더해 완화한다.

문헌 근거: RouteNav(ASSETS '23)는 이산 이벤트를 "타일당 1회 + 선행 chime"으로, StreetNav(UIST '24)는 지속 상태를 "고정 주기 10초 + 불감대 50°"로 가른다. `bundleReread`는 이산 이벤트를 주기 반복하는 것이라 그 구분을 어긴다.

⚠ **`bundleReread` 식별자를 폐기하면 재사용하지 않는다**(B3 재사용 금지와 같은 규율 — fixture·로그·테스트 이름이 갈린다).

**지금 착수하지 않는 이유**: 위원장 판정(2026-08-08)으로 최종 접근 안내 spec에서 분리했다. 두 사안은 원인이 같지만("리듀서가 말할 값이 있는지 보지 않고 말할 시각인지만 본다") 마일스톤을 묶으면 신규 설계 판정과 기존 결함 수정이 한 리뷰에 섞인다.

**이 축은 A1(동결)·A5 두 건이다**(A4는 2026-08-08 종결). 새 정합 결함이 발견되면 다른 무엇보다 먼저 올라온다.

---

## B. 플랫폼 갭 (웹과 iOS가 다름)

전수 갭 조사(2026-07-29) 잔여분. 주 사용 창구가 iOS라 같은 장소에서 정보량이 다르면 결함으로 본다.

⚠ **섹션 제목이 2026-08-08 넓어졌다**(종전 "웹에 있고 iOS에 없음"). 축 4의 정의는 원래 방향이 없는데("같은 앱인데 웹과 iOS가 다른가") 제목이 한쪽 방향만 적어 두어, **iOS에 있고 웹에 없는** 갭(B4)이 들어올 자리가 없었다. 역사적 방향이 그랬을 뿐이다.

**2026-08-02~08-08 이 축은 비어 있었다.** B1(목적지 거리 비컨)이 위원장 실보행 통과로 확정되며 마지막 항목이 닫혔고, 2026-08-08 a11y 감사가 B4를 새로 올렸다. ⚠ **B3은 쓰지 않는다** — 2026-08-01 이전 "대중교통 브리핑 출발지 변경"이 쓰고 폐기한 식별자다(재사용 금지).

### B4. 웹 안내 세션 시작 시 포커스 착지 부재 (iOS엔 있음) — 낮음

웹 `TransitGuidePanel`은 세션을 시작하면 트리거 버튼이 `{!open && …}`(`src/components/TransitGuidePanel.tsx:131`)으로 unmount되며 **커서가 body로 떨어진다**(헌장 §5 "포커스를 쥔 요소를 제거하는 상태 전이"). `useTransitGuide.start`는 `announce`만 하고, `waitingLabelRef.focus()`는 **목록 항목 소실 복귀** 경로 하나뿐이다. iOS는 시트 제시가 VO 커서를 시트로 옮겨 해당 없다.

- **우선순위가 낮은 이유**: live region이 시작을 알리므로 사용자는 "시작됐다"를 안다. 잃는 것은 커서 위치뿐이다.
- **그런데도 남기는 이유는 재규명 비용이다**(a11y 감사 판단 2026-08-08). 이 결함은 **증상이 없다** — 침묵이 아니고, 커서가 어디 있는지는 코드에 드러나지 않는다. 이번에도 스펙 문장 하나를 검증하다 나왔지 증상에서 역추적된 게 아니다. 이 줄이 없으면 다음 사람이 "여기 착지가 없다"를 처음부터 다시 발견해야 한다.
- ⚠ **지금 고치지 않는다**: 착지 후보가 둘(패널 컨테이너 vs `waitingLabel`)이고 어느 쪽이든 `arrived`·`riding` 전이의 기존 선점 이동과 순서가 얽힌다. 그 전이를 함께 다루는 작업에 얹는다.
- 이 repo에서 **웹·iOS 비대칭은 여러 번 실제 결함으로 판명된 계열**이다(`stepFreeNotice` 통지 축이 그랬다). 심각도와 무관하게 "비대칭이 존재한다"는 사실은 남긴다.

⚠ **"비었다"에 예외가 하나 있다**(2026-08-08 명시): 봉인된 실시간 안내 계열은 웹 prod에 있고 iOS에 없으므로 **정의상 이 축에 해당**하지만, 의도된 봉인이라 **G3에 계상하고 여기서는 세지 않는다**. 중복 계상 방지가 목적이지 갭이 없다는 뜻이 아니다.

**신규 기능의 갭 예방 규칙**: 아래 E의 신규 도메인은 착수 시점에 웹·iOS를 같은 마일스톤에 넣는다. B 축이 존재했던 이유가 "웹 먼저 내고 iOS는 나중에"의 누적이었고, 공유 계층(`useNearbyFetch`/`NearbyPanelShell` · `NearbyLoadCore`/`RevealWindow`)은 정확히 그 동시 착수를 싸게 만들려고 뽑아 둔 것이다.

---

## C. 시한 있는 유지보수 (지금 착수 X, 시한 3개월 전 착수)

| 시한 | 남은 기간 | 항목 | 미조치 시 결과 |
|---|---|---|---|
| 2027-01-04 | 5개월 | ODsay URI 앱 키 만료 | 대중교통 길찾기 전면 중단 |
| 2027-06-30 | 11개월 | 네이버 지역검색 → NAVER API Hub(NCP) 인증 이관 | ko 장소 병합 사망. NCP 계정 기보유·비용 영향 없음 |
| 2027-07 | 11개월 | iOS 프로비저닝 프로파일 만료 | 실기기 배포 중단 |

착수 트리거: **2026-10** (ODsay 3개월 전).

### C4. data.go.kr 공유 쿼터: 날짜가 아니라 **사용량 트리거**

지하철 4-state(2026-08-02)가 `/api/station/subway-arrival/nearby`에서 처음으로 `DATA_GO_KR_API_KEY` 일 1,000회 공유 쿼터를 쓰기 시작했다(실시간이 빈 역에만 시간표를 조인). 원 관찰은 "배포 후 Vercel 로그로 호출 빈도 확인"이었는데, **코드 대조로 상한이 확인돼 로그 확인은 불필요해졌다**: TAGO 호출은 전부 `fetchTago`(`revalidate: 86_400`)를, 공휴일 판정은 `fetchIsHoliday`(동일 86,400)를 지나므로 **하루 비용이 요청 수가 아니라 "그날 실제로 조회된 고유 (역·노선·방향) 조합 수"로 묶인다**. 개인 사용 규모에서 안전하다.

**다만 상한이 쿼터보다 크다**(역당 최대 ~17콜 × 심야에 훑는 역 수). 남기는 것은 **관측 트리거 하나**다: 코레일·NMC 소아진료·에어코리아·기상청·무장애 중 **어느 하나라도 쿼터 초과가 관측되면 이 라우트를 가장 먼저 의심**한다(이 라우트만 IP 레이트리밋이 없다, 선재 조건). 잦은 재배포는 fetch 캐시를 비워 상한을 다시 태우므로 배포 직후 심야 사용이 겹치는 날이 최악이다.

---

## D. 내부 품질 (사용자 비가시: 기본은 편승, 편승할 작업이 없으면 별도 정리)

⚠ **편승은 얹힐 작업이 실제로 올 때만 유효한 정책이다.** 남은 D1·D4·D5는 다음 도보 작업·다음 릴리스·다음 커버리지 수정이 반드시 그 코드를 지나가므로 성립한다. 그렇지 않았던 D2·D3은 얹힐 작업(따릉이+문화행사 동시 수정, 라우트 14곳 동시 통과)이 예정에 없어 **편승 정책이 무기한 보류로 작동**했고, 결국 2026-08-01 단독 정리로 종결했다. 번호는 이력 추적용이라 재사용하지 않는다.

**그래서 D에 새 항목을 넣을 때는 "무엇에 얹을 것인가"를 함께 적는다.** 그 답이 없으면 편승이 아니라 **크기가 작을 뿐인 독립 항목**으로 취급한다.

- **D9. ASC 프로모션 텍스트 승계가 자동화되지 않았다**(D4 검증에서 남은 구멍, 2026-08-04): `asc-submit.mjs`는 프로모션 텍스트를 채우지 않고 "비어 있다"고 경고만 한다(빈 값 덮어쓰기 방지 설계). 그런데 **새 버전 초안은 그 값을 승계하지 않으므로**([[asc-new-version-promo-text-not-inherited]]) 제출 전에 직전 버전 값을 복사하는 단계가 매번 필요하고, 지금은 세션마다 임시 스크립트를 다시 쓴다. 모르고 제출하면 스토어에서 그 줄이 사라진다. **얹힐 곳**: 다음 릴리스 제출.
- **D5. TAGO 커버리지 seed의 무해한 오염 항목**(2026-08-02 리뷰 검출): `src/lib/tago-coverage.ts`의 `COVERED` 집합이 `Math.floor(code / 1000)`로 도 접두를 만드는데, 2자리 코드(세종 12·광역시 21~26·제주 39)에서는 이 값이 0이 되어 `"0:세종특별시"` 같은 항목이 섞인다. **지금은 무해하다**: 그 지역들은 `WHOLE_PROVINCE_COVERED`·제주 분기가 먼저 잡아 이 매칭에 도달하지 않는다. 다만 무해한 이유가 "다른 분기가 먼저 잡아서"라는 우연에 가까워, 분기 순서를 바꾸는 수정이 조용히 이 오염을 깨울 수 있다. seed 적재 단계에서 2자리 코드를 제외하는 한 줄이면 끝난다. 다음에 이 계층을 손댈 때 편승.

- **D7. iOS 실패 카피의 꼬리 문장 잔재**(2026-08-02 A3 리뷰가 부수 검출): `ios.common.retryLater` = "잠시 후 다시 시도해 주세요"가 그대로 남아 있다. 2026-08-02 꼬리 문장 정리에서 **제거 대상으로 명명된 바로 그 문장**인데 iOS 카탈로그에서 살아남았고, 소비자가 셋이다(`NearbyOverlayCopy.defaultFailure` · `SearchView` · `WhereAmIView`). A3의 비콘 단락 게이트가 `.failedLocation` 경로 트래픽을 늘려 더 자주 들린다. ⚠ **같이 있는 `ios.voice.errorDownload`("네트워크 연결 후 다시 시도해 주세요")는 유지 대상**이다(조건을 알리므로 새 정보다). 다음에 iOS 문구를 손댈 때 편승.
- **D8. iOS `transitLegText` 도보 4분기가 테스트되지 않는다**(2026-08-07 길찾기 재편에서 발견): 앱 타깃에 테스트 레인이 없어 spec §4.3 결정표(`toName`·`distanceMeters` 유무 → 4개 키)가 iOS에서 검증되지 않는다. 같은 표를 웹 `TransitRouteBriefing.test.tsx`가 덮는 미러 구조에 기대고 있다. **위험은 좁다**: `(name, distance)` 튜플 스위치라 Swift 망라성 검사가 분기 누락을 막고, 키 오타는 `check-xcstrings-keys.mjs`가 잡으며, 위치 인자 어순은 카탈로그 실문자열 실행으로 확인했다. 남는 것은 "맞는 키를 틀린 case에 배치"뿐이고 4줄이라 육안 검토로 확인했다. `formatDistance`처럼 Kit으로 옮기면 테스트 가능해진다. **얹힐 곳**: 다음에 iOS 대중교통 문구를 손댈 때.
- **D6. 카카오 응답 `revalidate` 캐시가 약관 회색지대**(2026-08-02 조사에서 부수 발견, 근거는 `docs/research/RESEARCH-2026-08-02-realtime-walk-navigation.md` §8.2): 카카오 담당자가 2026-07-23 devtalk에서 **Next.js `revalidate 3600`을 "임시 DB 역할"로 규정하고 운영 정책 위반**이라고 답했다. 우리 코드의 해당 지점은 `kakao-address` 4곳(3600) · `kakao-walk`(3600) · `kakao-local`(300)이다. ⚠ **단정하지 말 것**: 그 답변은 **로컬(검색) API 문맥**이고, 같은 조항에 대해 2024-10 담당자는 "1~2시간 이내 캐싱은 가능"이라고 답해 **해석이 시점에 따라 갈린다**. 공식 정정 공지는 없다. 이건 코드 품질이 아니라 정책 판단이라 편승 대상이 아니며, 확정하려면 도보 API 한정 질의가 필요한데 **외부 발신이라 위원장 판단 사안**이다. 실시간 안내(E4)와 무관하게 이미 존재하던 사안이므로 그 착수 여부와 분리해 기록한다.
- **D10. `tsc --noEmit` 기준선이 0이 아니다**(2026-08-08 재실측 → 같은 날 절반 종결): lint 1 error는 E5의 세션 화면 작업에 편승해 닫았고(`npm run lint` error 0), **`tsc --noEmit` 5 error가 남았다**(전부 선재 테스트 파일). **왜 남기는가**: 출력이 "0 errors"가 아니면 새로 들어온 에러 1건이 기존 노이즈 사이에 묻힌다. 실제로 계단 회피 마일스톤 내내 `tsc` 출력의 선재 5건을 매 실행 눈으로 걸러야 했다.
  - **lint 1건**: `src/components/TransitGuidePanel.tsx`의 렌더 중 ref 쓰기(`react-hooks/refs`). 최신 콜백을 ref에 미러하는 패턴이라 **동작 결함은 아니고** React 19 규칙이 렌더 중 쓰기를 금지한 것이다. `7f01a53` 이전부터 있었다(worktree 대조로 확인). **얹힐 곳**: 다음에 대중교통 패널을 손댈 때.
## E. 신규 기능 후보

**실호출로 확인한 것과 문서로만 아는 것을 섞지 않는다.** 아래 순위는 2026-08-01 실호출 재조사 결과다.

> 종전 1순위였던 **실시간 인구 혼잡도는 2026-08-01 출시**했다(상세 `PROGRESS.md`). 보류 사유였던 "영역 경계 미공개"는 전제가 틀렸음이 실호출로 드러났다: 전체 `citydata`가 구성 정류장 좌표를 준다. **문서가 아니라 실호출로 판정한다는 '데이터 실재' 축이 항목 하나를 통째로 되살린 사례다.**

### E7. 최종 접근 기준 도로명(`roadName`) — **미도달, 소스 미정**

`FinalApproachGeometry.roadName`은 계약에도 i18n 문구에도 있지만 **서버가 채우지 않는다**(라우트 핸들러가 `computeFinalApproach(guide, dest)`를 인자 2개로 부른다). 결과적으로 spec §3.3의 대표 예시 "**성내로 끝입니다.** 강동구청, 왼쪽 약 16미터"는 프로덕션에서 발화되지 않고 항상 "경로 끝입니다…" 변형만 나간다(독립 리뷰 검출 2026-08-08).

**폴백은 안전하다** — 문장이 어색해지는 것이 아니라 기준절이 빠질 뿐이고, "지어내지 않는다"는 §3.1 계약을 지킨다. 그래서 결함이 아니라 미구현이다.

**소스 후보 둘, 둘 다 대가가 있다**:
- **마지막 스텝 설명 파싱**("성내로를 따라 100m 이동"): upstream 호출이 없어 공짜지만 **파싱 규칙이 provider 문장에 종속된다**. 카카오와 Tmap 폴백의 문장 모양이 다르고, `rewriteWalkGuidance`가 이미 그 문장을 재작성한 뒤라 정규식이 두 계층에 얽힌다.
- **경로 종점 역지오코딩**(`/api/geocode/reverse` 재사용, 도로명 3단 보장 체인 보유): 정확하지만 **호출 지점이 갈린다**. 라우트 핸들러에서 부르면 종점 좌표가 경로 응답 뒤에야 나오므로 **직렬 왕복이 세션 시작 지연에 더해진다**(같은 라우트에서 71초 지연 실사고가 있었다). 최종 접근 **진입 시점에 클라이언트가 부르면** 임계 경로 밖이지만(그때는 사용자가 이미 경로를 다 걸었다) 발화 직전 비동기가 되어 헌장 §6 ⑨ 이탈 게이트가 필요하다.

**권고**: 후자를 **진입 시점 클라이언트 조회**로. 착수 시 spec §3.3에 호출 지점과 이탈 게이트를 먼저 못 박는다.

### E6. 최종 접근 랜드마크 문장 — **착수 유예(계약 미해결)**

spec `2026-08-08-final-approach-guidance-design.md` §3.7의 "진입 서술 다음 문장으로 랜드마크 1개". 나머지 최종 접근은 2026-08-08 출하됐고 이 문장만 빠져 있다.

**유예 사유(플랜이 예상하지 못한 계약 문제)**: §3.7은 "방향과 거리를 함께 말한다, 방향을 모르면 문장 자체를 생략한다"를 안전 근거로 못 박았다(목적지 반대편 횡단보도를 표지로 오인하면 사용자를 차도로 보낼 수 있다). 그런데 사용자 기준 상대 방향을 내려면 랜드마크의 **좌표**가 필요한데, `AudioSignalSite`(`src/lib/providers/audio-signals.ts`)는 좌표 없이 `distanceMeters`·`bearing`(8방위)·`deviceCount`만 준다. 8방위 버킷 중심으로 각도를 복원하면 오차 ±22.5°이고 4분할 반폭이 45°라 경계에서 방향이 뒤집힌다 — 정확히 §3.7이 막으려던 실패다.

**OSM 피처만으로 부분 구현하지 않는다**: `WalkFeature`는 좌표가 있어 방향이 정확하지만, 그러면 **음향신호기를 구조적으로 절대 말할 수 없는 기능**이 된다. §2.5 실측에서 음향신호기는 지팡이 감지 10/10으로 최상위이고, 침묵은 시각장애 사용자에게 "여기 음향신호기가 없다"로 읽힌다 — 3-state 불변식이 금지하는 거짓 부재다.

**닫는 조건 (2026-08-08 오후 조사로 선택지가 둘로 늘었다)**: ①`AudioSignalSite`에 대표 좌표를 추가한다(`clusterSites`가 이미 최근접점을 대표로 고르므로 그 점을 실어 보내면 된다). 그 provider는 라우트·채팅이 공유하므로 소비자 회귀 확인이 함께 필요하다. ②또는 **전국횡단보도표준데이터**([15028201](https://www.data.go.kr/data/15028201/standard.do))로 갈아탄다 — **위경도와 음향신호기설치여부·점자블록유무·보도턱낮춤여부를 한 행에** 담아 좌표 문제가 애초에 없고, 서울 한정인 현행 OA-15543(EPSG:5186)을 **전국으로 확장**하는 부수 효과가 있다. ⚠ 55개 지자체가 각자 올리는 구조라 채움률 편차 검증이 선행이고 EPSG 명시값·총 건수는 미확인이다.

**랜드마크의 목적이 재확인됐다**: 시각장애 당사자 실사용기(에이블뉴스 서인환) 원문 — "시각장애인들은 주변에 어떤 시설물이 있는지보다 **주변 시설물을 통하여 현 위치를 파악하는 데** 더 주변 정보를 활용한다." §3.7이 랜드마크를 **최대 1개**로 제한하고 **방향+거리를 함께** 요구한 것이 이 관찰과 정합한다 — 목록을 늘리는 방향은 근거가 지지하지 않는다.

### 후보

| # | 항목 | 선행 조건 | 판정 |
|---|---|---|---|
| E1 | 지하철 혼잡도 통계 (서울교통공사 OA-12928) | 기보유 키 | 요일·30분 단위 분기 갱신 통계라 실시간이 아니고, 1~8호선 한정(9호선·코레일 공백). ⚠ API 서비스명 미확인(후보 3종 전부 `ERROR-500`) — 착수 전 실호출 확인 필요 |
| E2 | ITS 돌발상황 브리핑 (사고·공사·통제) | **위원장 액션**: its.go.kr 자체 가입·apiKey 발급(LINK형이라 data.go.kr 키 불가) | 커버리지가 고속도로·국도 중심이라 도심 이면도로 공백. 자동차 브리핑 한정 가치 |
| E3 | 장거리 교통 (고속·시외버스·열차) | 기보유 키 | 앱 스코프(도시 내 이동)와 어긋남 — 낮음 |
| E4 | **경로 추종형 도보 안내 → "실시간 길 안내"로 구현 완료(2026-08-03, 이후 4차례 증축)** | **코드 잔여 0 — 남은 것은 전부 F-a 판정** | 스펙 `2026-08-03-realtime-route-guidance-design.md`. 그 위에 라운드1 피드백(M0~M5, 2026-08-06) · 안내문 재작성(2026-08-07) · **백그라운드 사운드·톤 커버리지**(`2026-08-08-background-tone-coverage-design.md`) · **계단 회피 전달 A4**(`2026-08-08-walk-guidance-stepfree-design.md`)가 쌓였다. 위원장 판단 3건 확정: 약관=질의 없이 세션 메모리(코드 계약 §7.3), 백그라운드=**위치 승격 + 톤만 재생·음성 억제**(2026-08-07 실측 판정), 정지 임계=비장애 보행 90% 기준(0.4/0.6/2.0초, 재계산 금지). 사운드 9종 전부 청취 선정 완료(`unreliable` 음가 A, 2026-08-08) |
| E5 | **빠른하차 출입문 병치 → 구현 완료(2026-08-08)** | **코드 잔여 0 — 남은 것은 F-a 실승차 판정 4건** | 스펙 `2026-08-08-subway-quick-exit-design.md`(codex 적대적 리뷰 24건 + 구현 중 실측 정정 4건 반영), 계획 `2026-08-08-subway-quick-exit.md`. 정적 seed 1,806행·가드 11종, 방향 긍정 확정 + 선형 쌍 최적화, 웹 브리핑·안내 세션 대기·CLI·iOS 4소비자 배선. 실호출 게이트 전량 통과(PROGRESS 해당 절). 역명 네임스페이스 전제도 실측으로 닫았다(35 leg 불일치 0, 역방향 침묵 2역 확인) — 남위례 역방향 1건만 실승차 잔여 |
| E6 | **경로 대안 데이터원 확장 — 채택 보류(위원장 판정 2026-08-06)** | 다음 실승차에서 현행 대안(ODsay, 안내 시작 버튼 배포됨)이 부족하다는 판정 | 후보 3 실측 완료(PROGRESS 라운드1 절): 서교공 path2(수도권 전역·시간표·3유형·경유역/제외환승역, 비표준 봉투) / ODsay subwayPathSchedule(지하철 2경로+시간표·첫차/막차 모드, 기존 키) / 카카오 publictraffic(버스 포함 13경로, 첫·끝 도보 leg 부재로 7/30 비권장 유지). 부족 판정이 나오면 이 비교표에서 고른다 — 재조사 불요 |

**E4 상세.** 직선거리 비콘의 한계("경로를 알려주지 않는다")에 대한 답이다. **GPS 단독 자동 턴바이턴은 폐기 목록으로 내렸고**(아래), 대신 채택 가능한 형태는 이렇다.

- 경로 폴리라인을 응답에 보존 → 누적 진행거리 추적 → **긴 구간에서만 자동 발동**
- **안내 내용은 합치지 않는다.** 횡단보도·계단·신호등은 각각 별도 안내로 유지하고, 합치는 것은 *GPS 발동 지점*뿐이다. ⚠ 이 저장소의 초안은 반대로 갔다가 정정했다(Valhalla가 시각장애 프로파일을 다중 큐 병합에서 **명시적으로 제외**하고 오히려 더 잘게 쪼갠다). 긴 구간이 짧은 것을 흡수하면 횡단보도가 577m 앞당겨 안내된다(실측)
- **마지막 구간은 비콘이 인계받는다.** 상용 선례 Sendero가 목적지 15m 이내에서 턴바이턴을 끄고 직선 추적으로 전환한다. **우리는 이 하이브리드의 어려운 쪽을 이미 만들어 검증까지 마쳤다**
- 위치 확신도를 문장에 반영(Soundscape: 정확도 10m 이내면 수치, 20m 이내면 "약", 그 밖이면 "쯤"). GPS 불량은 **음성이 아니라 톤 채널**로(Sendero drip, 임계 15m). 비콘에 톤 채널이 이미 있다
- **수동 제어는 "반복"이지 "건너뛰기"가 아니다.** 초안의 "다음 단계" 컨트롤은 선례가 없고(전수 조사) 사용자가 진행을 잘못 주장하면 안내와 실제가 어긋난다. 선례는 사전 전체 목록 통독 · 직전 안내 반복 · 임의 시점 현위치 조회 셋이다
- 이탈 시 **자동 재조회 금지**(쿼터 + 자동 재설정이 도로 중앙 안내를 낳은 기록)

**선행 조건 2건은 코드가 아니라 판단이다.**
1. **약관**: 한 세션 동안 경로를 들고 있는 것이 카카오 "저장"인지 유권해석이 없다. Tmap은 24시간 명문이라 문언상 안전. 정식 도보 TBT는 카카오모빌리티 **제휴 계약**이 경로이며 시각장애인 보행 지원 앱의 신청 선례도 제휴 회신을 받았다(비용·계약 사안).
2. **백그라운드 위치 승격 여부**: 주머니에 넣고 걸으려면 필요하다. ⚠ 비콘 설계 §8.1이 근거로 든 "항상 허용 권한 필요"는 **사실이 아니다**(When In Use로 성립, 파란 표시줄은 못 끔).

---

## F. 위원장 몫 (실사용 검증 + 직접 액션)

여러 마일스톤에 흩어져 있던 "실기기 VoiceOver 확인 잔여"를 한 곳에 모은다. 실사용에서 자연 관찰하거나, 별도 QA 세션으로 한 번에 소화한다(선택은 위원장).

### F-a. 실기기·실사용 검증 부채

- **실시간 길 안내(E4) 최종 판정 3묶음**(2026-08-03 빌드, 설치 완료 → **같은 날 위원장 실사용 1차 판정 반영 완료(묶음 A)**: ④"현재 안내 반복" 버튼 제거 확정·단행, closer 통지 잔여 거리 적응형 마일스톤 완화(100/200/500/1000m, farther 50m 유지), 상세 모드 경로 기준 잔여 거리·예상 시간 상시 표시 추가): 잔여 확인 — ①**실보행 완주**: 상세 안내로 실경로 1회(선행 낭독·묶음 통독·인계·전환·이탈 시나리오) — 상수 튜닝(40m·50m·주기) 판정 ③**a11y·사운드 실기기 확인**: 시트 컨트롤 응답이 VO 활성화에 잠식 없이 발화되는가(.high), 사운드 게인 위계 체감(2026-08-08 `unreliable` 추가로 **9종**)·햅틱 3종(이탈·도착·멀어짐) 체감, 새 잔여 상시 표시 행의 VO 낭독(m 단위 오독 없음). ⚠ ②"차량 이동 실측(도보 경로 승차)"는 B1 자동차 안내 출하로 **대체 폐기** — 아래 B1 실주행 판정이 정본.
- **B1 자동차 안내·진입점 재편 실주행 판정**(2026-08-03 출하, PROGRESS B1 절): ①분기 예고가 통과 전에 들리는가(원거리 1.5km + 임박 v×15초, 과밀 여부 포함) ②"지금 어느 도로" 진행 상황 응답(도로명+진행+ETA 병기 문장 길이) ③멀쩡한 주행 중 거짓 재확보·이탈 통지 없음 ④car 프로파일 상수 튜닝(§4.3 표 — 판정 전 임의 변경 금지) ⑤a11y 실기기 6건: 시트 복귀 착지(도보 인라인 긴 경우 scrollTo 필요 판정), carStart 시작 톤 잠식, 중지 통지(`beacon.stopped`) 실발화, en "Simple guidance started" 낭독, 수단 시작 버튼 스와이프 동선, 원거리→임박 간격 체감.
- **피드백 라운드1 반영분 실승차 재판정**(2026-08-06 M0~M5 선행분 배포 — 1차 실승차 2026-08-05의 피드백 13건 반영, 종전 "B2 실승차 판정" 항목을 이 재판정이 승계): 선행조건이던 실기기 배포는 완료됐다(2026-08-06 밤, 라운드1 전체 반영 실험판 설치·실행 확인). 판정 대상: ①추적 지연 체감(recptnDt 신선도 게이트+폴 15초, "N초 전 기준" 문장) ②상태 문구 프레임 어휘("{stop}까지, {message}")·현재 역 병치 ③대기 국면 탈출구(새로고침·"이미 탑승했습니다" 라벨 포함)·탑승 변경 취소, 대기 고정 재발 시 `TransitGuideDiag` 계측 로그 회수(#4 원인 6후보 판별) ④경유역 disclosure 위치·현재 위치 병치 체감 ⑤대중교통 완료 후 "남은 도보 안내 시작" 핸드오프 제안 타이밍 ⑥수단별 짧은 라벨·대안 경로 안내 시작 버튼(로터 구분) ⑦백그라운드 위치 승격: 잠금·백그라운드에서 도보·자동차 세션 생존 ⑧속도 가드 문구(계단에서 "속도 빠름" 오판 소멸). 판정 전 상태 머신 상수 임의 변경 금지 유지. ⚠ **종전 ⑦에 달려 있던 "잠금 중 소리 가청은 별도 축(무음이면 오디오 세션 카테고리 재설계)"은 해소됐다** — 2026-08-07 실측이 무음을 확인했고 그 재설계가 2026-08-08 마일스톤이다. 소리 판정은 아래 **2026-08-08 추가분 ①**이 승계한다.
- **B2 아침 실호출 게이트 잔여**(첫차 05:30 이후 — 심야 출하라 시간대 의존분 이월, 스펙 §8.5 ④⑤): 부역명 회귀 행렬(천호·군자·강동·양평 주간 대조), 승차→하차 vehId 조인 재현, 열차 선택 목록 실값(방면·급행·종착 실데이터), 2호선 내선/외선 ↔ wayCode 대응 확정. 폴백 설계라 미확정분은 안전 열화(오동작 아님)이지만 게이트 완결은 이것으로 닫는다.
- **최종 접근(마지막 몇 미터) 실보행 판정**(2026-08-08 출하, 실험판): spec §6의 동결 상수 7종이 판정 대상이고 **판정 전 임의 변경 금지**다. ①`ARRIVAL_TOLERANCE_M = max(10, accuracy)` — 경로 종점 근처는 투영이 불안정한 구간이라 `reacquiring`이 잦아지면 완화한다 ②`FINAL_INTERVAL_S = 15` 주기가 톤 계층과 경합하는가 ③`ARRIVE_M = 15` 도착 선언이 이른가 늦은가(내리면 §3.6 "목적지 근처" 분기가 그때 살아난다) ④`BEARING_WINDOW_M = 15` — 근거가 실측 하나뿐이다 ⑤뒤쪽 버킷 경계 ±135° ⑥랜드마크(위 E6, 미구현) ⑦`beacon.nearby`의 `±{n}m` 표기 존폐. 함께 들을 것: 진입 배치 서술이 종점에 선 순간 나오는가(조사 침묵이 사라졌는가), 조사 없는 문형("성내로 끝입니다. 강동구청, 왼쪽 약 16미터")이 어색하지 않은가, 목적지를 지나쳤을 때 "뒤"가 나오는가.
- **거리 표기 소수 km 전환 확인**(2026-08-03 빌드): 1km 넘는 거리(경로 요약·지하철 최근접·둘러보기 원거리)가 `1.1km`·`6.285km`처럼 소수 원값으로 표시되고 **kilometers로 정확히 낭독**되는가. 1km 미만 `900m`은 여전히 **미터로 낭독**되는가(minutes 오독 소멸 유지). ⚠ 2026-08-02 확인분("1km 200m" 나눠쓰기)은 이 재개정으로 무효다. 새 표기 기준으로 다시 듣는다.
- **잔여 관찰 2건**(2026-08-02 확인 때 판정 보류): ①**간헐 미착지 양태**: "커서가 아무 데도 안 감"이면 재시도 2회가 대응하지만, "엉뚱한 행에 착지"면 의도적으로 교정하지 않는 축이라(사용자 조작 무효화가 더 큰 손해) 별도 대응 필요 ②빈/실패 오버레이에서 같은 문장이 연속 두 번 들리지 않는가(아이콘 라벨 전파 수정 검증).
- ✅ 2026-08-02 확인 완료(위원장): QA 1~4순위(설정 해결 버튼·포커스 착지 11종·거리 낭독·모달 헤딩 착지 수용), 정밀 위치 OFF 문구·임시 정밀 팝업 동작·허용 후 자동 조회.

**1.3까지 출시돼(2026-08-06) 아래 항목은 대부분 스토어 배포본으로 확인된다.** 실험판이 따로 필요한 것은 **봉인된 실시간 안내 계열뿐**이다(2026-08-08 추가분 ①②와 E4·B1·B2 계열).

⚠ **실기기 검증 방식이 2026-08-04부터 바뀌었다**: 실험판(`CONFIGURATION=Experimental ./ios/deploy-device.sh`)은 번들 ID가 달라 **공식판을 덮어쓰지 않는다**. 종전에는 개발 빌드를 넣으면 스토어판이 사라져 둘 중 하나만 쓸 수 있었고, 그래서 릴리스 지연이 검증 부채의 상환 비용을 올렸다. 이제 두 앱이 공존하므로 그 결합이 끊겼다(홈 화면 구분은 이름 접미사 `…실험`).

- 내 주변 → 장소 상세 iOS 이식 (2026-07-26)
- 소아 진료 커버리지 확장 (2026-07-26)
- 웹→iOS 갭 이식 1차: 계단 회피 토글·보행 인프라 화면·추천 질문 (2026-07-29)
- 웹 nearby 공유 계층 4시나리오: 패널 교대·로딩 중 Esc·done 재조회·트리거 복원 (2026-07-30)
- 단축어 진입 마이크 행 포커스 착지 (2026-07-26)
- **iOS 채팅 멀티 턴 먹통 재발 관찰** (2026-07-20 원인 확정·수정 후 관찰 중)
- 장소 상세 "이 장소 주변" 3행: 섹션 heading 발견 경로, push 후 뒤로가기 맥락 회수 (2026-08-01)
- **1.2 신기능 3종**: 혼잡도 등급 행(`ConditionsView` 안, 날씨·공기질과 한 그릇), 문화행사 "내 주변" 목록, 대중교통 경로의 운행 밖 표기. 셋 다 시뮬 실측·실호출은 끝났으나 실기기 VO 확인 기록이 없다. **스토어 배포본으로 확인 가능**(1.2 출시 완료).

**2026-08-02 추가분(1.3에 실려 출시됨 — 이제 스토어판으로 확인 가능).** PROGRESS의 마일스톤 줄에만 적혀 있던 것을 이 파일로 회수했다. ⚠ **예외는 "거리 추적 모달 포커스 2종" 하나**로, 그 기능은 봉인돼 있어 실험판이 필요하다:

- **"이 장소 주변"·"내 주변" 4종 로드 완료 포커스 착지**: 판정이 **실기기 VO뿐**이다. 시뮬 AX 트리는 "어느 행이 포커스를 주장하는지"를 보여주지 않아 착지 성패를 원리적으로 검출하지 못한다. 지하철·버스는 역/정류소 heading, 따릉이는 첫 행, 날씨·공기질은 첫 섹션 헤더에 닿아야 한다. 새로고침 때는 **이동하지 않는 것이 정상**(첫 로드에만 착지).
- **장소 상세 "이 장소 주변" 지하철 행**: 시뮬 UI 자동화가 그 세션에서 입력을 전혀 받지 못해 **화면 실측 자체를 못 했다**(빌드·리뷰까지가 게이트). 3행 순서와 낭독 확인 필요. ⚠ 역 장소에서는 첫 항목이 그 역 자신이라 인라인 "역 도착 정보"와 데이터가 겹치는데, **숨기지 않기로 판정**한 결과가 실제로 거슬리는지가 확인 포인트다.
- **채팅 심야 지하철 상태 구분**(실사용 관찰): `get_subway_arrivals`가 4-state 필드를 패스스루만 하고 declaration에 상태 설명을 넣지 않았다(프롬프트 덧대기는 실측으로 필요가 입증될 때만). 심야에 채팅으로 물어 **"운행 종료"와 "정보 없음"을 혼동해 답하지 않는지** 확인. 혼동하면 그때 declaration을 보강한다.
- **채팅 지역별 미제공 요약**(실사용 관찰): 서울 밖에서 따릉이·문화행사를 물었을 때 채팅이 `unavailableHere`를 **"근처에 없음"으로 요약하지 않는지**. 도구 `data`에 notice 문장을 함께 넘겨 뒀다. "이 지역엔 서비스가 없다"와 "근처에 없다"는 사용자 행동이 갈리는 다른 사실이다.
- **버스 미제공 안내 낭독**(A2 iOS 몫): 강릉 등 TAGO 미커버 지역에서 "내 주변"·"이 장소 주변" 버스를 열면 `APIError.unavailableHere(.noBusData)` 경로로 "이 지역은 정류소 정보가 제공되지 않습니다"가 나와야 한다. 로컬·prod 실호출과 시뮬 빌드까지는 확인했으나 **실기기 VO 낭독 기록이 없다**. 확인 포인트는 이 문장이 0건("주변에 정류소가 없습니다")과 **다르게 들리는가**다. 두 상태를 구분하려고 만든 기능이라 낭독이 같으면 만든 값이 없다.
- **채팅이 타 앱을 다시 권하지 않는지**(실사용 관찰): systemInstruction의 "구체적 대안 한 가지"가 "네이버 지도·카카오맵을 이용하세요"를 낳아 **긍정 트리거 최소판**으로 교체했다(2026-08-02). 프롬프트 행동은 latent라 실호출 3건으로는 표본이 얇다. 다양한 실패 상황(권한 거부·조회 실패·미제공 지역)에서 타 앱 유도가 재발하면 그때 금지문을 덧댄다. **실측으로 필요가 입증되기 전에는 덧대지 않는다**([[prompt-edits-minimalism-first]]).
- **거리 추적 모달 포커스 2종**: 열릴 때 **중지 버튼**에, 닫힐 때 **시작 버튼**에 착지해야 한다. 시뮬 AX 트리는 어느 요소가 포커스를 주장하는지 보여주지 않아 **판정이 실기기 VO뿐**이다. 중지 버튼·스와이프·VO escape 세 경로가 모두 추적을 멈추고 같은 자리로 돌아오는지 함께 본다. ⚠ 도착지를 "현재 위치"로 바꾸는 경로는 추적 중 물리적으로 닿을 수 없다고 코드로 확인했으나 실기기로는 미확인이다(리뷰 Minor 2). 시도해 볼 여지가 있으면 빈 시트가 스치는지 본다.
- **거리 표기 전면 변경 낭독**(범위가 비콘 밖이다): `1km 200m` 표기가 검색 결과·"내 주변" 각 도메인·경로 요약·현재 위치 정위까지 **앱 전역**에 적용됐다. 특히 **경로 요약 2종**(자동차 `총 …`, 도보 `총 …, 약 N분`)은 문구에서 단위를 빼고 `{distance}`가 통째로 넣도록 바꿨으므로 단위 누락·이중 표기가 없는지 확인. 6로케일 전부 같은 치환이라 en에서도 본다.

**2026-08-08 추가분 ①(백그라운드 사운드·톤 커버리지, 실험판 필요).** 스펙 §11.2의 판정 10종을 그대로 옮긴다. 이 마일스톤은 **위원장 실측("잠금·백그라운드에서 아무것도 안 들린다")이 발단**이라, 그 실측을 되풀이하는 것이 곧 판정이다.

- **오디오 채널 4종**: ①잠금·백그라운드에서 **톤이 들리는가**(핵심 — 이게 아니면 마일스톤 전체가 무효) ②**무음 스위치를 무시**하는가(`.playback` 승격의 수용된 부작용) ③**배경 미디어(음악·팟캐스트) 위에서 톤이 식별**되는가(⚠ `.mixWithOthers`라 저이득 톤이 묻힐 수 있다 — 공존 확인만으로는 안 잡힌다) ④안내 **종료 후 다른 앱 소리가 원복**되는가.
- **세션 경합 3종**: ⑤받아쓰기와의 경합(받아쓰기 중 시작한 세션이 `.ambient`로 영구히 남지 않는가) ⑥전화 인터럽션 후 복귀 ⑦이어폰 연결·해제(route 변경) 후 재조정.
- **판정 축 2종**: ⑧**정지 임계 실보행**(0.4/0.6/2.0초 — 걷는 내내 정지 톤이 나지 않는가, 실제로 멈추면 나는가) ⑨**차량 빈도 실주행**(closer 최소 간격 수단별 분리가 30분 주행 900회 문제를 실제로 없앴는가).
- **비용 1종**: ⑩배터리 증분(백그라운드 위치 + 주기 톤).
- **음가·게인 판정 1종**: **`tick` 게인 0.3 재검토**. 이 값은 하트비트 시절 것인데 뜻이 **정지 신호로 바뀌었다** — 배경 미디어 위에서 묻히면 "정지"와 "침묵=고장"이 구분되지 않는다. 신규 `unreliable`(음가 A 확정) 청취와 함께 판정한다.
- **초기값 조정 대상 4개**(판정 전 임의 변경 금지): `unreliableIntervalS` 10초 · `noFixSeconds` 8초 · 차량 closer 10초 · `speedAccuracyCeiling` 1.0m/s.

**2026-08-08 추가분 ②(계단 회피 실시간 안내, 실험판 필요).**

- **계단 회피 열화 통지 3종**: 계단 회피를 켜고 도보 안내를 시작했을 때 ①**열화 문장이 시작 발화에 이어 끊기지 않고** 낭독되는가(결합 문자열이 길어 앞이 잘릴 여지) ②**정상 적용이면 침묵**인가 ③이탈 재조회에서 열화로 바뀔 때 들리는가. ⚠ ③은 실측으로 재현하기 어렵다 — 무계단 경로가 없는 좌표쌍을 21쌍 훑었는데 카카오가 전부 우회로를 찾아냈다(PROGRESS 게이트 ③). 자연 발생을 기다리는 항목으로 둔다.
- **백그라운드에서 놓친 경고를 복귀 때 갚는가**(a11y 리뷰 H1 수정분): 안내 시작 후 곧바로 화면을 끄고 걷다가 되돌아왔을 때, 계단 경고가 현재 상태 문장과 **한 문장으로 결합돼** 들려야 한다. 코드로는 닫았으나 백그라운드 게이트가 실기기에서만 재현된다.
- **재조회 발화 어순 판정**(a11y 리뷰 L4, 결함 아님): 이탈 재조회에서 지금은 경고 두 문장 뒤에 행동 지시가 온다. 걷기 전인 시작 경로는 경고 우선이 옳지만 재조회는 이미 걷는 중이라 조건이 다르다 — 실사용으로 어느 쪽이 나은지 판정한다.
- **문구 관찰 1건**(a11y 리뷰 부수): `unavailable` 열화가 시작 발화와 결합하면 "…일반 경로를 **안내**하며… 상세 **안내** 시작. **안내** 12개…"로 "안내"가 세 가지 뜻으로 연달아 낭독된다. 오해를 부르진 않으나 거슬리는지 듣고 판정한다(이번 변경이 만든 문제는 아니다 — `guide.detailStart`의 `"안내 {count}개"`가 원인).

**2026-08-08 추가분 ③(지하철 빠른하차 E5, 실험판 불요 — 웹은 배포 즉시, iOS는 다음 릴리스).**

- **칸 번호가 실제 승강장 표기와 맞는가.** 스냅숏(`crtrYmd` 20241231)이 늙었는지 검출할 **유일한 수단**이다. 운영기관 문의는 외부 발신이라 자율 진행 대상이 아니고, 자동 만료 차단도 두지 않았다(빌드 가드 11이 다음 갱신 때 변화를 보고할 뿐).
- **`"엘리베이터 6-4 문"`이 엘리베이터의 문 번호로 오해되지 않는가**(codex #19 기각 시 실사용 판정으로 넘긴 항목). 위원장 승인 문구지만 실낭독에서 다시 듣는다.
- **대기 화면에서 방해가 되지 않는가.** 열차 목록 앞에 놓았으므로 목록에 닿기까지 스와이프가 한 번 늘었다.
- **환승역 반복 노출이 과한가.** 지하철 leg마다 자기 하차역 기준으로 나오므로 2회 환승이면 3번 들린다. 별도 처리를 하지 않은 것이 옳은지 판정한다.
- **남위례 8호선 역방향 접근 1건**: 결측을 단방향으로 오인해 반대편 문을 낭독하는 시나리오다. 같은 구조의 한양대·도곡은 실호출로 침묵을 확인했고(PROGRESS 해당 절), 남위례만 좌표가 다른 역으로 해석돼 걸리지 않았다. 산성 쪽에서 남위례로 내리는 경로를 타면 `quickExit`가 나오지 않아야 한다.

### F-b. 코드로 못 닫는 직접 액션

- **거리 추적 간섭 시나리오**(실보행 자체는 2026-08-02 통과, 아래는 주변 경로): ①추적 중 다른 탭 전환 후 복귀 ②백그라운드 왕복 후 절대거리 재발화 없는지. ⚠ **종전 항목이던 "추적 중 목적지 재검색 + 받아쓰기"는 모달 전환으로 도달 경로가 사라졌다**(추적 중엔 시트가 검색 버튼을 덮는다). `outputSuppressed` 가드는 이중 방어로 남겨 뒀으므로 확인 대상에서만 뺀다. (길찾기·검색 포커스 이동은 2026-08-02 실기기 확인 완료.)
- **출입구 좌표 확보 — 경로가 바뀌었다**(최종 접근 안내 spec §3.8, 2026-08-08 오후 재판정): 목적지 좌표를 건물 중심에서 출입문으로 올리면 오프셋이 실측 **8.77m**만큼 정밀해진다. ⚠ **종전 항목이 지목한 "좌표제공 API 승인키 발급"은 더 이상 권장 경로가 아니다** — 그 API는 주소 문자열을 받지 않아 `admCd`·`rnMgtSn` 선행 조회가 필요한 **2단계 호출**이고 **5초 10건** 제한이 걸려 실시간 경로로 부적합하다(키 자체는 즉시 자동승인이고, 검색용 `JUSO_CONFM_KEY`로 부르면 `E0001`인 것은 공식 오류표가 "(검색API 승인키 사용불가)"로 명시한 정상 동작이다). **정적 seed 경로가 정본이고 선택지는 셋**이다: ①**서울시 보행자 출입구 정보**(OA-21699) — WGS84·즉시 다운로드·심사 없음이지만 서울 공공시설 3,212곳 한정 ②**juso 내비게이션용DB** — 전국 + **`출입구유형 03 보행자용`**(차량용 출입구로 시각장애 보행자를 보내는 것을 막는 유일한 필드, 상용 지도 API에 없다)이지만 **이용목적 심사** 필요(소요 기간 미확인) ③**Tmap `frontLat`/`frontLon`** — 키 보유·전국이지만 약관상 **24시간 초과 저장 금지**라 런타임 전용. **위원장 액션은 ②의 심사 신청**이고, 그 전에 ①로 서울 범위를 먼저 덮을 수 있다. 상세 판정표는 spec §3.8, 원자료는 `docs/research/RESEARCH-2026-08-08-last-few-meters.md` §10.5.
- **Windows 다운로드 폴더의 ASC 개인키 정리**: `hunyong-samsung` 다운로드 폴더에 `AuthKey_SZXQP7DLC7.p8` 원본과 2026-07-14 자 키 2개(`8A857RVBN2`·`R8Q8M92P9Y`)가 상주 중이다(Tailscale `scp`로 가져오기만 하고 지우지 않았다). App Manager 권한 자격 증명이라 정리를 권한다. **파괴적 삭제라 자율 진행 대상이 아니다**(현행 키는 `~/.appstoreconnect/private_keys/`에 권한 600으로 사본이 있고 .p8는 재다운로드 불가이므로, 지우기 전에 사본 존재를 먼저 확인할 것).

---

## 폐기 (재부활 금지 — 근거 포함)

같은 조사를 반복하지 않기 위해 왜 뺐는지를 남긴다.

| 항목 | 폐기 근거 |
|---|---|
| **웹 거리 추적도 모달로** | 2026-08-02 판정. 모달의 동기는 "걸으면서 목록 속 중지 버튼을 찾는 비용"인데 **웹앱 주 사용처가 컴퓨터라 실보행 자체가 성립하지 않는다**(B1 게이트 재정의와 같은 근거). 없는 문제를 위해 구조를 바꾸지 않는다. 웹은 disclosure 패널 유지, 거리 표기만 정본 공유로 함께 바뀐다. iOS·웹 UI 구조가 갈리는 것은 의도이지 갭이 아니다 |
| **딥링크 수단 선택** | 2026-08-01 코드 대조로 전제 소멸. 웹 `RouteLinks.tsx`도 수단별 링크를 내지 않는다(프로바이더당 대표 1개, "모드 선택은 길찾기 뷰 책임" 2026-07-30 스펙 §4). 남는 차이는 여는 화면(웹=장소 핀, iOS=도보 경로)이고 선택 UI 부재와 별개라 기능 갭이 아니다 |
| **대중교통 브리핑 출발지 변경** | 2026-08-01 코드 대조로 이미 구현 확인. iOS `DirectionsTabView`에 출발지 필드(`searchTarget = .from`)와 스왑이 있다 |
| **en 도보 경로** | 데이터 부재 3중 확정: Google Routes 한국 WALK 200+빈 배열, Tmap 보행자 en 미지원(3방식 실측), 카카오 도보 `lang`·`Accept-Language` 무시. 해결 수단 없음 |
| **Google Maps provider 교체** | 2026-08-01 4구간 동좌표 실호출 평가 탈락. 도보·자동차 기능 부재, ko는 지번 주소 반환으로 카카오 열위, 무료 한도도 국내보다 낮음 |
| **idle 홈 heading 레벨 점프** | 2026-07-30 옴니박스 IA 재편으로 전제 소멸. 현행 계층은 h1(Header) → h2(NearbyHub·검색 결과) → h3(패널) → h4(항목)로 점프 없음(실측 확인) |
| **둘러보기 기능 B (OSM 횡단보도·점자블록)** | 2026-07-22 보행 인프라로 출시 완료. 중복 항목 |
| **자전거 라우팅** | 국내 공개 REST 라우팅 API 부재, OSM 태깅 편차로 부적합 |
| **점자블록 턴바이턴** | 통합 공공데이터 부재. 국내 벤치마크조차 소수 역세권 자체 구축 |
| **GPS 단독 자동 도보 턴바이턴** | 2026-08-02 조사로 폐기(정본 `docs/research/RESEARCH-2026-08-02-realtime-walk-navigation.md`). 근거 넷이 독립적으로 같은 결론: ①턴바이턴 요구 정확도 1~3m vs 도심 실측 7~13m로 한 자릿수 차이 ②국내 도보 경로 구간의 **21%가 25m 미만**(하위 10% 12m)이라 안내 단위가 GPS 오차보다 작고, 경로가 자기 자신과 12m까지 근접해 최근접 매칭이 엉뚱한 스텝을 고름 ③같은 것을 시도한 국내 앱이 시각장애 사용자 실사용에서 실패("20번 중 1번 정확", "철로 안으로 안내") ④국내 3사 어디에도 **보행자 TBT SDK가 없다**(카카오내비·Tmap Navi 모두 자동차 전용). ⚠ **대체안은 폐기가 아니다**: 경로 추종형 안내는 E4로 열려 있다. 폐기 대상은 "위치로 안내를 자동 결정한다"는 전제뿐 |
| **Apple MKDirections로 도보 경로 교체** | 2026-08-02 실측 대조 탈락. 한국 도보 경로를 **주기는 한다**(Google과 다름, 길동역→강동구청 2,488m/11스텝 정상 응답, 턴바이턴 제공국에도 한국 포함). 그러나 지시문이 도로명 회전 안내뿐이고 **횡단보도·계단·지하보도 언급이 0**이라 우리 사용자에게 가장 중요한 정보가 빠진다. 폴리라인 밀도도 카카오의 절반(34점 vs 67점). **쿼터 소진·장애 시 폴백 후보로는 유효**(약관 부담 없음) |
| **실시간 칸별 혼잡도 / 장애인콜택시 연계** | 전자는 유료(Tmap Puzzle), 후자는 일별 통계 API만 존재 |
| **상업 예약(캐치테이블·야놀자) / 카카오 T 택시** | 공개 API 전무 재검증 완료(2026-06-14). 딥링크 링크아웃이 최대치 |
| **지도 렌더링** | 의도적 미도입. "지도 없이 완결되는 흐름"이 구조적 보장 장치(SPEC §3) |
| **KRIC 교통약자 이동경로 2종** | 2026-06-14 가입 후 무승인. 그 사이 코레일 15125774·서울교통공사 wksn·엘리베이터 OA-21212·음성유도기 OA-22526 4겹으로 역 접근성이 충족됨 → **동결**. 승인 통보가 오면 그때 재평가 |
| **NCP Directions ko 자동차 A/B** | 2026-07-30 ko 기본 Tmap 확정(완성 문장 우위). NCP는 en 전용으로 역할 확정 |
| **검색 자연어 라우터** | 2026-06-27 폐기 확정. deterministic 위에 얹은 LLM 안티패턴 |
| **명소 별도 섹션 / 버킷 섹션 그룹핑** | 2026-07-20·21 폐지 확정. 정확도순 전환으로 랜드마크가 자연 부상, 고정 서열이 1위를 매몰시킨 실측 6사례 |
| **택시 요금·통행료 표기 / 정거장별 하차 안내 / 저상버스 판정 / 배차간격** | 전부 기구현(2026-07-21 코드 대조 및 구현 완료) |

---

## 다음 마일스톤

직전 마일스톤 둘은 2026-08-08 같은 날 완결됐다 — **백그라운드 사운드·톤 커버리지**와 **계단 회피 실시간 안내 전달(A4·D1)**. 상세는 `PROGRESS.md`. 위 섹션을 재서술하지 않는다(종전에 이 자리에 두었던 후보 표가 §B의 게이트 재정의를 따라가지 못해 혼자 낡은 전례가 있다).

**병목이 코드에서 판정으로 완전히 옮겨갔다.** 결함 축(A)과 갭 축(B)이 비었고, 남은 큰 덩어리는 **이미 만들어 놓고 봉인해 둔 실시간 안내 계열**이다. 그것을 여는 열쇠는 코드가 아니라 실기기·실보행·실승차 판정이다.

1. **실사용 판정 세션**(위원장): F-a의 실시간 안내 5계열을 소화한다 — E4 잔여 · B1 실주행 · B2 실승차 · **백그라운드 톤 10종** · **계단 회피 3종**. 첫 단계는 `CONFIGURATION=Experimental ./ios/deploy-device.sh`(2026-08-08 설치본이 이미 기기에 있다). **이 판정이 G3의 유일한 선행 조건이고, E5까지 뒤에 줄 서 있다.**
2. **B2 아침 실호출 게이트**(첫차 05:30 이후 — 시간대 의존이라 계속 이월 중이다. 이월이 반복되면 그 자체가 신호다).
3. **코드 착수 대상 0건.** E5(빠른하차)가 2026-08-08 구현 완료로 빠지면서, 열린 항목이 전부 판정·발행 대기다. 새 코드 작업은 실사용 판정이 무엇을 요구하는지 본 뒤에 정한다.

나머지는 전부 조건 대기다: **C**는 2026-10 트리거(ODsay 3개월 전)와 쿼터 관측, **E1**은 착수 전 서비스명 실호출, **E2**는 위원장의 its.go.kr 가입, **E3**은 앱 스코프와 어긋나 사실상 보류. **D**는 편승 대기(D5 커버리지 · D7 iOS 문구 · D8 iOS 대중교통 문구 · D9 다음 릴리스)이고, **D10은 lint 절반이 2026-08-08 E5에 편승해 종결돼 tsc 절반만 남았는데 얹힐 곳이 없어 독립 소품**이다.

⚠ **"위원장 몫밖에 없다"는 결론이 나오면 축 1(도달)부터 다시 본다.** 2026-08-02에 그 결론이 틀렸던 이유가 정확히 이 축의 부재였다. 지금은 축 1에 G3가 서 있고, 그것이 1번 판정에 묶여 있다는 것까지 확인했다.
