# BACKLOG — 길동무 (gildongmu)

> **열려 있는 항목의 정본.** 완료 이력·실호출 검증 로그는 `PROGRESS.md`, 설계는 `docs/superpowers/specs`, 항구 규칙은 `CLAUDE.md`가 정본이다. 이 파일엔 **아직 하지 않은 것**만 둔다.
>
> 기준선(2026-08-01): 웹 prod 가동, iOS 1.0 App Store 출시(2026-07-30 승인)·1.1 심사 대기(2026-07-31 제출), CLI/MCP npm v0.6.1 발행, dodo 전량 이식 완결.

## 판정 기준

항목을 남기는 축 네 가지. 어디에도 안 걸리면 폐기 목록으로 내린다.

1. **정합** — 이미 있는 기능이 틀린 답을 내는가
2. **시한** — 안 하면 언제 고장 나는가
3. **플랫폼 갭** — 같은 앱인데 웹과 iOS가 다른가
4. **데이터 실재** — 그 기능을 뒷받침할 데이터가 실제로 존재하는가

---

## A. 정합 결함 (있는 기능이 틀린 답을 냄)

### ~~A1. 지하철 구간 운행 시간 미판정~~ — ✅ 완결 (2026-08-01)

스펙·플랜 `docs/superpowers/{specs,plans}/2026-08-01-subway-service-hours*`. 상세는 PROGRESS 운영 표. 선재 결함 2건(조인 키 없는 leg의 rank 0 통과 · 수도권 토요일 다이어 부재로 역 상세 첫차·막차가 매주 사라지던 버그)을 동반 수정했다.

### A2. 막차 후 중간 정류장 오강등 — **동결**

첫차·막차가 차고지 출발 기준이라 막차 직후 중간 정류장 승차 가능 구간이 `outside`로 나온다. 강등일 뿐 제외가 아니고, 정확히 하려면 노선별 소요시간이 필요하다. 데이터 확보 전까지 수용(2026-08-01 판정). 기록만 유지.

---

## B. 플랫폼 갭 (웹에 있고 iOS에 없음)

주 사용 창구가 iOS인데 같은 장소를 열어도 정보량이 다르다. 전수 갭 조사(2026-07-29) 잔여분.

| # | 항목 | 상태 | 비고 |
|---|---|---|---|
| ~~B1~~ | ~~장소 상세 3섹션~~ | ✅ 완결 (2026-08-01) | 스펙·플랜 `2026-08-01-ios-place-nearby*`. "이 장소 주변" 섹션 3행이 기존 화면을 장소 좌표로 앵커해 push |
| ~~B2~~ | ~~딥링크 수단 선택~~ | 폐기 | 아래 폐기 목록 |
| ~~B3~~ | ~~대중교통 브리핑 출발지 변경~~ | 폐기(이미 구현) | 아래 폐기 목록 |
| B4 | 목적지 거리 비컨 | 전체 부재 | 웹조차 실보행 스모크 미완 — **가치 검증 후** 이식 판단. 순서 역전 금지 |

남은 플랫폼 갭은 B4 하나이고, 웹 실보행 스모크가 선행 게이트다.

---

## C. 시한 있는 유지보수 (지금 착수 X, 시한 3개월 전 착수)

| 시한 | 항목 | 미조치 시 결과 |
|---|---|---|
| 2027-06-30 | 네이버 지역검색 → NAVER API Hub(NCP) 인증 이관 | ko 장소 병합 사망. NCP 계정 기보유·비용 영향 없음 |
| 2027-01-04 | ODsay URI 앱 키 만료 | 대중교통 길찾기 전면 중단 |
| 2027-07 | iOS 프로비저닝 프로파일 만료 | 실기기 배포 중단 |

---

## D. 내부 품질 (사용자 비가시 — 다른 마일스톤에 편승)

- ~~**D1. data.go.kr envelope 파서 공용화**~~ — ✅ 2026-08-01 완결. 실제 중복은 25지점(items 9·resultCode 7·totalCount 4·XML 가드 3·게이트웨이 2)이었고, `src/lib/providers/datagokr-envelope.ts`로 합쳤다. **경계: 모양은 공용, 정책은 provider** — 허용 resultCode("00"/"0"/"0000"/"03")·throw vs null은 서비스 계약이라 각자 자리에 남겼다(합쳤으면 백로그가 경고한 분기 주머니가 됐다). TOPIS·서울지하철 실시간·서울 열린데이터는 `response` 래퍼가 없어 스코프 밖. 설계 `docs/superpowers/specs/2026-08-01-datagokr-envelope-design.md`.
- **D2. CLI `route walk --accessible` 플래그** — 다음 `cli-v*` 릴리스 편승.
- **D3. 카카오 도보 잔여** — spec 테스트 3케이스 미커버, `no_stepfree_route` 문구 미세 부정확. 다음에 도보를 손댈 때 편승.

---

## E. 신규 기능 후보 (데이터 실재 확인됨)

| # | 항목 | 선행 조건 | 판정 |
|---|---|---|---|
| E1 | ITS 돌발상황 브리핑 (사고·공사·통제) | **위원장 액션**: its.go.kr 자체 가입·apiKey 발급(LINK형이라 data.go.kr 키 불가) | 커버리지가 고속도로·국도 중심이라 도심 이면도로 공백. 자동차 브리핑 한정 가치 |
| E2 | 지하철 혼잡도 (서울 OA-12928) | 기보유 키 | 요일·30분 단위 통계값. "이 시간대 보통 혼잡" 참고 안내. A1과 같은 지하철 축이라 합류 가능 |
| E3 | 근처 가족 행사·프로그램 (서울 문화행사 OA-15486 `이용대상`=영유아) | 기보유 키 | dodo 여행 도메인 직행. 10번째 "내 주변" |
| E4 | 장거리 교통 (고속·시외버스·열차) | 기보유 키 | 앱 스코프(도시 내 이동)와 어긋남 — 낮음 |

---

## F. 검증 부채 (위원장 실사용 몫)

여러 마일스톤에 흩어져 있던 "실기기 VoiceOver 확인 잔여"를 한 곳에 모은다. 1.1 출시 후 실사용에서 자연 관찰하거나, 별도 QA 세션으로 한 번에 소화한다(선택은 위원장).

- 내 주변 → 장소 상세 iOS 이식 (2026-07-26)
- 소아 진료 커버리지 확장 (2026-07-26)
- 웹→iOS 갭 이식 1차: 계단 회피 토글·보행 인프라 화면·추천 질문 (2026-07-29)
- 웹 nearby 공유 계층 4시나리오: 패널 교대·로딩 중 Esc·done 재조회·트리거 복원 (2026-07-30)
- 단축어 진입 마이크 행 포커스 착지 (2026-07-26)
- **iOS 채팅 멀티 턴 먹통 재발 관찰** (2026-07-20 원인 확정·수정 후 관찰 중)
- 장소 상세 "이 장소 주변" 3행: 섹션 heading 발견 경로, push 후 뒤로가기 맥락 회수 (2026-08-01)

---

## 폐기 (재부활 금지 — 근거 포함)

같은 조사를 반복하지 않기 위해 왜 뺐는지를 남긴다.

| 항목 | 폐기 근거 |
|---|---|
| **딥링크 수단 선택 (구 B2)** | 2026-08-01 코드 대조로 전제 소멸. 웹 `RouteLinks.tsx`도 수단별 링크를 내지 않는다(프로바이더당 대표 1개, "모드 선택은 길찾기 뷰 책임" 2026-07-30 스펙 §4). 즉 수단 선택 갭은 없다. 남는 차이는 여는 화면(웹=장소 핀, iOS=도보 경로)이고 이건 선택 UI 부재와 별개라 기능 갭이 아니다 |
| **대중교통 브리핑 출발지 변경 (구 B3)** | 2026-08-01 코드 대조로 이미 구현 확인. iOS `DirectionsTabView`에 출발지 필드(`searchTarget = .from`)와 스왑이 있다 |
| **en 도보 경로** | 데이터 부재 3중 확정: Google Routes 한국 WALK 200+빈 배열, Tmap 보행자 en 미지원(3방식 실측), 카카오 도보 `lang`·`Accept-Language` 무시. 해결 수단 없음 |
| **Google Maps provider 교체** | 2026-08-01 4구간 동좌표 실호출 평가 탈락. 도보·자동차 기능 부재, ko는 지번 주소 반환으로 카카오 열위, 무료 한도도 국내보다 낮음 |
| **idle 홈 heading 레벨 점프** | 2026-07-30 옴니박스 IA 재편으로 전제 소멸. 현행 계층은 h1(Header) → h2(NearbyHub·검색 결과) → h3(패널) → h4(항목)로 점프 없음(실측 확인) |
| **둘러보기 기능 B (OSM 횡단보도·점자블록)** | 2026-07-22 보행 인프라로 출시 완료. 중복 항목 |
| **자전거 라우팅** | 국내 공개 REST 라우팅 API 부재, OSM 태깅 편차로 부적합 |
| **점자블록 턴바이턴** | 통합 공공데이터 부재. 국내 벤치마크조차 소수 역세권 자체 구축 |
| **실시간 칸별 혼잡도 / 장애인콜택시 연계** | 전자는 유료(Tmap Puzzle), 후자는 일별 통계 API만 존재 |
| **상업 예약(캐치테이블·야놀자) / 카카오 T 택시** | 공개 API 전무 재검증 완료(2026-06-14). 딥링크 링크아웃이 최대치 |
| **지도 렌더링** | 의도적 미도입. "지도 없이 완결되는 흐름"이 구조적 보장 장치(SPEC §3) |
| **KRIC 교통약자 이동경로 2종** | 2026-06-14 가입 후 무승인. 그 사이 코레일 15125774·서울교통공사 wksn·엘리베이터 OA-21212·음성유도기 OA-22526 4겹으로 역 접근성이 충족됨 → **동결**. 승인 통보가 오면 그때 재평가 |
| **NCP Directions ko 자동차 A/B** | 2026-07-30 ko 기본 Tmap 확정(완성 문장 우위). NCP는 en 전용으로 역할 확정 |
| **검색 자연어 라우터** | 2026-06-27 폐기 확정. deterministic 위에 얹은 LLM 안티패턴 |
| **명소 별도 섹션 / 버킷 섹션 그룹핑** | 2026-07-20·21 폐지 확정. 정확도순 전환으로 랜드마크가 자연 부상, 고정 서열이 1위를 매몰시킨 실측 6사례 |
| **택시 요금·통행료 표기 / 정거장별 하차 안내 / 저상버스 판정 / 배차간격** | 전부 기구현(2026-07-21 코드 대조 및 구현 완료) |

---

## 다음 마일스톤 순서

1. ~~**A1 지하철 구간 운행 시간 판정**~~ — ✅ 2026-08-01 완결
2. ~~**B1 iOS 장소 상세 3섹션**~~ — ✅ 2026-08-01 완결(B2·B3은 전제 소멸로 폐기)
3. ~~**D1 envelope 파서 공용화**~~ — ✅ 2026-08-01 완결

**남은 후보**: D2(CLI `--accessible` 플래그, 다음 릴리스 편승)·D3(카카오 도보 잔여) 둘 다 편승형이라 단독 마일스톤이 아니다. E2(지하철 혼잡도)·E3(가족 행사)는 기보유 키로 착수 가능한 신규 기능. C(시한)는 캘린더 항목, E1은 위원장 액션 선행, F는 실사용 관찰.
