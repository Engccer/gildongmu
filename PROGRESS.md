# PROGRESS — 길동무 (gildongmu)

> 마일스톤 상태·실호출 검증 로그·미해결 결정을 담는다. **항구 규칙/패턴은 `CLAUDE.md`**, 설계·검증 정본은 `docs/superpowers/specs`(26개)·`plans`(20개). 이 파일은 "지금 무엇이 동작하고, 무엇이 막혀 있고, 다음은 무엇인가"만 추적한다.

## 운영 중인 기능 (실호출 검증 완료)

> **App Store 현황(2026-08-01 ASC API 조회 실측)**: `1.2 WAITING_FOR_REVIEW · 1.1 READY_FOR_SALE · 1.0 READY_FOR_SALE`, 빌드 `8 VALID`. 조회 명령은 `node ios/scripts/asc-submit.mjs --check`.
>
> - **1.1 출시 완료**(2026-07-31 제출, 빌드 1.1.0(7)): 1.0 승인 후 쌓인 110커밋(iOS 37건)을 담은 첫 업데이트. 계단 없는 경로 토글·보행 인프라 화면·대중교통 대안 비교/펼침·자동차 경로 Tmap 완성 문장·채팅 추천 질문·출처/진행 통지 로컬라이즈·영문 역명·해외 사용 안정성. **en 스토어 로컬라이제이션이 이 제출과 함께 공개됐다.**
> - **1.2 심사 대기**(2026-08-01 16:06 KST 제출, 빌드 1.2.0(8)): 혼잡도·문화행사·운행 밖 표기·장소 상세 주변 3행·받아쓰기 2건. 심사 통과 시 자동 출시(전체 사용자 즉시).
>
> 릴리스 노트·절차·함정은 `docs/appstore/1.0-submission-draft.md` §13(1.1)·§14(1.2)·§15(ASC API 제출 전환).

| 도메인 | 상태 | 비고 |
|---|---|---|
| 스크린리더 통지 문장 정리 | ✅ 전수 조사·정리 (2026-08-02) | 위원장 지시. **판정선은 "뒷문장이 새 정보를 주는가"**다. 1차로 지시형 꼬리 문장 29건 제거("잠시 후 다시 시도해 주세요" 23건 · "목록에서 선택해 주세요" · "출발지와 도착지를 확인해 주세요" · "다른 검색어로 시도해 보세요" 등). ⚠ **1차가 놓친 게 있었다**: 정규식을 `이용해`로 잡아 `이용하세요`를 못 걸렀다. 2차는 패턴이 아니라 **여러 문장인 문자열 41건 전부**를 놓고 판정해 6건 추가 제거: `beacon.straightLineNote`·`route.briefing.disclaimer`의 "…길찾기 앱을 이용하세요"(잉여인 데다 **타 앱 사용을 권하는 문구로 읽힌다**. 앱이 딥링크 버튼을 이미 제공한다), `ios.beacon.soundUnavailable`·`voice.errors.no_text`·`voice.errors.too_short`의 자명한 행동 유도, `offline.body`의 **첫 문장**(heading이 "오프라인 상태입니다"라 첫 문장이 잉여. **잉여가 항상 뒤에 오는 건 아니다**). **유지 판정도 근거와 함께 남긴다**: 출처 주석의 한계 정보(신고 시간 기준·오늘만·실내외 추정·도시철도 미포함·북 기준 방위), 커버리지 안내, geoDenied의 "어디서 허용하는가", 새로고침 실패의 "기존 정보를 유지합니다"(3-state 신호), `beacon.screenHint`의 "화면이 꺼지면 멈춥니다"(전경 전용 동작 계약). 이것들은 잉여가 아니라 정보다. 여러 문장 문자열 41→35건, 6로케일 동조. |
| 길찾기 탭 자동 초점 점검 (M2) | ✅ 판정·수정 (2026-08-02) | 스펙 §10. 실기기 VoiceOver로 가설 3종 판정. **기각 2 · 확인 1**이고 수정은 확인된 하나뿐이다. ①`List` 렌더 전 대입으로 포커스 유실: **기각**. 조회 완료 시 첫 성공 수단 heading으로 정상 이동한다. 코드 정황은 강했다(채팅 `ChatConversationView`는 같은 계열을 400ms 지연+대입+600ms 검증+1회 재시도로 해결해 뒀는데 길찾기는 `onChange` 동기 대입 한 번뿐). 그래도 재현되지 않아 손대지 않았다. 고쳤다면 없는 문제에 지연·재시도를 덧대 커서를 붙잡는 새 결함을 만들었을 것이다. ②시트 확정 후 착지점 부재: **확인**. 목적지를 고르고 시트가 닫히면 VO 커서가 화면 최상단으로 이탈해, 방금 고른 다음 행동 지점까지 다시 스와이프해 내려가야 했다. 웹 `focusAfterResolve`(출발지 확정→도착지 입력, 도착지 확정→조회 버튼)를 이식했고 지연·재시도는 채팅의 검증된 패턴을 따랐다(시트 dismiss가 끝나며 시스템이 포커스를 되돌리므로 그보다 늦게 대입해야 이긴다). 취소로 닫으면 콜백이 없어 예약도 없고 iOS 기본 복원에 맡긴다. ③같은 값 재대입 no-op: **기각**(①이 정상이라 전제 불성립 + `@AccessibilityFocusState` 양방향). ⚠ 조회 완료의 포커스 이동(헌장 §1 새 콘텐츠로 이동)과 시트 복귀의 착지(헌장 §5 경계에서 이탈 방지)는 **방향이 다른 규칙**이라 따로 다뤘다. **교훈: 코드 정황이 강해도 재현이 정본이다.** ①은 선례·구조·주석이 전부 결함을 가리켰는데 실기기에서 멀쩡했다 **2차·3차 실기기 확인에서 3건이 더 나왔다**(가설 목록에 없던 것들이라, 첫 판정이 "조회 완료 시점"에만 집중했음을 보여 준다). ①시트 진입 시 검색 필드로 커서가 안 감 → `.searchFocused` 배선(⚠ VO에서 정상 작동하고 키보드까지 열렸다. [[ios-voiceover-no-programmatic-keyboard]]의 "프로그래밍 편집 모드 진입 불가"는 **필드 밖 요소에서 시도한 경우**의 결론이고 이 경로엔 적용되지 않는다. 메모리 범위를 좁혔다) ②후보 도착 시 첫 후보로 안 감 → **두 결함이 겹쳐 있었다**. (a)`.accessibilityFocused($binding, equals:)`에 **Bool 바인딩**을 모든 행에 붙여, 첫 행만 `true`이고 나머지가 전부 `false`가 되는 바람에 초기 상태에서 **나머지 행 전부가 포커스를 주장**했다(도착지=5~6번째 랜덤 착지, 출발지=무이동. 같은 모호성의 다른 표현이라 증상만 보면 원인이 안 보인다). (b)그것만 고쳐도 안 됐다. **`List` 오프스크린 행은 AX 트리에서 컬링**되고 대상이 없으면 SwiftUI가 대입을 조용히 되돌린다. 출발지 시트엔 "현재 위치 사용" 행이 하나 더 있어 첫 결과가 더 밀리는데, **출발지만 무이동이던 증상 차이가 이걸로 설명된다**. 채팅의 검증된 순서(가시화 `scrollTo` → 지연 → 경합 바인딩 해제 → 대입 → 되돌림 검증 → 1회 재시도)를 그대로 따라 해소. ⚠ **`scrollTo` 인자는 포커스 키와 다르다**(포커스는 `"place-{id}"` 복합 키, ForEach 정체성은 원시 id). 복합 키를 넘기면 가시화가 조용히 실패한다. ③조회 후 커서가 조회 버튼에 머묾 → **회귀가 아니라 채택**(위원장 판정). 거리 추적 섹션이 조회 버튼과 수단 섹션 **사이**에 생기면서 종전의 "첫 수단 heading 점프"가 새 섹션을 건너뛰게 됐다. 머물면 다음 스와이프가 상태 → 거리 추적 → 수단으로 자연히 이어진다. **교훈 둘**: (1)코드 정황이 강해도 재현이 정본이다(가설 1은 선례·구조·주석이 전부 결함을 가리켰는데 실기기에서 멀쩡했다. 고쳤다면 없는 문제에 지연·재시도를 덧대 커서를 붙잡는 새 결함을 만들었을 것) (2)**UI 배치가 기존 포커스 계약을 무효화할 수 있다**. 새 섹션을 끼울 때는 그 위치를 지나가는 포커스 점프가 있는지 함께 봐야 한다. 같은 패턴을 **검색 탭에도 적용**(배선은 있었으나 동기 대입 한 줄이라 같은 컬링에 걸리는 형태였다). 실기기 확인 완료. |
| 목적지 거리 추적 iOS 이식 (백로그 B1) | ✅ 구현·리뷰·시뮬 실측 (2026-08-01), **실보행 검증 대기** | 스펙·플랜 `docs/superpowers/{specs,plans}/2026-08-01-ios-distance-beacon*`. **게이트 전제가 무너져 순서가 바뀐 사례**: 종전 B1 게이트는 "웹 실보행으로 가치 검증 먼저"였는데 웹앱 주 사용처가 컴퓨터라 실보행 자체가 불가능하다는 위원장 판단으로 무효화됐다. iOS 이식이 곧 가치 검증 경로가 됐다. **설계 게이트가 값을 했다**: 초안을 적대적 검토에 걸어 Critical 7·Important 14를 받았고 치명적 오판은 "웹 1:1 포팅"이라는 전제였다. 플랫폼 계약이 갈리는 지점이 넷(음수 `horizontalAccuracy`=좌표 무효 신호·`watchPosition` timeout 부재·캐시 첫 fix·오디오 세션 프로파일). 특히 **도착음이 매 fix 반복될 뻔했다** — 리듀서의 래치는 `speak`만 억제하고 톤은 매 fix 흐르므로, 도착해 서 있는 동안 가장 밝은 톤이 GPS 주기로 무한 반복된다(웹 원본의 잠복 결함, 실보행을 안 해서 아무도 못 겪었다). **구조 결정**: 앱 타깃 테스트 번들이 0건이라 판정을 앱에 두면 검증이 구조적으로 불가능한데 이 기능의 실제 결함 이력은 100% 오케스트레이터 계층이었다 → throttle·통지 억제·fix 신선도 판정까지 Kit 순수 함수로 내리고 `BeaconModel`을 얇은 껍데기로(`NearbyLoadCore` 선례). **구현 중 자체 발견**: 웹 `START_TONES`(660→990)가 `CLOSER_TONES`와 주파수·방향이 같아, iOS에서 한 기능에 담으면 "추적 시작"과 "가까워지는 중"이 구분되지 않는다(음높이 방향 = 정보라는 전제 붕괴) → 3음 패턴으로 분리하고 웹 값 복원 변이가 빨개지게 못 박음. **구현 리뷰(별도 컨텍스트) Critical 2·Important 11·Minor 12 + 변이 21종 주입**: 리뷰 총평이 정확했다. 스펙 진단은 맞았는데 이관 후 앱에 남은 부분이 같은 종류의 결함을 다시 냈다. C-1 노출 조건이 `results != nil`이라 추적 중 출발지 변경·재조회 시 **중지 버튼이 화면에서 사라져** 탭 이탈 말고는 멈출 수 없던 것, C-2 start Task 미보관으로 화면을 떠난 뒤 추적이 되살아나던 좀비 경로, I-1 권한 게이트가 `currentCoordinate()` 캐시 경로에 걸려 죽은 코드였던 것(권한 회수 후에도 "성공"), I-2 `.unavailable`이 한 번도 대입되지 않아 위치 서비스 꺼짐이 "권한 필요"로 낭독되던 것 등 전부 반영. I-9는 **코드가 아니라 스펙 결함**이었다(받아쓰기 중 통지 유지 → 헌장 §6 "녹음 중 발화 0"이 상위 정본이라 통지도 억제로 정정). **시뮬 실측 5건 확인**(섹션 렌더·라벨 전환 시 elementRef 유지=포커스 보존·추적 중 안내문 교체·무-fix 감시 발화·목적지명 헤더), ⚠ **거리 안내는 한 번도 못 봤다**. `simctl location`의 정적 위치가 신선도 게이트(5초)에 걸리는 것으로 보이며 시뮬로는 happy path를 못 태운다. 실기기에서 계속 "신호 약함"이면 이 창을 먼저 의심. 게이트: Kit 201→235 · 웹 1,507→1,518 · xcstrings 린터 0 누락 · 빌드 green · 변이 검출 Kit 11종+웹 2종. **남은 것**: 위원장 실보행(이 기능의 유일한 진짜 판정)과 M2 길찾기 초점 점검(기기 미연결로 미착수, 백로그 B2) |
| 좌표 파라미터·서울 열린데이터 위장 제거 (백로그 D2·D3) | ✅ 구현·변이·리뷰·prod 실호출 (2026-08-01) | 커밋 `d5b9e4a`. 사용자에게 **오류로 보이지 않는 오류** 둘을 함께 걷어냈다. **D3**: 좌표 누락이 `Number("")===0`으로 (0,0)이 돼 `200 {"outOfCoverage":true}`로 나가던 위장. 라우트 14곳을 `@/lib/coord-param`으로 백포트. ⚠ **14곳은 균질하지 않았고 백로그 서술이 과일반화였다**(코드 대조로 확정): 진짜 위장은 11곳, `walk/nearby`는 커버리지 마커가 없어 증상이 **널 아일랜드 실조회**이며 결측은 이미 막혀 있고 빈 문자열만 뚫려 있었고, `places`는 좌표가 **선택**이라 400이 아니라 '좌표 없이 검색'이 정답인데 빈 문자열이 적도 앞바다 기준 근접 블렌딩을 만들었고, `places/barrier-free/match`는 한국 bbox(33~43)라 0이 애초에 통과 못 해 **유일하게 행동 결함이 없었다**(수정이 아니라 잠금: 범위를 넓히는 변이에서 3/4가 빨개짐을 리뷰가 실증). **D2**: 무효 키가 `/json/` 경로에서도 200 + XML을 주는 탓에 `res.json()`이 `Unexpected token '<'`가 되어 원인이 키라는 사실을 지우던 위장. 공용 `readSeoulOpenJson`(`src/lib/providers/seoul-open-json.ts`)으로 뽑고 경계는 `datagokr-envelope`과 동일하게 **모양은 공용·봉투 정책은 provider**로 유지(정상 코드 판정은 따릉이 `rentBikeStatus.RESULT.CODE`/문화행사 `INFO-000`·`INFO-200`/혼잡도 평면 키에 그대로 남겼다). **리뷰(별도 컨텍스트) Critical 0 · Important 3 · Minor 5, 전부 반영**: ①**소비자를 셋으로 세고 `seoul-elevator`를 빠뜨렸다**: 같은 키·같은 호스트인데 호출부가 `Promise.allSettled`로 실패를 강등하며 `reason`까지 버려, 고쳐도 진단이 로그에 도달하지 못하던 터라 `console.error`도 함께 넣었다 ②**`geocode/reverse`의 D3 테스트 2건은 검출력이 0이었다**: 그 파일만 `makeRequest(lat,lng)` 2인자인데 형제 파일의 1인자 형태를 복붙해 `?lat=&lng=undefined` 같은 **쓰레기 값**을 검증하고 있었고(리뷰가 변이 주입으로 확정: 라우트를 되돌려도 11/11 통과), 이 라우트는 `isInKorea`가 없어 진짜 증상도 outOfCoverage가 아니라 (0,0) 카카오 실호출이었다 ③400 메시지가 `Too small: expected string to have >=1 characters`라 **D3이 겨냥한 CLI 소비자가 '좌표를 안 보냈다'가 아니라 '문자열이 짧다'를 읽었다** → `위도(lat)가 필요합니다` ④**CLI `resolve-location.ts`에 같은 함정이 한 겹 더**: `Number("")===0`이 `Number.isFinite`를 통과해 클라이언트가 (0,0)을 확정해 보내면 서버 수정으로는 못 막는다(서버가 보기엔 정상 좌표). **재발 차단은 정적 가드 2개**(둘 다 변이로 검출력 실측): `coord-param-usage.test.ts`는 좌표를 읽는 라우트가 헬퍼를 쓰는지 보되 **수신자를 묶지 않아** `const sp = …searchParams` 별칭 우회까지 잡고, `seoul-open-json-usage.test.ts`는 그 호스트를 부르는 provider에 `res.json()`이 없는지 본다. **이 가드가 있었으면 엘리베이터 누락은 사람 없이 잡혔다**(가드 자체도 처음엔 주석 속 `res.json()`과 `swopenapi` 호스트를 오탐해, 주석 제거와 호스트 정확 매칭으로 좁혔다). **prod 실호출 검증**: 좌표 누락 3라우트 전부 `400 {"error":"위도(lat)가 필요합니다"}`, 해외 좌표는 `200 {"outOfCoverage":true}` 유지, 정상 좌표 200. **누락·지역밖·정상 3상태가 비로소 갈린다**. 게이트: 웹 1,466→1,507 · CLI 146→147 · MCP 5 · lint 0 errors · build green |
| 실시간 인구 혼잡도 (서울 `citydata_ppltn`) | ✅ 구현·변이·실호출·리뷰 (2026-08-01) | 스펙·플랜 `docs/superpowers/{specs,plans}/2026-08-01-realtime-congestion*`. 백로그 E1. **보류 사유가 사실이 아니었다.** 종전 판정은 "핫스팟 영역 경계가 미공개라 이름을 지오코딩하면 근사 2겹(이름→점 + 점→반경)이 된다"였고 그래서 착수 불가였다. 혼잡도 API에 좌표 필드가 0개인 것은 맞지만, **전체 `citydata`의 `SUB_STTS`·`BUS_STN_STTS`가 그 영역을 구성하는 지하철역·버스정류장을 WGS84 좌표와 함께 준다**(서울시가 그 영역의 센서로 쓰는 바로 그 점들). 근사 두 겹이 모두 사라지면서 보류 사유가 통째로 소멸했다 — **문서가 아니라 실호출로 판정한다는 백로그 축 4가 항목 하나를 되살린 사례.** **판정은 중심-반경 원이 아니라 최근접 구성 지점 300m**: 영역 크기가 0m~1,872m로 제각각(중앙 475m)이라 원으로 잡으면 잠실 관광특구(1,872m)가 무관한 주택가를 삼킨다. 중첩 시 중심 최근접 1개(잠실역 1번 출구는 잠실 관광특구·롯데타워·잠실역 셋 다 7m 동률 → 중심 78m<527m<893m로 가장 국소적인 "잠실역"). **임계값 근거**: 매칭 대상 8지점 전부 120m 이하 vs 비대상 전부 676m 이상으로 **완전 분리**(길동 주택가 1,319m·강동역 676m·일산 10.5km), 임계값 150~500m 전 구간 오판 0이라 결정이 임계값에 둔감. 서울 격자 18,590점에서 300m는 8.9% 판정(500m 14.7%·750m 21.5%는 과잉). **seed는 오프라인 스크립트**(`scripts/build-congestion-areas.mjs`, 코드 POI001~131 중 실재 121개·구성 지점 보유 116영역·1,969지점, 가드 4종=영역 수·지점 수·서울 bbox·강남역 golden 17m). 지점 0개 5곳(한강공원류)·2개 이하 9곳은 footprint가 실제보다 좁아 **미탐(침묵)**이 나지만 오탐이 아니라 별도 제외 목록을 두지 않았다. **봉투 3형째**: 정상은 `RESULT`가 아예 없는 `SeoulRtd.citydata_ppltn` 배열, 오류는 점이 들어간 **평면 키** `"RESULT.CODE"` → `datagokr-envelope`·문화행사 어느 쪽과도 달라 공용 파서 스코프 밖. **캐시는 좌표가 아니라 영역 코드 단위 5분** — 좌표별로 캐시하면 같은 영역이 서울 열린데이터 일 1,000회(따릉이·문화행사와 공유)를 반복해 태운다. 영역 밖이면 upstream 미호출(서울의 91%). **등급어는 provider가 원문 통과, 번역은 표시 계층**(`congestion-level.ts`) — 열거형으로 좁히면 서울시가 단계를 늘렸을 때 조용히 빈 값이 된다. API가 한국어만 주므로 **완성 문장은 ko 로케일에서만** 노출하고 비한국어는 번역된 등급어가 답. 인구수는 라우트에서 제거(해석 불가 수치, 등급어가 정본). **노출은 새 패널이 아니라 `LocalConditions`**(웹)·`ConditionsView`(iOS) — 좌표 하나 → 단일 등급어라 모양이 날씨·공기질과 같고, 그 그릇이 현재 위치와 장소 상세 양쪽에서 렌더된다(11번째 nearby 패널을 만들면 목록 골격에 단일 값을 끼우고 발견 경로만 하나 더 는다). 예보 12개는 **UI 미표기·채팅 `data`에만** — 실호출에서 Gemini가 이를 "20시 이후 또는 23시 이후 방문 권장"으로 압축해 답했다. **변이 13종 중 13종 검출**(1차 12/13 → `FCST_YN` 검사 변이가 생존. 테스트가 플래그와 배열 존재를 동시에 바꿔 축을 격리하지 못한 탓이라 배열을 남긴 채 플래그만 N으로 두도록 고치고 재주입 검출 확인). **실호출 8경로 통과**(강남역 붐빔·잠실역 중첩→POI119·성수 약간 붐빔·길동 area:null·부산 area:null·도쿄 outOfCoverage·좌표 누락 400·빈 문자열 400) + CLI `--output text` + 채팅 실호출. ⚠ **`next build`가 Vitest가 못 잡는 타입 오류를 잡았다**: JSON import는 `[number, number]`가 아니라 `number[]`로 추론된다(튜플 단언은 `as unknown`을 강요해 검사를 끄게 되므로 타입을 `number[]`로 두고 쌍 보장은 빌드 스크립트 가드에 맡겼다). ⚠ **봉투 실측이 두 가지를 더 드러냈다**: ①정상 응답에도 `RESULT` **객체 안에** `"RESULT.CODE": "INFO-000"`이 실려 온다 — 코드 존재만으로 오류 처리하면 **모든 정상 응답이 502가 된다**(오류는 최상위 평면 키에 온다). ②인증키가 무효하면 `/json/` 경로여도 **HTTP 200 + XML 본문**(`INFO-100 인증키가 유효하지 않습니다`)이라 `res.json()`이 `Unexpected token '<'`로 죽고 원인이 키라는 사실이 가려진다 → `readSeoulOpenJson`으로 원인을 말하고 죽게 했다(같은 함정이 따릉이·문화행사에 잔존, 백로그 D3). **리뷰**: 별도 컨텍스트 2건(전체 diff·핵심 계층) **Critical 0 · Important 0 · WARNING 1 · INFO 2**, 4건 전부 반영(커밋 200bb4c). ①**게이트 안정성**(리뷰어가 부하 중 실측 재현): 격자 동치 테스트가 격자마다 1,969회 하버사인을 **동기로** 돌아 수백 ms를 블로킹했고 그 블로킹이 병렬 워커 스케줄링을 흔들어 **무관한 테스트가 산발적으로 실패**했다(부하 중 10회 실행 5회 실패, 부하 없으면 8/8 통과 — 즉 코드 결함이 아니라 게이트 자체가 동전 던지기가 됐다). 위경도 사전 필터로 1.3초로 낮춤. ②**HTTP 계층 커버리지 공백**: `parseCongestion`만 fixture로 덮여 있어 `fetchCongestion`이 `readSeoulOpenJson` 대신 `res.json()`으로 되돌아가도 아무 테스트가 안 깨졌다(변이 R로 확인 후 fetch 목킹 4케이스 추가). **실호출 게이트는 유효한 키로 하므로 이 경로를 구조적으로 못 잡는다.** ③**단일 객체 방어**: `SUB_STTS`·`BUS_STN_STTS`를 배열로만 받고 있었다 — 단건이 단일 객체로 오는 API가 실재하고([[datagokr-single-item-shape-varies]]), 지점 1개짜리 영역에서 그러면 **영역이 통째로 seed에서 사라져 진짜 인프라 부재와 구분되지 않는다.** ④스펙 §1-6 정합. **변이 18종 전량 검출**(초기 13종 → 봉투 보강 3종·리뷰 반영 2종 추가). 백로그 **D1(CLI `route walk --accessible` 플래그) 편승 처리**. **CLI/MCP npm v0.7.0 발행 완료**(2026-08-01, 위원장 승인 후 `cli-v0.7.0` 태그 → Trusted Publishing). 버전 4곳 동조 후 **빌드본 실보고값까지 확인**(CLI `--version`·MCP `serverInfo.version` 둘 다 0.7.0 — package.json만 올려 발행본이 옛 버전을 보고한 실사고 재발 방지), 발행본 `npx gildongmu@0.7.0`으로 혼잡도·계단 회피 실호출 검증. 부수로 CLI completion의 nearby 목록이 `events` 누락 상태였던 드리프트와 `formatters-transit.test.ts`의 TS2835(확장자 누락, `tsc --noEmit` 상시 실패)를 함께 고쳤다. |
| 근처 문화행사 (서울 `culturalEventInfo`, OA-15486) | ✅ 구현·변이·실호출·리뷰 (2026-08-01) | 스펙·플랜 `docs/superpowers/{specs,plans}/2026-08-01-nearby-culture-events*`. 백로그 E1. **10번째 "내 주변" 도메인**이자 웹·iOS·채팅·CLI/MCP를 한 마일스톤에 담은 첫 신규 기능(B축 플랫폼 갭 재발 방지 규칙 적용). **착수 전 전수 스캔(19,477건)이 설계를 두 번 바꿨다**: ①**안전한 페이지 절단선이 없다** — 정렬이 시작일 내림차순이라 오늘 진행 중 244건이 183행~18,587행에 흩어진다(2021-11 시작 장기 전시). 상위 1,000행=63%·2,000행=91%만 잡혀 **조용한 누락**이 되므로 전수 수집 후 코드가 판정하고, 매 요청 20페이지는 불가능하니 캐시가 선택이 아니라 필수 설계 요소가 됐다(**일자 키 `unstable_cache` 6h** — 진행 여부는 날짜 함수라 revalidate만으론 자정 경계에서 어제 목록을 최대 6시간 낭독). 캐시 대상은 **좌표 무관 슬림 투영**(거리를 provider가 계산하면 좌표마다 캐시가 갈라져 무의미, 원본 캐시는 수십 MB라 한도 초과로 조용히 꺼짐) ②**`DATE` 파라미터를 쓰면 안 된다** — "그날 열리는 행사"가 아니라 DATE 문자열 부분일치라 7월 시작·9월 종료 행사가 8월 조회에서 탈락(판정은 `STRTDATE`/`END_DATE`). **반경 3km는 실측 결정**: 1km 기준 시청 31건 vs 길동 0건으로 밀도가 30배 갈려 도보권으로 잡으면 주택가에선 기능이 없는 것과 같다(3km면 길동 5·강남 9·노원 13·홍대 20·시청 84). 좌표는 **244/244 유효**라 근사가 0겹이고, id는 `HMPG_ADDR`의 `cultcode`가 전량 고유. 봉투는 서울 열린데이터형(`culturalEventInfo.RESULT.CODE`+`.row[]`)이라 **`datagokr-envelope` 스코프 밖**(따릉이 동형). 정상 코드는 `INFO-000`+**`INFO-200`(범위 밖 페이지=끝 신호, throw 금지)**. **변이 13종 중 13종 검출**(1차 12/13 → `INFO-200` 정책이 첫 페이지용으로 한 벌 더 있어 테스트가 한 벌만 덮고 있었다. 봉투 정책을 한 함수로 합쳐 재주입 검출 확인 — 변이 주입이 중복을 찾아낸 사례). **실호출 7경로 통과**(시청 total=84·길동 total=5·부산 빈 배열·도쿄 outOfCoverage·좌표 누락 400·CLI text·채팅 도구). ⚠ **좌표 누락 400은 이 라우트만의 개선**: 형제 14개 라우트는 `Number("")===0`으로 (0,0)이 돼 **파라미터 누락이 "서비스 지역 밖"으로 위장**된다(백로그 D3로 이월). 리뷰(별도 컨텍스트) Critical 0·Important 1 수용: **CLI `FORMATTERS` 등록 누락** — `runEndpoint`가 조용히 `JSON.stringify` 폴백으로 떨어져 text 모드에서만 통짜 JSON이 나온다. 파이프로 돌린 초회 실호출은 비-TTY라 JSON 모드가 정상이라 못 잡았다([[real-call-gate-weak-predicate]] 재현 — 통과 사실보다 **무엇으로 판정했는가**). `formatEvents` 추가 + `formatter-coverage.test.ts`(예외는 명시 열거)로 재발 차단, 가드 검출력은 등록 제거 재주입으로 확인. 리뷰 지적 1건은 오탐(카탈로그 両미러 drift 테스트는 `catalog-drift.test.ts`에 byte 해시로 실재). |
| data.go.kr envelope 파서 공용화 (+ 단건 응답 모양 계열별 실측) | ✅ 구현·변이·실호출 (2026-08-01) | 스펙·플랜 `docs/superpowers/{specs,plans}/2026-08-01-datagokr-envelope*`. 백로그 D1. **중복은 6벌이 아니라 25지점**(items 추출 9·resultCode 7·totalCount 4·XML 가드 3·게이트웨이 가드 2). 신규 `src/lib/providers/datagokr-envelope.ts`(`readItems`·`readResultCode`·`readResultMsg`·`readTotalCount`·`fetchDataGoKrJson`)로 흡수, provider 11개 교체. **설계 경계: 모양(shape)은 공용, 정책(policy)은 provider** — "items가 ''면 빈 결과인가"는 전 계열이 같은 답이라 모양이고, "정상 resultCode가 뭔가"는 `"00"`/`"0"`/`"0000"`/`"03"`로 갈리는 정책이다. 후자까지 합치면 `okCodes: string[]` 같은 분기 주머니가 생겨 백로그 경고("설계 없이 합치면 조건 분기만 한 곳에 모인다")에 정확히 걸린다. 그래서 공용 fetch는 **resultCode를 아예 보지 않는다**(HTTP·XML·게이트웨이까지만). **DRY보다 큰 성과 둘**: ①9벌이 같은 모양을 다르게 읽고 있었다(원시값 `item`을 절반은 버리고 절반은 감쌌다 — 감싸면 전 필드 `undefined`인 "이름 없는 항목"이 SR에 낭독된다. `items` 직접 배열은 에어코리아만 읽어 나머지 8벌은 조용한 전멸) ②`tago-bus`·`air-quality`만 갖던 진단(XML 본문 감지·`OpenAPI_ServiceResponse` 게이트웨이 에러)이 나머지 6곳에 확산 — `res.json()`은 키 만료를 `Unexpected token '<'`로 위장해 [[deepgram-prod-key-401]] 계열 오진을 부른다. **스코프 밖**: TOPIS(`msgHeader`/`msgBody`)·서울지하철 실시간(`errorMessage.code`)·서울 열린데이터(`<서비스명>.row`)는 `response` 래퍼가 없어 봉투가 다르다. **변이 7종 중 6종 검출**(1종은 등가 변이) — 특히 에어코리아 분기 제거가 계약 테스트와 `air-quality.test.ts`를 **둘 다** red로 만들어 공용화가 서비스별 커버리지를 흡수하지 않았음을 확인. **실호출 9경로 통과**(TAGO 버스·에어코리아·기상청·코레일·wksn·TAGO 지하철+특일정보·NMC 소아진료·TourAPI 무장애·TourAPI 검색). ⚠ **실호출이 초안 주장을 반박**: "tour-api가 단건 응답에서 죽는다"고 썼는데 `numOfRows=1` 직접 실측 결과 **단건 모양이 기관코드마다 다르다** — B551011(TourAPI)·B551457(코레일)은 1건에도 배열 유지, B552657(NMC)·1613000(TAGO)은 단일 객체. 즉 tour-api의 배열 단정도 night-clinic의 단건 방어도 각자 계열에선 옳았고, 틀린 것은 "계열을 보고 알 수 있다"는 생각이었다(새 API는 호출해 봐야 알고, 틀리면 런타임 TypeError 또는 조용한 누락). 리뷰(별도 컨텍스트) Critical 0·Important 1 수용: `air-quality.test.ts`의 HTTP 실패 목에 `text`가 없어 공용 fetch 이관 후 그 테스트가 HTTP 500이 아니라 `res.text is not a function`을 잡고 있었다(매처 없는 `.rejects.toThrow()`라 green) — 목 보강 + `/HTTP 500/`·`/XML/` 매처로 의미 복원. |
| iOS 장소 상세 "이 장소 주변" 4행 (+ 카탈로그 수기 편집 선재 결함) | ✅ 구현·시뮬 실측 (2026-08-01), **지하철 행 추가 (2026-08-02)** | **2026-08-02 갱신 — 지하철 추가로 4행**(위원장 지시, 순서는 지하철 → 버스 → 따릉이 → 날씨·공기질). 웹 장소 상세엔 지하철이 없어 **이번엔 iOS가 선행**한다. 새 화면을 만든 게 아니라 `SubwayNearbyView`만 앵커 축(`anchor: PlaceAnchor? = nil`)이 없던 것을 아래 3종과 동형으로 열었다(허브 호출처 무변경). ⚠ **역 장소에서 첫 항목이 그 역 자신이라 인라인 "역 도착 정보"와 데이터가 겹친다**(앵커=역 좌표 → 근접역 1위 거리 0m). 리뷰가 이를 Important로 제기했고 **숨기지 않기로 판정**했다: ①겹침이 강제 낭독이 아니다(인라인은 선형 주파 경로, 이 행은 눌러야 도달하는 push) ②역에서도 근접 2개 역은 새 정보(도보권 환승 대안) ③장소 종류에 따라 행이 사라지면 섹션 구성이 매번 달라져 위치를 외워 쓰는 SR 탐색이 무너진다(키 게이트의 死기능 제거와 성격이 다르다 — 저쪽은 데이터가 없고 이쪽은 있다). ⚠ **지하철 실시간은 서울 도시철도만 커버**라 비서울 장소에선 항상 0건이다(따릉이와 같은 정직한 부재). 검증 한계: 시뮬레이터 UI 자동화가 이 세션에서 입력을 전혀 받지 못해(탭 전환조차 무반응) **화면 실측을 못 했다** — 빌드 green·xcstrings 린터 0 누락·별도 컨텍스트 리뷰(Critical 0·Important 1 판정 후 유지·Minor 1 수용)까지가 게이트이고 순서·낭독은 실기기 확인 대기. prod 지하철 nearby가 빈 배열이라 키를 의심했으나 **upstream `INFO-200`(새벽이라 운행 데이터 없음)이 원인**으로 확정(로컬 키 직호출 대조, 같은 좌표 버스 nearby는 정상). 위 심사 현황 줄의 "장소 상세 주변 3행"은 **1.2 제출본의 사실이라 갱신하지 않는다**(지하철은 그 빌드에 없다). 이하 2026-08-01 원기록 — 스펙·플랜 `docs/superpowers/{specs,plans}/2026-08-01-ios-place-nearby*`. 백로그 B1 — 웹 장소 상세의 버스 도착·따릉이·날씨/공기질이 iOS엔 없던 갭. **데이터 계층은 이미 있었다**(`BusNearbyView`·`BikeNearbyView`·`ConditionsView` 완성 화면 존재) — 없던 것은 "현재 위치 대신 장소 좌표로 앵커하는 길" 하나뿐. Kit `NearbyCoordinateSource`에 `.fixed(NearbyCoord)` 신설(전이표에 새 상태·새 전이 없음 — `.current`의 진부분집합)+커버리지 선분기를 `.current` 분기 안에서 밖으로 이동(판정 조건은 "좌표가 있는가"이지 "어디서 얻었는가"가 아니다). 3개 모델·뷰에 `anchor: PlaceAnchor? = nil`(기본 nil = 현재 위치, 허브 호출처 무변경), 장소 상세엔 `이 장소 주변` 섹션 3행 NavigationLink. **인라인 섹션이 아니라 push인 이유**: 기존 화면의 새로고침·8종 상태 오버레이·완료 통지·경유 정류소 push 계약이 그대로 따라오고 상세 화면이 짧게 유지된다(SR 선형 주파). **앵커 화면 제목에 장소명 병기**(`nearbyTitle` — "따릉이 대여소, 해운대해수욕장"): 앵커 화면 문구는 전부 "주변 …"인데 기준점이 화면 어디에도 없으면 SR 사용자가 "주변에 대여소가 없습니다"를 자기 주변으로 읽는다(뒤로 가기 라벨은 되돌아가야 얻는 맥락). 카피는 불변, 제목만 보정(리뷰 수용). **섹션 위치는 길찾기 다음·역 4종 앞으로 웹(역 4종 뒤)과 반대인데 의도다**: 웹 역 시설 2종은 접힌 패널이라 뒤에 둬도 비용이 없지만 iOS `StationSectionsView`는 인라인 전개라 천호역 실측 수백 행 — 뒤에 두면 선형 주파 비용이 3행에서 수백 행으로 뒤집힌다(heading 점프로는 동등). 웹 순서를 따르려면 iOS 역 시설의 접힘 전환이 선행. **선재 결함 동반 수정**: `ios.route.serviceOutside`가 생성물인 xcstrings에만 수기로 존재해(정본 JSON 어디에도 없음) 재생성 시 소멸하는 상태였다 — 웹 정본 키 `route.transit.legServiceOutside`로 수렴시켜 사본 제거. 그 키의 en·es·fr·it 문구가 `First bus`로 버스 고정이었는데 마일스톤 A1이 지하철까지 확장해 오낭독 중이었던 것도 수단 중립으로 교정(ko `첫차`·ja `始発`는 원래 중립). **시뮬 실측**: 현재 위치 캐시가 서울인 상태에서 해운대해수욕장(321km) 앵커가 부산 노선(185·1003·307)·부산 좌동 측정소(1.8km)를 냈다(앵커 실동작 확정). 해운대 따릉이는 "0건 안내"(서울 전용 서비스의 정직한 부재). ⚠ `LocationService`가 좌표를 캐시하므로 시뮬 좌표만 바꾼 대조 실험은 결론이 안 난다 — 앵커 판정은 먼 장소로. 게이트: Kit 195 green(`.fixed` 계약 3건 신규, 변이 주입으로 검출력 실측)·웹 1,312 green·lint 0 errors·빌드 green·xcstrings 키 린터 0 누락·별도 컨텍스트 리뷰(Critical 0·Important 0·Minor 5 중 4 수용 1 기각, 리뷰어가 변이 4종 독립 주입으로 선분기 이동 무영향 재증명). **백로그 B2·B3은 착수 전 코드 대조로 전제 소멸 확인**(웹도 수단별 링크를 내지 않는다 / iOS에 출발지 필드가 이미 있다) — 구현 없이 폐기 |
| 지하철 구간 운행 시간 판정 (+ 토요일 다이어 선재 결함) | ✅ 구현·실호출 검증 (2026-08-01) | 스펙 `docs/superpowers/specs/2026-08-01-subway-service-hours-design.md`·플랜 동일 날짜. 2026-08-01 ODsay 심야 결함 수정이 버스만 커버해 지하철 전용 경로가 심야에도 강등 없이 최상단에 남던 나머지 절반. 조인은 ODsay `wayCode`(1=상행·2=하행, 양방향 실측 확정)와 TAGO SubwayInfo 시간표를 (승차역·노선명·방향)으로 맞춘다 — 신규 키 0. **노선명 2단계 매칭**: ODsay는 `"수도권 5호선"`인데 TAGO는 인천만 지역을 남긴다(`인천1호선`) → 지역 유지 변형 우선, 0건일 때만 지역 제거 변형(부평에서 수도권 1호선 ∩ 인천1호선이 갈린다). 교차 도시 동명이역이 같은 노선번호면(시청 1호선은 서울·부산·대전 3중) 판정하지 않고 `unknown`. **선재 결함 2건 동반 수정**: ① 조인 키 없는 leg가 상태 없이 통과해 rank 0(최상단)을 받던 버그(버스도 해당, codex 적대적 리뷰 검출) ② **수도권은 토요일 다이어를 제출하지 않아**(`02` 정상응답 0행, `01`·`03`만 보유) 역 상세 첫차·막차가 매주 토요일 사라지던 버그 — 실호출 게이트가 잡았다(fixture 1,309건 전량 green이었다). 폴백 단위는 소비자마다 다르다(경로=구간, 역 상세=역 2패스 — 대저역은 부산3호선 150행·김해경전철 0행이라 구간 폴백 시 라벨이 거짓, 코드 리뷰 검출). **실호출 검증**: 길동→강남 지하철 6구간 전량 시간표 확보 / 부평 인천1호선 05:37~00:50 vs 수도권1호선 05:25~00:12 정확 분리 / 부산 2호선 05:36~00:16 / 대저 라벨 `saturday` 유지+3호선만 노출. 변이 3종(지하철 분기 제거·방향 무시·폴백 제거) 전부 테스트가 검출 |
| iOS Nearby 11모델 상태 골격 추출 (Kit 상태 머신 수렴 + 취소 결함 수정) | ✅ 구현 (2026-07-31) | 스펙 `docs/superpowers/specs/2026-07-31-ios-nearby-skeleton-design.md`(codex 적대 검토 수용 9·부분 3·기각 1)+플랜 8태스크 SDD(`docs/superpowers/plans/2026-07-31-ios-nearby-skeleton.md`, `8086518..0a884f3` 중 마일스톤 커밋 21개 — 같은 구간의 병렬 세션 커밋 4개 제외, 이후 문서 정정 2건은 범위 밖). 레거시 감사 이월 ③번 종결 — 웹 nearby 중복 추출(2026-07-30)의 iOS 판. **검증 전략이 웹과 다르다**: 앱 타깃에 테스트가 없고 모델이 `LocationService.shared` 싱글턴에 직결이라 "현행 코드 계약 테스트 선행"이 구조적으로 불가능(seam을 만들면 이미 수정 — 동결의 순환) → **전이표 17케이스를 스펙에 동결**하고 Kit 기계+계약 테스트를 선행 green으로 만든 뒤 이관. **공유 계층 5종**: Kit `NearbyLoadCore<Payload>`(좌표 소스 current/none·coverage korea/none·fetch·`willCommit`·이벤트 sink 주입, SwiftUI 무의존·통지 무발화)+`RevealWindow`+앱 `nearbyAnnouncer`(이벤트→VO 통지 매퍼)·`LocationService.nearbyCoordinateSource()`(어댑터, denied 외 전부 unavailable로 total-close)·`NearbyOverlayDescriptor`(.list/.plain/.absentCapable 팩토리+private init 봉인)·`NearbyStateOverlayView`. **잠복 결함 11곳 동시 수정**(승인 예외 1호): 취소를 catch-all이 실패로 오판해 떠난 화면의 "새로고침 실패"가 전역 VO로 발화하던 결함 → 오류형(`CancellationError`·`URLError.cancelled`)+**커밋 게이트**(`Task.isCancelled` — 협력적 취소가 성공값을 반환해도 커밋·통지 폐기) 2겹 방어, 취소 시 entry 상태 복원(`.loading` 고착 금지). **승인 예외 2호**: Conditions 스테일 조각 미부활 — 구 코드는 조각을 모델 프로퍼티로 누적해 `loaded→권한회수/커버리지 이탈→복귀(조각 부분 실패)` 엣지에서 옛 좌표 기준 값을 현재 정보처럼 재표시했고(3-state 위반, SR 사용자는 판별 불가) 両조각 전멸 시 통지-화면 불일치까지 있었다. **결과 수치**: 도메인 뷰 11개 1,572→1,258행(-20%), 공유 계층 신설 279행+어댑터·매퍼 36행이라 **순 프로덕션 행수는 사실상 동일** — 이 마일스톤의 성과는 행수가 아니라 "복제된 검증 불가 상태 머신 11벌 → 테스트가 지키는 정본 1벌"이다. Kit 테스트 153→186(+33: 전이표 계약 28·RevealWindow 4·previous 계약 1). **검출력 실증**: 구현자·리뷰어가 뮤테이션 배터리 11종을 돌려 전부 red 확인, 그 과정에서 계약 공백 2건을 잡음 — ①재조회 성공 분기의 이벤트·willCommit 미단언(새로고침 완료 통지 소실 회귀가 통과하던 공백) ②`previous` 계약 테스트가 동어반복(최초 실패 경로엔 직전 성공이 없어 자명하게 참, 스테일 부활 변이 185 전량 생존) → loaded→전락→복귀 경로로 재작성. hang 방어는 `.timeLimit` trait가 취소 비반응 continuation에 무력함을 실측하고 게이트 워치독 2종으로 해결(무한 hang→2~4초 진단 실패). ③최종 브랜치 리뷰가 잡은 마지막 공백 — 취소 가드를 커버리지 선분기 뒤로 옮기는 변이가 185건 전량을 통과(표제 수정인 취소에서 유일한 무방비 축, 변이 시 증상이 원 결함과 동형: 떠난 화면에 "서비스 지역 밖" 오발화+데이터 전락) → `cooperativeCancellationBeforeCoveragePrecheckDiscards` 추가로 폐쇄. **시뮬 실측(11화면 전수 pass)**: 크래시·행·빈 화면 0, coverage 대비 확인(도쿄 좌표에서 지하철=outOfCoverage 카피 vs 보행 인프라=정상 데이터 — `.none` 의도 계약), 권한 거부 카피, "더 보기" 10→12 확장. 게이트: Kit 186 green·앱 빌드 green·태스크별 리뷰 7회(fix 3라운드)+a11y 감사 6항목 전부 합격+최종 브랜치 리뷰 SHIP(Critical 0, fix 1라운드). **실기기 VO 스모크(위원장 2026-07-31)**: ①로딩 중 화면 이탈 시 무통지 ④"더 보기" 포커스 이동 **확인 완료** — ①은 이 마일스톤 표제 수정(취소 2겹 방어)의 실기기 실증이다. ②loaded 새로고침 실패 통지+데이터 유지 ③권한 회수 전락 통지는 인위적 재현 대신 **실사용 관찰로 전환**(이상 발생 시 보고). 마일스톤 종결 |
| 장소 상세 카카오 진입점 단일화 (웹+iOS) | ✅ 구현 (2026-07-31) | 커밋 `be0013d`. 카카오 출처 장소의 `link`가 카카오맵 장소 상세(`place.map.kakao.com/{id}`)임을 프로덕션 실호출로 확정 — 웹 '카카오맵에서 보기'(핀 지도)+'상세 정보 페이지', iOS '상세 정보 페이지'+'카카오맵 장소 정보'가 같은 장소 화면으로 가는 중복이었다. 웹 `RouteLinks`는 카카오 버튼 href를 `link` 직결(무 link 시 핀 지도 폴백)+상세 버튼 제거, iOS `PlaceDetailView`는 상세 링크 행 숨김(`kakaoPlaceId == nil` 게이트)+**앱 미설치 폴백을 도보 경로 URL(오동작)→`place.map.kakao.com/{id}`로 교정**. 비카카오(네이버 보강 홈페이지) link는 유지하되 라벨을 `place.detailPage`→`place.homepage`('홈페이지')로 6로케일 개명, xcstrings 재생성(7-30 옴니박스 커밋분 카탈로그 드리프트 캐치업 동반 — 죽은 키 7종 제거·신규 키 5종 수록). RouteLinks 계약 테스트 3건 신규. 리뷰 Critical 0, 실기기 배포 완료 |
| 웹 nearby 중복 추출 (9종 공유 계층 수렴 + 잠복 결함 수정) | ✅ 구현 (2026-07-30) | 스펙 `docs/superpowers/specs/2026-07-30-nearby-dedup-design.md`(codex 적대 검토 수용 9·기각 4)+플랜 10태스크 SDD(`docs/superpowers/plans/2026-07-30-nearby-dedup.md`, 커밋 범위 `2d90f47..ce17000`). 레거시 감사 이월 ①번 후속. **범위 경계**: 감사 목록(8종)에 없던 `BikeStations` 포함(7개 축 동형 — 드리프트 차단), `LocalConditions` 제외(트리거·패널·live 없는 자동 등장 region 구조). **계약 테스트 선행**(무테스트 리팩토링 금지): 무수정 현행 코드에 공통 계약 스위트 팩토리(`nearby-contract.tsx`)+도메인 fixture로 9종 118건 동결 후 추출 — 상태 전이·live 문자열(키+보간 인자)·fetch URL 정확 일치·포커스 계약·더 보기·복원. 구현자·리뷰어가 뮤테이션 표적 red로 계약 구속력을 반복 검증. **공유 계층 4종**: `useNearbyFetch`(위치→fetch 상태 머신, `fetchAt`/`parse` 주입, source current/place/autoLoad 3조합, coverage korea/none)+`NearbyPanelShell`(트리거·단일 polite live 단독 소유·패널 골격 — 클래스 byte-identical)+`useRevealMore`(doneSeq 렌더 단계 동기 리셋)+`nearbyLiveMessage`(순수 함수). **잠복 결함 8곳 동시 수정**(유일한 의도적 행동 변경): 닫힌 패널의 in-flight 응답이 패널을 재열고 VO 포커스를 탈취(자동 재닫힘이 포커스 쥔 h3를 제거해 body 완전 이탈 — 실측 재규명) → 요청 ID latest-wins(load·close마다 증가, 검사 2지점 — 위치 후·fetch 전/반영 전, 잠금 해제도 ID 소유권)로 차단. 교차 패널 통합 테스트를 `it.fails` red 문서화 커밋(`2b1f51c`)→이관 후 `it` green 전환으로 수정 증명(실질 판정자는 마지막 포커스 단언 단독 — 약화 금지). **결과 수치**: 9종 2,615→1,325행+공유 342행(순 -948행, 도메인 컴포넌트만 -49%), 컴포넌트 상태 머신 테스트 0→138건(계약 118+교차 1+훅 11+셸·리빌 8+live 10), 전체 스위트 1,053→1,191건. 부수 성과: 계약 팩토리·교차 패널의 동기 포커스 단언 flake 2건 근본 수정(패시브 이펙트 플러시 vs waitFor setTimeout 드레인 경합 — 부하 재현·수정 후 부하 12회+15회 무실패). **판정 기록**: WalkInfra 커버리지 부재는 의도 계약(OSM 전 지구 — coverage:"none" 보존), BikeStations 서울 경계 신설·onClose 생명주기 확장·AbortController·포커스 revision 카운터는 기각(행동 불변 원칙, 재도입 금지), autoLoad는 lint 강제 queueMicrotask 한 틱 지연(계약 무영향 실측). 게이트: lint·test:run·build green, 태스크별 리뷰 10회+최종 브랜치 리뷰(Critical 0)+a11y 감사 7항목 전부 합격. **잔여**: 실기기 VO 수동 4시나리오(①패널 A 로딩 중 B 열기 ②로딩 중 Esc 후 늦은 응답 ③done 재조회 헤딩 재발화 ④직접 닫기 트리거 복원)는 위원장 실사용 확인 몫 |
| 옴니박스 중심 웹 IA 재편 (딥링크 압축·채팅 진입 수렴·내 주변 허브·듀얼 액션) | ✅ 구현 (2026-07-30) | 스펙 `docs/superpowers/specs/2026-07-30-omnibox-web-ia-redesign-design.md`(codex 적대적 검토 5건 반영)+플랜 6태스크 SDD(`.superpowers/sdd/2026-07-30-omnibox-web-ia-redesign/`, 커밋 범위 `4deeaf7..1e60c1b`+정리 커밋). 목표는 홈 최상위 컨트롤 과밀 해소(9개 내 주변 섹션 상시 노출+장소 상세 딥링크 9~10개+분산된 채팅 진입 5곳). **① 장소 상세 딥링크 압축**(Task 1, `RouteLinks.tsx`): 프로바이더당 대표 "열기" 1개로(9~10개→3~4개) — 네이버는 `isMobileUserAgent` 판별로 앱 스킴 시도 후 웹 폴백(모바일)/웹 URL 직행(데스크톱), 카카오는 웹 URL 앵커 1개, 상세페이지 링크는 조건부. **② 채팅 진입점 축소**(Task 2): WhereAmI·NightClinicsNearby·KidsPlacesNearby·SurroundingsNearby의 항목별 "이 장소에 관해 물어보기" 버튼 전량 제거, 장소 상세 경유로 수렴. **③ ChatOverlay 범용 모드**(Task 3): `place` prop 옵셔널화+`initialMessage` 1회 자동 전송으로 장소 앵커 없는 대화 지원. **④ 내 주변 허브 뷰**(Task 4, `NearbyHub.tsx` 신설): 9개 섹션을 홈에서 전용 뷰로 이동, `?panel=nearby` URL+History 연동(칩 진입·forward 재진입·직접 진입 방어·아코디언 Esc 우선순위 2단 6갈래 분기), 홈은 "길찾기"·"내 주변" 결정론 칩 2개로 축소. 리뷰 검출 Critical 1건(방어 경로 포커스 rAF 미적용 no-op)+Important 1건(`subscribeOpenPlace` nearby 정규화 누락) 수정. **⑤ 옴니박스 듀얼 액션**(Task 5): `SearchBar`에 `onAsk` 옵셔널 prop으로 [AI에게 질문] 버튼 추가(Enter=검색과 완전 분리 경로, `?q=` 미기록·1회성 seed 전달), 컨트롤러 판정으로 [내 주변] 칩도 상시 노출 통일. **⑥ 정리+문서**(Task 6): 고아 함수 `buildRouteDeeplink`(`deeplink.ts`)·`buildKakaoWebRouteUrl`(`deeplink-kakao.ts`, 딸린 `MODE_TO_WEB_BY`)+전용 테스트 제거(참조 0 grep 확인, 이 정리로 딥링크 버튼 대응 실함수도 3개로 수렴), 죽은 i18n 키 7종×6로케일 제거(`route.naverLabel`·`kakaoLabel`·`naverModeAction`·`kakaoModeAction`·`deeplinkHint`·`placeChat.launchFor`·`whereAmI.chatButton`), RouteLinks 네이버 버튼 시각 정렬(`inline-flex items-center`)+`isMobileUserAgent` 주석을 실제 분기(비모바일=웹 URL 직행, 폴백 타이머 없음)와 일치하게 정정. **결과 수치**: 홈 최상위 컨트롤 17→약 9(칩 2+옴니박스 듀얼 액션), 장소 상세 딥링크 9~10→3~4, 채팅 진입 5→2(장소 상세 경유+옴니박스 범용). 게이트: lint 0 errors·test:run 전부 통과(i18n 6로케일 패리티 게이트 포함)·build green(태스크별 code-reviewer 게이트 포함, Task 4는 fix round 1 후 clean). 잔여: 스펙 검증 행렬(데스크톱+모바일 실기기 Safari) 수동 확인은 최종 게이트에서 별도 수행 |
| 레거시·중복 감사 수정 묶음 (웹 UI·iOS·lib·라우트/i18n 4도메인) | ✅ 구현 (2026-07-30) | 4도메인 병렬 감사(웹 UI·iOS·lib·라우트/i18n) 후 17태스크 SDD 수정 배치(`.superpowers/sdd/2026-07-30-legacy-audit-fix-batch/`). **채팅 경로 카드**: 커버리지 미체크로 해외 좌표에서 크래시 나던 자동차·대중교통 카드 2종에 선분기 추가, 위치 취득을 공유 싱글턴으로 이관, 대중교통 대안 disclosure를 길찾기 뷰와 동일 신계약으로 통일, 카드 내 출발지 미니 검색은 중복이라 제거(위원장 결정). **SR 결함 4건**: 채팅 진행 통지 도구 라벨 3종이 키 원문을 그대로 낭독하던 문제 보강(웹·iOS), 주소 검색 실패가 "결과 없음"에 뭉개지던 것 분리 통지(3-state 복구), `BusRouteStops` 통지를 `BusArrivals` 단일 live region으로 승격(+노선 식별 보강), 채팅 오류 코드에 `rate_limited` 신설(레이트리밋·키 미설정을 일반 실패와 구분). **신규 기능**(위원장 결정): 채팅 장소·주소 카드를 탭하면 상세로 진입(`place-open-request` 모듈 싱글턴 브릿지, 상세 열림 중 다른 카드 탭은 `pushState` 대신 `openSeq` 증가로 재마운트, 주소 카드는 `/api/geocode` 확보 후 진입+`aliveRef` 이탈 가드). **죽은 코드**: `findNightClinicsNear`·`ALL_DECLARATIONS`·`buildKakaoLookDeeplink`·`canShow` prop·미참조 export 5곳·고아 i18n 키 2종(`addressSearching`·`place.callAction`)·레거시 의존성 3종(`react-naver-maps`·`@types/navermaps`·`@types/proj4`, 부트스트랩 잔재) 제거. **리팩토링**: `walk-infra` 캐시를 라우트 주입으로 전환해 `src/lib`의 Next 의존 0 달성, haversine 계산 `geo.ts` 1벌로 단일화, 경로 폴백 로그 공용화, iOS 4계열 중복 제거(`Format`·`CoordQuery`·`firstNonEmpty`·추천 버튼 목록), iOS `normalizeStationName` 낡은 미러 정리, `BarrierFreeService.detail` 제거, Kit xcstrings 243→63키로 축소. **env 정합**: STT·TTS 키를 zod env 계층으로 일원화(게이트 우회 제거), `GOOGLE_CLOUD_TTS_API_KEY` 스키마 등재. **판정 기록**: WhereAmI done 시점 live 통지 추가는 감사가 SR 결함으로 지적했으나 접근성 헌장 §5(재포커스 라벨=상태 신호, 중복 announce 금지)와 충돌해 **의도적 미실행**(감사가 형제 컴포넌트의 레거시 중복 관례를 오판정한 사례). 게이트: `npm run lint && npm run test:run && npm run build`+`swift test --package-path ios/GildongmuKit` 전부 통과(태스크별 code-reviewer 리뷰 게이트 포함). 의도적 보류 3건은 아래 미해결·보류 참조 |
| 장소 상세 경로 미리 듣기 진입점 일원화 (중복 제거, 웹+iOS) | ✅ 구현 (2026-07-30) | 장소 상세의 "자동차/대중교통 경로 미리 듣기"(웹 인라인 2섹션·iOS 단독 화면 2종)가 "여기까지 길찾기"(DirectionsView/DirectionsTab 3수단 비교)의 부분집합이라 제거(위원장 판정 — 원탭 손실은 수용, 결과 렌더 컴포넌트는 원래 공유라 진입점만 삭제). 웹 래퍼 `CarRouteBriefing`·`TransitRouteBriefing`은 **채팅 렌더 카드가 계속 소비하므로 유지**(PlaceDetail 사용부·게이트 props만 제거), iOS는 `CarBriefingView`·`TransitBriefingView`+전용 인프라(RouteLoadState·routeStateOverlay·모델) 삭제·행 렌더러(`CarRouteRows`류)만 존치. 미사용 i18n 키 9종×6로케일 제거+xcstrings 재생성(Kit `stepFreeToggle` 기존 재생성 누락 드리프트 동기화 포함). 트레이드오프 기록: iOS 대중교통 대안 경로 노출은 단독 화면 전용이었어서 iOS에선 사라짐(웹은 채팅 카드에 잔존, DirectionsTab은 의도적 추천만). 게이트: lint 0오류·vitest 1050·웹 build·iOS 시뮬 빌드·키 린터·code-reviewer 클린. **후속(같은 날, 위원장 지시)**: 길찾기 뷰에 대중교통 **대안 요약 1행씩** 추가(웹 DirectionsView·iOS DirectionsTab 동형, legs 미표시 미니멀 — ODsay 상한 대안 2개라 토글 없이 상시, `route.transit.alternativeHeading` 기존 키 재사용, joinText 한 줄=한 객체). 리뷰 검출 fix 1건: iOS `transitSummaryText` 도보 0분 상시 낭독 → `walkMinutes > 0` 게이팅(추천 행 기존 비대칭까지 동시 해소, 웹 동형). **2차 후속(같은 날, 위원장 실사용 판정 "요약만으론 정보 가치 낮음")**: 대안 요약 탭 → 추천 수준 구간 상세 펼침(웹 `<button aria-expanded>` disclosure 형제 동형 / iOS `DisclosureGroup` 네이티브 시맨틱, `expandedAlts` Set 상태·새 결과 시 리셋, 라벨이 요약 전문이라 펼침 본문은 `includeSummary=false`로 구간만 — 인접 중복 금지). 시뮬 실측 합격(강남→서울역: 탭 토글·구간 렌더·두 대안 독립 상태). 리뷰 fix: 필드 편집 경로 리셋 2지점 보강. **실기기 VO 확인 완료(2026-07-30 위원장 "잘 작동")** — 시뮬 AX 트리의 무라벨 셰브런 노드는 실기기 VO 비문제(원시 트리≠VO 낭독 기지 제약의 재확인), 커스텀 disclosure 대체 불필요 종결 |
| 웹→iOS 기능 갭 이식 1차 (계단 회피 토글+보행 인프라 화면+추천 질문+홈페이지 링크) | ✅ 구현+시뮬 실측 (2026-07-29~30, 커밋 5502392+후속) — 실기기 VO 확인 잔여 | 전수 갭 조사(웹 컴포넌트 40여 개 ↔ iOS 뷰 1:1 + i18n 키 548개 Swift 참조 기계 검사)로 전체 부재 2·부분 부재 6 확정 후 우선순위 이식. ① **길찾기 도보 "계단 없는 경로" 토글**: `RouteService.walk(accessible:)`(false는 파라미터 생략 — 기존 요청 byte-identical), `toggleStepFree` 도보 단독 재조회(대중교통·자동차 보존, "조회"와 `isInFlight`·`queryTask` 공유로 교차 레이스 구조 차단), Toggle 켬/끔 낭독=상태 신호+재조회 중 라벨 "조회 중" 병기(리뷰 fix), 완료 시 도보 heading 포커스. `stepFree` 응답 필드는 의도적 미디코딩(미적용 안내는 서버 steps[0] 안전 문장이 정본). ② **보행 인프라 "내 주변" 화면**(9번째 허브 항목): Kit `WalkInfraModels`(WalkSourceStatus ok/unsupported/error 독립 강등, 미지 status→error)+`WalkInfraService`, 프로덕션 실호출 픽스처 3종(강남 両ok·강남 osm error 부분 강등 200·부산 unsupported) 계약 테스트, 그룹 헤더 3개 항상 렌더·"N곳 중 가까운 M곳" cap 전 실개수·출처는 성공 소스만·단일 통지(buildLive 미러). 시뮬 강남 좌표 실측: 음향신호기 5기+지점 5행+횡단보도 항목 실데이터 렌더 확인. xcstrings 36키는 사전 미러돼 있어 코드만 신설(`walkInfra.ready`는 navigationTitle 중복이라 의도적 생략). ③ **장소 채팅 추천 질문**: Kit `placeChatPromptKeys`(웹 place-prompts.ts 미러, 역>음식>일반·키즈카페 비-food 경계 테스트) → 장소 sheet 빈 화면에 ChatTabView 동형 3버튼. ④ **장소 홈페이지 링크**(`place.link`→`place.detailPage` Link 행). 리뷰 2회(code-reviewer): 계단 토글 aria-busy 대응 1건+보행 인프라 asOf 헤딩 시맨틱·방위 미상 문구 아티팩트 2건 검출·수정, 크리티컬 0. 게이트: Kit 153·앱 빌드·키 린터 전부 통과. **잔여 갭(별도 마일스톤 후보)**: 목적지 거리 비컨(전체 부재, CoreLocation 상시 업데이트 필요)·장소 상세 3섹션(버스 도착·따릉이·공기질 — Kit 서비스 기존재, 뷰 조립만)·딥링크 수단 선택(현 도보 고정)·대중교통 브리핑 출발지 변경·대안 경로 legs·무장애 펼침(의도적 대체 확인됨) |
| 서비스 지역 커버리지 계약 + 받아쓰기 재설계 (App Review 2.1(a) 반려 대응) | ✅ prod·**1.0.0(6) 승인, App Store 출시(2026-07-30)** | 반려(1.0(4), 7/29 15:45 통보, Submission 28eca73c): 심사 환경(미국 좌표·iPhone 17 Pro Max)에서 날씨·공기질 "Couldn't load"·WhereAmI "Failed to get current location"·받아쓰기 무반응. 원인은 버그가 아니라 한국 전용 upstream×해외 좌표의 처리 공백(서버 bbox 400을 클라가 오류로 렌더)+홀드 기본값의 비-VO 무반응. 스펙 `2026-07-29-coverage-contract-dictation-design.md`+플랜 13태스크(SDD, 태스크 리뷰 10회+fix 3라운드+최종 광역 리뷰 SHIP). 핵심: ① 커버리지 술어 1벌(`coverage.ts`↔Kit `Coverage.swift`) ② 좌표 라우트 13종 200 `{outOfCoverage:true}` 마커(파싱→마커→키게이트→upstream, 정상 응답 byte-불변) ③ 웹 11컴포넌트·iOS 10화면 선분기+이중 방어(오류 아닌 안내, `map` 아이콘·단일 통지 채널) ④ 채팅 앵커 기준 게이트(장소 앵커 한국이면 해외 사용자 정상) ⑤ CLI/MCP 마커 소비+`cli-v0.6.0` 재발행(구버전 8+1 명령 마커 크래시 노출 창 완화 — 최종 리뷰 발견) ⑥ 받아쓰기 기본 탭 토글 전환(위원장 결정)+홀드 짧은 탭 3초 가시 안내+전사 모델 preflight(`.preparing` 가시 상태·installedLocales 선판정·reserve)+오류 4종별 알럿+Siri 영어 문구+NSSpeechRecognitionUsageDescription ⑦ WhereAmI 문구 분리(위치 실패 vs 서버 실패). 검증: vitest 1036·cli 132·swift 143, 미국 좌표 curl 39케이스, 시뮬레이터 SF 심사 동선 재현 PASS, a11y 감사 위반 0, prod curl 5건. **실기기 VO 회귀 fix(같은 날)**: 홀드 짧은 탭 안내가 VO에서 무발화 — 원인 2단(① 커스텀 요소에 명시 `accessibilityAction` 부재로 VO 더블탭의 합성 터치가 탭 인식기에 미도달 ② 활성화 핸들러의 기본 우선순위 announcement가 VO 활성화 처리에 잠식). 실기기 파일 로그(`chat-focus-diag.log`)로 단계별 확정 후 액션 명시+`accessibilitySpeechAnnouncementPriority = .high`로 해소(ko·en `didFinish success=true`). 접근성 헌장 §5에 일반 규칙 승격. **재제출(2026-07-29 22:46 KST)**: 빌드 1.0.0(6)(VO fix 포함) 업로드·교체, ASC 설명·프로모션·심사 노트를 3차 개정본으로 갱신(탭 토글 기본·서비스 지역 소절·일본어), 서울 위치 시연 영상(1분 54초, 영어 UI) 첨부, 회신 2건 발송 후 Resubmit → **Waiting for Review**. ⚠ ASC 함정 2건: Reply 다이얼로그 첨부는 UI·`input.files`에 전혀 반영되지 않지만 서버엔 실제 업로드된다(재시도가 그대로 중복 첨부 — 성공 판정은 발송 후 Message Attachments로만), Rejected 제출은 버전 페이지 "Add for Review"로 아이템을 다시 담아야 Resubmit이 활성화된다. 이월 처리(2026-07-30): whereAmIServerFailed es/fr/it/ja 검수 완료(위치 실패/서버 실패 구분 유지·문안 자연, 수정 0건), CFBundleDisplayName 6로케일 로컬라이즈(`InfoPlist.xcstrings`, en "Gildongmu"·ja "キルドンム"로 Siri 영어 문구 실효화), `AssetInventory.reserve` 반환값 소비+이전 로케일 예약 release 회수(SpeechService, 예약 상한 잠식 방지), 홀드 모드 짧은 탭 가시 안내 실기기 확인 완료(위원장 2026-07-30 — 정상 표시·VoiceOver로도 확인 가능). **이월 전량 종결**. **심사 통과(2026-07-30 23:19 KST 승인 통보, 재제출 약 24시간 만)**: 1.0.0(6) 승인·App Store 공개, KR 스토어 등재 실확인(iTunes Lookup resultCount 1, 2026-07-31). 반려 대응(커버리지 마커+탭 토글 기본값) 유효 판정 |
| 일본어(ja) 로케일 + iOS 설정 언어 메뉴 피커 | ✅ 구현+실기기 확인 (2026-07-28) | 6번째 UI 언어. 신규 번역 3파일(`messages/ja.json` 545키·`ios-extra/ja.json` 175키·`kit-extra/ja.json` 3키)+전 로케일 `nav.japanese`("日本語"). 코드는 정본 6곳만(`routing.ts`·`LanguageSwitcher LABEL_KEY`·`stt-validate`·`chirp`·`/api/tts` zod·iOS `AppLanguage`/`SettingsView`/`LOCALES`/`knownRegions`/`InfoPlist.xcstrings`), `dataLocale`는 ko 이분법이라 무수정(ja→en 데이터 자동). **외부 계약 실호출 확정(2026-07-28)**: Deepgram `nova-3`+`language=ja` 200(model_info로 nova-3 서빙 확인)·Google TTS `ja-JP-Chirp3-HD-Puck` voices API 존재. 미지원 픽스처 ja를 쓰던 테스트 2파일은 zh로 교체+ja 지원 케이스 승격. **iOS 설정 언어는 `.inline`→`.pickerStyle(.menu)`**(dodo-planet 동형 — 라벨 행에 현재값, 6개 언어 인라인 목록 압도 방지. 즉시 전환 구조 유지라 dodo의 확인 다이얼로그·재시작 안내는 불채택). 게이트: 웹 931·xcstrings 720/242 전키 ja·키 린터·lint·build·iOS 시뮬 빌드(ja.lproj 포함 확인)+code-reviewer(치명·중요 0, 사소 4 중 잔존 주석·픽스처 즉시 정리). 의도적 결정 2건: ja 받아쓰기 라벨은 "音声入力"(iOS 일본어 공식 용어 — ディクテーション은 일본 사용자에게 생경, 내용-라벨 충돌 미확인이라 자연스러움 우선)·`app.title`은 가타카나 "キルドンム"(라틴 자모 유지하는 es/fr/it와 달리 일본 관례상 음역이 자연). 실기기 확인 완료(2026-07-28 위원장): 언어 메뉴 피커 VO 낭독·일본어 즉시 전환 합격, "音声入力"·"キルドンム" 표기 승인. 잔여: ASC 메타데이터(설명 문구 지원 언어)만 다음 제출 편승 |
| 최근 검색 기록 (검색 탭 + 길찾기 출발/도착지, 웹+iOS) | ✅ 구현+실측 (2026-07-26) — 실기기 VO 최종 확인 잔여 | 스펙 `docs/superpowers/specs/2026-07-26-recent-searches-design.md`+플랜 8태스크(SDD). **삼원 분리 기록**(2026-07-26 당일 개정 — 당초 출발/도착 공유였으나 위원장 실사용 지시로 완전 분리): 검색어(제출 시, 선택=재검색)·출발지 장소·도착지 장소(endpoint 확정 시, 선택=재검색 없이 즉시 확정) 각 20건 dedupe 끌어올림, 기기 로컬 전용(웹 localStorage `gildongmu:recent-queries:v1`·`recent-endpoints-{from,to}:v1`, iOS UserDefaults `recentQueries.v1`·`recentEndpoints.{from,to}.v1` — 서버 전송 0, privacy 3자 일치 영향 없음. 구 공유 키는 마이그레이션 없이 고아, 출시 당일이라 데이터 가치 0). 저장 로직은 순수 모듈 미러(`src/lib/recent-searches.ts` ↔ Kit `RecentSearchStore`, 각 단위테스트). 삭제: iOS `.swipeActions`→VO 로터 자동 노출, 웹 항목별 "○○ 삭제" 버튼+모두 지우기, 포커스 계약 다음→이전→진입점(마이크 행/검색 input)+polite 1건. 웹 EndpointField는 5건 표시+`recent.titleFor` 필드명 헤딩("출발지 최근 검색"). **리뷰 검출 fix 4건**: 헤딩 h2→h3 형제 관례·동일 헤딩 2중복→필드명 포함·iOS 길찾기 노출 조건 hasSearched tri-state·**프리필 기록 `State(initialValue:)` 부수효과로 삭제 항목 부활**(App body 재평가마다 재실행) → `consumeDirectionsPrefill` 1회 지점 이동. **검증**: 게이트(웹 870·Kit 118·lint·build)+웹 Safari 실측(기록·재검색·삭제 3단 포커스·전체 삭제·필드별 분리 저장 전항목 — 분리는 웹 871·Kit 119 단위테스트 게이트)+시뮬 6/6 PASS. 잔여: 마이크 행 포커스 착지·로터 삭제 실기기 VoiceOver 확인(위원장) |
| CLI(`gildongmu`)+MCP(`gildongmu-mcp`) | 운영(npm 발행 완료 2026-07-16) | 씬 클라이언트(REST 카탈로그 22항목 중계). 스펙 `docs/superpowers/specs/2026-07-15-cli-mcp-design.md`. **실호출 검증(프로덕션)**: search 경복궁(명소 5 최상단+장소 15+주소 10, JSON 모드 동작)·nearby subway 강동역(실시간 도착 산문)·route transit 강동역→서울역(추천+대안, legs)·weather/air 길동(등급 단어)·station info 서울역(3섹션 병렬)·whereami·chat 단발(약국 5건)·MCP stdio(tools/list 22, nearby_subway 실도착 4건). 게이트가 실결함 3건 검출·수정: 장소명 지오코딩 폴백(/api/geocode는 주소 전용 → /api/places 2단 폴백), nearby류 지오코딩 실패 스택 노출 제거, station-meta "서울역역" 접미 중복. **npm 발행(2026-07-16)**: `gildongmu@0.1.0`·`gildongmu-mcp@0.1.0` 로컬 발행(웹 2FA·Windows 원격 브라우저 인증), 両패키지 Trusted Publisher(Engccer/gildongmu·cli-publish.yml·npm publish) 등록 완료 — 이후 릴리스는 `cli-v*` 태그 push 자동발행(파이프라인 실증은 차기 릴리스). 글로벌 설치 스모크: `npm i -g gildongmu` 후 `gil search 경복궁` 실왕복 PASS. **v0.2.0(2026-07-20)**: attractions-search 제거(명소 섹션 폐지 동조, breaking-soft)·places-search 설명 정확도순 갱신 — `cli-v0.2.0` 태그 자동발행(파이프라인 첫 실증). **v0.4.0(2026-07-22, plan `2026-07-22-cli-mcp-catalog-mirror-v040.md`)**: 카탈로그 24항목(station-timetable 신설, MCP 도구 23종 자동 파생)+`station timetable` 명령+`station info` 4섹션. 0.3.0 실버그 2건 수정: 시설 보강 그룹(voiceGuide·elevatorLocation) 미지원 라벨 "undefined" 출력·route-walk `{result:null}` 크래시(죽은 step 거리 분기도 제거). transit 배차간격 병기·metro-facilities detail·supplementFailed 반영. 이월 백로그 해소: coordSchema 3중복 공용화(`src/lib/route-coord-schema.ts`). **실호출 게이트가 실결함 2건 검출·수정**: 김포공항 서해선 하행 terminus:"" → "행" 대롱 낭독(CLI+웹 StationTimetable 両가드)·이름 없는 시설 빈 줄(여의도 도우미). 검증: timetable 강동/김포공항(5노선·익일)·국회의사당 elevatorLocation 폴백·transit 배차 10/5/5분·walk 서울역→제주 null graceful·MCP tools/list 23·prod coordSchema 3라우트 회귀 0. 최종 리뷰(opus) C/I 0. `cli-v0.4.0` 태그 자동발행+`npx gildongmu@0.4.0` 스모크 PASS. **v0.5.0(2026-07-28)**: 보행 인프라 카탈로그 반영(`nearby-walk-infra` 25항목, MCP 24종 자동 파생)+`gil nearby walk`(그룹 3종·3-state 분리·등록 기준 각주·출처)+기본 API URL `gildongmu.dodoplanet.space` 릴리스. 리뷰 클린(C/I/M 0), 실호출 4분기(길동 ok·강남 부분실패 200·절단 헤더·부산 unsupported), `cli-v0.5.0` 태그 자동발행+`npx gildongmu@0.5.0 nearby walk --output text` prod 스모크 PASS(음향신호기 20기). **v0.6.1(2026-07-31)**: 발행본 버전 표시 고착 수정 — 0.6.0 tarball이 `gil --version`에 0.5.0을, MCP initialize `serverInfo.version`에도 0.5.0을 보고했다(두 `index.ts` 하드코딩이 릴리스에서 누락된 경로, 홍보 글 설치 안내 검증 중 `npx` 실행으로 발견). package.json과 0.6.1 동조 + `version-drift` 테스트 2종으로 재발 차단(카탈로그 byte-mirror와 같은 계열, MCP는 import가 곧 stdio 서버 기동이라 소스 텍스트 대조). `cli-v0.6.1` 태그 자동발행 후 실검증: npm 両패키지 0.6.1, `npx gildongmu@0.6.1 --version` → 0.6.1, MCP initialize `serverInfo.version` → 0.6.1 |
| 보행 인프라 (내 주변 7번째 — 음향신호기+횡단보도·점자블록) | ✅ prod (2026-07-22 배포·스모크 통과 — 길동 audio 9기·OSM 16 로컬 동일) | 스펙 `docs/superpowers/specs/2026-07-22-walk-infrastructure-design.md`(codex 적대적 리뷰 22건 반영 v2)+plan 8태스크. 음향신호기 seed(`scripts/build-audio-signals.mjs` — OA-15543 Shapefile 2026-05-28 기준, DBF 순수 파싱→EPSG:5186→WGS84 pyproj golden 5점 가드·유효율/centroid assert, 16,822기)+Overpass provider(remark 부분응답 throw·고정 매핑 표·다중 라벨 dedup)+서비스 `walk-infra.ts`(discriminated status·타일 anchor 캐시+실좌표 재계산·single-flight·분당 30 예산)+`/api/walk/nearby`(부분실패 200·両전멸만 503)+`WalkInfraNearby`(그룹 h4 3개·항목 무헤딩·그룹별 총수 "N곳 중 가까운 M곳"·단일 polite·5로케일)+채팅 `get_walk_infrastructure`(17종, 성공 소스만 인용). **실호출 게이트**: 길동 audio 9기/OSM 16(crossing 11·tactile 5)·강남 truncated(29→10)·부산 unsupported·Overpass 강제실패 error 200·**실패 미캐시 즉시 회복+성공 캐시 실증**(리뷰 지정 항목)·채팅 산문(12기·방위·등록 기준 각주). 최종 리뷰(opus)+a11y 감사 통과(헌장 위반 0). 이월 백로그: crossingSignal "no"(무신호 횡단) 안내 여부 위원장 검토(유지)·~~빈 문자열 좌표 noCoord 흡수·tactile predicate 공유 헬퍼·OSM way/area 확장~~(2026-07-28 보류 종결: A안 전환으로 반경 패널 확장 투자 근거 약화, 실결함 증거 없음)·~~CLI/MCP 카탈로그 반영~~(✅ 0.5.0). **도보 브리핑 음향신호기 주석(2026-07-28)**: 반경 집계의 실사용 가치 한계(위원장 판정 — OSM 커버리지 복불복·나침반 방위)를 경로 통합으로 전환한 A안. 스펙 `2026-07-28-walk-route-audio-signal-annotation-design.md`, `walk-route.ts` 서비스 신설(라우트·채팅 공용, provider 직접 호출 금지 전환) — description "횡단보도"+단계 좌표 40m 내 seed 시에만 ", 음향신호기 있음" 흡수(실측 분포 4~15m vs 127m+ 완전 분리), positive-only, coord 응답 전 제거(스키마 불변 — 웹·iOS·CLI 자동 적용). 리뷰 SHIP(C/I 0, M1 배선 계약 테스트 반영). **prod 실호출 게이트 합격(2026-07-28)**: 길동 실측 구간에서 우마주(15m)·LG유플러스(4m) 횡단보도 2단계만 주석, 고향국시 횡단보도(193m)·비횡단보도 인접 단계(5m) 무주석, coord 응답 유출 0. iOS 반경 패널 이식은 보류 종결 |
| 장소 검색 (정확도순+좌표 블렌딩) | ✅ prod | **2026-07-20 거리순→정확도순 전환**(스펙 `2026-07-20-accuracy-first-search-design.md`, 위원장 실사용 판정 "정확도순이 더 유용"). 카카오 `x`/`y`만·`sort` 미지정, 서버 병합·클라 재정렬 제거, 거리 표기는 `searchPlaces` 진입점 `annotateDistances` 주석. **로컬 실호출 3종**: 맥도날드(길동)→둔촌DT 928m 1위(근접 블렌딩)·경복궁(길동 15.6km)→본체 1위+경회루·근정전 top5(랜드마크 부상)·백년찌개집(여의도)→네이버 전용 "백년찌개집 1971" 819m 생존·좌표 중복 0. 네이버 보강분은 카카오 뒤 이어붙임(하단 수용 트레이드오프). **2026-07-20 버킷 오분류 fix**: attraction 정규식의 단독 `문화`가 카카오 최상위 "문화,예술" 트리(사진관·즉석사진 등 비명소)까지 관광·명소로 끌어옴(키자니아 검색 실측 — 인생네컷이 첫 항목) → `문화시설`로 좁힘(웹 `category.ts`·Kit `SearchFilters.swift` 미러 동조). CT1 실호출 검증: 공연장·미술관·전시관은 `문화,예술 > 문화시설 > …` 계층이라 attraction 유지, 영화관(`문화,예술 > 영화,영상`)만 other 이동(명소 아님 — 개선). **prod 실호출 대조(키자니아 12건)**: 수정 전후 분류 차이는 인생네컷 1건뿐(attraction→other), 나머지 11건 불변 — 부작용 0. ⚠ 부수 관측: "키자니아 서울" 카카오·네이버 이중 노출(좌표 4자리 dedupe 미적중) → **2026-07-21 dedupe 보강으로 해소**(아래 플랫 리스트 행) |
| 장소 결과 플랫 리스트 + 칩 (버킷 섹션 폐지) | ✅ 구현 (2026-07-21) | **버킷 섹션 그룹핑 폐지 → 정확도순 플랫 리스트 + 칩 필터**(웹 ResultList·PlaceSearch·채팅 MessageBubble + iOS SearchView, 위원장 선택). 근거: 고정 `BUCKET_ORDER` 서열이 정확도 1위를 최하단 '기타'에 매몰(실측 6검색어: 키자니아·신명중학교·롯데타워·키즈카페·강동구청·이룸센터 — 구청 본체가 자기 광장·주차장보다 아래). 원칙: **분류 완전성은 이길 수 없는 싸움 — 순위는 정확도가 전담하고 버킷은 칩 필터 축일 뿐, 'other'는 실패가 아니라 솔직한 잔여 라벨**. 동반 수정 3건: ① `public`(공공·교육) 버킷 신설(카카오 `사회,공공기관`·`교육,학문`+네이버 `사회,복지`·`공공,사회기관` 커버, 5로케일+xcstrings 라벨) ② 오탐 정리 `미술`→`미술관`(가죽공예 공방 회귀)·`카페`→`(?<!키즈)카페`(키즈카페 15건 음식 오탐, ICU·JS 両엔진 lookbehind 검증) ③ mergePlaces dedupe 보강 — 좌표 4자리 일치 OR (50m 이내+정규화 이름 가장자리 일치 ≥5자): 카카오·네이버 20~30m 좌표 오프셋의 이중 노출("키자니아 서울") 해소, primary 내부는 무판정(한 건물 내 별개 장소 보존— 응급의학센터·약국 부활). **실호출 대조(7검색어, 코드 경로)**: 전 검색어 본체 1위 최상단·이중 노출 0·오탐 0. 트레이드오프 수용: 네이버 부속시설명("신명중학교주차장")이 앵커 접두 일치로 흡수될 수 있음(이중 노출 방지의 반대급부), 50m 밖 좌표 오프셋(세븐일레븐 키자니아점 150m)은 미병합. 리뷰 검출 fix: Kit `SearchFiltersTests.swift` drift(swift build만 돌리고 swift test 누락 — 이후 両실행 필수), iOS 장소 섹션 헤더 복원(둘 이상 섹션 공존 시 `search.placeSection`, 웹 미러). **후속(같은 날, 위원장 "아쿠아리움·맥도날드 거리순 안 됨" 실사용 보고)**: ① iOS 검색 좌표 미탑재 결함 — `SearchModel`이 `lastCoordinate` 캐시만 읽어 "내 주변" 미사용 세션은 전국 정확도순(충주아쿠아리움 1위 실측) → `coordinateIfAuthorized()`(권한 허용 세션만 취득, 팝업 절대 없음 — "권한 요청은 내 주변 최초 사용 시" 계약 유지) ② 보강 꼬리(네이버·TourAPI) 거리 정렬 `orderSupplementTail` — 좌표 파라미터 없는 보강 소스가 전국 순서로 꼬리에 붙어 낭독 거리 흐름 파괴(…6.5km→15km 시청점) → 좌표 있을 때 꼬리 내부만 `sortByDistanceFrom`(카카오 축 불변). **"정확도 우선+거리 2차" 질의의 답은 카카오 블렌딩 자체**(길동 앵커 실측: 아쿠아리움=롯데월드 4.3→코엑스 8.0, 맥도날드=둔촌 0.9→…) — 좌표만 실리면 원하는 순서이며 클라 재정렬 금지 원칙 유지. 실호출 확인: 両검색어 꼬리 오름차순·본체 불변. ③ **커버리지 공백 승격 `promoteCoverageGapMatches`**(위원장 "백년찌개집 최하단" 보고): 카카오가 질의명 항목 0건일 때만 질의명 포함 보강 항목을 최상단 승격(정규화 이름 포함, 질의 ≥2자) — 네이버 보강의 존재 이유(카카오 미등록 가게)가 유사명+거리 꼬리 정렬에 밀려 최하단에 깔리는 회귀 수정. 카카오가 질의명을 찾는 일반 검색(맥도날드·경복궁)은 무발동(실호출 확인: 백년찌개집 1971 1위 승격·맥도날드/경복궁 불변). ko·en 병합 공통 적용 |
| 검색창 3섹션 병렬 (장소+주소+웹 0건폴백) | ✅ prod | 2026-06-27 LLM 라우터 폐기 후 결정론 전환. **2026-07-20 명소 4번째 섹션 폐지**(아래 행) — 3섹션 회귀, 칩 필터가 검색 장소 전체 커버 |
| ~~관광지·명소 섹션~~ (2026-07-20 폐지) | 폐지 | 2026-07-01 신설(거리순의 랜드마크 매몰 보완) → **2026-07-20 정확도순 전환으로 존재 이유 소멸, 웹·iOS·CLI/MCP 전 계층 제거**. 라우트 `/api/places/attractions` 삭제(구 CLI는 allSettled graceful, 구 MCP 도구만 404 — 승인된 breaking-soft). 랜드마크 노출은 정확도순 부상+`BUCKET_ORDER` 관광명소 그룹 최상단이 대체. 카카오 명소 판별이 다시 필요하면 category_name 계층 필터(AT4 아님) 참조 |
| en 장소 병합 (카카오+TourAPI, juso 영문주소) | ✅ prod | 2026-06-19 |
| 주소·우편번호 검색 (juso) | ✅ prod | 2026-06-19 `JUSO_CONFM_KEY` 등록+배포 |
| 역 교통약자 편의시설 (코레일 406역) | ✅ | 2026-06-14 |
| 서울 지하철역 교통약자 시설 (9 오퍼레이션) | ✅ | 2026-06-17 강동·신도림·서울역 |
| 전국 도시철도역 메타 (정적 seed 1,098역) | ✅ | 2026-06-17 |
| 서울 지하철 실시간 도착 (역상세 + 내주변) | ✅ prod | 2026-06-17 `SEOUL_SUBWAY_REALTIME_KEY` |
| 시내버스 (TAGO 지방 + 서울 TOPIS 병합) | ✅ prod | 2026-06-24 서울 키 전파 9일째 완료 |
| 따릉이 공공자전거 | ✅ | 2026-06-16 |
| 내 주변 소아 야간·휴일 진료 (NMC) | ✅ prod (2026-06-17 · 단계 0 개선 2026-07-26 · 더 보기 2026-07-27) | **표시 절단 제거(2026-07-26, 커밋 9f22f80·b837780)**: 진입점 `findNightClinicsNow`(라우트·채팅 공용)가 **진료중 우선 정렬**(`prioritizeOpen`: open>unknown>closed, 안정 정렬로 거리순 보존) 후 상위 10 — 거리순으로만 자르면 문 연 곳이 닫힌 곳에 밀려 절단된다(옛 상한 5). 절단은 `total`로 노출(침묵 금지). 공휴일은 `fetchIsHoliday`(특일정보) 재사용해 dutyTime8 판정, 실패 시 요일 폴백+`basis` 라벨. 채팅 `get_night_clinics`는 서버 계산 `openStatus` 동봉(LLM이 HHMM 배열로 "지금 진료중"을 날조하던 경로 차단). ⚠ **잔여 한계는 데이터셋 범위** — 달빛 지정 명부 153건뿐이라 미지정 소아과는 부재(아래 미해결 항목). **더 보기 단계 공개(2026-07-27, 커밋 df7a751..42c7f33, 플랜 `2026-07-27-nearby-show-more-v1-clinics.md`)**: 서버 상한 10→50(`SERVER_CAP`), 웹·iOS 10건 초기 표시+더 보기마다 +10, 포커스 계약=첫 새 항목 이동(웹 `useLayoutEffect` 페인트 전 재포커스 — 마지막 배치 버튼 소멸 이탈 창 제거, iOS `scrollTo` 선행 가시화+`AccessibilityFocusState`), 라벨 수치 없음(불변식 5 유지)·별도 live region 없음. prod 실호출: 길동 50건/total 134, 연세도우리 강동점 35위 포함(마감 시각에도 더 보기로 도달 가능). **V2 웹 완료(2026-07-28)**: 둘러보기·아이 놀 곳·무장애 3종에 더 보기 복제(b00121d 웹 UI·ea18f6b provider 캡 50) + 라우트 옵트인 limit 계약(cb83477, 플랜 `2026-07-28-nearby-show-more-v2-web.md`) — ea18f6b가 라우트 기본 응답까지 50으로 부풀린 회귀(CLI/MCP·iOS 출력 팽창)를 기본 12/8/8 복원+웹만 `limit=50`(1~50 밖 400)+`total` 노출로 교정, prod curl 3라우트 검증. 사후 리뷰 Minor 3건 처분 확정(2026-07-28 검토): V2 total 의미 차이(provider 캡 50 이내라 V1 무제한 카운트와 다름)는 소비 UI 생기면 재검토 유지, V1 키없음 total 부재 비대칭은 로컬 개발 시나리오라 불채택 종결, MAX_LIMIT/FETCH_LIMIT 50 6곳 공용화는 iOS phase 편승. **V2 iOS 완료(2026-07-28)**: 3뷰 더 보기 메커니즘(12e3f29, 2026-07-27 선행)에 Kit fetch `limit=50` 옵트인을 더해 재료 확보(5df5ad6 — `NearbyService.kidsPlaces`·`surroundings`·`BarrierFreeService.nearby`, StubURLProtocol로 대상 3+대조군 clinics 무-limit 단언 4테스트), prod around `limit=50` 50건/total 50 확인·실기기 배포·실행 완료. 편승분 공용화도 완료(a78d20e — `src/lib/nearby-limits.ts` `NEARBY_LIMIT_MAX` 1곳을 라우트 3·컴포넌트 3이 직접 참조, Kit `fetchLimit`은 주석 미러). 실사용 VO 확인은 위원장 몫(패턴은 V1 실기기 합격본 복제라 플랜 판정상 재실측 생략) |
| 이 지역 공기질·날씨 (에어코리아+기상청) | ✅ | 2026-06-20 활용신청 승인+검증 |
| 근처 아이 놀 곳 (카카오 화이트리스트) | ✅ | 2026-06-18. 더 보기 V2(2026-07-27, 캡 50·10+10, 플랜 `2026-07-27-nearby-show-more-v2-three-domains.md`) — 상류가 3키워드×15라 실반환은 ≤45 |
| 내 주변 둘러보기 (카카오 카테고리+8방위) | ✅ | 2026-06-20. 더 보기 V2(2026-07-27, 캡 50·10+10) — where-am-i landmarks 소비자는 산문 캡 6으로 무영향 확인 |
| 현재 위치 정위 카드 (where-am-i) | ✅ prod | 2026-06-28 |
| 자동차 경로 브리핑 (ko Tmap 기본+카카오모빌리티 폴백 / en NCP) | ✅ prod (2026-07-30 ko 기본 Tmap 전환) | 실호출 대조 결과: 카카오 `guidance`는 도로명 없는 조각("우회전 (228m)"·"12시 방향")인 반면 Tmap `description`은 도로명 포함 완성 문장("교차로에서 우회전 후 명일로를 따라 244m 이동"). 위원장 판정으로 ko 기본을 Tmap으로 교체하고 카카오모빌리티는 폴백(Tmap throw 시에만)으로 전환(커밋 9a5601f·66959c3·3b0d158·b209281·207245d). 게이트 `hasCarRouteKey`(tmap∥kakao), guide 수치는 0(미제공 의미론)이라 소비자(웹·iOS·CLI) 3곳이 >0일 때만 수치 병기(0m 중복 낭독 차단), 채팅 `get_car_route`도 동일 서비스 경유(출처 Tmap·카카오모빌리티 병기). **머지 게이트 실호출 재확인(2026-07-30, prod)**: 길동→강남 15,556m·1,681초(28분)·택시 19,700원·통행료 0원, guide 13건 전부 도로명 포함 완성 문장이고 guide `distanceMeters`·`durationSeconds` 전부 0, 총계 4필드 전부 정수 확인. en은 NCP 현행 그대로(무변경) |
| 대중교통 길찾기 (ODsay) | ✅ prod | 2026-07-04 URI(referer) 식별 전환 — 길동→강남 prod 실호출 검증. 아래 해소 기록 |
| 음성 받아쓰기 (Deepgram nova-3 / iOS 온디바이스) | ✅ prod | 2026-06-19 키 401 사고 복구. **2026-07-26 검색어 정규화+단축어 포커스**(위원장 실사용 보고 2건): ① STT가 붙이는 후행 마침표에 주소 검색이 전멸. **juso 실호출 확정**: "강동구 성내로 12" 15건 → "…12." **0건**, "천호대로 1077" 2건 → 0건(마지막 토큰을 건물번호로 파싱하는데 "12."가 유효 번호가 아니라 매칭 붕괴, 주소는 대부분 숫자로 끝나 주소만 유독 전멸). 한글로 끝나면 무해("강동구청." 6건 그대로). 카카오도 "강동구 길동 맛집" 4,663 → 19건(전멸은 아니라 "가끔 되는 것처럼" 보였던 이유). 해법은 엔진이 아니라 소비 지점: iOS 온디바이스 `SpeechTranscriber`엔 문장부호 옵션 자체가 없고(SDK 헤더 확인 결과 `TranscriptionOption`은 `etiquetteReplacements` 하나뿐, 형제 `DictationTranscriber`에만 `punctuation`), Deepgram `punctuate=false`는 같은 STT 라우트를 쓰는 채팅 입력까지 망친다 → `normalizeVoiceQuery`(웹 `src/lib/format.ts` ↔ Kit `VoiceQuery.swift` 미러, 후행 부호·공백만 제거·중간 보존·전부 지워지면 원문 복원) 검색 4개 소비 지점 적용(웹 PlaceSearch·DirectionsView, iOS SearchView·DirectionsEndpointSearchView), 채팅은 미적용(문장부호가 정보). 통지 문자열도 정규화본이라 "들은 것 = 검색된 것" 일치. 테스트 웹 878·Kit 126(미러 동형 7건 대칭). ② 단축어 "음성 검색" 진입 시 VO 커서가 화면 최상단이라 정지하려면 마이크 행까지 스와이프해야 했던 문제 → `.task`에서 `micRowFocused` 선점(`speech.start()` 전 + 준비 대기 중 이탈 대비 완료 후 1회 재확정, `@AccessibilityFocusState` 양방향이라 재대입이 no-op 아님). 동반 계층 정리: "녹음 시작 순간 VO 낭독 차단"을 홀드 버튼 private에서 `SpeechService.start()`로 이관(홀드 없이 시작되는 외부 세션엔 그 차단이 아예 없었다). **2026-07-27 받아쓰기 방식 설정**(위원장 요청): 설정에 "받아쓰기 방식" Picker 추가 — 홀드(기본) / 클래식 탭 토글(73fb4e7 이전 계약 복원: 탭=시작·재탭=정지, 라벨 "받아쓰기 시작"↔"중지" 전환이 상태 신호, 슬라이드 없음). `DictationStyle` enum+AppStorage("dictationStyle"), 버튼 분기는 `HoldDictationButton` 내부, 채팅 전달 정책만 소비자 분기(탭 토글=자동 전송 없음: 초안 병합 확정+보내기 버튼 포커스+원문 통지 — 헌장 §6 받아쓰기 완료 계약, 홀드=릴리스 즉시 전송 유지). 검색·길찾기는 정지=즉시 검색이 両방식 동일이라 무변경. 리뷰 fix 2건: 시작 실패 후 stale startTask가 다음 탭을 no-op으로 소모(phase 동반 판정, 시뮬레이터 재현·해소)·외부 시작 세션 `.requesting` 중 탭은 무반응(홀드 판 동형). 신규 키 4종(ios.voice.start 재추가+settings.dictation* 3종) 5로케일. 잔여: 실기기 VoiceOver 확인(단축어 진입 포커스 착지·녹음 중 무발화, 탭 토글 방식 시작→발화→정지→초안 확정·검색 실행) |
| PWA (수제 서비스워커) | ✅ prod | 2026-06-21 |
| 채팅 (Gemini FC 18도구 + Perplexity 웹) | ✅ prod | 2026-06-21 장소별 진입 재배치, 2026-06-30 무장애 도구 추가(게이트). **2026-07-28 `get_weather` 추가**(위원장 질문 "날씨 API는 채팅에 미연결?"이 발단): weather provider(기상청 단기예보)는 있었으나 소비가 LocalConditions뿐이었고, 옛 지시문 예시 "날씨·공기질"은 존재하지 않는 도구를 가리키며 공기질 과잉 호출만 부추겼다. declaration은 긍정 트리거(공기질과 동형)·게이트 `hasDataGoKrKey`·출처 `source.kma`·**카드 없이 산문 정본**(walk-infra 선례, 시각 카드 정본은 내 주변 LocalConditions). 진행 라벨 `chat.progress.tool.get_weather` 5로케일+iOS `toolLabel`+xcstrings 재생성(715키). 실호출 4종: "날씨 어때"·"비 와?" → get_weather, "산책하기 좋아?" → **weather+air 병렬 접지**(종전 공기질 단독의 반쪽 근거 해소), 둘러보기 → 両미호출(억제 유지). **2026-07-27~28 공기질 과잉 조회 억제**(위원장 실사용 보고: "둘러봐 줘"에 공기질이 응답 서두 점유): systemInstruction의 연쇄 조회 예시("경로 질문이면 날씨·공기질 등")가 선제 곁들임을 유도(날씨 도구는 부재라 예시의 실제 지시 대상은 공기질뿐). 1차 fix는 전역 금지문 강화+declaration 금지문의 이중 방어였으나 위원장 overfix 지적으로 절제 실측 → **최소판 확정: 예시 삭제(전역 문장 원형 복원)+declaration 긍정 트리거("공기질·미세먼지·대기 상태 직접 질문·야외 활동 적합성일 때만")만**. 절제 실호출 6종: 둘러보기 2회·가 볼 만한 곳·강남역 경로 → 공기질 0, "미세먼지 어때"·"산책하기 좋아?" → 호출 유지. 교훈: LLM 행동 수정은 최소판 먼저 실측, 금지문 덧대기는 실패 시에만(메모리 `prompt-edits-minimalism-first`). **2026-07-28 미세먼지→날씨 동반 조회**(위원장 실사용: 미세먼지 질문에 미세먼지만 답함): get_weather 트리거에 "공기질·미세먼지" 추가만으론 3/3 미동반(모델이 직격 도구 하나로 충족) → get_air_quality declaration에 co-call 지시("호출할 때는 날씨가 긴밀히 연관되므로 get_weather도 함께") 추가로 해소. 실호출: 미세먼지 질문 4/4 両도구 병렬+답변 날씨 접지, 둘러보기 2/2 미호출(억제 유지), 날씨 질문에도 공기질 동반(대칭 페어링, 연관 도메인이라 수용). **같은 날 iOS 채팅 전송·완료 효과음 이식**(위원장 보고: iOS에 효과음 없음, 웹만 useChatSound 보유): dodo SoundPlayer(mp3 번들) 이식, ChatModel.send()=단음·appendAssistant()=2음 상승(성패 무관 턴 경계, 웹 동형). soundEnabled 서버 게이트는 비이식(무인증·웹도 토글 없음). ⚠ TTS·받아쓰기 사용 후엔 세션 카테고리 잔존으로 효과음이 무음 스위치를 무시할 수 있음(dodo 동일 상속 한계, 주석 명시). **2026-07-31 `gemini-3.5-flash`→`gemini-3.6-flash` 교체**(위원장 질의 발단, 3.6 Flash는 2026-07-21 GA): 교체 비용은 `client.ts` 모델 문자열 1줄: `@google/genai` 1.52.0에서 스모크 통과(SDK 업그레이드 불요), 코드가 `temperature`/`top_p`/`top_k`를 안 써 7월 릴리스의 샘플링 파라미터 deprecation(에러 없이 조용히 무시)과도 무관. **문서로는 판정 불가**였다: 공식 "출력 토큰 -17%" vs 독립 측정(EvoLink) "medium 기본에선 +1.7%, high에서만 -6.4%", Artificial Analysis 지능 지수는 3.5와 동일. → 18도구+실제 systemInstruction을 물린 실호출 A/B 48회로 판정. **결정 근거는 도구 절제**: "오늘 날씨 어때?"에 공기질 과잉 호출이 3.5는 2/2인데 3.6은 0/2, "안녕하세요"에 도구 호출이 3.5 1/2 vs 3.6 0/2, 소아과 질의에서 3.5는 "전용 도구가 정본" 지시를 어기고 `search_web`을 2회 추가(3라운드 30.3초·유료 Perplexity) vs 3.6은 전용 도구만으로 2라운드 19.4초(**-36%**). thinking 토큰 4,331→3,001(**-30.7%**), 동일 조건 총비용 -16.5%(출력 단가 $9.00→$7.50, 입력 $1.50 동일이라 도구 결과가 누적되는 실제 루프에선 절감률이 이보다 낮다). 불변식 両통과: 장소 특징 날조 금지(카페 "분위기" 요청에 둘 다 정보 부재 명시)·공기질↔날씨 co-call 유지. ⚠ **미확정 관측**: 3.6이 공기질 답변 헤딩에 이모지 사용 1건(4케이스 중 1, 3.5는 0건): SR 낭독 노이즈지만 축약 조건 8회 재현 0이라 성향 미확정. 프롬프트 미니멀리즘 원칙대로 금지문 선제 추가 없이 관측하다 반복 시에만 한 줄(메모리 `prompt-edits-minimalism-first`). ⚠ 단순 1라운드 질의는 3.6이 오히려 1~2초 느리다. 이득 구간은 다중 라운드 연쇄다 |
| 채팅 응답 듣기 TTS (`/api/tts` Chirp 3 HD) | ✅ prod | 2026-07-20 dodo 이식(full 모드만, 무인증이라 IP 레이트리밋 60초 10회로 비용 방어, `GOOGLE_CLOUD_TTS_API_KEY` prod 등록). 실호출: 로컬·prod 両 200+유효 MP3. iOS `TtsPlayer` 싱글턴이 소비. **2026-07-27 정본 역전(위원장 결정)**: 온디바이스 `AVSpeechSynthesizer`(rate 0.55)가 정본(지연·비용 우위), 서버 Chirp는 로케일 보이스 부재 시만 폴백(지원 5개 로케일 전부 내장 보이스라 실사용 거의 미도달 — 웹 PWA는 서버 TTS 유지). 같은 날 재생 중지 크래시 수정: delegate를 재생마다 생성·정지 시 해제하던 구조가 TextToSpeech 지연 콜백 UAF(EXC_BAD_ACCESS) 유발 → 싱글턴 수명 단일 delegate(@MainActor)+발신자 아이덴티티 가드로 전환(시뮬레이터 재현·해소 실측). ⚠ dodo `Core/Audio/TtsPlayer.swift`도 동일 패턴 잠복 — 역이식 필요. 듣기·공유 버튼(iOS 채팅 응답 말미)은 실기기 VO 확인 대기: ①탭 전환·장소 sheet 개폐가 무관한 재생을 죽이지 않는지 ②TTS 재생 중 홀드 마이크 시작 시 선정지 동작. **2026-07-28 듣기 속도 3단 설정 이식**(dodo Round 199 역이식, plan `2026-07-28-listen-speed-setting-port.md`): 설정 "듣기 속도" 픽커(1배 기본/1.5배/2배, `@AppStorage("listenSpeed")` 기기 로컬, 5로케일 +4키). 규칙은 Kit `ListenSpeed` — **⚠ `AVSpeechUtterance.rate`는 시간 배율이 아니라 곱셈 금지, dodo 실측 캘리브레이션 테이블(1→0.55, 1.5→0.65 실달성 1.41배, 2→0.75 실달성 1.92배) 그대로 이식**(테이블임을 고정하는 회귀 테스트 포함). 서버 Chirp MP3 폴백은 `AVAudioPlayer.enableRate`+배율 직대입(진짜 시간 배율이라 축이 다름). 재생 시점 조회라 설정 변경은 다음 재생부터. Kit 테스트 134/134, 시뮬레이터 픽커 표시·영속(1.5 저장 후 재실행 유지) 실측. **실기기 청감 확인 완료(ko 3단 정상 구분, 위원장 2026-07-28)**. 비-ko 로케일은 불만 보고 시에만 로케일별 테이블 검토(YAGNI) |
| 미지원 언어 첫 방문 en 폴백 (proxy Accept-Language 치환) | ✅ prod | 2026-07-03 ja/zh→/en 확인, ko·fr·en·헤더없음(ko)·쿠키 우선순위 기존대로 — 커밋 3abf158 |
| 무장애 여행 정보 (한국관광공사 KorWithService2) | ✅ prod | 2026-06-30 활용신청 자동승인+실호출 검증 완료(서울도서관 130183·덕수궁 1605981). nearby·장소상세 region·채팅 도구 3계층. 라벨 실키 교정(brailepromotion 등)·값 HTML/접미 정제·매칭 50m∩이름. 더 보기 V2(2026-07-27, 캡 50=numOfRows·10+10, `matchBarrierFreePlace` limit 10 불변) |
| ko 장소 병합 (카카오+네이버 지역검색) | ✅ prod | 2026-07-18 커밋 f3c0032. 발단: 여의도 "백년찌개" 검색 미노출 — 카카오 로컬 DB에 어떤 상호 변형으로도 미등록(커버리지 공백), 네이버에만 "백년찌개집 1971" 등재. 両키 시 `searchPlacesMergedKo` 병합(allSettled 부분실패 보존, 좌표 4자리 dedupe, 좌표 시 Haversine 재정렬 — 네이버는 거리 정렬·좌표 필터 없어 전국 정확도순). **prod 실호출 검증**: 백년찌개(여의도 좌표)→両도메인 1위 백년찌개집 1971 609m·merged 10건, 회귀 맥도날드(길동)→1위 천호로데오점 837m·merged 20건. 네이버 지역검색 일 25,000회·최대 5건 |
| 유휴 복귀 초기화 + iOS 제목 탭 리셋 | 구현 완료(2026-07-18, 실기기 검증 대기) | 웹: `IdleReset` 컴포넌트 — localStorage 마지막 사용 시각, 10분 초과 복귀 시 쿼리 없는 로케일 홈으로 하드 리로드. 첫 마운트 리셋은 standalone(설치형 PWA)+`?q=` 복원일 때만(리뷰 검출: 재방문자 브라우저 탭 공유 링크 보존). iOS: `sessionEpoch`+`.id`로 TabView 재생성 — scenePhase `.background` 10분 초과 복귀 + 내비 바 principal "길동무" 버튼(웹 제목 하드 링크 동형, ⚠ 2026-07-19 길동무 메뉴로 대체 — 전체 리셋 경로는 유휴 복귀·단축어만 잔존). 검증 시나리오: 백그라운드 10분+ 복귀→초기 화면 / 10분 미만→상태 유지 / 브라우저 탭 공유 링크→검색 유지 / iOS 제목 탭→즉시 초기 화면. 스펙 `docs/superpowers/specs/2026-07-18-idle-reset-title-refresh-design.md` |
| iOS 단축어(App Intents) 진입 — 음성 검색·내 주변 | 구현 완료(2026-07-18, 실기기 검증 대기) | `AppShortcuts.swift`: `StartVoiceSearchIntent`(앱 열고 초기 화면 리셋 후 즉시 마이크)·`OpenNearbyIntent`(내 주변 탭)+`AppShortcutsProvider` 한국어 Siri 문구("길동무 음성 검색" 등). 라우팅은 `LaunchActionStore` 싱글턴 2단 소비(App=pending 소거·리셋·탭 전환, SearchView=`voiceStartRequested` 소비·`speech.start()`). 리뷰 검출 2건 반영: 잔존 플래그 액션 무관 소거(유령 마이크 차단)·인텐트 소비 시 `backgroundedAt=nil`(유휴 리셋과의 탭 경합 결정화). 검증 시나리오: 단축어 앱 두 액션·Siri 문구·콜드/웜 진입·10분+ 백그라운드 후 "내 주변" 단축어 재진입(탭이 내 주변인지). 스펙 `docs/superpowers/specs/2026-07-18-ios-app-intents-shortcuts-design.md` |
| iOS 채팅 탭 (일반 채팅, 첫 탭) | 구현+prod 검증+시뮬레이터 E2E 실측 완료(2026-07-18), 기기 설치·실행 완료 — VoiceOver 실사용 검증만 잔여 | 3탭 전환: 채팅·검색·내 주변. 기존 장소 채팅 sheet 자산 일반화 — `ChatModel` place 옵션화(placeContext 키 생략=웹 byte-identical 계약, 보호 테스트 기존 `chatRequestBodyOmitsNilOptionals`), 대화 UI `ChatConversationView` 공용 추출(sheet·탭), `ChatTabView` 빈 화면 고정 추천 질문 4개. 일반 채팅만 전송 직전 `currentCoordinate()` 1회(최초만 권한 팝업, 실패 허용). **prod 실호출 검증**: placeContext 없는 `/api/chat` → `get_surroundings`가 userLocation 앵커로 동작·status/done NDJSON 정상(길동 좌표). 실측 검출 1건: 답변의 블록 마크다운(### 헤딩·`*` 리스트)이 인라인 전용 파서에서 리터럴 노출+VoiceOver 기호 낭독 → `flattenBlockMarkdown`(Kit, 헤딩 마커 제거·리스트→`•`, 테스트 2건) 렌더 직전 적용(커밋 e6346f6). 리뷰 검출 반영 2건: ① 세션 리셋 시 스트림 유령 생존(강한 self 캡처) → chatModel App 소유+`resetChatModel()` 요청째 폐기 ② a11y 감사 — 44pt 히트 영역 label 안쪽 이동(+contentShape)·마이크 in-flight 가드·전송 중 라벨 신호("전송 중")·잉여 combine 제거. **시뮬레이터 E2E 실측(2026-07-18, AX 자동화)**: ① 첫 탭=채팅·추천 질문 4개·3탭 구성 확인 ② "지금 미세먼지 어때?" 탭→위치(길동 고정)가 도구 전달→**천호대로 측정소** 답변(등급 단어 좋음, `•` 변환·볼드 정상·`###` 리터럴 없음 화면 확인) ③ 추천 질문 4개·음성 입력·보내기가 각각 독립 AX 버튼 객체로 노출 ④ 검색 탭 "강동역"→카테고리 그룹 결과→장소 상세→"이 장소에 관해 물어보기" sheet 회귀 없음(장소 앵커 답변+PlaceRow 카드+웹 결과 Link 카드+출처 2건). **VoiceOver 실기기 실측 결함(위원장 2026-07-18)**: 산문 단일 Text → 질문·답변이 통짜 객체 하나로 낭독, 구조 탐색 불가 → `parseChatMarkdownBlocks` 블록 분할(헤딩 `.isHeader`·리스트 `•`·단락)로 블록당 별도 AX 객체화, 시뮬레이터 AX 덤프로 5객체 분리 확인(커밋 8f54614, ⚠ dodo 채팅 동일 결함 — 이식 시 적용). VoiceOver 1차 확인(위원장): 헤딩·불릿 블록 분리 정상 판정 + 추가 실측 2건 반영(커밋 a0671be) — 질문 말풍선 `.isHeader`(턴 단위 로터 점프)·마이크 라벨 "음성 입력"→"받아쓰기 시작/중지"(받아쓴 내용 속 "음성"과 라벨 혼동 해소, SearchView 동일). 완료 포커스는 마지막 질문 헤딩으로 변경(dodo 동형, 위원장 지시 2026-07-19, 커밋 de5427a) + 생성 중 최상단 이탈 차단(전송 즉시 질문 헤딩 선점, `questionRevision`, 커밋 266fcf4). **포커스 계약 개정 역이식(2026-07-19, dodo R184 실기기 판정)**: 구계약(전송 즉시 질문 헤딩 선점+완료 재확정)은 결함 3종(같은 값 재대입 no-op·ScrollView AX 컬링 침묵 실패·append 스크롤 경합)으로 폐기 → 전송 시 보내기 버튼 유지(`isSendFocused` 선점)·완료 시에만 질문 상단 scrollTo 가시화→400ms 후 포커스→nil 리셋 감지 시 1회 재시도, VO 실행 중 완료 스크롤 목적지 질문 상단 단일화. DEBUG 계측 `ChatFocusDiag.swift`(dodo LiveDiagFileLog 이식, VO 포커스 관찰자+단계 로그→기기 파일). 시뮬레이터 AX 브리지 구조 회귀 없음 확인(질문 AXHeading·답변 블록 5객체·에어코리아 실호출). **받아쓰기 완료 패턴 이식(2026-07-19, 헌장 §6 신설·dodo R184 후속 실기기 합격)**: 채팅 전사 성공 시 보내기 버튼 포커스 이동+원문 polite 통지, 검색은 자동 검색+settled 포커스라 원문 통지만. `SpeechService` 자동 정지 경로 부재 확인(반환값 소비 유지, dodo 콜백 전환 비해당). 잔여: VoiceOver 최종 판정(위원장 실기기). 스펙 `docs/superpowers/specs/2026-07-18-ios-chat-tab-design.md` §5 |
| iOS 길동무 제목 메뉴(새로고침·설정)+테마 | 구현+리뷰+실기기 설치·실행 완료(2026-07-19), VoiceOver 실사용 검증 잔여 | 검색 탭 전용 principal "길동무" 버튼(탭=전체 리셋+채팅 복귀)을 세 탭 공통 `Menu`(새로고침·설정)로 대체 — `TitleMenu.swift` `.gildongmuTitleMenu()`, environment `\.refreshTab`. 새로고침은 탭별 epoch(`chatEpoch`·`searchEpoch`·`nearbyEpoch`) `.id` 재생성으로 **현재 탭만 초기화·탭 이동 없음**(채팅은 `resetChatModel()`로 스트림 요청째 취소). 설정 시트 `SettingsView`: 테마 3택(시스템/라이트/다크) `@AppStorage("themePreference")`→루트 `.preferredColorScheme`(저시력 배려). 언어 설정은 후속 마일스톤(dodo `messages-to-xcstrings` 파이프라인 이식, 스펙 "제외 범위" 참조). 채팅 예시 프롬프트 문구 교정("둘러봐 줘"). 리뷰 검출 1건 반영: 검색 탭 마이크 teardown 누락 — `onDisappear`에서 `speech.cancel()`(ChatConversationView 동형, 새로고침 재생성 시 오디오 세션 고아 방지). VoiceOver 확인 포인트: 메뉴 "길동무, 팝업 버튼" 낭독·새로고침 후 탭 유지·테마 즉시 반영. 스펙 `docs/superpowers/specs/2026-07-19-ios-title-menu-refresh-theme-design.md` |
| iOS 다국어(5개 언어)+설정 언어 픽커 | 구현+리뷰(fix 3건)+Kit 테스트 87 green+실기기 설치 완료(2026-07-19), 실기기 언어 전환·VoiceOver 검증 잔여 | 웹 `messages/{locale}.json`(5 로케일 438키) 정본에서 dodo 이식 결정론 스크립트(`ios/scripts/messages-to-xcstrings.mjs`, flat 로더·타깃명 인자만·카탈로그 2개 출력)로 앱 타깃(588키)·Kit(230키) String Catalog 생성. iOS 고유 문구·웹 카피 상이는 `ios/i18n/ios-extra`·`kit-extra` 오버라이드(**ko 표시 문자열 전후 동일 불변식**). 키 존재 린터 `check-xcstrings-keys.mjs`(앱 316·Kit 47 참조 전수 대조)가 머지 게이트 — 누락 키는 런타임 무증상 노출이라 빌드가 못 잡음. Kit는 Apple lproj 파이프라인 대신 **카탈로그 JSON 직접 파싱**(`kitLocalized(key, lang:)`, `.copy` 리소스) — SPM `swift test`가 xcstrings를 컴파일하지 않아 lproj 조회가 깨지는 문제의 단일 경로 해법, 기존 한글 단정 테스트 87개가 ko 조회로 그대로 green(=불변식 검증). 언어 픽커는 설정 시트(자국어 표기 5종, dodo 동형 `AppleLanguages` 오버라이드, 다음 실행 적용 polite 통지). 배선: 검색 `lang`=dataLocale(ko 외 en, 웹 동형)·채팅 `body.locale`·STT `SpeechTranscriber` locale 매핑(ko-KR·en-US·es-ES·fr-FR·it-IT, 미지원 기기는 기존 `localeUnsupported`)·Kit lang 호출부=`AppLanguage.current`(Bundle 해석 기반 단일 정본). 권한 문구 `InfoPlist.xcstrings` 수기 5로케일. 리뷰 검출 3건 반영(61bffe4): ① 포맷 키 리터럴 `%`를 스크립트가 `%%` 자동 이스케이프(`String(format:)`이 조용히 삼켜 "습도 55%"→"습도 55" 회귀, 실측) ② 공기질 측정소 줄(유일 누락)을 `ios.nearby.airStationLine` 키화(웹 `airQuality.station`은 가운뎃점이라 헌장 위반, 쉼표 신설) ③ 언어 픽커 초기값 지원 언어 필터(미지원 시스템 언어 기기에서 무선택 상태 방지). **즉시 전환 fix(2026-07-19, 커밋 20e7911, 위원장 실기기 보고 발단)**: "언어를 바꿔도 앱을 완전히 닫았다 열어야 반영, 앱 내 새로고침으로는 그대로". 실측 확정 — **Bundle은 언어 협상을 프로세스 시작 시 1회만 하고 캐싱**(같은 프로세스에서 `AppleLanguages`를 fr로 바꿔도 `preferredLocalizations`는 en 유지)이라 `String(localized:)`도 그 위의 `AppLanguage.current`도 옛 언어를 반환 → 뷰 재생성해도 값이 같았다. 해법은 Kit의 명시 언어 조회 계약을 앱 타깃에도 도입: `appLocalized()`가 언어별 `.lproj` 하위 번들 직접 조회(앱 타깃은 xcstrings→lproj 컴파일되므로 번들 조회, Kit은 SPM이라 JSON 파싱 — 목적 동일). 호출부 310건 치환+린터 정규식 동조, `AppLanguage`는 자체 키 `appLanguage`가 정본(`AppleLanguages`는 권한 다이얼로그·단축어·다음 실행 Bundle 해석용 병기). 재렌더는 언어를 루트 `.id`에 편입(SwiftUI는 UserDefaults 변경을 모름, 탭 내용 초기화는 로케일 의존 데이터라 의도), 설정 시트는 App 레벨로 승격해 전환 중에도 닫히지 않게(`\.openSettings` environment, SR 맥락 유지). `ChatTabView` 추천 질문 `static let`→computed(저장 프로퍼티는 최초 1회 초기화라 언어 고정 회귀). 재시작 안내 통지는 폐기하고 "언어를 바꿨습니다"(바뀐 언어로 낭독돼 전환 자체가 신호). 실측 검증: 동일 프로세스에서 ko/en/es/fr/it 5개 즉시 전환 확인. 제외: Siri 구문(AppShortcuts phrase, 별도 카탈로그 체계)·명소 en 데이터. 실기기 확인 포인트: 설정에서 English 선택 즉시 화면 전환(재시작 불필요)·설정 시트 유지, ko 복귀 후 문구 동일, 언어 픽커 VO 낭독·전환 통지, en 상태에서 채팅 영어 답변·STT en-US. 스펙 `docs/superpowers/specs/2026-07-19-ios-i18n-design.md` |
| 도보 길찾기(Tmap)+길찾기 뷰·iOS 4탭 | ✅ 웹·iOS 배포+실기기 QA 통과(2026-07-22, 위원장 "의도했던 기능이 잘 구현") | 스펙 `docs/superpowers/specs/2026-07-21-walk-route-directions-tab-design.md`(§10-A 보강 계약 14조)+plan 웹 W1~W7. 커밋 dd1c0f5..061154c(11개). provider `tmap-pedestrian`(3102=경로없음 graceful, 실호출 관측)+`/api/route/walk`(404/429/502, 레이트리밋 공용 모듈)+`DirectionsView`(원자 필드·`?dir=` cur 토큰 프라이버시·세대 ref+15초 abort·첫 성공 heading 포커스·단일 polite)+채팅 `get_walk_route`(steps 20 캡)+CLI/MCP `route-walk`. **실호출 게이트**: 길동→강동역 25 steps 2,078m·서울→제주 3102→null·채팅 도보 답변·홈 진입·canShowWalk 게이트. 최종 리뷰(opus) 머지 가능 C/I 0. i18n 키는 `route.pedestrian.*`(route.walk는 딥링크 모드 라벨 리프 — 충돌 회피). **iOS phase(2026-07-22)**: Kit 도보 모델(`RouteService.walk`, `{result:null}`=nil)+4탭 개조(`AppTab` String enum·`directionsEpoch`)+`DirectionsTabView`(3수단 async let 병렬·15초 타임아웃·ko 전용 도보·단일 Announcement·첫 성공 heading 포커스)+프리필 진입(장소 상세 버튼·검색 행 VO 커스텀 액션·`directionsPrefill` 1회 소비+리셋 3경로 소거). opus 리뷰가 3-state 오분류 2건(transit `{result:null}` 디코딩 throw→"조회 실패" 오표시 복원 — **장소 상세 기존 브리핑도 개선**·503 게이트)+경합 2건(필드 변경 미취소·cancel isInFlight 고착) 검출·해소. Kit 테스트 104, 시뮬레이터 AX 스모크 4/4 PASS(4탭·필드 독립 객체·수단 AXHeading·step 행 분리·기존 브리핑 회귀 0, 1회성 336ms 실패 관측 비재현). 실기기 설치 완료. CLI/MCP 0.3.0 태그 발행(`route walk`). **실기기 QA 피드백 라운드(2026-07-22, 5커밋+후속 1)**: ①수단 순서 대중교통→자동차→도보(위원장 결정: 도보=분량 최대·빈도 최소, 스펙 §10-A 12 정정) ②현위치 구체 주소 병기+`/api/geocode/reverse` 신설 — **도로명 3단 체인**(카카오 road→NCP 최근접 도로명→지번, road null 실좌표 폴백 prod 실증 "천호대로203길 26-2") ③"현재 위치 사용"=force 재측위+주소 갱신 ④출발·도착 검색 받아쓰기(iOS 홀드 §6·웹 VoiceRecordButton 재사용). 통합 리뷰(opus) SHIP. 잔여 미세 확인: 도로명 수정 후 현위치 낭독·시트 홀드 받아쓰기(위원장). 이월 백로그(2026-07-22 v0.4.0에서 coordSchema 공용화·formatRouteWalk 죽은 분기 해소): 현위치 라벨 리터럴 질의(선재)·시뮬 1회 비재현 조회 실패 관찰 |
| 역 상세 보강 (첫차·막차 시간표+시설 패널 보강+역명 매칭 수정) | ✅ 웹 prod 검증+iOS 미러 실기기 설치(2026-07-22), VoiceOver QA 잔여 | 스펙 `docs/superpowers/specs/2026-07-22-station-detail-enrichment-design.md`(codex 적대적 리뷰 17건 반영, §7 처리 기록)+plan 9태스크. ① **선재 결함 수정**: 카카오 역명("강동역 5호선")이 정규화 미매칭이라 카카오 검색 진입 역 상세의 역 섹션 4종(메타·실시간 도착·시설 2종) 전부 死 상태였음 — `normalizeStationName` 확장(괄호 부가명·후행 노선 토큰 제거)+`parseStationQuery` lineHint(동명이역 양평 5호선 vs 경의중앙선 분리)로 부활, 실서버 재현·검증 ② **첫차·막차 자동 섹션**(전국): TAGO SubwayInfo(15098554) provider `tago-subway`+`/api/station/timetable`(레이트리밋 60초 10회)+`StationTimetable` 컴포넌트(region·기준 라벨·익일 표기·실패 문장, 5로케일). 함정 확정: depTime HHMMSS·00시대 심야가 배열 앞(+24h 서비스데이 보정, 요일 타입도 KST-3h 경계)·당역종착 제외·keyword 포함검색 정확매칭·item 1건 객체. 공휴일 보정은 특일정보(15012690) 게이트형 — **활용신청 승인·키 전파 완료(2026-07-22, 신청 후 ~7분 전파, 광복절·대체공휴일 실응답 확인)**, 조회 실패 시 요일 폴백+기준 라벨 ③ **시설 패널 보강**: 음성유도기 정적 seed(OA-22526 CSV cp949 5,551건 211역, `build-voice-guides.py`)+엘리베이터 위치 폴백(OA-21212, wksn 엘베 부재 시만, 최근접 seed 좌표 기준 방위·거리, 9호선·우이신설 커버)+`supplementFailed`(보강 실패 은폐 금지, groups 전멸 시에도 보존). **실호출 게이트 6/6 PASS**: 강동 5호선(첫차 05:32·막차 익일 00:42)·서면(부산 1·2호선)·양평 lineHint(경의중앙 미혼입)·강동 voiceGuide 24·봉은사 elevatorLocation 2·meta 비-null. 리뷰 검출 fix: iOS `metroKindLabel` 신규 kind 라벨(웹 push 전 xcstrings 재생성 동반), 컴포넌트 catch 미커버 위장(a11y 감사), supplementFailed 전멸 유실(최종 리뷰). 테스트 812 green. 잔여: 위원장 실기기 VoiceOver QA·CLI/MCP 카탈로그 이월 |
| 커스텀 도메인 `gildongmu.dodoplanet.space` | ✅ prod | 2026-07-18 gildongmu 프로젝트에 추가(`vercel domains add`). dodoplanet.space DNS는 Cloudflare 관리인데 기존 와일드카드 CNAME(DNS-only)이 있어 레코드 추가 없이 즉시 해석·인증서 발급. 실호출 검증: 페이지(길동무 타이틀)·places(강남역 15건)·ODsay transit(국회의사당→강남 36분). **API 재신청 불필요** — 전 키가 서버 전용이고 브라우저→외부 API 직호출 0, 유일한 도메인 식별형 ODsay는 서버가 등록 Referer(gildongmu.vercel.app)를 코드로 명시 전송하므로 접속 도메인 무관 |

## 프로덕션 env 등록 현황

`vercel env ls production`으로 확인. 등록됨: `KAKAO_REST_API_KEY`, `TOUR_API_KEY`/`DATA_GO_KR_API_KEY`(동일값), `NCP_MAPS_CLIENT_ID/SECRET`, `DEEPGRAM_API_KEY`, `SEOUL_OPEN_DATA_KEY`, `SEOUL_SUBWAY_REALTIME_KEY`, `JUSO_CONFM_KEY`, `GEMINI_API_KEY`, `PERPLEXITY_API_KEY`, `ODSAY_API_KEY`(2026-07-04, URI 앱 키 — URL 인코딩 형태로 저장), `NAVER_LOCAL_CLIENT_ID/SECRET`(2026-07-18, 네이버 개발자센터 gildongmu 앱 — 사용자 수동 발급, Claude in Chrome이 naver 도메인 차단).

⚠ **env 변경 후 반드시 재배포** — 키는 배포 시점에 함수로 주입된다(`vercel deploy --prod --yes` 또는 push). 키만 추가하고 재배포 안 하면 기존 함수는 옛 env를 본다.

⚠ **prod 키 검증은 `vercel env pull` 길이가 아니라 실호출로** — pull은 Encrypted 값을 복호화하지 않아 정상 키도 빈값으로 내려온다([[vercel-prod-env-pull-redacts-encrypted]]).

## 미해결·보류

> **열려 있는 백로그 정본은 `docs/BACKLOG.md`**(2026-08-01 분리). 아래는 그 항목들이 어떻게 처리됐는지의 **이력 로그**다 — 대부분 종결됐고, 새 항목을 여기 추가하지 말고 BACKLOG.md에 둔다.

- **Google Maps provider 후보 실호출 평가(2026-08-01, ✅ 평가 종결 · 도입 없음)**: 발단은 `@google/genai` 2.15.0 릴리스 노트의 "Support GoogleMaps Tool grounding_types **places and routing**". 두 접근을 분리해 판정했고 **둘 다 기본 교체 후보에서 탈락**, 단 후속 가치 있는 축 2개와 **현행 결함 1건**을 확정했다. 대조 구간은 카카오 도보 전환 때 쓴 4구간 동좌표 재사용(강동역→길동생태공원·서울역→서울로7017·노량진역→수산시장·광나루한강공원→천호역), 검증 스크립트는 scratchpad 일회성(repo 미포함).
  - **(a) Gemini SDK Maps grounding — 길동무 인증 모드에서 사용 불가 확정**: `groundingTypes.places`·`routing` 명시 시 **400 실패**, 메시지가 정확히 `groundingTypes parameter is only supported in Gemini Enterprise Agent Platform mode, not in Gemini Developer API mode`. 길동무는 `generativelanguage.googleapis.com`만 허용하는 API 제한 키(`gildongmu-prod`)라 **릴리스 노트 항목은 우리 경로에 오지 않는다**. SDK 타입 주석도 동일 진술("This data type is not supported in Gemini API")이나 판정 근거는 실호출. 기본 `googleMaps:{}`는 동작하고 `groundingChunks[].maps`(cid URI·장소 설명)로 **실제 장소 접지**가 확인된다(도구 미지정 대조군은 chunk 0건). **그러나 경로 질문에 채택 불가**: 접지 청크가 장소 3건뿐이고 경로 데이터가 0건인 상태에서 모델이 "광나루한강공원→천호역, **네, 휠체어나 유모차로 계단 없이 이동이 가능합니다**, 전 구간 평지 보도·경사로·엘리베이터 구비"라고 **단정**했다. 같은 구간 카카오 ACCESSIBLE 실측은 `ROUTE_RESULT_NOT_FOUND`(무계단 경로 부재)다. 서울역→서울로7017 도보 거리도 "약 50~200m"로 답해 실측 765m와 어긋난다. 접지 도구 부착이 사실성을 보장하지 않는 전형이며 [[agentic-llm-fabricates-unstated-fields]]가 경고한 지점과 정확히 일치 — 시각장애·휠체어 사용자가 검증 불가능한 단정이라 **도입 시 위험이 이득을 초과**. 비용은 Vertex 기준 $14/1,000 프롬프트(Gemini 3), Developer API 기본 도구의 과금 여부는 **미확정**(요금표에 별도 항목 없음, 실계량 미실시).
  - **(b) 직접 호출 — Routes API: 한국 도보·자동차 부재가 결정타**: 4구간 전부 `travelMode` WALK·DRIVE가 **HTTP 200 + 빈 `routes`**(ko·en 무관, 오류가 아니라 조용한 부재). 같은 키·같은 요청 형식으로 **도쿄역→황궁은 WALK 14스텝·DRIVE 12스텝 전부 완성 문장 정상**이라 키·referrer 문제가 아닌 **한국 지역 제약으로 확정**(지도 데이터 국외반출 규제로 추정, 근거는 응답 대조). ⚠ **사전 가설 "en 도보 공백을 Google이 메운다"는 이로써 기각** — en도 동일하게 빈 응답이라 메울 대상 자체가 없다. 계단 회피는 `routeModifiers.avoidStairs`가 400 `Unknown name`으로 **미지원 확정**(유효 필드는 avoidTolls·avoidHighways·avoidFerries 계열뿐). 판정축 1(완성 문장)·2(접근성)는 한국에서 **평가 자체가 불성립**.
  - **(b) 직접 호출 — Routes API TRANSIT은 유일하게 동작하며 축이 갈린다**: **우위** ① **시간대 정확 반영**(동일 구간을 `departureTime` 주간 14시=370·342·**5호선** 추천, 심야 03:30=N30·N31 심야버스 추천) ② **다음 차 출발 시각 제공**("N30@오전 4:11") ③ 대안 6건(ODsay 3건) ④ **정류소·역명 en 영문화**("Gangdong Station"·"Sungnyemun Gate"·"Seoul Station 5 Platform") ⑤ **700m 이내 근거리도 응답**(ODsay는 `-98 출, 도착지가 700m이내입니다`로 경로 자체를 거부) ⑥ 응답 66~252ms. **열위** ① **도보 구간 안내 문장 전무**(거리만 있고 `navigationInstruction` 빈 객체 — 낭독 정본 계약 불충족) ② **요금 `transitFare:{}` 빈 값**(ODsay는 1,500원 제공) ③ 안내 문장이 "버스 강동공영차고지행" 수준이라 **노선번호가 문장에 없다**(`transitDetails.transitLine.nameShort`에 `N30`·`342`로 존재하므로 재조합하면 되나, 그 순간 "provider 완성 문장" 계약을 벗어난다).
  - **(b) 직접 호출 — Places API: ko 열위 · en 우위로 뚜렷이 갈림**: ko는 **카카오 우위** — Google이 **지번 주소**("성내동 244-3"·"길동 산7-3")를 주는 반면 카카오는 도로명("성내로 19")을 주고, "길동생태공원" 질의에 카카오만 탐방객안내소·**개방화장실** 등 부속 시설을 반환한다(접근성 직결). ⚠ 더 나쁜 축: "백년찌개집 1971"(카카오 미등록 보강 실측 사례)에 카카오는 **0건**인데 Google은 백채김치찌개 방배점·국민떡볶이 등 **무관한 결과를 조용히 반환** — 0건이라는 정직한 실패보다 위험하다. en은 **Google 압승** — "Gyeongbokgung Palace"에 영문 장소명+영문 도로명("161 Sajik-ro, Jongno District")을 주고, "pharmacy near Gangdong Station"은 **카카오 0건 대 Google 4건**이다.
  - **판정 요약**: 도보·자동차는 **기능 부재로 후보 탈락**(en 포함). 대중교통·장소는 **기본 교체 비권장**(도보 문장·요금·ko 주소 열위), 다만 **en 장소 검색·영문 주소**와 **대중교통 시간대 인지**는 실측으로 확인된 진짜 우위라 보완 후보로 남긴다. 도입은 하드 스톱(비용) 사안이며 이 평가는 **승인 요청이 아니라 축 판정**이다. 무료 한도는 Routes·Places 공히 Essentials 10,000회/월·Pro 5,000회/월이라 평가·소규모 보완은 한도 내 가능(이번 평가 총 호출 100건 미만, dodo 키 경유).
  - **⚠ 부수 발견 → 현행 결함 확정(2026-08-01 심야 실측) — ODsay가 출발 시각을 반영하지 않는다**: ODsay는 출발 시각 파라미터가 없어 심야·주간 결과가 동일하다. 03:58 실호출에서 **2구간 6개 대안 전부가 운행 시간 밖**으로 판정됐다(강동역→길동생태공원 342 `04:00~22:30`·370 `04:00~22:50`·30-3 `04:15~23:40`, 광나루→천호역 4318 `04:00~22:30`·340 `04:30~23:10`·3411 `04:30~23:10`). 같은 시각 Google은 심야버스 N30 `23:10~03:50`을 "[막차] 16분후"로 정확히 반환했고, TOPIS 실시간 도착도 342·370을 **"출발대기"**(차고지 대기 = 첫차 전)로 일치 확인했다. 2026-07-30 대조의 "01시 운행 종료 지하철 1순위는 両사 계획용 시각표 모드(실결함 아님)" 판정은 **지하철 축에 한정**되며, 버스 축은 실결함이다. ⚠ **앱 내부 모순이 핵심**: 길찾기 브리핑은 "342, 22분"이라 하고 같은 앱 도착정보 화면은 같은 노선을 "출발대기"로 낭독한다(`arrmsg1` 정본 계약 덕에 도착 화면은 정확 — 결함은 ODsay 브리핑에 국한). 시각장애 사용자는 정류장에서 오지 않는 버스를 기다려도 원인을 알 수 없다.
  - **결함 해결책 실증(Google 도입 불요)**: ODsay 응답 자체엔 첫차·막차가 없으나(`lane[0]`=busNo·type·busID·busLocalBlID·busCityCode·busProviderCode, `info`에도 부재), **`lane[0].busLocalBlID`가 TOPIS `busRouteId`와 동일 값**이다(342=`124000038` 실측 일치). ID 직결 조인이라 이름 매칭 술어가 불필요하고([[real-call-gate-weak-predicate]] 회피), `busRouteInfo/getRouteInfo`의 `firstBusTm`/`lastBusTm`로 운행 여부를 판정할 수 있다 — 위 6개 대안 전부 **조인 실패 0건**. 기존 `DATA_GO_KR_API_KEY` 재사용이라 **추가 비용·의존성 0**. ⚠ 설계 시 확인 필요: ① 막차가 첫차보다 이른 심야 노선은 자정 넘김 처리 필수(실측 판정식에 반영) ② TOPIS는 서울 전용이라 지방은 TAGO 경유 분기 필요(`lane[0].busCityCode`로 판별, 서울=1000) ③ 지하철 구간은 별도 축(기존 `/api/station/timetable` 보유) ④ 표기는 [[honesty-disclosures-belong-to-api-layer]]에 따라 "사용자 행동을 바꾸는가"로 심사 — 운행 시간 밖 경로는 **경고보다 순위 강등·제외**가 행동을 바꾸는 처리.
- **ODsay 심야 결함 수정(2026-08-01, ✅ 구현·실호출 검증 완료)**: 스펙 `docs/superpowers/specs/2026-08-01-odsay-service-hours-design.md`, 플랜 `docs/superpowers/plans/2026-08-01-odsay-service-hours.md`(8태스크). 위원장 승인 후 자율 완주. **보강 지점은 provider 진입점 `getTransitRoute`** — 채팅이 라우트를 거치지 않고 이 함수를 직접 호출하므로(`chat/router.ts:253`) 라우트 계층에 두면 채팅만 결함이 남는다(카카오 도보 단일 진입점 패턴 동형, 소비자 6곳 자동 커버). 신규 `src/lib/service-hours.ts`(순수 판정, 시각 주입이라 심야 재현 불요)+`src/lib/providers/bus-service-hours.ts`(조회). 정렬은 `running`·`unknown`·`outside` 안정 정렬(`prioritizeOpen` 동형), **`unknown`을 `outside`보다 위**에 둬 조회 실패를 결함으로 단정하지 않는다. 낭독은 `outside`만 표기(웹 6개 언어·CLI·iOS 미러). **설계 2건이 실측으로 뒤집혔다**: ① 착수 전 가정한 "TOPIS 12자리"가 실제로는 **14자리**(`20260801040000`)라 fixture를 실응답으로 교체 ② 스펙의 "서울=TOPIS·지방=TAGO" 이분법이 통합 검증에서 깨졌다 — 하남 30-3(`busCityCode` 1070)이 unknown으로 새는 것을 잡았고, **TOPIS가 수도권 광역 노선도 보유**함을 확인해(`"30-3하남"` 04:15~23:40) 분기를 도시 코드가 아니라 **TOPIS 보유 여부**로 바꿨다(미보유만 TAGO). 수도권 통합 환승 탓에 서울 경로에 경기 노선이 흔해 이 차이가 커버리지를 크게 가른다. 지방 TAGO는 `routeid.endsWith(busLocalBlID)` 대조인데 **지역마다 blID 형식이 다르다**(부산 `5200141000`→`BSB…`, 인천 `165000313`→`ICB…`, **대구는 `DGB3000323101`로 이미 접두사 포함**) — 접두사 조립이었으면 대구에서 `DGBDGB…`로 깨진다. 숫자 꼬리 우연 일치를 막으려 남는 접두사가 알파벳뿐인지 함께 검사. 매칭은 3개 지역 실호출로 확인된 것만 매핑에 넣고 나머지는 unknown. **실호출 검증(04:56)**: 342 `running 04:00~22:30`·370 `running 04:00~22:50`·30-3 `running 04:15~23:40`(수정 전 unknown). 게이트: 웹 1285 테스트·lint 0 error·build·Kit 전체·iOS 실기기 배포 완료. ⚠ **알려진 한계**: 첫차·막차가 차고지 출발 시각이라 막차 직후 중간 정류장 승차 가능 구간도 `outside`로 나온다(첫차 전은 오판 없음, 강등일 뿐 제외가 아니라 수용 — 정확히 하려면 노선별 소요시간 필요). ⚠ **지하철은 범위 밖**: 결함은 실측 확인했으나(04:11 강동역 도착 0건인데 ODsay는 지하철 3환승 추천) 판정 수단 미확정 — 기존 `/api/station/timetable`이 강동역에 빈 `lines`를 반환해 신뢰 불가, 실시간 도착 대체는 일 1,000회 쿼터 추가. 별도 spec 대상. 이월(비차단): CLI 버전 올림은 다음 `cli-v*` 릴리스 편승, dodo 이식(동일 ODsay 결함 보유).
  - **무료 한도 재검토(위원장 질의 2026-08-01, "국내 서비스와 같은 수준")**: 수치는 **같지 않고 Google이 낮다**. Google Routes·Places Essentials 10,000회/**월**(일 환산 약 333건)인 반면 카카오 도보·Tmap·ODsay는 각 **일** 1,000건(월 환산 약 30,000건)이라 **일 기준 약 1/3**이다. ⚠ 더 중요한 차이는 **초과 시 동작**: 카카오는 유료 전환 미신청이라 초과=오류=폴백=**비용 상한 구조적 0원**인데, Google Maps는 결제 계정 연결이 전제다(실측 `gen-lang-client-0040162498` `billingEnabled:true`, 결제 계정 `018134-F43D34-1EF609`) → 초과분이 **자동 과금**된다. **단 등가화 가능**: Service Usage 조회에서 `Directions - ComputeRoutes per request quota`에 `unit=1/d/{project}` 버킷이 존재하고 기본값이 무한대·override 없음이라, **일일 상한 override를 무료 한도 이하로 걸면 초과=요청 거부=폴백**이 되어 카카오와 같은 0원 구조를 만들 수 있다(설정은 미실시 — 도입 결정 후). 현재 트래픽은 한도 대비 압도적 여유(최근 30일 routes 50건·places 12건·geocoding 34건, 그나마 대부분이 이번 평가 호출). ⚠ 도입 시 **전용 GCP 프로젝트 분리 필수** — 현재 Maps 키(`dodo-planet-server-maps`)는 Converters TTS·이미지·dodo와 섞인 프로젝트에 있어 Gemini 분리(2026-07-31) 때와 **동일한 사용량 귀속 불가** 문제를 그대로 반복한다. ⚠ 미확정: `navigationInstruction` 필드마스크가 Essentials인지 Pro(5,000회/월)인지 — 공식 문서가 SKU별 필드 목록을 명시하지 않아 실계량 전까지 미확정.
  - **en 도보 경로 공백은 현재 해결 수단 없음(확정)**: Google 부재에 더해 **Tmap 보행자도 en 미지원 실측**(기본·`Accept-Language: en-US`·`reqLang:EN` 3방식 전부 한국어 `description` 반환, 57피처 동일). 카카오 en 미지원은 2026-07-22 기록. 즉 en 도보는 **후보 provider가 존재하지 않는다** — 자체 합성(도로명+거리 결정론 템플릿)이 유일한 경로이나 "완성 문장 낭독 정본" 계약과 충돌하므로 별도 설계 사안.
- **`@google/genai` 1.52.0 → 2.15.0 업그레이드(2026-07-31, ✅ 완료)**: 위원장 제안, 프로젝트 분리와 **별도 커밋·별도 검증**으로 진행. (한 번에 하면 채팅 장애 시 키 문제와 SDK 문제를 가를 수 없다). **breaking change 해당 없음이 근거로 확정**: 공식 CHANGELOG가 "The breaking changes are only in interactions. GenerateContent usage is unaffected"라고 명시하고, 길동무 사용 표면은 `GoogleGenAI` 생성자·`ai.models.generateContent`·타입 4종(Content·Part·FunctionCall·FunctionDeclaration)뿐이라서 Interactions API를 쓰지 않는다. ⚠ 웹 검색 결과가 말하는 "`response.text`·`candidates[0].content.parts` 접근이 깨진다"는 **Interactions 이야기**라 오해하기 쉽다(1차 조사에서 실제로 오판했다). 게이트: 빌드 통과·테스트 1254 통과·SDK 직접 실호출(candidates·parts·functionCall 정상, `parametersJsonSchema` 유지)·**로컬 전체 에이전트 루프 실호출**(다중 라운드 `get_subway_arrivals` → `get_station_meta`+`search_web` 병렬, 렌더 2종). ⚠ `res.text`는 functionCall만 있는 라운드에서 undefined이고 경고 로그를 출력하나 코드가 `?? ""`로 받아 무해(1.x 동일). ⚠ 선재 타입 에러 2건(`geolocation.test.ts` PositionOptions 튜플, `remark-tight-lists.test.ts` MdastNode 변환)은 SDK와 무관하며 빌드·테스트 게이트는 통과한다. ⚠ 로컬 dev는 **3001 포트**(3000은 lg-thinq-console launchd 상주).
- **과금·쿼터 상태 리포트 1단계(2026-07-31, ✅ 구현·실행 검증 완료)**: 스펙 `docs/superpowers/specs/2026-07-31-usage-cost-report-design.md`, 플랜 `docs/superpowers/plans/2026-07-31-usage-cost-report.md`. `node scripts/usage-report.mjs`가 돈·가용성·시한·걱정불필요 4섹션을 평문 문장으로 출력(로컬 전용, 외부 의존성 0, 앱 코드·배포 무변경, 13프로브 6.4초). **착수 게이트 실호출 3건**: ① **Vercel Hobby 확정** → 위 Phase 0의 "스펜드 알림 설정" 항목 종결(Pro 전용이라 설정 불가, Hobby는 초과 시 과금 대신 일시정지). 부수 확정: Hobby 런타임 로그 보존 1시간이라 로그 집계 접근은 원천 봉쇄 ② **Deepgram usage/balances는 STT 키로 403**이라 별도 키 필요(`DEEPGRAM_MANAGE_KEY`, **로컬 전용·Vercel 미등록**, 서버가 쓸 일 없음). **✅ 발급 완료(2026-07-31, 브라우저 자동화 + 위원장 로그인)**: ⚠ Deepgram 콘솔은 `usage:read`/`billing:read`를 개별 스코프 체크박스로 주지 않고 **역할 4단계**(Default=usage:write만 / Member=사용량·로그 조회 / **Admin**=Member+모든 키 목록+비-Owner 관리+**plan and balance 조회** / Owner=빌링 설정 변경까지)로 묶어 제공한다. **잔액 조회는 Admin부터**라 사용량만 필요하면 Member로 충분하다. 위원장 판정: 잔액 자동화 가치가 최소 권한보다 우선이라 **Admin 채택**(Owner는 제외). 키 이름 `gildongmu usage-report (read-only)`, 만료 Never. 실측 반영: 잔액 197.57달러(콘솔 표시와 일치)·최근 30일 요청 46건. 사용량은 잔액과 엔드포인트가 달라 **프로브 2개로 분리**(프로브 하나 = 요청 하나 = 판정 하나 유지), 응답이 일별 배열이라 창 전체 합산. 미발급이어도 그 칸만 `키 미설정`이고 나머지는 동작 ③ **유료 무과금 프로브 확정**: Gemini `GET /v1beta/models` 200, Perplexity는 무효 모델 400(`invalid_model`) vs 무효 키 401 **대조**로 키 유효성만 무과금 판정(잔액 조회 API 부재 확정). **벤더 API로 채울 칸이 Deepgram 하나뿐**이라 "얼마나 남았나" 대신 프로브로 "지금 살아 있나"를 실측하는 5-state(정상·쿼터 초과·인증 실패·조회 실패·키 미설정)를 채택. ⚠ **200에 오류를 담는 벤더 4종은 judge 필수**(ODsay `ApiKeyAuthFailed`·서울지하철/따릉이 `INFO-100`·juso `E0001`). **무효 키 변이 주입 실측으로 검출력 증명**: judge 없으면 넷 다 HTTP 200이라 "정상"으로 거짓 보고되며, 실응답 본문을 fixture로 테스트에 고정했다. 따릉이는 정상만 JSON이고 오류는 XML이라 문자열 폴백 병행. 부수 fix 2건(실데이터가 드러낸 결함): 인증 실패를 조회 실패에서 분리, 응답 본문 개행이 출력 줄을 쪼개 "한 줄 = 한 접근성 객체"가 깨지던 것을 렌더 계층 공백 정규화로 해소. `VERCEL_TOKEN`은 Vercel CLI 로컬 토큰(`~/Library/Application Support/com.vercel.cli/auth.json`)을 폴백으로 읽어 수동 발급 불요. ⚠ 그 파일의 `expiresAt`은 **초 단위**(밀리초로 비교하면 1970년이라 항상 만료로 읽힘)이고, 만료 토큰을 그대로 보내면 403 `invalidToken`이 "인증 실패"로 표시되어 계정 문제로 오해된다. 만료 검사 후 `키 미설정`+`vercel login` 안내로 분리(실측: 같은 날 오전 유효·저녁 만료). **2단계(자체 계측) 승격 기준**(스펙 §10): 월 지출 10달러 초과 / 유료 4칸 중 2칸 이상 지속 정보 없음 / 쿼터 초과 1회 이상 관측 중 하나. ⚠ 2단계는 외부 저장소 필수(위 로그 보존 1시간). **Gemini 전용 GCP 프로젝트 분리(2026-07-31, ✅ 완료)**: 위원장 제안. 종전에는 `GEMINI_API_KEY`가 Converters(TTS·이미지)·dodo-planet과 공유하는 프로젝트에 묶여 있어 사용량 귀속이 불가능했다. ⚠ **model 라벨 분리도 성립하지 않았다**: dodo도 `gemini-3.6-flash`를 쓴다(`dodo-planet/src/lib/gemini/client.ts`) → 리포트가 보고하던 "길동무 91,978토큰"에 dodo 몫이 섞여 있었다(발견 즉시 시정). **신설**: 프로젝트 `gildongmu-prod`(기존 결제 계정 `018134-F43D34-1EF609` 연결, 티어는 계정 누적 지출 기준이라 등급 승계. ⚠ 무료 티어는 입출력이 모델 학습에 쓰일 수 있어 프로덕션 부적합이므로 결제 연결이 필수), `generativelanguage.googleapis.com`만 허용하는 **API 제한 키** 발급. 실호출 검증: 모델 목록 200 + `gemini-3.6-flash` 생성 200(`serviceTier:"standard"`로 유료 티어 확인), **prod 채팅 정상**(웹검색 도구 호출·렌더 카드·출처 포함). ⚠ **prod 검증 함정 2건(둘 다 내 오진)**: ① `/api/chat` 요청 필드는 `content`가 아니라 **`text`**다(`route.ts`가 `m.text`를 읽는다). 잘못 보내면 Gemini가 400 `INVALID_ARGUMENT`(parts[0].data 미초기화)를 내는데 이걸 키 문제로 오인했다. **400은 인증 통과의 증거**다(무효 키는 401·403). ② Vercel REST `env?decrypt=true`도 **sensitive 타입은 값을 비워 준다**. 정상 동작 중인 카카오·ODsay·Deepgram도 전부 길이 0이라 "길이 0 = 빈값"은 오진이다([[vercel-prod-env-pull-redacts-encrypted]]가 이미 경고한 함정, 검증은 실호출로). 교훈: 그럴듯한 가설을 쌓기 전에 **런타임 로그부터** 본다(로그 한 줄이 두 번의 헛발질을 대체했다). 교체 3곳: `.env.local`·Vercel prod env(제거 후 재등록)·리포트 상수 `GEMINI_GCP_PROJECT`. 프로젝트가 전용이 되어 **리포트의 model 라벨 필터를 제거**했다(필터를 남기면 모델 교체 시 오류 없이 0을 보고하는 위험만 남는다. 모델 상수는 집계 필터가 아니라 단가 기준으로 역할 변경, drift 테스트 유지).
- **Gemini 사용량 해소(2026-07-31 같은 날 후속)**: 키 문자열 대조로 `GEMINI_API_KEY`가 GCP 프로젝트 `gen-lang-client-0040162498`(AI Studio 자동 생성)에 묶인 것을 확정했고(`GOOGLE_CLOUD_TTS_API_KEY`도 같은 프로젝트의 `dodo-planet-tts` 키), Cloud Monitoring이 이미 활성이라 별도 enable·서비스 계정 없이 `gcloud auth print-access-token`으로 조회된다. 쿼터 외 실사용 메트릭은 **출력 토큰 하나뿐**(`generate_content_usage_output_token_count`, 라벨 model·thinking_enabled·output_modality)이고 **입력 토큰 메트릭은 없다** → 비용은 출력 기준 하한이며 표기에 "출력 기준"을 남긴다(단가 $7.50/M). ⚠ **이 프로젝트는 길동무 전용이 아니다**: Converters의 TTS·이미지 생성과 dodo-planet이 같은 키를 공유해 18개 모델 시계열이 섞여 나온다 → `model` 라벨로 길동무 모델만 분리(실측 30일 91,978토큰·0.69달러). **프로젝트 전체 합계는 의도적으로 미표기**: 길동무 비용이 아닌 데다 정렬 구간 경계에 따라 실행마다 흔들려(실측 1.8M vs 0.91M) 없는 문제를 쫓게 만든다. 정렬은 창 길이와 같은 주기 대신 **하루(86400s)**로 균일하게 덮어 재현성 확보(3회 동일 확인). ⚠ 모델명 상수는 `src/lib/gemini/client.ts`의 `GEMINI_MODEL`과 **동조 필수**. 어긋나면 오류 없이 0토큰을 보고하는 최악의 실패라 drift 테스트로 강제(CLI 버전 4곳 동조와 같은 구조).
- **레거시 감사 후속 보류 3건(2026-07-30) — 2건 종결, 1건 잔여**: ① **data.go.kr envelope 파서 6벌 공용화**(에어코리아·서울버스·서울지하철 등 envelope 비표준 3종, CLAUDE.md 횡단 함정에 이미 기록, 계약 다양성이 높아 추상화 설계 선행 필요) — **잔여, 다음 마일스톤 후보**. ~~② 웹 nearby 8종 중복 추출~~(✅ 2026-07-30 종결 — 위 운영 표 "웹 nearby 중복 추출" 행). ~~③ iOS Nearby 11모델 상태 골격 추출~~(✅ 2026-07-31 종결 — 위 운영 표 "iOS Nearby 11모델 상태 골격 추출" 행).
- ~~경로 provider 실호출 대조 2차 — 대중교통~~ **종결(2026-07-30 주간 재대조 완료)**: 01시 대조(첫·끝 도보 leg 부재·배차간격 부재·요금 null 사례로 카카오 publictraffic 기본 교체 비권장 판정)의 잔여 확인 축이던 "심야 노선 처리"를 16시대 동좌표 3구간(길동→강남역·경복궁→남산서울타워·서울역→여의도공원) 재호출로 해소 — 両사 모두 운행 중 노선을 정상 추천(ODsay 5→8→2호선 36분 등, 카카오도 지하철 체인+요금 1,650원 정상 반환). 01시의 "운행 종료 지하철 1순위"는 両사 계획용 시각표 모드로 확정(실결함 아님). **판정 유지: 기본 교체 비권장**, 카카오는 정차역 전체 목록·환승 출구 번호 보강 소스로만 후속 가치. 자동차 부분은 전환 완료(2026-07-30) — '운영 중인 기능' 표 '자동차 경로 브리핑' 행 참조.
- **Phase 1 App Store 심사 제출→1.0 승인·출시 완료(제출 2026-07-28, 2.1(a) 반려 1회 대응 후 승인 2026-07-30 — 경위는 위 운영 표 "서비스 지역 커버리지 계약" 행)**: ① 빌드 1.0.0(3) — 7/21 이후 40여 커밋 반영, 실기기 설치 + TestFlight 업로드·ASC 처리 완료(절차 `ios/ExportOptions.plist` 정본화) ② 스크린샷 ko 7컷(`docs/appstore/screenshots/ko/`, 1320×2868, 시뮬레이터 한글 입력은 `-AppleLanguages (ko)`+두벌식 QWERTY 변환 우회) ③ VO QA 통합 체크리스트 49항목(`docs/appstore/1.0-voiceover-qa-checklist.md`, Windows에도 사본) ④ **ASC 콘솔 입력 완료(2026-07-27, Claude in Chrome·Windows)**: 새 이름 "길동무: 텍스트 기반 접근성 길찾기 앱" 저장(중복 통과)·부제·카테고리(내비/여행)·Content Rights(제3자 권리 보유)·연령 등급 설문 7단계(4+ 산정, 한국 ALL)·스크린샷 7컷 6.9" 슬롯 순서대로 업로드·프로모션/설명(2차 개정 확정판)/키워드/지원 URL/저작권·심사 정보(로그인 불필요, 연락처+전화, 영문 심사 노트)·빌드 3 연결·영양 라벨 3유형(정확한 위치·검색 기록·기타 사용자 콘텐츠, 각 앱 기능/비연결/비추적) 발행까지. ASC 함정 메모: 6.9" 스크린샷은 버전 페이지 드롭존(6.5" 전용)이 아니라 Media Manager의 6.9" 섹션에만 업로드 가능, 다중 파일 동시 업로드는 순서가 뒤섞이므로 1장씩 순차 업로드, React 체크박스는 form_input 불가·좌표 클릭만 유효(텍스트 필드는 form_input 정상). **잔여 처리(2026-07-28)**: 듣기(TTS) 수정 완료(속도 3단 포함) → 빌드 1.0.0(4) 아카이브·TestFlight 업로드. **VO QA 게이트 완화(위원장 결정)**: 49항목 완주는 애플 요구 아닌 자체 게이트라 제외, 스모크 4항목(첫 실행+동의/검색→상세/채팅 전송+듣기/받아쓰기 홀드) 위원장 실기기 합격으로 대체 — 잔여 항목은 출시 후 점진 개선 백로그. 접근성 라벨 근거는 위원장 일상 실사용+스모크. **제출 실행(2026-07-28)**: 스토어 설명 받아쓰기 줄에 탭 토글 선택 반영(ko 저장, en 로컬라이제이션은 ASC 미등록 — 추가는 출시 후 백로그, en 키워드·스크린샷 미준비) → 버전 페이지 빌드 3→4 교체 저장 → 접근성 라벨 VoiceOver 지원 초안 저장(iPhone, VoiceOver만 — ⚠ Publish는 앱이 스토어에 출시된 뒤에만 가능, 출시 후 발행 필요) → Add for Review가 가격 미설정으로 차단되어 Pricing $0.00(175개국)+Availability 전체 국가 설정(체크리스트 누락 항목이었음) → Submit for Review 완료, 상태 1.0 Waiting for Review(최대 48h, 이메일 통보). EU DSA 배너는 제출을 막지 않았음. **출시 후 할 일(2026-07-30 출시로 착수 가능 상태)**: ① ~~접근성 라벨 Publish~~(✅ 2026-07-31 발행 완료 — iPhone·VoiceOver, Published 1·Drafts 0, 발행 후 취소 불가) ② ~~en 스토어 로컬라이제이션~~(✅ **2026-07-31 완료** — ASC 1.1 초안 생성 후 English (U.S.) 추가: 이름 "Gildongmu: Korea Navigation"·부제 "Built for screen reader users"·프로모션·설명(3차 개정 영어판)·키워드·en 지원 URL + **스크린샷 7컷 업로드**(Media Manager en 6.9", 6.5"는 상속 — ko 폴백 해제 확인). 1.1 출시와 함께 공개되고 What's New만 릴리스 시 작성. 등록값·촬영 이력 정본은 `docs/appstore/1.0-submission-draft.md` §12. ⚠ 03 지하철 실시간·04 대중교통 브리핑은 **운행 시간대에만 촬영 가능**(심야 재촬영 불가로 06:30 촬영). ja 등 추가 언어는 후속) ③ VO QA 잔여 항목 점진 개선. **부수 fix 2건(2026-07-31, 둘 다 en 스크린샷 촬영이 드러낸 en 전용 결함 — ko 사용만으로는 영원히 안 보인다)**: ① iOS 채팅 출처 라벨이 원시 i18n 키("source.airkorea")로 노출되던 V1 잔존 TODO 해소 — `sourceDisplayLabel` switch(리터럴 키 린터 계약 준수)로 `chat.source.*` 17종 로컬라이즈, 시뮬 실측 "AirKorea"·"Korea Meteorological Administration" 확인. ② 내 주변 지하철 목록이 `nameEn`을 무시하고 한국어 역명만 표시하던 웹-iOS 동등성 갭 해소 — 웹 `SubwayArrivalsNearby`의 `isEn ? nameEn || stationName` 미러를 `displayStationName`으로 이식(`AppLanguage.dataLocale == "en"` 판정, 빈 문자열도 폴백), 시뮬 실측 "Seoul Station" 확인. **노선명·도착 메시지는 외부 API가 한국어만 제공해 en에서도 한국어 유지**(웹 동일, 데이터 현실). ⚠ `NearbySubwayStation.nameEn`은 seed 메타 기반 옵셔널이라 미보유 역은 한국어 폴백이 정상. ⚠ 제출 후 빌드 교체는 제출 취소+재제출이므로 반드시 빌드 확정 후 제출. 참고: Apps 목록에 EU DSA trader status 배너 — 제출 시 걸리면 트레이더 신고 또는 EU 제외 선택. 무장애 컷은 내 주변 목록(06)+편의시설 상세(07)로 커버(경복궁 상세는 매칭 불성립으로 섹션 미노출).
- **Phase 0 출시 전 방어(2026-07-27, ✅ 구현·prod 검증 완료)**: 플랜 `docs/superpowers/plans/2026-07-27-phase0-launch-defense.md`, 커밋 407a091..38f8b7f(6개, SDD 5태스크 전부 리뷰 클린+최종 리뷰 SHIP). ① STT 라우트 레이트리밋(60초 10회)+크기 상한 25MB→5MB(유일 무방비 유료 라우트 해소) ② 채팅 search_web 1시간 in-memory 캐시(chat 계층 Next 비의존이라 unstable_cache 대신 순수 Map, 실패 비캐시) ③ ODsay·Tmap 좌표 4자리(±5.5m)·공기질 측정소 3자리 반올림으로 revalidate 캐시 키 실효화(kakao-navi no-store·region-scan 반올림 금지 판단은 불가침) ④ vercel.json 리전 icn1 고정 ⑤ 정본 도메인 코드·문서 반영. **prod 실호출 검증**: `x-vercel-id: icn1::icn1`(함수 리전 반영), STT 12연속 POST → 429 발화(11회 허용은 인스턴스별 in-memory 리밋의 수용된 오차). 후속 백로그(최종 리뷰 Minor, 비차단): Tmap POST fetch의 revalidate 캐시 성립 여부 실호출 확인(동일 좌표 2연속 호출 시 Tmap 콘솔 사용량 증가 대조 — 일 1,000건 쿼터 대비 현 트래픽 여유라 트래픽 증가 시 확인으로 보류)·검색창+채팅 캐시 중첩 최악 2시간 스테일 창·~~STT 429의 클라 `rate_limited` 코드 분리~~(✅ 2026-07-28: 429 전용 코드+`voice.errors.rate_limited` 6로케일 — "실패→즉시 재시도"와 "과다→대기"를 분리, iOS는 온디바이스 STT라 비해당). **위원장 콘솔 작업 잔여**: 유료 3종(Google AI Studio·Perplexity·Deepgram) 확인은 과금·쿼터 리포트(`scripts/usage-report.mjs`, 2026-07-31)로 대체. ~~Vercel 스펜드 알림 설정~~(**수행 불가이자 수행 불요로 종결 2026-07-31**: 팀이 Hobby라 Spend Management는 Pro 전용이고, Hobby는 한도 초과 시 과금이 아니라 프로젝트 자동 일시정지라 비용 위험이 구조적으로 0. 실측 `GET /v2/teams` plan="hobby", `/v1/billing/charges` 404 costs_not_found).
- **정본 도메인 확정(2026-07-27, 위원장)**: 대외 정본 = gildongmu.dodoplanet.space(코드·문서 반영 완료). 사용량 증가 시 독립 도메인 구매 재검토. ⚠ odsay.ts Referer는 URI 키 묶임이라 vercel.app 유지, 교체하려면 ODsay 콘솔에 새 도메인 URI 등록 선행.
- **CLI/MCP 제3자 쿼터 정책(2026-07-27, 위원장)**: 자체 키 없이 개방 유지. 유료 경로(Gemini 채팅·Perplexity)는 서버 IP 레이트리밋으로 방어(채팅 60초 10회 기존 + search_web 캐시 신규). CLI/MCP 기본 URL은 dodoplanet.space 서브도메인으로 교체(다음 릴리스 0.5.0에 반영).
- **내 주변 → 장소 상세 UI 이식(iOS, 2026-07-26, ✅ 구현·실기기 배포 — 실기기 VO 확인 잔여)**: 스펙 `docs/superpowers/specs/2026-07-26-nearby-place-detail-port-design.md`, 플랜 7태스크(SDD, 커밋 8306ad4..13349e4+). 내 주변 4종(소아 진료·아이 놀 곳·둘러보기·무장애 관광지) 항목을 `NavigationLink { PlaceDetailView } label: { PlaceRow(secondaryOverride:) }`로 통일 — 검색 탭과 동일한 상세(주소 복사·전화·길찾기 프리필·딥링크·브리핑·채팅·무장애 자동 섹션)와 로터 액션 6종 획득. 신규: Kit `barrierFreePlaceToPlace`(TourAPI addr1=도로명→roadAddress 슬롯), `PlaceDetailView` 도메인 섹션 제네릭 슬롯(소아 진료: 진료 상태·찾아오는 길·달빛 지정 조건부 — 위원장 판정대로 목록 미표기·상세 조건부), `ios.clinic.designated` 5로케일. 제거: 소아 진료 항목 Section·heading·주소/전화 행(평면 1행=1객체, M2·M3 결정 동형), 무장애 DisclosureGroup lazy 상세(발견 경로가 상세 자동 섹션으로 대체, match 라우트 시뮬 실호출 확인), contextMenu·하드코딩 chatLabel. 게이트: Kit 128·빌드·시뮬 4화면 실측 PASS, 최종 리뷰 SHIP+a11y 감사. **잔여: 위원장 실기기 VO 확인** — ① 행 낭독(1행=1객체) ② 로터 액션 순서 ③ 상세 진입 흐름 ④ 소아 진료 heading 제거 체감 ⑤ 진료 도메인 섹션 무헤더 판단(감사 지적을 "최상단 위치=발견 경로·기존 무헤더 관례"로 파킹 — VO에서 불편하면 헤더 추가).
- **소아 진료 커버리지 확장 단계 1(2026-07-26, ✅ 구현·prod 가동 — 실기기 VO 확인 잔여)**: 병합 진입점 `src/lib/clinics.ts`(달빛 20km + 일반 소아과 3km, hpid dedupe 지정 승, 실패 비대칭: 정본 throw·보완 supplementFailed 표기)+`region-scan.ts`(좌표→시도 합집합, 8방위 9점 — **시군구 정적 seed 불요로 판명**: Q1 생략 가능·numOfRows=1000 실측)+`pediatric-clinics.ts`(QD=D002, parseClinics 재사용). UI 지정 태그·소스별 반경 표기·5로케일, 채팅 designated 동봉, iOS 미러(Kit 127·xcstrings 714키)+실기기 배포 완료. **prod 실호출(강동, 일 20시)**: total=194(지정 21+일반 173), 제보 병원 연세도우리(1.56km) 병합 풀 진입 확정(마감 시각이라 closed 정렬 — 의도 동작). 원계획 기록: 스펙 `docs/superpowers/specs/2026-07-26-clinic-coverage-expansion-design.md`. 위원장 실사용 제보(일요일 진료 중인 강동 소아과가 "내 주변"에 없음)에서 출발. **착수 게이트 실호출 완료(강동구청 좌표, `scripts/verify-clinic-sources.mjs`)** — 원인은 **(A) 데이터셋 범위 확정**: 달빛 지정 명부 153건에 그 기관이 없다. ⚠ **동명이원 함정** — 같은 이름 기관이 강남 개포동(6.9km)에도 있고 그쪽은 명부에 있어, 이름 매칭 게이트가 "명부에 있음 → (B) 절단, 해결됨"으로 잘못 종결할 뻔했다. **기관 동일성은 이름이 아니라 좌표·주소로 판정**(스크립트 수정 완료). 별개로 (B) 절단도 실재(그 강남 기관이 20km 내 21곳 중 7위 = 옛 상한 5에 잘림) → 단계 0은 (A)와 무관하게 값을 함. **계약 확정**: 정본 = `getHsptlMdcncListInfoInqire` + **`QD=D002`**(소아청소년과 — 강남구 D001~D016 전수 스캔 역판정, ⚠ **스펙 초안의 D009 추정은 오류**, 그건 신경외과류) → 좌표+`dutyTime` 8칸 완비라 `parseClinics`·`clinicOpenStatus`·`prioritizeOpen` 그대로 재사용, **추가 활용신청 불필요**. 탈락: `getHsptlMdcncLcinfoInqire`(요일별 진료시간 부재 + QD·QT·QN 무시하고 `totalCount=239` 고정), HIRA `getHospBasisList`(403 미신청 → 단계 2 불필요). **커버리지 갭**: 강동 20km·6개 구에서 일요일 진료 소아청소년과 123곳 중 명부 중복 8곳 ⇒ **115곳 누락(명부 커버리지 6.5%)**, 1.2km 안에도 바른소아청소년과·새솔키즈·아이봄소아청소년과 전부 부재. **남은 설계 결정**: 정본이 시도+시군구 기반이라 반경 검색이 아니다 → `coordToAddress` 역지오코딩으로 행정구를 풀고 인접 구를 병렬 조회(인접 구 산출 방식이 핵심 선택). 정밀도 대가는 실측 확인 — `D002`는 "전문"이 아니라 "진료과목 보유"라 이비인후과·내과가 섞이므로 지정 여부를 항목 텍스트에 흡수해야 한다(스펙 §6-1).
- **길찾기 강화 로드맵 잔여(2026-07-22 갱신)**: 정본 `docs/RESEARCH-2026-07-routing-enhancement.md`. 완료: 배차간격(2026-07-21)·도보+길찾기 탭 마일스톤(2026-07-22)·**역 상세 보강 웹 phase(2026-07-22, 위 운영 표 — 시간표·시설 보강·역명 매칭 수정)**. 완료 추가: **역 상세 보강 iOS phase(2026-07-22, 커밋 b8ca32d — 시간표 섹션 미러+supplementFailed·voiceGuideSource 표기, Kit 테스트 110, 실기기 설치·VoiceOver QA 잔여)**. 완료 추가 2: **특일정보(15012690) 활용신청·전파(2026-07-22, Windows Chrome 원격 자동화+위원장 로그인·확인 클릭, 승인 후 ~7분 전파 — 공휴일 보정 재배포 없이 활성)**. **다음 마일스톤(A-트랙, 위원장 재조정 2026-07-22 — gildongmu 내 항목 우선, 순서대로 진행)**: ~~① CLI/MCP 카탈로그 미러~~(✅ 2026-07-22 cli-v0.4.0 발행 완료 — 위 운영 표 CLI/MCP 행) ~~② 카카오 신규 도보 API 실호출 검증~~(✅ 2026-07-22 완료, RESEARCH §미확정 2·3 해소 — **판정: 완성 문장 턴바이턴 있음(유효 후보)**, `dapi.kakao.com/v2/routing/walk` 실호출 길동 1.66km 11 steps 교차로·도로명·랜드마크 안내문+`route_mode=ACCESSIBLE` 존재+경로 불가 200 `TOO_FAR_AWAY` graceful. 쿼터: 07-21 이전 이용 앱 무료 쿼터 유지 조항+単일 공유 앱이라 첫 활성화 경합 무성립, 공유 키 무활성화 200 실증. **en 미지원**(lang·Accept-Language 무시)이라 en 도보 공백은 카카오로 해소 불가 종결. **위원장 판정(2026-07-29)으로 방향 역전 확정**: 동좌표 문체 대조 실측 근거로 **기본=카카오, Tmap=폴백**("이견의 여지가 없을 정도로 카카오 우월"). spec 작성·codex 적대적 리뷰 반영 완료(`docs/superpowers/specs/2026-07-29-kakao-walk-primary-design.md` — 계단 회피 `stepFree` union·안전 문구 결정론 삽입·병합 스텝 주석 생략·fetch 단위 캐시·유료 전환 미신청 유지). **✅ 구현·prod 검증 완료(2026-07-29, 커밋 a14be5f..26ae7e4 10개)**: SDD 4태스크(전부 리뷰 SHIP, Task 4는 fix 2라운드 — URL 복원 microtask 레이스·토글/조회 busy 상호 신호)+최종 브랜치 리뷰 fix 1라운드(토글 aria 통일·폴백 로그 좌표·거리시간 `Math.round` iOS Int 방어) 후 SHIP. **prod 실호출 5종 합격**: ① 강동역→길동생태공원 카카오 문체·병합 스텝 3건 무주석·단일 횡단보도 주석 4건·coord 무누출·정수 ② 서울역→서울로7017 `stepFree:"applied"` 1,024m 우회 ③ 광나루→천호역 `no_stepfree_route`+안내 문장 steps[0] ④ 서울→제주 null ⑤ `accessible=1` 400. **폴백 로컬 실증**: 무효 카카오 키 기동 → Tmap 브리핑+좌표 포함 warn 로그+unavailable 안내 문장. 캐시 계약은 최종 리뷰어가 Next 소스 실측 확증(카카오 GET revalidate 3600 유효·200만 캐시·**Tmap POST revalidate 실효 확인 — Phase 0 백로그 의문 해소**). 이월(비차단): spec 테스트 3케이스 미커버·iOS 토글 phase(xcstrings 재생성 편승·`RouteModels.swift:91` 주석 정정)·CLI `route walk` accessible 플래그(다음 cli-v 편승)·`no_stepfree_route` 문구 미세 부정확·출처 병기 배포 수준 필터 개선안. **카카오 앱 유료 전환 미신청 확정(위원장 확인 2026-07-29: "유료 신청을 한 적 없음")** — 쿼터 초과=오류=Tmap 폴백 사슬 성립, **비용 상한 구조적 0원 확정**. 유료 전환 신청은 하드 스톱 유지(spec §캐시·쿼터). 선행 확인: dodo 공유 앱 도보 쿼터 배분(일 1,000건 공유, spec 단계 설계 몫)·~~ACCESSIBLE 실차이 검증~~(✅ 2026-07-29 실호출 4구간 대조 완료, **장벽 지형에서 실차이 확정**: 서울역→서울로7017 계단 회피 765m→1,019m 우회, 노량진역→수산시장 646m→691m, 평지 강동역→길동생태공원은 경로 동일. 3구간 공통으로 역 진출입을 "엘레베이터를 이용하여 진출" 안내로 교체. ⚠ 광나루한강공원→천호역은 ACCESSIBLE에서 200+`status:"ROUTE_RESULT_NOT_FOUND"`+빈 legs — **무계단 경로 부재 시 경로 자체를 거부**하므로 spec에 3-state 폴백(ACCESSIBLE 불가→기본 경로+사유 안내) 필수. 지하보도는 회피 대상 아님(노량진 両모드 경유 — 계단 특정 회피로 추정). envelope는 top-level `route` 단수 객체+`status`(복수 `routes` 아님), 경로 불가 status는 `TOO_FAR_AWAY` 외 `ROUTE_RESULT_NOT_FOUND`도 존재. **Tmap 문체 대조(2026-07-29 동좌표 4구간)**: Tmap=미세 분절·상대방향 중심(강동역→길동생태공원 29스텝, "횡단보도 후 15m"+"직진 후 15m" 분리, 계단·지하보도·역사 내 이동 무언급), 카카오=의미 단위 병합·랜드마크/교차로명 목적지형(동구간 20스텝, "길동사거리에서 2개의 횡단보도 이용" 묶음, "역사 내 이동"·"계단이용"·"지하보도 이용" 명시). 미리 듣기 브리핑 용도 기본 provider 선택은 위원장 판단 대기)) ~~③ 둘러보기 기능 B~~(✅ 2026-07-22 완료 — 위 운영 표 "보행 인프라" 행. 참고: 음향신호기 정본은 지시된 data.go.kr 15047259(2019 스냅샷)가 아니라 그 원 출처인 서울 열린데이터 OA-15543(2026-05-28 갱신)로 추적·교체, 활용신청 불필요·좌표계 EPSG:5186 .prj 실측 확정) ~~④ dodo-planet 2차 이식~~(✅ 2026-07-23 dodo 세션 Round 192 완결 — 검색 정확도 전환·역 상세 보강·보행 인프라·Tmap 도보·/map 경로 섹션, 함수 63개·최종 리뷰 SHIP. 상세·후속 이월은 dodo PROGRESS/CHANGELOG Round 192 정본. **A-트랙 ①~④ 전체 종료**). 이월 Minor 백로그(위 운영 표)는 각 마일스톤에 편승. 보류(위원장 선행 액션 대기): App Store 심사 제출(실기기 VoiceOver QA 게이트)·ITS 돌발 브리핑(its.go.kr 가입). **키 확보 현황(2026-07-21 밤)**: ① Tmap appKey 발급 완료(SK open API 앱 `gildongmu`, T아이디 신규 가입 경유, IPS "Any IP allowed" 유지, `TMAP_APP_KEY` 로컬+Vercel prod 등록). 실호출 통과: 보행자(길동 2.1km, 완성 문장 24단계)+자동차(길동→강남, 실시간 교통·택시요금 22,800원) 単일 키로 両축 커버. ② TAGO 지하철정보(15098554): 활용신청 자동승인·키 전파 완료(2026-07-22 확인). ⚠ 엔드포인트 `apis.data.go.kr/1613000/SubwayInfo/GetKwrdFndSubwaySttnList`(서비스명 `SubwayInfo`, 오퍼레이션 첫 글자 대문자 G — 소문자 get·`SubwayInfoService`는 "API not found"). ③ 엘리베이터: data.go.kr 15098158은 **LINK형**(활용신청 없음) → 정본은 서울 열린데이터 OA-21212, 서비스명 `tbTraficElvtr`, 기존 `SEOUL_OPEN_DATA_KEY`로 실호출 성공(552건, `NODE_WKT` POINT(lng lat)+`SBWY_STN_NM`/`SBWY_STN_CD`). ④ ITS 돌발(15040465): **LINK형으로 판명, data.go.kr 키 불가** → its.go.kr 자체 회원가입+apiKey 필요(사용자 작업, 보류). ⑤ 음성유도기 seed(OA-22526 CSV 무인증) 착수 가능.미확정 게이트는 RESEARCH §미확정 참조.
- **네이버 지역검색 NAVER API Hub 이관 데드라인 2027-06-30(2026-07-21 메일 수신)**: 개발자센터 Search API가 NCP "NAVER API Hub"로 이관·단계 종료. 지역검색은 즉시 종료 3종(쇼핑·책·학술정보, 2026-07-31)에 비해당, 기존 발급(2026-07-18)이라 **2027-06-30까지 현행 무변경**. 그 전에 `naver-local.ts` 인증을 클라이언트 ID/Secret → NCP API Key로 이관(NCP 계정 기보유, Search API 무료 정책 유지 명시 → 비용 영향 없음). 상세는 RESEARCH-2026-07-routing-enhancement.md §6.

- **App Store 출시 필수 요건 완비(2026-07-21, 실기기 VoiceOver QA 대기)**: 스펙 `docs/superpowers/specs/2026-07-21-appstore-release-gates-design.md` 마일스톤 완료(커밋 d9986a1..6672676, 태스크 7+최종 리뷰 fix 1). ① 채팅 AI 동의 5.1.2(i): 인라인 `ChatConsentView`(채팅 탭·장소 sheet 게이트)+`ChatModel.send` 가드 이중 방어+설정 "AI 채팅" 섹션(동의 토글·처리방침·문제 신고 mailto), 동의 직후 입력 필드 선점 포커스는 `@Binding` 1회 소비 후 리셋(탭 재방문 반복 강탈 방지, 리뷰 fix) ② `PrivacyInfo.xcprivacy`(UserDefaults CA92.1, 위치·채팅 콘텐츠 not linked/App Functionality, plutil OK·번들 포함 확인) ③ 웹 `/{locale}/privacy` 5개 언어(ASC 처리방침·지원 URL 겸용, 홈 하단 링크) ④ 정식명 "길동무"·1.0.0(빌드 2) 승계 ⑤ `/api/chat` IP 레이트리밋 60초 10회. **prod 실호출 검증(2026-07-21)**: privacy 5로케일 전부 200, `/api/chat` 12연속 호출 = 10×400(rate 통과·body 거절) → 429×2 정확. 실기기 설치·실행 완료. **잔여 게이트: 위원장 실기기 VoiceOver QA 6항목**(plan Task 8 Step 6 체크리스트). **심사 제출(~1주 후)의 ASC 입력값 정본은 `docs/appstore/1.0-submission-draft.md`**(설명·키워드·연령 등급·영양 라벨 표·심사 노트·스크린샷 계획, 제출 버튼은 사용자 확인 후). Search History는 매니페스트에 선반영 완료(2026-07-21 후속, 3자 일치 성립).
- **iOS 채팅 멀티 턴 먹통(메인 스레드 100% CPU) 원인 확정·수정(2026-07-20, 재발 관찰 대기)**: 몇 턴 대화 후 앱 전면 무응답 빈발(위원장 보고). 먹통 생존 상태에서 `devicectl`로 `Gildongmu.cpu_resource-*.ips`(microstackshot) 회수 → 23F77 DeviceSupport 심볼로 심볼화 → **`LazySubviewPlacements.placeSubviews`/`_LazyLayoutViewCache` 진동 루프 확정**(90초+ 100% CPU, `flushTransactions` 매 사이클 재발행). 채팅 리스트 `LazyVStack`의 lazy 크기 추정이 대화 성장에 따라 수렴 실패. 전일(07-19 07:40) 동일 리포트 존재 = 받아쓰기 전환 이전부터의 잠복 결함. **수정: eager `VStack` 교체**(히스토리 유한, 진동 기제 제거 + 화면 밖 AX 컬링 부작용 동시 해소). 재발 시 다음 후보는 말풍선 중첩 스택 평탄화. 진단 절차(먹통 시 systemCrashLogs 도메인 cpu_resource 회수→atos 심볼화)는 재사용 가치 있음.
- **iOS 받아쓰기 WhatsApp식 홀드 전환(2026-07-20, 실기기 게이트 대기)**: 탭 토글 폐기 → `HoldDictationButton`(누른 동안 녹음·떼면 입력, 채팅만 위로 밀어 잠금·왼쪽으로 밀어 취소, 짧은 탭=사용법 안내/청취 중 정지). 위원장 직접 결정(VO 사용 중에도 WhatsApp 홀드가 더 편하다는 실사용 경험) — 헌장 §6은 토글만 기술하므로 **실측 합격 시 헌장에 받아쓰기 버튼 "토글/홀드 택1"로 보강 예정**. 핵심 계약: 녹음 시작 순간 interrupting 무음 통지로 VO 낭독 중단+세션 중 라벨 불변(받아쓰기 중 VO 발화 0). 리뷰 fix 4건(잠금 후 대각선 드리프트 취소 차단·취소 in-flight 재홀드 무음 소실 레이스·취소 시 locked 잔류·requesting 중 탭 안내 오발화) 반영, 빌드 PASS. **실기기 1차(2026-07-20) 불합격 2건 → 제스처 계층 교체**: 검색 탭 홀드 미성립(List 스크롤 팬이 SwiftUI 제스처 가로챔)·채팅 잠금 슬라이드 미동작(VO pass-through 드래그 유실) — SwiftUI LongPress+Drag 조합을 UIKit `UILongPressGestureRecognizer`+`UITapGestureRecognizer`(`require(toFail:)`) 캐처로 교체(WhatsApp 표준 계층, 재도입 금지). 동시 반영: **채팅 릴리스=즉시 전송**(위원장 지시, 스트리밍 중엔 초안 보존 폴백)·채팅 입력 필드 지우기 버튼(§5 포커스 선점). **실기기 2차(2026-07-20)**: 검색 홀드·채팅 홀드·잠금 슬라이드·즉시 전송 전부 합격(잠금 "미동작" 오판은 잠금 상태 전용 UI 부재가 원인 — 계측 로그로 정상 확정). **잠금 재설계(위원장, 3차 배포)**: 핸즈프리 계속 녹음 → **일시정지+확정**으로 변경 — 위로 밀면 녹음 정지·전사를 초안에 병합·"받아쓰기 잠김"+세그먼트 통지(녹음 정지 후라 발화 금지 무충돌), 수정 후 재홀드로 이어쓰기, 릴리스=초안 병합 전송. `locked` 상태 제거로 상태 머신 단순화. **실기기 3차 합격(2026-07-20 위원장): "아주 잘 작동" — 홀드·잠금(일시정지)·이어쓰기·전송 전체 플로 확정.** 접근성 헌장 §6에 "받아쓰기 버튼 토글/홀드 택1" 계약으로 승격 문서화(구현 정본=이 repo `HoldDictationButton.swift`). `.startsMediaSession` 후보는 현행 합격으로 미채택 종결. dodo-planet·webfortd 이식 예정(각 세션 프롬프트 전달). 먹통(VStack fix)은 수동 테스트 재발 0, 상용 관찰 계속.

- **iOS TestFlight 외부 테스터 배포 개시(2026-07-18, Beta App Review 대기)**: 아내(유정, youjung3@gmail.com) 초대 목적. App Store Connect 앱 신규 등록 — 이름 "길동무: 접근성 길찾기"(단독 "길동무"는 전역 중복), App ID 6792234349, SKU gildongmu-001, bundle `space.dodoplanet.gildongmu`. 사전 작업: 1024px 마케팅 아이콘 신설(asset catalog 자체가 없었음 — 남색 그라데이션+흰 "길", 알파 없음) + `ITSAppUsesNonExemptEncryption=NO`(커밋 c638140). 0.1.0(1) 업로드는 `xcodebuild -exportArchive` destination=upload + `-allowProvisioningUpdates`(Cloud Managed Apple Distribution 인증서 자동 발급, ~2027-07-18). 최신 ASC UI엔 사이드바 External Testing 섹션이 없고 **빌드 상세 → Individual Testers → Add New Testers**가 외부 테스터 개별 초대 플로(그룹 불요, 심사 제출까지 한 다이얼로그): Sign-in required 해제 → What to Test → Submit for Review. Beta App Review 승인 시 초대 메일 자동 발송(자동 통지 on). 테스터 상태 "No Builds Available"은 승인 전 정상. ⚠ 함정: ASC React 폼은 form_input 프로그래매틱 select가 안 먹혀 JS 네이티브 setter+이벤트 디스패치 필요. 다음: 심사 승인 확인 → 아내 기기 설치(최소 iOS 26 필요) → 이후 빌드는 심사 없이 자동 배포.: spec `docs/superpowers/specs/2026-07-06-ios-native-rewrite-design.md`(사용자 승인) + M0 plan `docs/superpowers/plans/2026-07-06-ios-native-rewrite-m0.md`. 개인 기기 우선(무료 프로비저닝)·전체 기능 동등성(M0~M8)·Vercel API 전량 재사용·같은 repo `ios/`·최소 iOS 26. **M0 완료**: GildongmuKit(계약 모델·APIClient·SearchService, 테스트 12/12) + 검색 화면(3섹션+명소·포커스 이동·3-state) + Xcode 26.6 환경 + 실기기 설치(iPhone 13 Pro, Personal Team 72JQ7VD4V5 pbxproj 직접 설정으로 GUI 우회). 표시 이름 "길동무 베타"(PWA "길동무"와 구분, M8에 정식 이름 승계). 실기기 VoiceOver 게이트: 사용자 확인(헤딩 등 핵심 동작 정상, 6항목 개별 전수 검증은 생략 — 비행기모드 실패 문구·웹 폴백은 M1 게이트에서 재확인). code-reviewer 리뷰 4건 반영(URL 언래핑 크래시·allFailed 3-state·id 충돌·포커스). **M1 완료(2026-07-07 사용자 확인)**: 장소 상세(한영 주소·전화·딥링크+웹 폴백)·검색 행 VoiceOver 커스텀 액션(전화·네이버·카카오)·SectionState 3-state 정식화·AppConfig base URL 주입. Kit 테스트 17/17. plan `2026-07-06-ios-native-rewrite-m1.md`. **M2·M3 코드 완료(2026-07-07, 실기기 게이트 대기)**: M2=LocationService(권한 continuation 배열)+탭 구조+내 주변 6종(병렬 서브에이전트 3기, 규범 패턴 미러), M3=역 상세 자동 섹션 4종(heading 발견 경로)+날씨·공기질(등급 단어 정본). Kit 테스트 33/33, 리뷰 fix 3건(권한 continuation 레이스·통지 판정·재진입 가드) 반영, iPhone 설치 완료. **의도적 비수정 결정 2건**: ① 0건 완료 통지와 ContentUnavailableView 병존은 복제 아님(iOS Announcement는 일회성, 회전자 비잔존 — 웹 sr-only 복제 금지 원칙과 다른 매체) ② 평면 단일행 리스트(따릉이·키즈·둘러보기)의 항목 heading 미부여: 웹 h4 규칙의 목적(항목 단위 점프)이 1행=1객체로 이미 충족, heading 전행 부여는 잉여. plans `2026-07-07-ios-native-rewrite-m{2,3}.md`. **M4·M5 코드 완료(2026-07-07)**: M4=자동차·대중교통 브리핑(출발지=실위치 불변식, 단위 회귀 테스트, 상세에서 "미리 듣기" 진입), M5=장소 채팅 sheet(NDJSON 스트림 실캡처 fixture, status 진행 통지 ko.json 카피 미러, done 포커스 이동+햅틱, 렌더는 **props-driven 3종만**(places·addresses·web-results) 의도적 축소: self-fetch 카드류는 산문 정본+전용 화면 존재로 V1 미탑재). Kit 테스트 45/45. **실기기 설치 대기**: M4·M5 빌드는 iPhone 재연결 시(기기 unavailable). 게이트 대기 누적: M2·M3(설치됨)·M4·M5(미설치). **M6 코드 완료(2026-07-09)**: SpeechService 온디바이스 음성 인식(시작·정지 소리+햅틱)+검색·채팅 마이크 버튼(라벨 상태 신호·검색은 자동 실행), 한국어 품질 실측은 실기기 게이트. plan `2026-07-07-ios-native-rewrite-m6.md`. **M6b 웹 동등성 누락분 7건 코드 완료(2026-07-10)**: 사용자 요청("미구현 기능 파악·구현")으로 웹·iOS 전수 대조 → 로드맵(M0~M8) 어디에도 없던 진짜 누락 7건 확정 — ①무장애 관광지 내 주변(M2가 "6종"으로 계획했으나 웹은 7종) ②장소 상세 무장애 편의시설 자동 섹션(match 비-throw, false positive 무음) ③현재 위치 정위 WhereAmI(ko 산문 빌더 Kit 이식+채팅 진입) ④버스 도착 행→노선 경유 정류소 push ⑤내 주변 항목 채팅 진입(진료·키즈·둘러보기, contextMenu+로터 커스텀 액션, Place 합성 4종 헬퍼 — category가 프롬프트 라우팅 키) ⑥주소 복사 버튼(영문>도로명>지번, Announcement 통지) ⑦검색 분류·지역 2축 필터(카운트는 전체 고정·축 1개 이하 숨김)+버킷 그룹핑+보유 좌표 거리 정렬(신규 권한 요청 없음). plan `2026-07-10-ios-native-rewrite-m6b-web-parity-gaps.md`, fixture 5종 실캡처(2026-07-10). Kit 테스트 80/80, 병렬 서브에이전트 11태스크+태스크별 리뷰(fix 3라운드: 거리 km 표기 plan 리터럴 결함·재취득 실패 무신호·산문 빈 문자열 트레일링 쉼표)+a11y 감사 PASS+최종 whole-branch 리뷰(opus) "머지 가능(C/I 0)". **의도적 제외 재확인**: 채팅 self-fetch 렌더 카드류(V1 축소)·지도·PWA/SW·URL 동기화·인앱 언어 메뉴(시스템 언어 추종, M8 String Catalog). **실기기 게이트 항목(M6b)**: contextMenu+accessibilityAction 로터 중복 실측(중복이면 accessibilityAction 제거), 펼침 후 비동기 로드 통지, Dynamic Type 200%(필터 픽커·펼침). 백로그 처분(2026-07-28 검토): ~~chatLabel 공유화~~(07-26 내 주변→장소 상세 이식에서 하드코딩 chatLabel 자체가 제거되어 소멸), formatDistanceKo Kit public 승격은 Kit에서 필요해지는 시점 편승(현재 앱 타깃 단일 선언·중복 없음), ~~실패 announce 전 화면 일괄 검토~~(마일스톤별 a11y-auditor 감사로 대체 종결). **부수 진단(2026-07-10 오전)**: prod 실시간 3종(TAGO·TOPIS·지하철) 일시 장애 — 서울시 계열 ConnectTimeout+TAGO 세션 고갈(upstream), 키·코드 무관, ~45분 내 자연 회복 실측. 재발 지속 시 Vercel 함수 리전 icn1 전환 검토. 다음: 실기기 일괄 게이트(M2~M6b) 후 M7 비콘(실기기 실측이 개발 루프 자체에 필요)·M8 마감(String Catalog 5개 언어·설정·아이콘·통합 VoiceOver QA).
- ~~**ODsay 대중교통 길찾기 prod 미동작**~~ → **해소(2026-07-04)**. 결정 기록(spec 생략, 이 단락이 정본): 후보 4안(Web키 referer/자가 프록시/클라 직접/유료 고정 IP) 중 **URI(도메인) 식별 + 서버 Referer 헤더** 채택(무료·최소 diff·키 서버 전용 유지, 커밋 f64831a). 시행착오 2건 — ① ODsay **apiKey는 발급 시점 플랫폼에 묶임**: 기존 Server 앱에 URI 환경을 추가해도 그 키는 referer 식별 불가(전 variant `ApiKeyAuthFailed`, 프로브 실측). **URI 전용 앱**(`gildongmuweb`, ~2027-01-04)을 새로 만들어 해결, 비화이트리스트 IP에서 referer 유/무 대조 실호출로 증명. ② 콘솔 신규 등록 404의 정체는 **앱 이름 하이픈 불가**("영문·숫자·한글만") — 에러 표시 없이 registerPay 404로 죽음. 새 키는 `+`/`/` 포함이라 **URL 인코딩 형태로 env 저장**(provider가 raw로 붙이는 계약과 정합). 기존 `gildongmu` 앱(Server·119.71.23.38)은 백업으로 보존. URI는 앱 간 중복 등록 불가.
- ~~**둘러보기 기능 B (OSM 횡단보도·점자블록 + 음향신호기)**~~ → **종결(2026-07-22 보행 인프라로 출시)**. 길동 OSM 보행 태깅 희박 확인(2026-06-20)으로 OSM은 카카오가 비운 칸만 채우는 보완재로 확정, 음향신호기는 OSM 공백 때문에 data.go.kr로 피벗([[overpass-osm-korea-pedestrian-coverage]]). `BACKLOG.md` 폐기 목록에 근거와 함께 수록.
- ~~**idle 홈 heading 레벨 점프**~~ → **전제 소멸(2026-07-30 옴니박스 IA 재편)**. 홈이 칩 2개로 축소되고 nearby 9종이 허브 뷰로 이동하면서 현행 계층은 h1(Header) → h2(NearbyHub·검색 결과) → h3(패널) → h4(항목)로 점프가 없다(실측 확인). `BACKLOG.md` 폐기 목록 수록.
- ~~**dodo-planet 이식**~~ → **Phase A~E 전량 이식 완결(2026-07-04, dodo Round 150~155)**. 정본 spec `docs/superpowers/specs/2026-07-03-dodo-full-port-design.md`의 이식 대상 자산 전부 dodo 합류 — 마지막 Phase E(ODsay 대중교통)는 dodo 전용 URI 앱 `dodoplanet`(www.dodoplanet.space, ~2027-01-04) 신규 등록 + 비화이트리스트 IP referer 유/무 대조 실호출 6/6 PASS로 §7.1 절차 그대로 재현(dodo `docs/plans/2026-07-04-gildongmu-port-phase-e.md`). 콘솔 신규 함정 실측 1건 추가: **URI 입력은 프로토콜 제외 도메인만** — `https://` 포함 시 에러 표시 없이 "다음"이 무반응(폼 아래 경고 문구만). gildongmu는 인큐베이터로 존속 — 신규 국내 API는 여기서 검증 후 같은 경로로 졸업.
- ~~**`DistanceBeacon` 보류**~~ → **패치·재마운트 완료(2026-07-04), 실보행 스모크 대기**. 감사로 죽은 원인 규명 후 3개 수정 + `PlaceDetail` 재마운트(route 헬퍼 그룹, 자체 geolocation 게이트라 미지원 시 null). lint·673 test·build 통과. **⚠ 최종 게이트=실기기 보행 스모크**(폰에서 장소 상세 → "거리 추적" 펼침 → 시작 → 걸으며 톤·거리 확인) — 자동 테스트로는 검증 불가, 신뢰 전 실보행 필수.
  - **감사 근본 원인(2026-07-04)**: 설계(순수 리듀서 `beacon.ts`·spec)는 건실. 죽은 원인은 훅 계층 버그 2개(최초 커밋 `fd8d0fc`부터 존재). **①치명**(수정): `useScreenWakeLock`이 매 렌더 새 객체 `{acquire,release}`를 반환(useMemo 없음)하는데 `useDistanceBeacon`의 정리 effect가 `[wakeLock]` 의존 → 렌더마다 cleanup → `start()`가 방금 등록한 `watchPosition`이 자신의 setState 리렌더에서 즉시 `clearWatch`됨(시작 톤 후 fix 0회, 영원한 침묵). → `useScreenWakeLock` 반환값 `useMemo`로 안정화(참조 동일성 3곳 동시 해소). **②2차**(수정): `routeTone` 단일 throttle을 hold tick이 점유해 closer/farther 소실 → 추세 톤/tick 독립 throttle 창 분리(`lastTrendToneAtRef`·`lastTickAtRef`). **③조사 반영**(수정): iOS 무음 스위치가 Web Audio를 침묵시키는 문제 → `useBeaconSound`의 lazy AudioContext 생성 시 `navigator.audioSession.type='playback'`(iOS 17+) 선언, 미지원 graceful.
  - **후속(미적용)**: 조사 권장 first-fix 게이팅(캐시된 첫 fix가 수백 m 튀는 문제 — 첫 판독 무시 + desiredAccuracy 도달까지 대기 + maxWait 폴백)은 `beacon.ts` 리듀서 변경+fixture 필요라 별도 처리 — **실보행 스모크 선행으로 판정(2026-07-28)**: 기능 가치 검증 전 리듀서 개조 투자는 순서 역전, 스모크에서 첫 fix 튐이 실제 체감되면 착수. 백그라운드/공간오디오 승격(Capacitor+네이티브 위치 플러그인)은 dodo 이식 로드맵과 합류. 기술스택 조사 정본 `docs/RESEARCH-2026-07-04-realtime-pedestrian-nav-stack.md`.
  - **모델 계층화 교훈(횡단)**: 이 조사는 내장 `deep-research` 스킬로 돌렸는데 하위 에이전트 207회가 전량 세션 모델(Fable 5) 상속 → 12.5M 토큰 일괄 소진·세션 한도. 워크플로 `agent()`는 세션 모델 상속이 기본이고 그 스킬은 per-agent `model` 미지정이 원인. 계층화 포크 `~/.claude/workflows/deep-research-tiered.js`(scope/synthesize=opus·verify=sonnet·search/fetch=haiku) 신설 — 대량 조사 기본값. Anthropic 이슈 등록 [#74171](https://github.com/anthropics/claude-code/issues/74171). [[workflow-fanout-inherits-session-model]]

## 신규 data.go.kr API 추가 절차

같은 `DATA_GO_KR_API_KEY`로 data.go.kr 활용신청만 하면 즉시 자동승인(전파 ~5~30분, 직접호출 방식은 7일+). 서울 ws.bus.go.kr·swopenapi 계열은 별도 인증키 동기화 배치 지연 있음([[seoul-bus-datagokr-sync-delay]]).
