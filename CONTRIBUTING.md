# 기여 안내

이 프로젝트는 개인이 운영하는 접근성 우선 길찾기 앱입니다. 이슈·PR을 환영하지만, 응답과 머지는 운영자의 시간이 허락하는 만큼만 이루어집니다. 큰 변경은 이슈로 먼저 이야기해 주세요.

## 코드를 만지기 전에

- [`CLAUDE.md`](CLAUDE.md)를 읽습니다. 사람 이름이 아니라 "이 코드를 다음에 만질 사람이 모르면 틀리는 것"의 목록입니다. 항구 규칙·함정·통합별 계약이 여기 있습니다.
- 접근성이 머지 조건입니다. 스크린 리더로 전체 흐름이 닫혀야 하고, 지도에만 있는 정보·"0건"과 "정보 없음"을 뭉개는 표시·시각 스타일용 인라인 분절은 결함으로 봅니다.
- 기능·버그픽스는 같은 PR에 테스트를 동반합니다(`npm run test:run`). 외부 API 통합은 fixture만이 아니라 실호출로 확인한 결과를 PR 본문에 적어 주세요.

## 로컬 실행

```bash
npm install
cp .env.example .env.local   # 필요한 키만 채운다. 없는 키의 기능은 조용히 빠진다
npm run dev
npm run test:run
```

## 언어

이슈·PR·주석·커밋 메시지는 한국어 또는 영어 어느 쪽이든 좋습니다. 저장소 문서는 한국어가 기본입니다.

## 라이선스

기여한 코드는 이 저장소와 같은 [MIT 라이선스](LICENSE)로 배포됩니다.
